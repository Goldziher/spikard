//! Auto-generated Spikard-Rust benchmark server
//!
//! DO NOT EDIT - Generated by app-generator

use axum::{
    Router,
    extract::{Path, Query},
    response::Json,
    routing::{delete, get, patch, post, put},
};
use serde::{Deserialize, Serialize};
use serde_json::{Value, json};
use std::collections::HashMap;
use std::net::SocketAddr;

async fn handler_post_root() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_api_id() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_lang() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_id() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_nested() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_list() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_validated() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_optional_all() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_list_validated() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_events() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_2(Query(query): Query<HashMap<String, String>>) -> Json<Value> {
    let mut response = json!({});
    if let Some(val) = query.get("limit") {
        response["limit"] = json!(val);
    }
    Json(response)
}

async fn handler_patch_items_id(Path((id)): Path<String>) -> Json<Value> {
    let mut response = json!({});
    response["id"] = json!(id);
    Json(response)
}

async fn handler_post_users() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_products() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_items_3() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_payment() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_contact() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_api_v1_data() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_config() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_billing() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

async fn handler_post_data() -> Json<Value> {
    let mut response = json!({});
    Json(response)
}

#[tokio::main]
async fn main() {
    let port: u16 = std::env::args().nth(1).and_then(|s| s.parse().ok()).unwrap_or(8000);

    let app = Router::new()
        .route("/health", get(|| async { Json(json!({"status": "ok"})) }))
        .route("/", post(handler_post_root))
        .route("/api/{id}", post(handler_post_api_id))
        .route("/{lang}", post(handler_post_lang))
        .route("/{id}", post(handler_post_id))
        .route("/items/", post(handler_post_items))
        .route("/items/nested", post(handler_post_items_nested))
        .route("/items/list", post(handler_post_items_list))
        .route("/items/validated", post(handler_post_items_validated))
        .route("/items/optional-all", post(handler_post_items_optional_all))
        .route("/items/list-validated", post(handler_post_items_list_validated))
        .route("/events/", post(handler_post_events))
        .route("/items/{id}", patch(handler_patch_items_id))
        .route("/users", post(handler_post_users))
        .route("/products", post(handler_post_products))
        .route("/items", post(handler_post_items_3))
        .route("/payment", post(handler_post_payment))
        .route("/contact", post(handler_post_contact))
        .route("/api/v1/data", post(handler_post_api_v1_data))
        .route("/config", post(handler_post_config))
        .route("/billing", post(handler_post_billing))
        .route("/data", post(handler_post_data));

    let addr = SocketAddr::from(([0, 0, 0, 0], port));
    eprintln!("Starting Spikard-Rust server on port {}", port);

    let listener = tokio::net::TcpListener::bind(addr).await.unwrap();
    axum::serve(listener, app).await.unwrap();
}
