#ifndef SPIKARD_H
#define SPIKARD_H

#pragma once

/* This file is auto-generated by alef. DO NOT EDIT. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
/* Opaque type forward declarations */
typedef struct SPIKARDApiKeyConfig SPIKARDApiKeyConfig;
typedef struct SPIKARDBackgroundJobMetadata SPIKARDBackgroundJobMetadata;
typedef struct SPIKARDBackgroundTaskConfig SPIKARDBackgroundTaskConfig;
typedef struct SPIKARDCompressionConfig SPIKARDCompressionConfig;
typedef struct SPIKARDContactInfo SPIKARDContactInfo;
typedef struct SPIKARDCorsConfig SPIKARDCorsConfig;
typedef struct SPIKARDFullSchemaConfig SPIKARDFullSchemaConfig;
typedef struct SPIKARDGraphQLError SPIKARDGraphQLError;
typedef struct SPIKARDGraphQLRouteConfig SPIKARDGraphQLRouteConfig;
typedef struct SPIKARDGrpcConfig SPIKARDGrpcConfig;
typedef struct SPIKARDJsonRpcConfig SPIKARDJsonRpcConfig;
typedef struct SPIKARDJsonRpcMethodInfo SPIKARDJsonRpcMethodInfo;
typedef struct SPIKARDJwtConfig SPIKARDJwtConfig;
typedef struct SPIKARDLicenseInfo SPIKARDLicenseInfo;
typedef struct SPIKARDMethod SPIKARDMethod;
typedef struct SPIKARDOpenApiConfig SPIKARDOpenApiConfig;
typedef struct SPIKARDProblemDetails SPIKARDProblemDetails;
typedef struct SPIKARDQueryMutationConfig SPIKARDQueryMutationConfig;
typedef struct SPIKARDQueryOnlyConfig SPIKARDQueryOnlyConfig;
typedef struct SPIKARDRateLimitConfig SPIKARDRateLimitConfig;
typedef struct SPIKARDResponse SPIKARDResponse;
typedef struct SPIKARDSchemaConfig SPIKARDSchemaConfig;
typedef struct SPIKARDSecuritySchemeInfo SPIKARDSecuritySchemeInfo;
typedef struct SPIKARDServerConfig SPIKARDServerConfig;
typedef struct SPIKARDServerInfo SPIKARDServerInfo;
typedef struct SPIKARDSseEvent SPIKARDSseEvent;
typedef struct SPIKARDStaticFilesConfig SPIKARDStaticFilesConfig;
typedef struct SPIKARDUploadFile SPIKARDUploadFile;


/**
 * Return the last error code (0 means no error).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_last_error_code(void);

/**
 * Return the last error message. The pointer is valid until the next FFI call on this thread.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *spikard_last_error_context(void);

/**
 * Free a string previously returned by this library.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_free_string(char *ptr);

/**
 * Return the library version string. The pointer is static and must NOT be freed.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *spikard_version(void);

/**
 * Create a `UploadFile` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_upload_file_free`.
 */
SPIKARDUploadFile *spikard_upload_file_from_json(const char *json);

/**
 * Serialize a `UploadFile` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_upload_file_to_json(const SPIKARDUploadFile *ptr);

/**
 * Free a `UploadFile` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_upload_file_free(SPIKARDUploadFile *ptr);

/**
 * Get the `filename` field from a `UploadFile`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_upload_file_filename(const SPIKARDUploadFile *ptr);

/**
 * Get the `content_type` field from a `UploadFile`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_upload_file_content_type(const SPIKARDUploadFile *ptr);

/**
 * Get the `size` field from a `UploadFile`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_upload_file_size(const SPIKARDUploadFile *ptr);

/**
 * Get the `content` field from a `UploadFile`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint8_t *spikard_upload_file_content(const SPIKARDUploadFile *ptr,
                                     uintptr_t *out_len);

/**
 * Get the `content_encoding` field from a `UploadFile`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_upload_file_content_encoding(const SPIKARDUploadFile *ptr);

/**
 * Get the raw file content as bytes.
 *
 * This provides zero-copy access to the underlying buffer.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uint8_t *spikard_upload_file_as_bytes(const SPIKARDUploadFile *this_);

/**
 * Read the file content as a UTF-8 string.
 *
 * # Errors
 *
 * Returns an error if the content is not valid UTF-8.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_upload_file_read_to_string(const SPIKARDUploadFile *this_);

/**
 * Get the content type, defaulting to "application/octet-stream".
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_upload_file_content_type_or_default(const SPIKARDUploadFile *this_);

/**
 * Create a `CorsConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_cors_config_free`.
 */
SPIKARDCorsConfig *spikard_cors_config_from_json(const char *json);

/**
 * Serialize a `CorsConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_cors_config_to_json(const SPIKARDCorsConfig *ptr);

/**
 * Free a `CorsConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_cors_config_free(SPIKARDCorsConfig *ptr);

/**
 * Get the `allowed_origins` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_allowed_origins(const SPIKARDCorsConfig *ptr);

/**
 * Get the `allowed_methods` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_allowed_methods(const SPIKARDCorsConfig *ptr);

/**
 * Get the `allowed_headers` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_allowed_headers(const SPIKARDCorsConfig *ptr);

/**
 * Get the `expose_headers` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_expose_headers(const SPIKARDCorsConfig *ptr);

/**
 * Get the `max_age` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_cors_config_max_age(const SPIKARDCorsConfig *ptr);

/**
 * Get the `allow_credentials` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_cors_config_allow_credentials(const SPIKARDCorsConfig *ptr);

/**
 * Get the cached joined methods string for preflight responses
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_cors_config_allowed_methods_joined(const SPIKARDCorsConfig *this_);

/**
 * Get the cached joined headers string for preflight responses
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_cors_config_allowed_headers_joined(const SPIKARDCorsConfig *this_);

/**
 * Check if an origin is allowed (O(1) with wildcard, O(n) for exact match)
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_cors_config_is_origin_allowed(const SPIKARDCorsConfig *this_,
                                              const char *origin);

/**
 * Check if a method is allowed (O(1) with wildcard, O(n) for exact match)
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_cors_config_is_method_allowed(const SPIKARDCorsConfig *this_,
                                              const char *method);

/**
 * Check if all requested headers are allowed (O(n) where n = num requested headers)
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_cors_config_are_headers_allowed(const SPIKARDCorsConfig *this_,
                                                const char *requested);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDCorsConfig *spikard_cors_config_default(void);

/**
 * Create a `CompressionConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_compression_config_free`.
 */
SPIKARDCompressionConfig *spikard_compression_config_from_json(const char *json);

/**
 * Serialize a `CompressionConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_compression_config_to_json(const SPIKARDCompressionConfig *ptr);

/**
 * Free a `CompressionConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_compression_config_free(SPIKARDCompressionConfig *ptr);

/**
 * Get the `gzip` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_compression_config_gzip(const SPIKARDCompressionConfig *ptr);

/**
 * Get the `brotli` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_compression_config_brotli(const SPIKARDCompressionConfig *ptr);

/**
 * Get the `min_size` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_compression_config_min_size(const SPIKARDCompressionConfig *ptr);

/**
 * Get the `quality` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_compression_config_quality(const SPIKARDCompressionConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDCompressionConfig *spikard_compression_config_default(void);

/**
 * Create a `RateLimitConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_rate_limit_config_free`.
 */
SPIKARDRateLimitConfig *spikard_rate_limit_config_from_json(const char *json);

/**
 * Serialize a `RateLimitConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_rate_limit_config_to_json(const SPIKARDRateLimitConfig *ptr);

/**
 * Free a `RateLimitConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_rate_limit_config_free(SPIKARDRateLimitConfig *ptr);

/**
 * Get the `per_second` field from a `RateLimitConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_rate_limit_config_per_second(const SPIKARDRateLimitConfig *ptr);

/**
 * Get the `burst` field from a `RateLimitConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_rate_limit_config_burst(const SPIKARDRateLimitConfig *ptr);

/**
 * Get the `ip_based` field from a `RateLimitConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_rate_limit_config_ip_based(const SPIKARDRateLimitConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDRateLimitConfig *spikard_rate_limit_config_default(void);

/**
 * Create a `JsonRpcMethodInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_json_rpc_method_info_free`.
 */
SPIKARDJsonRpcMethodInfo *spikard_json_rpc_method_info_from_json(const char *json);

/**
 * Serialize a `JsonRpcMethodInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_json_rpc_method_info_to_json(const SPIKARDJsonRpcMethodInfo *ptr);

/**
 * Free a `JsonRpcMethodInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_json_rpc_method_info_free(SPIKARDJsonRpcMethodInfo *ptr);

/**
 * Get the `method_name` field from a `JsonRpcMethodInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_method_name(const SPIKARDJsonRpcMethodInfo *ptr);

/**
 * Get the `description` field from a `JsonRpcMethodInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_description(const SPIKARDJsonRpcMethodInfo *ptr);

/**
 * Get the `deprecated` field from a `JsonRpcMethodInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_json_rpc_method_info_deprecated(const SPIKARDJsonRpcMethodInfo *ptr);

/**
 * Get the `tags` field from a `JsonRpcMethodInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_tags(const SPIKARDJsonRpcMethodInfo *ptr);

/**
 * Create a `ProblemDetails` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_problem_details_free`.
 */
SPIKARDProblemDetails *spikard_problem_details_from_json(const char *json);

/**
 * Free a `ProblemDetails` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_problem_details_free(SPIKARDProblemDetails *ptr);

/**
 * Get the `type_uri` field from a `ProblemDetails`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_type_uri(const SPIKARDProblemDetails *ptr);

/**
 * Get the `title` field from a `ProblemDetails`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_title(const SPIKARDProblemDetails *ptr);

/**
 * Get the `status` field from a `ProblemDetails`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t spikard_problem_details_status(const SPIKARDProblemDetails *ptr);

/**
 * Get the `detail` field from a `ProblemDetails`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_detail(const SPIKARDProblemDetails *ptr);

/**
 * Get the `instance` field from a `ProblemDetails`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_instance(const SPIKARDProblemDetails *ptr);

/**
 * Set the detail field
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDProblemDetails *spikard_problem_details_with_detail(SPIKARDProblemDetails *this_,
                                                           const char *detail);

/**
 * Set the instance field
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDProblemDetails *spikard_problem_details_with_instance(SPIKARDProblemDetails *this_,
                                                             const char *instance);

/**
 * Create a not found error
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDProblemDetails *spikard_problem_details_not_found(const char *detail);

/**
 * Create a method not allowed error
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDProblemDetails *spikard_problem_details_method_not_allowed(const char *detail);

/**
 * Create an internal server error
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDProblemDetails *spikard_problem_details_internal_server_error(const char *detail);

/**
 * Create a bad request error
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDProblemDetails *spikard_problem_details_bad_request(const char *detail);

/**
 * Serialize to JSON string
 *
 * # Errors
 * Returns an error if the serialization fails.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_problem_details_to_json(const SPIKARDProblemDetails *this_);

/**
 * Serialize to pretty JSON string
 *
 * # Errors
 * Returns an error if the serialization fails.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_problem_details_to_json_pretty(const SPIKARDProblemDetails *this_);

/**
 * Free a `GraphQLError` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_graph_ql_error_free(SPIKARDGraphQLError *ptr);

/**
 * Convert error to HTTP status code
 *
 * Maps GraphQL error types to appropriate HTTP status codes:
 * - 400: Bad Request for parse/request-handling errors
 * - 401: Unauthorized for authentication errors
 * - 403: Forbidden for authorization errors
 * - 404: Not Found for resource not found
 * - 422: Unprocessable Entity for validation failures
 * - 429: Too Many Requests for rate limit errors
 * - 500: Internal Server Error for schema/serialization/internal errors
 * - 200: OK for GraphQL execution errors returned in GraphQL response body
 *
 * # Examples
 *
 * ```ignore
 * use spikard_graphql::error::GraphQLError;
 *
 * let error = GraphQLError::AuthenticationError("Invalid token".to_string());
 * assert_eq!(error.status_code(), 401);
 *
 * let error = GraphQLError::ExecutionError("Query failed".to_string());
 * assert_eq!(error.status_code(), 200); // GraphQL spec: errors return 200 with errors in body
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
uint16_t spikard_graph_ql_error_status_code(const SPIKARDGraphQLError *this_);

/**
 * Free a `GraphQLRouteConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_graph_ql_route_config_free(SPIKARDGraphQLRouteConfig *ptr);

/**
 * Set the HTTP path for the GraphQL endpoint
 *
 * # Arguments
 *
 * * `path` - The URL path (e.g., "/graphql", "/api/graphql")
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDGraphQLRouteConfig *spikard_graph_ql_route_config_path(SPIKARDGraphQLRouteConfig *this_,
                                                              const char *path);

/**
 * Set the HTTP method for the GraphQL endpoint
 *
 * # Arguments
 *
 * * `method` - The HTTP method (typically "POST")
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDGraphQLRouteConfig *spikard_graph_ql_route_config_method(SPIKARDGraphQLRouteConfig *this_,
                                                                const char *method);

/**
 * Enable or disable the GraphQL Playground UI
 *
 * # Arguments
 *
 * * `enable` - Whether to enable playground
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDGraphQLRouteConfig *spikard_graph_ql_route_config_enable_playground(SPIKARDGraphQLRouteConfig *this_,
                                                                           int32_t enable);

/**
 * Set a custom description for documentation
 *
 * # Arguments
 *
 * * `description` - Documentation string
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDGraphQLRouteConfig *spikard_graph_ql_route_config_description(SPIKARDGraphQLRouteConfig *this_,
                                                                     const char *description);

/**
 * Get the configured path
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_graph_ql_route_config_get_path(const SPIKARDGraphQLRouteConfig *this_);

/**
 * Get the configured method
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_graph_ql_route_config_get_method(const SPIKARDGraphQLRouteConfig *this_);

/**
 * Check if playground is enabled
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_graph_ql_route_config_is_playground_enabled(const SPIKARDGraphQLRouteConfig *this_);

/**
 * Get the description if set
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_graph_ql_route_config_get_description(const SPIKARDGraphQLRouteConfig *this_);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDGraphQLRouteConfig *spikard_graph_ql_route_config_default(void);

/**
 * Create a `SchemaConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_schema_config_free`.
 */
SPIKARDSchemaConfig *spikard_schema_config_from_json(const char *json);

/**
 * Serialize a `SchemaConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_schema_config_to_json(const SPIKARDSchemaConfig *ptr);

/**
 * Free a `SchemaConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_schema_config_free(SPIKARDSchemaConfig *ptr);

/**
 * Get the `introspection_enabled` field from a `SchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_schema_config_introspection_enabled(const SPIKARDSchemaConfig *ptr);

/**
 * Get the `complexity_limit` field from a `SchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_schema_config_complexity_limit(const SPIKARDSchemaConfig *ptr);

/**
 * Get the `depth_limit` field from a `SchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_schema_config_depth_limit(const SPIKARDSchemaConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDSchemaConfig *spikard_schema_config_default(void);

/**
 * Create a `QueryOnlyConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_query_only_config_free`.
 */
SPIKARDQueryOnlyConfig *spikard_query_only_config_from_json(const char *json);

/**
 * Serialize a `QueryOnlyConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_query_only_config_to_json(const SPIKARDQueryOnlyConfig *ptr);

/**
 * Free a `QueryOnlyConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_query_only_config_free(SPIKARDQueryOnlyConfig *ptr);

/**
 * Get the `introspection_enabled` field from a `QueryOnlyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_query_only_config_introspection_enabled(const SPIKARDQueryOnlyConfig *ptr);

/**
 * Get the `complexity_limit` field from a `QueryOnlyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_only_config_complexity_limit(const SPIKARDQueryOnlyConfig *ptr);

/**
 * Get the `depth_limit` field from a `QueryOnlyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_only_config_depth_limit(const SPIKARDQueryOnlyConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDQueryOnlyConfig *spikard_query_only_config_default(void);

/**
 * Create a `QueryMutationConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_query_mutation_config_free`.
 */
SPIKARDQueryMutationConfig *spikard_query_mutation_config_from_json(const char *json);

/**
 * Serialize a `QueryMutationConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_query_mutation_config_to_json(const SPIKARDQueryMutationConfig *ptr);

/**
 * Free a `QueryMutationConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_query_mutation_config_free(SPIKARDQueryMutationConfig *ptr);

/**
 * Get the `introspection_enabled` field from a `QueryMutationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_query_mutation_config_introspection_enabled(const SPIKARDQueryMutationConfig *ptr);

/**
 * Get the `complexity_limit` field from a `QueryMutationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_mutation_config_complexity_limit(const SPIKARDQueryMutationConfig *ptr);

/**
 * Get the `depth_limit` field from a `QueryMutationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_mutation_config_depth_limit(const SPIKARDQueryMutationConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDQueryMutationConfig *spikard_query_mutation_config_default(void);

/**
 * Create a `FullSchemaConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_full_schema_config_free`.
 */
SPIKARDFullSchemaConfig *spikard_full_schema_config_from_json(const char *json);

/**
 * Serialize a `FullSchemaConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_full_schema_config_to_json(const SPIKARDFullSchemaConfig *ptr);

/**
 * Free a `FullSchemaConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_full_schema_config_free(SPIKARDFullSchemaConfig *ptr);

/**
 * Get the `introspection_enabled` field from a `FullSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_full_schema_config_introspection_enabled(const SPIKARDFullSchemaConfig *ptr);

/**
 * Get the `complexity_limit` field from a `FullSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_full_schema_config_complexity_limit(const SPIKARDFullSchemaConfig *ptr);

/**
 * Get the `depth_limit` field from a `FullSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_full_schema_config_depth_limit(const SPIKARDFullSchemaConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDFullSchemaConfig *spikard_full_schema_config_default(void);

/**
 * Create a `BackgroundTaskConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_background_task_config_free`.
 */
SPIKARDBackgroundTaskConfig *spikard_background_task_config_from_json(const char *json);

/**
 * Serialize a `BackgroundTaskConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_background_task_config_to_json(const SPIKARDBackgroundTaskConfig *ptr);

/**
 * Free a `BackgroundTaskConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_background_task_config_free(SPIKARDBackgroundTaskConfig *ptr);

/**
 * Get the `max_queue_size` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_background_task_config_max_queue_size(const SPIKARDBackgroundTaskConfig *ptr);

/**
 * Get the `max_concurrent_tasks` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_background_task_config_max_concurrent_tasks(const SPIKARDBackgroundTaskConfig *ptr);

/**
 * Get the `drain_timeout_secs` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_background_task_config_drain_timeout_secs(const SPIKARDBackgroundTaskConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDBackgroundTaskConfig *spikard_background_task_config_default(void);

/**
 * Create a `BackgroundJobMetadata` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_background_job_metadata_free`.
 */
SPIKARDBackgroundJobMetadata *spikard_background_job_metadata_from_json(const char *json);

/**
 * Serialize a `BackgroundJobMetadata` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_background_job_metadata_to_json(const SPIKARDBackgroundJobMetadata *ptr);

/**
 * Free a `BackgroundJobMetadata` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_background_job_metadata_free(SPIKARDBackgroundJobMetadata *ptr);

/**
 * Get the `request_id` field from a `BackgroundJobMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_background_job_metadata_request_id(const SPIKARDBackgroundJobMetadata *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDBackgroundJobMetadata *spikard_background_job_metadata_default(void);

/**
 * Create a `GrpcConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_grpc_config_free`.
 */
SPIKARDGrpcConfig *spikard_grpc_config_from_json(const char *json);

/**
 * Serialize a `GrpcConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_grpc_config_to_json(const SPIKARDGrpcConfig *ptr);

/**
 * Free a `GrpcConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_grpc_config_free(SPIKARDGrpcConfig *ptr);

/**
 * Get the `enabled` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_enabled(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `max_message_size` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_grpc_config_max_message_size(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `enable_compression` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_enable_compression(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `request_timeout` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_grpc_config_request_timeout(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `max_concurrent_streams` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_grpc_config_max_concurrent_streams(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `enable_keepalive` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_enable_keepalive(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `keepalive_interval` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_grpc_config_keepalive_interval(const SPIKARDGrpcConfig *ptr);

/**
 * Get the `keepalive_timeout` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_grpc_config_keepalive_timeout(const SPIKARDGrpcConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDGrpcConfig *spikard_grpc_config_default(void);

/**
 * Create a `JsonRpcConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_json_rpc_config_free`.
 */
SPIKARDJsonRpcConfig *spikard_json_rpc_config_from_json(const char *json);

/**
 * Serialize a `JsonRpcConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_json_rpc_config_to_json(const SPIKARDJsonRpcConfig *ptr);

/**
 * Free a `JsonRpcConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_json_rpc_config_free(SPIKARDJsonRpcConfig *ptr);

/**
 * Get the `enabled` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_json_rpc_config_enabled(const SPIKARDJsonRpcConfig *ptr);

/**
 * Get the `endpoint_path` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_config_endpoint_path(const SPIKARDJsonRpcConfig *ptr);

/**
 * Get the `enable_batch` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_json_rpc_config_enable_batch(const SPIKARDJsonRpcConfig *ptr);

/**
 * Get the `max_batch_size` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_json_rpc_config_max_batch_size(const SPIKARDJsonRpcConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDJsonRpcConfig *spikard_json_rpc_config_default(void);

/**
 * Create a `OpenApiConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_open_api_config_free`.
 */
SPIKARDOpenApiConfig *spikard_open_api_config_from_json(const char *json);

/**
 * Serialize a `OpenApiConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_open_api_config_to_json(const SPIKARDOpenApiConfig *ptr);

/**
 * Free a `OpenApiConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_open_api_config_free(SPIKARDOpenApiConfig *ptr);

/**
 * Get the `enabled` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_open_api_config_enabled(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `title` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_title(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `version` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_version(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `description` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_description(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `swagger_ui_path` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_swagger_ui_path(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `redoc_path` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_redoc_path(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `openapi_json_path` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_openapi_json_path(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `contact` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDContactInfo *spikard_open_api_config_contact(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `license` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDLicenseInfo *spikard_open_api_config_license(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `servers` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_servers(const SPIKARDOpenApiConfig *ptr);

/**
 * Get the `security_schemes` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_security_schemes(const SPIKARDOpenApiConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDOpenApiConfig *spikard_open_api_config_default(void);

/**
 * Create a `ContactInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_contact_info_free`.
 */
SPIKARDContactInfo *spikard_contact_info_from_json(const char *json);

/**
 * Serialize a `ContactInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_contact_info_to_json(const SPIKARDContactInfo *ptr);

/**
 * Free a `ContactInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_contact_info_free(SPIKARDContactInfo *ptr);

/**
 * Get the `name` field from a `ContactInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_contact_info_name(const SPIKARDContactInfo *ptr);

/**
 * Get the `email` field from a `ContactInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_contact_info_email(const SPIKARDContactInfo *ptr);

/**
 * Get the `url` field from a `ContactInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_contact_info_url(const SPIKARDContactInfo *ptr);

/**
 * Create a `LicenseInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_license_info_free`.
 */
SPIKARDLicenseInfo *spikard_license_info_from_json(const char *json);

/**
 * Serialize a `LicenseInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_license_info_to_json(const SPIKARDLicenseInfo *ptr);

/**
 * Free a `LicenseInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_license_info_free(SPIKARDLicenseInfo *ptr);

/**
 * Get the `name` field from a `LicenseInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_license_info_name(const SPIKARDLicenseInfo *ptr);

/**
 * Get the `url` field from a `LicenseInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_license_info_url(const SPIKARDLicenseInfo *ptr);

/**
 * Create a `ServerInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_server_info_free`.
 */
SPIKARDServerInfo *spikard_server_info_from_json(const char *json);

/**
 * Serialize a `ServerInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_server_info_to_json(const SPIKARDServerInfo *ptr);

/**
 * Free a `ServerInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_server_info_free(SPIKARDServerInfo *ptr);

/**
 * Get the `url` field from a `ServerInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_info_url(const SPIKARDServerInfo *ptr);

/**
 * Get the `description` field from a `ServerInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_info_description(const SPIKARDServerInfo *ptr);

/**
 * Create a `Response` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_response_free`.
 */
SPIKARDResponse *spikard_response_from_json(const char *json);

/**
 * Serialize a `Response` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_response_to_json(const SPIKARDResponse *ptr);

/**
 * Free a `Response` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_response_free(SPIKARDResponse *ptr);

/**
 * Get the `status_code` field from a `Response`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t spikard_response_status_code(const SPIKARDResponse *ptr);

/**
 * Get the `headers` field from a `Response`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_response_headers(const SPIKARDResponse *ptr);

/**
 * Set a header
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
void spikard_response_set_header(SPIKARDResponse *this_,
                                 const char *key,
                                 const char *value);

/**
 * Set a cookie in the response
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
void spikard_response_set_cookie(SPIKARDResponse *this_,
                                 const char *key,
                                 const char *value,
                                 int32_t secure,
                                 int32_t http_only,
                                 int64_t max_age,
                                 const char *domain,
                                 const char *path,
                                 const char *same_site);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDResponse *spikard_response_default(void);

/**
 * Create a `SseEvent` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_sse_event_free`.
 */
SPIKARDSseEvent *spikard_sse_event_from_json(const char *json);

/**
 * Serialize a `SseEvent` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_sse_event_to_json(const SPIKARDSseEvent *ptr);

/**
 * Free a `SseEvent` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_sse_event_free(SPIKARDSseEvent *ptr);

/**
 * Get the `event_type` field from a `SseEvent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_sse_event_event_type(const SPIKARDSseEvent *ptr);

/**
 * Get the `id` field from a `SseEvent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_sse_event_id(const SPIKARDSseEvent *ptr);

/**
 * Get the `retry` field from a `SseEvent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_sse_event_retry(const SPIKARDSseEvent *ptr);

/**
 * Set the event ID for client-side reconnection support
 *
 * Sets an ID that clients can use to resume from this point if they disconnect.
 * The client sends this ID back in the `Last-Event-ID` header when reconnecting.
 *
 * # Arguments
 * * `id` - Unique identifier for this event
 *
 * # Example
 *
 * ```ignore
 * use serde_json::json;
 * use spikard_http::sse::SseEvent;
 *
 * let event = SseEvent::new(json!({"count": 1}))
 *     .with_id("event-1");
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDSseEvent *spikard_sse_event_with_id(SPIKARDSseEvent *this_,
                                           const char *id);

/**
 * Set the retry timeout for client reconnection
 *
 * Sets the time in milliseconds clients should wait before attempting to reconnect
 * if the connection is lost. The client browser will automatically handle reconnection.
 *
 * # Arguments
 * * `retry_ms` - Retry timeout in milliseconds
 *
 * # Example
 *
 * ```ignore
 * use serde_json::json;
 * use spikard_http::sse::SseEvent;
 *
 * let event = SseEvent::new(json!({"data": "value"}))
 *     .with_retry(5000); // Reconnect after 5 seconds
 * ```
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDSseEvent *spikard_sse_event_with_retry(SPIKARDSseEvent *this_,
                                              uint64_t retry_ms);

/**
 * Create a `JwtConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_jwt_config_free`.
 */
SPIKARDJwtConfig *spikard_jwt_config_from_json(const char *json);

/**
 * Serialize a `JwtConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_jwt_config_to_json(const SPIKARDJwtConfig *ptr);

/**
 * Free a `JwtConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_jwt_config_free(SPIKARDJwtConfig *ptr);

/**
 * Get the `secret` field from a `JwtConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_secret(const SPIKARDJwtConfig *ptr);

/**
 * Get the `algorithm` field from a `JwtConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_algorithm(const SPIKARDJwtConfig *ptr);

/**
 * Get the `audience` field from a `JwtConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_audience(const SPIKARDJwtConfig *ptr);

/**
 * Get the `issuer` field from a `JwtConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_issuer(const SPIKARDJwtConfig *ptr);

/**
 * Get the `leeway` field from a `JwtConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_jwt_config_leeway(const SPIKARDJwtConfig *ptr);

/**
 * Create a `ApiKeyConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_api_key_config_free`.
 */
SPIKARDApiKeyConfig *spikard_api_key_config_from_json(const char *json);

/**
 * Serialize a `ApiKeyConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_api_key_config_to_json(const SPIKARDApiKeyConfig *ptr);

/**
 * Free a `ApiKeyConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_api_key_config_free(SPIKARDApiKeyConfig *ptr);

/**
 * Get the `keys` field from a `ApiKeyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_api_key_config_keys(const SPIKARDApiKeyConfig *ptr);

/**
 * Get the `header_name` field from a `ApiKeyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_api_key_config_header_name(const SPIKARDApiKeyConfig *ptr);

/**
 * Create a `StaticFilesConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_static_files_config_free`.
 */
SPIKARDStaticFilesConfig *spikard_static_files_config_from_json(const char *json);

/**
 * Serialize a `StaticFilesConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_static_files_config_to_json(const SPIKARDStaticFilesConfig *ptr);

/**
 * Free a `StaticFilesConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_static_files_config_free(SPIKARDStaticFilesConfig *ptr);

/**
 * Get the `directory` field from a `StaticFilesConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_static_files_config_directory(const SPIKARDStaticFilesConfig *ptr);

/**
 * Get the `route_prefix` field from a `StaticFilesConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_static_files_config_route_prefix(const SPIKARDStaticFilesConfig *ptr);

/**
 * Get the `index_file` field from a `StaticFilesConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_static_files_config_index_file(const SPIKARDStaticFilesConfig *ptr);

/**
 * Get the `cache_control` field from a `StaticFilesConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_static_files_config_cache_control(const SPIKARDStaticFilesConfig *ptr);

/**
 * Create a `ServerConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_server_config_free`.
 */
SPIKARDServerConfig *spikard_server_config_from_json(const char *json);

/**
 * Serialize a `ServerConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_server_config_to_json(const SPIKARDServerConfig *ptr);

/**
 * Free a `ServerConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_server_config_free(SPIKARDServerConfig *ptr);

/**
 * Get the `host` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_config_host(const SPIKARDServerConfig *ptr);

/**
 * Get the `port` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t spikard_server_config_port(const SPIKARDServerConfig *ptr);

/**
 * Get the `workers` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_server_config_workers(const SPIKARDServerConfig *ptr);

/**
 * Get the `enable_request_id` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_enable_request_id(const SPIKARDServerConfig *ptr);

/**
 * Get the `max_body_size` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_server_config_max_body_size(const SPIKARDServerConfig *ptr);

/**
 * Get the `request_timeout` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_server_config_request_timeout(const SPIKARDServerConfig *ptr);

/**
 * Get the `compression` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDCompressionConfig *spikard_server_config_compression(const SPIKARDServerConfig *ptr);

/**
 * Get the `rate_limit` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDRateLimitConfig *spikard_server_config_rate_limit(const SPIKARDServerConfig *ptr);

/**
 * Get the `jwt_auth` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDJwtConfig *spikard_server_config_jwt_auth(const SPIKARDServerConfig *ptr);

/**
 * Get the `api_key_auth` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDApiKeyConfig *spikard_server_config_api_key_auth(const SPIKARDServerConfig *ptr);

/**
 * Get the `static_files` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_config_static_files(const SPIKARDServerConfig *ptr);

/**
 * Get the `graceful_shutdown` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_graceful_shutdown(const SPIKARDServerConfig *ptr);

/**
 * Get the `shutdown_timeout` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_server_config_shutdown_timeout(const SPIKARDServerConfig *ptr);

/**
 * Get the `openapi` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDOpenApiConfig *spikard_server_config_openapi(const SPIKARDServerConfig *ptr);

/**
 * Get the `jsonrpc` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDJsonRpcConfig *spikard_server_config_jsonrpc(const SPIKARDServerConfig *ptr);

/**
 * Get the `grpc` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDGrpcConfig *spikard_server_config_grpc(const SPIKARDServerConfig *ptr);

/**
 * Get the `background_tasks` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDBackgroundTaskConfig *spikard_server_config_background_tasks(const SPIKARDServerConfig *ptr);

/**
 * Get the `enable_http_trace` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_enable_http_trace(const SPIKARDServerConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDServerConfig *spikard_server_config_default(void);

/**
 * Convert an integer to a `Method` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_method_from_i32(int32_t value);

/**
 * Convert a `Method` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t spikard_method_from_str(const char *name);

/**
 * Convert an integer to a `SecuritySchemeInfo` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_security_scheme_info_from_i32(int32_t value);

/**
 * Convert a `SecuritySchemeInfo` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t spikard_security_scheme_info_from_str(const char *name);

/**
 * Create a simple schema configuration with only Query type.
 *
 * This is a convenience function for schemas that only have queries.
 *
 * # Returns
 *
 * A `QueryOnlyConfig` with default settings
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDQueryOnlyConfig *spikard_schema_query_only(void);

/**
 * Create a schema configuration with Query and Mutation types.
 *
 * This is a convenience function for schemas with queries and mutations but no subscriptions.
 *
 * # Returns
 *
 * A `QueryMutationConfig` with default settings
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDQueryMutationConfig *spikard_schema_query_mutation(void);

/**
 * Create a schema configuration with all three root types.
 *
 * This is a convenience function for fully-featured schemas.
 *
 * # Returns
 *
 * A `FullSchemaConfig` with default settings
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDFullSchemaConfig *spikard_schema_full(void);

#endif  /* SPIKARD_H */
