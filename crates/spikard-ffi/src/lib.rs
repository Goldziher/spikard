// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:f9b28c69cf1a0ace2d64bb1cf9515d1291c82472f2a568bdcbc52d5a478b8cce
// alef:handle-abi:1
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_mut, noop_method_call)]
#![allow(unsafe_op_in_unsafe_fn, unsafe_attr_outside_unsafe)]
#![allow(
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::redundant_locals,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::type_complexity,
    clippy::clone_on_copy,
    clippy::collapsible_if
)]
#![allow(
    clippy::missing_safety_doc,
    clippy::doc_lazy_continuation,
    clippy::doc_overindented_list_items
)]

use std::cell::RefCell;
use std::ffi::{CStr, CString, c_char};

pub mod service;

thread_local! {
    static LAST_ERROR_CODE: RefCell<i32> = const { RefCell::new(0) };
    static LAST_ERROR_CONTEXT: RefCell<Option<CString>> = const { RefCell::new(None) };
    static LAST_RETURN_LENGTHS: RefCell<std::collections::BTreeMap<&'static str, usize>> =
        const { RefCell::new(std::collections::BTreeMap::new()) };
}

#[repr(i32)]
pub enum AlefFfiErrorCode {
    SpikardAlefNone = 0,
    SpikardAlefConversion = 1,
    SpikardAlefUnknown = 2,
    SpikardAlefPanic = 3,
    SpikardAlefInvalidHandle = 4,
}

const ALEF_FFI_CONVERSION_ERROR: i32 = AlefFfiErrorCode::SpikardAlefConversion as i32;
const ALEF_FFI_UNKNOWN_ERROR: i32 = AlefFfiErrorCode::SpikardAlefUnknown as i32;
const ALEF_FFI_PANIC_ERROR: i32 = AlefFfiErrorCode::SpikardAlefPanic as i32;
const ALEF_INVALID_HANDLE_ERROR: i32 = AlefFfiErrorCode::SpikardAlefInvalidHandle as i32;

fn alef_ffi_error_code(error: &dyn std::any::Any) -> i32 {
    if let Some(error) = error.downcast_ref::<spikard::AppError>() {
        #[allow(unreachable_patterns)]
        return match error {
            spikard::AppError::Route(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard::AppError::Server(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard::AppError::Decode(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard::AppError::GraphQL(..) => ALEF_FFI_UNKNOWN_ERROR,
            _ => ALEF_FFI_UNKNOWN_ERROR,
        };
    }
    if let Some(error) = error.downcast_ref::<spikard_graphql::error::GraphQLError>() {
        #[allow(unreachable_patterns)]
        return match error {
            spikard_graphql::error::GraphQLError::ExecutionError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::SchemaBuildError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::RequestHandlingError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::SerializationError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::JsonError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::ValidationError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::ParseError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::AuthenticationError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::AuthorizationError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::NotFound(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::RateLimitExceeded(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::InvalidInput { .. } => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::ComplexityLimitExceeded => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::DepthLimitExceeded => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::IntrospectionDisabled => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::error::GraphQLError::InternalError(..) => ALEF_FFI_UNKNOWN_ERROR,
            _ => ALEF_FFI_UNKNOWN_ERROR,
        };
    }
    if let Some(error) = error.downcast_ref::<spikard_graphql::schema::SchemaError>() {
        #[allow(unreachable_patterns)]
        return match error {
            spikard_graphql::schema::SchemaError::BuildingFailed(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::schema::SchemaError::ValidationError(..) => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::schema::SchemaError::ComplexityLimitExceeded { .. } => ALEF_FFI_UNKNOWN_ERROR,
            spikard_graphql::schema::SchemaError::DepthLimitExceeded { .. } => ALEF_FFI_UNKNOWN_ERROR,
            _ => ALEF_FFI_UNKNOWN_ERROR,
        };
    }
    ALEF_FFI_UNKNOWN_ERROR
}

fn set_last_error(code: i32, message: &str) {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = code);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = CString::new(message).ok());
}

fn clear_last_error() {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = 0);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = None);
}

fn set_panic_error() {
    set_last_error(ALEF_FFI_PANIC_ERROR, "Rust panic contained at FFI boundary");
}

fn catch_ffi_panic<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    clear_last_error();
    catch_ffi_panic_preserving_error(fallback, body)
}

fn catch_ffi_panic_preserving_error<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(body)) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call). Unlike `catch_ffi_panic` (which always
            // clears first), this wrapper's whole contract is to leave that in place, so the
            // generic panic marker is stamped only when nothing more specific already is.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            fallback
        }
    }
}

fn set_last_return_len(function: &'static str, len: usize) {
    LAST_RETURN_LENGTHS.with_borrow_mut(|lengths| {
        lengths.insert(function, len);
    });
}

fn last_return_len(function: &'static str) -> usize {
    LAST_RETURN_LENGTHS.with_borrow(|lengths| lengths.get(function).copied().unwrap_or(0))
}

/// Return the last error code (0 means no error).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// This function does not allocate and returns no owned pointer.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_last_error_code() -> i32 {
    catch_ffi_panic_preserving_error(ALEF_FFI_PANIC_ERROR, || LAST_ERROR_CODE.with_borrow(|c| *c))
}

/// Return the last error message. The pointer is borrowed and valid until the next FFI call on this thread.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// The returned pointer is borrowed from thread-local storage and must NOT be freed.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_last_error_context() -> *const c_char {
    catch_ffi_panic_preserving_error(std::ptr::null(), || {
        LAST_ERROR_CONTEXT.with_borrow(|ctx| ctx.as_ref().map_or(std::ptr::null(), |c| c.as_ptr()))
    })
}

type AlefHandle = u64;
const HANDLE_INDEX_MASK: u64 = u32::MAX as u64;

#[derive(Debug)]
enum HandleError {
    InvalidZero,
    UnknownSlot,
    StaleGeneration,
    WrongType,
    RegistryPoisoned,
    HandleBusy,
    AliasedHandle,
}

impl std::fmt::Display for HandleError {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let message = match self {
            Self::InvalidZero => "zero is not a valid handle",
            Self::UnknownSlot => "handle refers to an unknown slot",
            Self::StaleGeneration => "handle generation is stale",
            Self::WrongType => "handle has the wrong type",
            Self::RegistryPoisoned => "handle registry lock is poisoned",
            Self::HandleBusy => "handle is currently in use",
            Self::AliasedHandle => "the same handle was passed more than once",
        };
        formatter.write_str(message)
    }
}

type ErasedHandleValue = std::sync::Arc<std::sync::Mutex<Box<dyn std::any::Any + Send>>>;

struct HandleSlot {
    generation: u32,
    value: Option<ErasedHandleValue>,
}

#[derive(Default)]
struct HandleRegistry {
    slots: Vec<HandleSlot>,
}

impl HandleRegistry {
    fn insert<T: std::any::Any + Send>(&mut self, value: T) -> AlefHandle {
        let entry = std::sync::Arc::new(std::sync::Mutex::new(Box::new(value) as Box<dyn std::any::Any + Send>));
        if let Some((index, slot)) = self.slots.iter_mut().enumerate().find(|(_, slot)| slot.value.is_none()) {
            slot.value = Some(entry);
            return encode_handle(index, slot.generation);
        }
        self.slots.push(HandleSlot {
            generation: 1,
            value: Some(entry),
        });
        encode_handle(self.slots.len() - 1, 1)
    }

    fn get(&self, handle: AlefHandle) -> Result<ErasedHandleValue, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        slot.value.clone().ok_or(HandleError::StaleGeneration)
    }

    fn remove<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<(), HandleError> {
        drop(self.take::<T>(handle)?);
        Ok(())
    }

    fn take<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<T, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get_mut(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        let value = slot.value.as_ref().ok_or(HandleError::StaleGeneration)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if !guard.is::<T>() {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        let value = slot.value.take().ok_or(HandleError::StaleGeneration)?;
        let mutex = std::sync::Arc::try_unwrap(value).map_err(|value| {
            slot.value = Some(value);
            HandleError::HandleBusy
        })?;
        slot.generation = next_generation(slot.generation);
        let boxed = mutex.into_inner().map_err(|_| HandleError::RegistryPoisoned)?;
        boxed
            .downcast::<T>()
            .map(|value| *value)
            .map_err(|_| HandleError::WrongType)
    }
}

static HANDLE_REGISTRY: std::sync::OnceLock<std::sync::Mutex<HandleRegistry>> = std::sync::OnceLock::new();

fn handle_registry() -> &'static std::sync::Mutex<HandleRegistry> {
    HANDLE_REGISTRY.get_or_init(|| std::sync::Mutex::new(HandleRegistry::default()))
}

fn insert_handle<T: std::any::Any + Send>(value: T) -> Result<AlefHandle, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    Ok(registry.insert(value))
}

struct SerializedHandle<T: 'static> {
    json: String,
    marker: std::marker::PhantomData<fn() -> T>,
}

fn insert_serialized_handle<T: serde::Serialize + 'static>(value: &T) -> Result<AlefHandle, String> {
    let json = serde_json::to_string(value).map_err(|error| error.to_string())?;
    insert_handle(SerializedHandle::<T> {
        json,
        marker: std::marker::PhantomData,
    })
    .map_err(|error| error.to_string())
}

fn with_handle<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_ref::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

struct HandleRequest {
    handle: AlefHandle,
    expected_type: std::any::TypeId,
}

fn acquire_handles(requests: &[HandleRequest]) -> Result<Vec<(AlefHandle, ErasedHandleValue)>, HandleError> {
    let mut ordered = requests.iter().collect::<Vec<_>>();
    ordered.sort_by_key(|request| request.handle);
    if ordered.windows(2).any(|pair| pair[0].handle == pair[1].handle) {
        return Err(HandleError::AliasedHandle);
    }
    let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let mut values = Vec::with_capacity(ordered.len());
    for request in ordered {
        let value = registry.get(request.handle)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if guard.as_ref().type_id() != request.expected_type {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        values.push((request.handle, value));
    }
    Ok(values)
}

fn locked_handle_ptr<T: std::any::Any + Send>(
    guards: &mut [(AlefHandle, std::sync::MutexGuard<'_, Box<dyn std::any::Any + Send>>)],
    handle: AlefHandle,
) -> Result<*mut T, HandleError> {
    let (_, guard) = guards
        .iter_mut()
        .find(|(candidate, _)| *candidate == handle)
        .ok_or(HandleError::UnknownSlot)?;
    guard
        .downcast_mut::<T>()
        .map(|value| value as *mut T)
        .ok_or(HandleError::WrongType)
}

fn with_handle_mut<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&mut T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let mut guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_mut::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

fn remove_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<(), HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.remove::<T>(handle)
}

fn take_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<T, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.take::<T>(handle)
}

fn encode_handle(index: usize, generation: u32) -> AlefHandle {
    ((generation as u64) << 32) | ((index as u64) + 1)
}

fn decode_handle(handle: AlefHandle) -> Result<(usize, u32), HandleError> {
    if handle == 0 {
        return Err(HandleError::InvalidZero);
    }
    let encoded_index = handle & HANDLE_INDEX_MASK;
    if encoded_index == 0 {
        return Err(HandleError::UnknownSlot);
    }
    Ok(((encoded_index - 1) as usize, (handle >> 32) as u32))
}

fn next_generation(generation: u32) -> u32 {
    let next = generation.wrapping_add(1);
    if next == 0 { 1 } else { next }
}

fn set_handle_error(error: &HandleError) {
    set_last_error(ALEF_INVALID_HANDLE_ERROR, &error.to_string());
}

/// Free a string previously returned by this library.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_free_string(ptr: *mut c_char) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by CString::into_raw; caller ensures no aliases.
            unsafe {
                drop(CString::from_raw(ptr));
            }
        }
    })
}

/// Free a byte buffer previously returned by this library via out-params.
/// `ptr`, `len`, and `cap` must match the values written by the library function,
/// or the call must pass `ptr = null` (in which case it is a no-op).
/// # Safety
/// Pointer must have been returned by this library (via out_ptr / out_len / out_cap
/// out-params), and ownership must not already have been released.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_free_bytes(ptr: *mut u8, len: usize, cap: usize) {
    catch_ffi_panic((), || {
        if ptr.is_null() {
            return;
        }
        if len != cap {
            set_last_error(1, "invalid byte buffer metadata: len and cap differ");
            return;
        }
        // SAFETY: The library returns a Box<[u8]> allocation and reports its length
        // as both len and cap; the caller contract preserves pointer and ownership.
        unsafe {
            let slice = std::ptr::slice_from_raw_parts_mut(ptr, len);
            drop(Box::<[u8]>::from_raw(slice));
        }
    })
}

/// Return the library version string. The pointer is static and must NOT be freed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_version() -> *const c_char {
    catch_ffi_panic(std::ptr::null(), || {
        static VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "\0");
        VERSION.as_ptr() as *const c_char
    })
}

#[allow(dead_code)]
fn lifecycle_hook_phase_from_i32_rs(v: i32) -> Option<spikard_core::LifecycleHookPhase> {
    match v {
        0 => Some(spikard_core::LifecycleHookPhase::OnRequest),
        1 => Some(spikard_core::LifecycleHookPhase::PreValidation),
        2 => Some(spikard_core::LifecycleHookPhase::PreHandler),
        3 => Some(spikard_core::LifecycleHookPhase::OnResponse),
        4 => Some(spikard_core::LifecycleHookPhase::OnError),
        _ => None,
    }
}

#[allow(dead_code)]
fn method_from_i32_rs(v: i32) -> Option<spikard::Method> {
    match v {
        0 => Some(spikard::Method::Get),
        1 => Some(spikard::Method::Post),
        2 => Some(spikard::Method::Put),
        3 => Some(spikard::Method::Patch),
        4 => Some(spikard::Method::Delete),
        5 => Some(spikard::Method::Head),
        6 => Some(spikard::Method::Options),
        7 => Some(spikard::Method::Connect),
        8 => Some(spikard::Method::Trace),
        _ => None,
    }
}

/// Create a `ApiKeyAuthConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_api_key_auth_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_auth_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::ApiKeyAuthConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ApiKeyAuthConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_auth_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::ApiKeyAuthConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ApiKeyAuthConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_auth_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::ApiKeyAuthConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `ApiKeyAuthConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_auth_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ApiKeyAuthConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `keys` field from a `ApiKeyAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_auth_config_keys(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::ApiKeyAuthConfig, _>(handle, |obj| match serde_json::to_string(&obj.keys) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `header_name` field from a `ApiKeyAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_auth_config_header_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::ApiKeyAuthConfig, _>(handle, |obj| {
            match CString::new(obj.header_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ApiKeyConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_api_key_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::ApiKeyConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ApiKeyConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::ApiKeyConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ApiKeyConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::ApiKeyConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `keys` field from a `ApiKeyConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_config_keys(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::ApiKeyConfig, _>(handle, |obj| match serde_json::to_string(&obj.keys) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `header_name` field from a `ApiKeyConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_api_key_config_header_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::ApiKeyConfig, _>(handle, |obj| match CString::new(obj.header_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `AsyncApiConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_async_api_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_async_api_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::AsyncApiConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `AsyncApiConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_async_api_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::AsyncApiConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AsyncApiConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_async_api_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::AsyncApiConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `AsyncApiConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_async_api_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::AsyncApiConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `spec` field from a `AsyncApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_async_api_config_spec(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::AsyncApiConfig, _>(handle, |obj| match &obj.spec {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `AuthorizationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_authorization_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::AuthorizationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `AuthorizationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::AuthorizationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AuthorizationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::AuthorizationConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `required_roles` field from a `AuthorizationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_required_roles(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::AuthorizationConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.required_roles) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `required_scopes` field from a `AuthorizationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_required_scopes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::AuthorizationConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.required_scopes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `required_permissions` field from a `AuthorizationConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_required_permissions(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::AuthorizationConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.required_permissions) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `require_all` field from a `AuthorizationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_require_all(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::AuthorizationConfig, _>(handle, |obj| obj.require_all as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_authorization_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::AuthorizationConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `BackgroundJobMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_background_job_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_job_metadata_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::BackgroundJobMetadata>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BackgroundJobMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_job_metadata_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::BackgroundJobMetadata, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BackgroundJobMetadata` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_job_metadata_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::BackgroundJobMetadata>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `BackgroundJobMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_job_metadata_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::BackgroundJobMetadata, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `request_id` field from a `BackgroundJobMetadata`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_job_metadata_request_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::BackgroundJobMetadata, _>(handle, |obj| match &obj.request_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_job_metadata_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::BackgroundJobMetadata::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `BackgroundTaskConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_background_task_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::BackgroundTaskConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BackgroundTaskConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::BackgroundTaskConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BackgroundTaskConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::BackgroundTaskConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `BackgroundTaskConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::BackgroundTaskConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_queue_size` field from a `BackgroundTaskConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_max_queue_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::BackgroundTaskConfig, _>(handle, |obj| obj.max_queue_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_concurrent_tasks` field from a `BackgroundTaskConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_max_concurrent_tasks(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::BackgroundTaskConfig, _>(handle, |obj| obj.max_concurrent_tasks);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `drain_timeout_secs` field from a `BackgroundTaskConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_drain_timeout_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::BackgroundTaskConfig, _>(handle, |obj| obj.drain_timeout_secs);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_background_task_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::BackgroundTaskConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `CompressionConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_compression_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::CompressionConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CompressionConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::CompressionConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CompressionConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::CompressionConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `gzip` field from a `CompressionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_gzip(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::CompressionConfig, _>(handle, |obj| obj.gzip as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `brotli` field from a `CompressionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_brotli(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::CompressionConfig, _>(handle, |obj| obj.brotli as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `min_size` field from a `CompressionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_min_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::CompressionConfig, _>(handle, |obj| obj.min_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `quality` field from a `CompressionConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_quality(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::CompressionConfig, _>(handle, |obj| obj.quality);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_compression_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::CompressionConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `ContactInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_contact_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_contact_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::ContactInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ContactInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_contact_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::ContactInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ContactInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_contact_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ContactInfo>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `ContactInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_contact_info_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ContactInfo, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `email` field from a `ContactInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_contact_info_email(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ContactInfo, _>(handle, |obj| match &obj.email {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `url` field from a `ContactInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_contact_info_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ContactInfo, _>(handle, |obj| match &obj.url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CorsConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_cors_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::CorsConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CorsConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::CorsConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CorsConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::CorsConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `allowed_origins` field from a `CorsConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_allowed_origins(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::CorsConfig, _>(handle, |obj| match serde_json::to_string(&obj.allowed_origins) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `allowed_methods` field from a `CorsConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_allowed_methods(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::CorsConfig, _>(handle, |obj| match serde_json::to_string(&obj.allowed_methods) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `allowed_headers` field from a `CorsConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_allowed_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::CorsConfig, _>(handle, |obj| match serde_json::to_string(&obj.allowed_headers) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `expose_headers` field from a `CorsConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_expose_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::CorsConfig, _>(handle, |obj| match &obj.expose_headers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `max_age` field from a `CorsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_max_age(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::CorsConfig, _>(handle, |obj| match &obj.max_age {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_age` field on a `CorsConfig` is `Some`.
///
/// `spikard_cors_config_max_age` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_has_max_age(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::CorsConfig, _>(handle, |obj| obj.max_age.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `allow_credentials` field from a `CorsConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_allow_credentials(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::CorsConfig, _>(handle, |obj| match &obj.allow_credentials {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `allow_credentials` field on a `CorsConfig` is `Some`.
///
/// `spikard_cors_config_allow_credentials` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_has_allow_credentials(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::CorsConfig, _>(handle, |obj| obj.allow_credentials.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the cached joined methods string for preflight responses
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_allowed_methods_joined(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::CorsConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::CorsConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.allowed_methods_joined();
        let result = result.to_owned();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Get the cached joined headers string for preflight responses
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_allowed_headers_joined(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::CorsConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::CorsConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.allowed_headers_joined();
        let result = result.to_owned();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Check if an origin is allowed (O(1) with wildcard, O(n) for exact match)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_is_origin_allowed(
    this: AlefHandle,
    origin: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::CorsConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<spikard::CorsConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if origin.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'origin'");
            return 0;
        }
        // SAFETY: null check above guarantees origin is a valid pointer; string is valid UTF-8 from caller.
        let origin_rs = match unsafe { CStr::from_ptr(origin) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'origin'");
                return 0;
            }
        };

        let result = obj.is_origin_allowed(&origin_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Check if a method is allowed (O(1) with wildcard, O(n) for exact match)
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_is_method_allowed(
    this: AlefHandle,
    method: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::CorsConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<spikard::CorsConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if method.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'method'");
            return 0;
        }
        // SAFETY: null check above guarantees method is a valid pointer; string is valid UTF-8 from caller.
        let method_rs = match unsafe { CStr::from_ptr(method) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'method'");
                return 0;
            }
        };

        let result = obj.is_method_allowed(&method_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_cors_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::CorsConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `DynamicSchemaConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_dynamic_schema_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::DynamicSchemaConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DynamicSchemaConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::DynamicSchemaConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DynamicSchemaConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::DynamicSchemaConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `introspection_enabled` field from a `DynamicSchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_introspection_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::DynamicSchemaConfig, _>(handle, |obj| obj.introspection_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_complexity` field from a `DynamicSchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_max_complexity(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::DynamicSchemaConfig, _>(handle, |obj| match &obj.max_complexity {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_complexity` field on a `DynamicSchemaConfig` is `Some`.
///
/// `spikard_dynamic_schema_config_max_complexity` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_has_max_complexity(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::DynamicSchemaConfig, _>(handle, |obj| obj.max_complexity.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_depth` field from a `DynamicSchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_max_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::DynamicSchemaConfig, _>(handle, |obj| match &obj.max_depth {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_depth` field on a `DynamicSchemaConfig` is `Some`.
///
/// `spikard_dynamic_schema_config_max_depth` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_has_max_depth(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::DynamicSchemaConfig, _>(handle, |obj| obj.max_depth.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `field_errors` field from a `DynamicSchemaConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_dynamic_schema_config_field_errors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::DynamicSchemaConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.field_errors) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FieldErrorSpec` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_field_error_spec_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_field_error_spec_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::FieldErrorSpec>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FieldErrorSpec` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_field_error_spec_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::FieldErrorSpec, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FieldErrorSpec` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_field_error_spec_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::FieldErrorSpec>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `path` field from a `FieldErrorSpec`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_field_error_spec_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::FieldErrorSpec, _>(handle, |obj| match CString::new(obj.path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message` field from a `FieldErrorSpec`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_field_error_spec_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::FieldErrorSpec, _>(handle, |obj| match CString::new(obj.message.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FullSchemaConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_full_schema_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::FullSchemaConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FullSchemaConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::FullSchemaConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FullSchemaConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::FullSchemaConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `introspection_enabled` field from a `FullSchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_introspection_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::FullSchemaConfig, _>(handle, |obj| obj.introspection_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `complexity_limit` field from a `FullSchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_complexity_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::FullSchemaConfig, _>(handle, |obj| match &obj.complexity_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `complexity_limit` field on a `FullSchemaConfig` is `Some`.
///
/// `spikard_full_schema_config_complexity_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_has_complexity_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::FullSchemaConfig, _>(handle, |obj| obj.complexity_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `depth_limit` field from a `FullSchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_depth_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::FullSchemaConfig, _>(handle, |obj| match &obj.depth_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `depth_limit` field on a `FullSchemaConfig` is `Some`.
///
/// `spikard_full_schema_config_depth_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_has_depth_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::FullSchemaConfig, _>(handle, |obj| obj.depth_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_full_schema_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::FullSchemaConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `GraphQLRouteConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::GraphQLRouteConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_new() -> AlefHandle {
    catch_ffi_panic(0, || {
        let result = spikard::GraphQLRouteConfig::new();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Set the HTTP path for the GraphQL endpoint
/// \param path The URL path (e.g., "/graphql", "/api/graphql")
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_path(
    this: AlefHandle,
    path: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::GraphQLRouteConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if path.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'path'");
            return 0;
        }
        // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
        let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'path'");
                return 0;
            }
        };

        let result = obj.path(path_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Set the HTTP method for the GraphQL endpoint
/// \param method The HTTP method (typically "POST")
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_method(
    this: AlefHandle,
    method: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::GraphQLRouteConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if method.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'method'");
            return 0;
        }
        // SAFETY: null check above guarantees method is a valid pointer; string is valid UTF-8 from caller.
        let method_rs = match unsafe { CStr::from_ptr(method) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'method'");
                return 0;
            }
        };

        let result = obj.method(method_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Enable or disable the GraphQL Playground UI
/// \param enable Whether to enable playground
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_enable_playground(this: AlefHandle, enable: i32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::GraphQLRouteConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let enable_rs = enable != 0;

        let result = obj.enable_playground(enable_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Set a custom description for documentation
/// \param description Documentation string
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_description(
    this: AlefHandle,
    description: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::GraphQLRouteConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if description.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'description'");
            return 0;
        }
        // SAFETY: null check above guarantees description is a valid pointer; string is valid UTF-8 from caller.
        let description_rs = match unsafe { CStr::from_ptr(description) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'description'");
                return 0;
            }
        };

        let result = obj.description(description_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Get the configured path
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_get_path(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::GraphQLRouteConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::GraphQLRouteConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.get_path();
        let result = result.to_owned();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Get the configured method
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_get_method(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::GraphQLRouteConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::GraphQLRouteConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.get_method();
        let result = result.to_owned();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Check if playground is enabled
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_is_playground_enabled(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::GraphQLRouteConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<spikard::GraphQLRouteConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_playground_enabled();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Get the description if set
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_get_description(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::GraphQLRouteConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::GraphQLRouteConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.get_description();
        let result = result.map(str::to_owned);
        match result {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_route_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::GraphQLRouteConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `GrpcConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_grpc_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::GrpcConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `GrpcConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::GrpcConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `GrpcConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::GrpcConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_message_size` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_max_message_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.max_message_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enable_compression` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_enable_compression(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.enable_compression as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `request_timeout` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_request_timeout(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| match &obj.request_timeout {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `request_timeout` field on a `GrpcConfig` is `Some`.
///
/// `spikard_grpc_config_request_timeout` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_has_request_timeout(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.request_timeout.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_concurrent_streams` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_max_concurrent_streams(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.max_concurrent_streams);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enable_keepalive` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_enable_keepalive(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.enable_keepalive as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `keepalive_interval` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_keepalive_interval(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.keepalive_interval);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `keepalive_timeout` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_keepalive_timeout(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.keepalive_timeout);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_stream_response_bytes` field from a `GrpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_max_stream_response_bytes(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::GrpcConfig, _>(handle, |obj| match &obj.max_stream_response_bytes {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_stream_response_bytes` field on a `GrpcConfig` is `Some`.
///
/// `spikard_grpc_config_max_stream_response_bytes` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_has_max_stream_response_bytes(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<spikard::GrpcConfig, _>(handle, |obj| obj.max_stream_response_bytes.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_grpc_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::GrpcConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `HandlerResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_handler_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::handler_trait::HandlerResult>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Create a `JsonRpcConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_json_rpc_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::JsonRpcConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `JsonRpcConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::JsonRpcConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `JsonRpcConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::JsonRpcConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `JsonRpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::JsonRpcConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `endpoint_path` field from a `JsonRpcConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_endpoint_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::JsonRpcConfig, _>(handle, |obj| match CString::new(obj.endpoint_path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `enable_batch` field from a `JsonRpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_enable_batch(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::JsonRpcConfig, _>(handle, |obj| obj.enable_batch as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_batch_size` field from a `JsonRpcConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_max_batch_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::JsonRpcConfig, _>(handle, |obj| obj.max_batch_size);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::JsonRpcConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `JsonRpcMethodInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_json_rpc_method_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_core::JsonRpcMethodInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `JsonRpcMethodInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `JsonRpcMethodInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_core::JsonRpcMethodInfo>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `method_name` field from a `JsonRpcMethodInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_method_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, |obj| {
            match CString::new(obj.method_name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `JsonRpcMethodInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `params_schema` field from a `JsonRpcMethodInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_params_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, |obj| match &obj.params_schema {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `result_schema` field from a `JsonRpcMethodInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_result_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, |obj| match &obj.result_schema {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `deprecated` field from a `JsonRpcMethodInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_deprecated(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, |obj| obj.deprecated as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tags` field from a `JsonRpcMethodInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_json_rpc_method_info_tags(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_core::JsonRpcMethodInfo, _>(handle, |obj| match serde_json::to_string(&obj.tags) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `JwtAuthConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_jwt_auth_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::JwtAuthConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `JwtAuthConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::JwtAuthConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `JwtAuthConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::JwtAuthConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `JwtAuthConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `secret` field from a `JwtAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_secret(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| match &obj.secret {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `public_key` field from a `JwtAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_public_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| match &obj.public_key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `algorithm` field from a `JwtAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_algorithm(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| match CString::new(obj.algorithm.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `audience` field from a `JwtAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_audience(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| match &obj.audience {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `issuer` field from a `JwtAuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_issuer(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| match &obj.issuer {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `leeway` field from a `JwtAuthConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_auth_config_leeway(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::JwtAuthConfig, _>(handle, |obj| obj.leeway);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `JwtConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_jwt_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::JwtConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `JwtConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::JwtConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `JwtConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::JwtConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `secret` field from a `JwtConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_secret(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtConfig, _>(handle, |obj| match CString::new(obj.secret.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `algorithm` field from a `JwtConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_algorithm(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::JwtConfig, _>(handle, |obj| match CString::new(obj.algorithm.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `audience` field from a `JwtConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_audience(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtConfig, _>(handle, |obj| match &obj.audience {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `issuer` field from a `JwtConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_issuer(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::JwtConfig, _>(handle, |obj| match &obj.issuer {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `leeway` field from a `JwtConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_jwt_config_leeway(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::JwtConfig, _>(handle, |obj| obj.leeway);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LicenseInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_license_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_license_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::LicenseInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LicenseInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_license_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::LicenseInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LicenseInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_license_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::LicenseInfo>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `LicenseInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_license_info_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::LicenseInfo, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `url` field from a `LicenseInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_license_info_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::LicenseInfo, _>(handle, |obj| match &obj.url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LifecycleHookRef` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_lifecycle_hook_ref_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::LifecycleHookRef>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LifecycleHookRef` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::LifecycleHookRef, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LifecycleHookRef` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::LifecycleHookRef>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `LifecycleHookRef`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::LifecycleHookRef, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `handler` field from a `LifecycleHookRef`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_handler(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::LifecycleHookRef, _>(handle, |obj| match CString::new(obj.handler.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `dependencies` field from a `LifecycleHookRef`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_dependencies(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::LifecycleHookRef, _>(handle, |obj| match serde_json::to_string(&obj.dependencies) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `config` field from a `LifecycleHookRef`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_config(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::LifecycleHookRef, _>(handle, |obj| match &obj.config {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `order` field from a `LifecycleHookRef`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_order(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::LifecycleHookRef, _>(handle, |obj| match &obj.order {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `order` field on a `LifecycleHookRef` is `Some`.
///
/// `spikard_lifecycle_hook_ref_order` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_ref_has_order(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::LifecycleHookRef, _>(handle, |obj| obj.order.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `LifecycleHooksConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_lifecycle_hooks_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::LifecycleHooksConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LifecycleHooksConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::LifecycleHooksConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LifecycleHooksConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::LifecycleHooksConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `on_request` field from a `LifecycleHooksConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_on_request(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::LifecycleHooksConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.on_request) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `pre_validation` field from a `LifecycleHooksConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_pre_validation(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::LifecycleHooksConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.pre_validation) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `pre_handler` field from a `LifecycleHooksConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_pre_handler(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::LifecycleHooksConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.pre_handler) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `on_response` field from a `LifecycleHooksConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_on_response(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::LifecycleHooksConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.on_response) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `on_error` field from a `LifecycleHooksConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hooks_config_on_error(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::LifecycleHooksConfig, _>(handle, |obj| match serde_json::to_string(&obj.on_error) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OpenApiConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_open_api_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::OpenApiConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OpenApiConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::OpenApiConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OpenApiConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::OpenApiConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `OpenApiConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `title` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match CString::new(obj.title.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `version` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match CString::new(obj.version.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `swagger_ui_path` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_swagger_ui_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| {
            match CString::new(obj.swagger_ui_path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `redoc_path` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_redoc_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match CString::new(obj.redoc_path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `openapi_json_path` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_openapi_json_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| {
            match CString::new(obj.openapi_json_path.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `contact` field from a `OpenApiConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_contact_info_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_contact(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match &obj.contact {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `license` field from a `OpenApiConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_license_info_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_license(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match &obj.license {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `servers` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_servers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::OpenApiConfig, _>(handle, |obj| match serde_json::to_string(&obj.servers) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `security_schemes` field from a `OpenApiConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_security_schemes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::OpenApiConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.security_schemes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_open_api_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::OpenApiConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `ParseRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::asyncapi::ParseRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `spec` field from a `ParseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_request_spec(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::asyncapi::ParseRequest, _>(handle, |obj| {
                match serde_json::to_string(&obj.spec) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ParseResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_parse_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::ParseResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ParseResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::ParseResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ParseResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ParseResult>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `spec_version` field from a `ParseResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_spec_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ParseResult, _>(handle, |obj| {
            match CString::new(obj.spec_version.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `title` field from a `ParseResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParseResult, _>(handle, |obj| match CString::new(obj.title.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `api_version` field from a `ParseResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_api_version(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ParseResult, _>(handle, |obj| {
            match CString::new(obj.api_version.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `channels` field from a `ParseResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_channels(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParseResult, _>(handle, |obj| match serde_json::to_string(&obj.channels) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `operations` field from a `ParseResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_operations(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParseResult, _>(handle, |obj| match serde_json::to_string(&obj.operations) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `messages` field from a `ParseResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parse_result_messages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParseResult, _>(handle, |obj| match serde_json::to_string(&obj.messages) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ParsedChannel` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_parsed_channel_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::ParsedChannel>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ParsedChannel` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::ParsedChannel, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ParsedChannel` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ParsedChannel>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `ParsedChannel`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParsedChannel, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `address` field from a `ParsedChannel`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_address(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParsedChannel, _>(handle, |obj| match CString::new(obj.address.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `messages` field from a `ParsedChannel`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_messages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParsedChannel, _>(handle, |obj| match serde_json::to_string(&obj.messages) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `bindings` field from a `ParsedChannel`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_channel_bindings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ParsedChannel, _>(handle, |obj| match &obj.bindings {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ParsedMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_parsed_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::ParsedMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ParsedMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::ParsedMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ParsedMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ParsedMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `ParsedMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParsedMessage, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `schema` field from a `ParsedMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_message_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ParsedMessage, _>(handle, |obj| match &obj.schema {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ParsedOperation` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_parsed_operation_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_operation_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::ParsedOperation>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ParsedOperation` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_operation_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::ParsedOperation, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ParsedOperation` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_operation_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ParsedOperation>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `ParsedOperation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_operation_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParsedOperation, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `action` field from a `ParsedOperation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_operation_action(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ParsedOperation, _>(handle, |obj| match CString::new(obj.action.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `channel` field from a `ParsedOperation`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_parsed_operation_channel(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ParsedOperation, _>(handle, |obj| {
            match CString::new(obj.channel.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ProblemDetails` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_problem_details_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_core::ProblemDetails>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Free a `ProblemDetails` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_core::ProblemDetails>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `type_uri` field from a `ProblemDetails`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_type_uri(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::ProblemDetails, _>(handle, |obj| {
            match CString::new(obj.type_uri.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `title` field from a `ProblemDetails`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_core::ProblemDetails, _>(handle, |obj| match CString::new(obj.title.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `status` field from a `ProblemDetails`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_status(handle: AlefHandle) -> u16 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard_core::ProblemDetails, _>(handle, |obj| obj.status);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `detail` field from a `ProblemDetails`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_detail(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::ProblemDetails, _>(handle, |obj| match &obj.detail {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `instance` field from a `ProblemDetails`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_instance(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::ProblemDetails, _>(handle, |obj| match &obj.instance {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `extensions` field from a `ProblemDetails`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_extensions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_core::ProblemDetails, _>(handle, |obj| {
            match serde_json::to_string(&obj.extensions) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard_core::ProblemDetails::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Set the detail field
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_with_detail(
    this: AlefHandle,
    detail: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard_core::ProblemDetails>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if detail.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'detail'");
            return 0;
        }
        // SAFETY: null check above guarantees detail is a valid pointer; string is valid UTF-8 from caller.
        let detail_rs = match unsafe { CStr::from_ptr(detail) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'detail'");
                return 0;
            }
        };

        let result = obj.with_detail(detail_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Set the instance field
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_with_instance(
    this: AlefHandle,
    instance: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard_core::ProblemDetails>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if instance.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'instance'");
            return 0;
        }
        // SAFETY: null check above guarantees instance is a valid pointer; string is valid UTF-8 from caller.
        let instance_rs = match unsafe { CStr::from_ptr(instance) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'instance'");
                return 0;
            }
        };

        let result = obj.with_instance(instance_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a not found error
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_not_found(detail: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if detail.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'detail'");
            return 0;
        }
        // SAFETY: null check above guarantees detail is a valid pointer; string is valid UTF-8 from caller.
        let detail_rs = match unsafe { CStr::from_ptr(detail) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'detail'");
                return 0;
            }
        };

        let result = spikard_core::ProblemDetails::not_found(detail_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a method not allowed error
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_method_not_allowed(detail: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if detail.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'detail'");
            return 0;
        }
        // SAFETY: null check above guarantees detail is a valid pointer; string is valid UTF-8 from caller.
        let detail_rs = match unsafe { CStr::from_ptr(detail) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'detail'");
                return 0;
            }
        };

        let result = spikard_core::ProblemDetails::method_not_allowed(detail_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create an internal server error
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_internal_server_error(detail: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if detail.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'detail'");
            return 0;
        }
        // SAFETY: null check above guarantees detail is a valid pointer; string is valid UTF-8 from caller.
        let detail_rs = match unsafe { CStr::from_ptr(detail) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'detail'");
                return 0;
            }
        };

        let result = spikard_core::ProblemDetails::internal_server_error(detail_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a bad request error
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_bad_request(detail: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if detail.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'detail'");
            return 0;
        }
        // SAFETY: null check above guarantees detail is a valid pointer; string is valid UTF-8 from caller.
        let detail_rs = match unsafe { CStr::from_ptr(detail) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'detail'");
                return 0;
            }
        };

        let result = spikard_core::ProblemDetails::bad_request(detail_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Serialize to JSON string
/// \note Returns an error if the serialization fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_to_json(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard_core::ProblemDetails>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard_core::ProblemDetails>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.to_json();
        match result {
            Ok(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Serialize to pretty JSON string
/// \note Returns an error if the serialization fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_problem_details_to_json_pretty(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard_core::ProblemDetails>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard_core::ProblemDetails>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.to_json_pretty();
        match result {
            Ok(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Create a `QueryMutationConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_query_mutation_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::QueryMutationConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `QueryMutationConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::QueryMutationConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `QueryMutationConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::QueryMutationConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `introspection_enabled` field from a `QueryMutationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_introspection_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::QueryMutationConfig, _>(handle, |obj| obj.introspection_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `complexity_limit` field from a `QueryMutationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_complexity_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::QueryMutationConfig, _>(handle, |obj| match &obj.complexity_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `complexity_limit` field on a `QueryMutationConfig` is `Some`.
///
/// `spikard_query_mutation_config_complexity_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_has_complexity_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<spikard::QueryMutationConfig, _>(handle, |obj| obj.complexity_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `depth_limit` field from a `QueryMutationConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_depth_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::QueryMutationConfig, _>(handle, |obj| match &obj.depth_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `depth_limit` field on a `QueryMutationConfig` is `Some`.
///
/// `spikard_query_mutation_config_depth_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_has_depth_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::QueryMutationConfig, _>(handle, |obj| obj.depth_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_mutation_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::QueryMutationConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `QueryOnlyConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_query_only_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::QueryOnlyConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `QueryOnlyConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::QueryOnlyConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `QueryOnlyConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::QueryOnlyConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `introspection_enabled` field from a `QueryOnlyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_introspection_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::QueryOnlyConfig, _>(handle, |obj| obj.introspection_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `complexity_limit` field from a `QueryOnlyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_complexity_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::QueryOnlyConfig, _>(handle, |obj| match &obj.complexity_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `complexity_limit` field on a `QueryOnlyConfig` is `Some`.
///
/// `spikard_query_only_config_complexity_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_has_complexity_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::QueryOnlyConfig, _>(handle, |obj| obj.complexity_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `depth_limit` field from a `QueryOnlyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_depth_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::QueryOnlyConfig, _>(handle, |obj| match &obj.depth_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `depth_limit` field on a `QueryOnlyConfig` is `Some`.
///
/// `spikard_query_only_config_depth_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_has_depth_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::QueryOnlyConfig, _>(handle, |obj| obj.depth_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_query_only_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::QueryOnlyConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `RateLimitConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_rate_limit_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::RateLimitConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RateLimitConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::RateLimitConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RateLimitConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::RateLimitConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `per_second` field from a `RateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_per_second(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::RateLimitConfig, _>(handle, |obj| obj.per_second);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `burst` field from a `RateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_burst(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::RateLimitConfig, _>(handle, |obj| obj.burst);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `ip_based` field from a `RateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_ip_based(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::RateLimitConfig, _>(handle, |obj| obj.ip_based as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_rate_limit_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::RateLimitConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `RequestData` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_request_data_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::handler_trait::RequestData>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Create a `RequestIdConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_request_id_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_request_id_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::RequestIdConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RequestIdConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_request_id_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::RequestIdConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RequestIdConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_request_id_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::RequestIdConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `RequestIdConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_request_id_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::RequestIdConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Response` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::Response>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Response` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::Response, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Response` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::Response>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `Response`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::Response, _>(handle, |obj| match &obj.content {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `status_code` field from a `Response`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_status_code(handle: AlefHandle) -> u16 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::Response, _>(handle, |obj| obj.status_code);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `headers` field from a `Response`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::Response, _>(handle, |obj| match serde_json::to_string(&obj.headers) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Set a header
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_set_header(
    this: AlefHandle,
    key: *const std::ffi::c_char,
    value: *const std::ffi::c_char,
) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::Response>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return;
        }
        let obj_ptr = match locked_handle_ptr::<spikard::Response>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        if key.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'key'");
            return;
        }
        // SAFETY: null check above guarantees key is a valid pointer; string is valid UTF-8 from caller.
        let key_rs = match unsafe { CStr::from_ptr(key) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'key'");
                return;
            }
        };

        if value.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'value'");
            return;
        }
        // SAFETY: null check above guarantees value is a valid pointer; string is valid UTF-8 from caller.
        let value_rs = match unsafe { CStr::from_ptr(value) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'value'");
                return;
            }
        };

        obj.set_header(key_rs, value_rs)
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Set a cookie in the response
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[allow(clippy::too_many_arguments)]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_set_cookie(
    this: AlefHandle,
    key: *const std::ffi::c_char,
    value: *const std::ffi::c_char,
    secure: i32,
    http_only: i32,
    max_age: i64,
    domain: *const std::ffi::c_char,
    path: *const std::ffi::c_char,
    same_site: *const std::ffi::c_char,
) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::Response>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return;
        }
        let obj_ptr = match locked_handle_ptr::<spikard::Response>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        if key.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'key'");
            return;
        }
        // SAFETY: null check above guarantees key is a valid pointer; string is valid UTF-8 from caller.
        let key_rs = match unsafe { CStr::from_ptr(key) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'key'");
                return;
            }
        };

        if value.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'value'");
            return;
        }
        // SAFETY: null check above guarantees value is a valid pointer; string is valid UTF-8 from caller.
        let value_rs = match unsafe { CStr::from_ptr(value) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'value'");
                return;
            }
        };

        let secure_rs = secure != 0;

        let http_only_rs = http_only != 0;

        // Optional numeric primitive: max value of type = None
        let max_age_rs = if max_age == i64::MAX { None } else { Some(max_age) };

        let domain_rs = if domain.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees domain is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(domain) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'domain'");
                    return;
                }
            }
        };

        let path_rs = if path.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(path) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'path'");
                    return;
                }
            }
        };

        let same_site_rs = if same_site.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees same_site is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(same_site) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'same_site'");
                    return;
                }
            }
        };

        obj.set_cookie(
            key_rs,
            value_rs,
            secure_rs,
            http_only_rs,
            max_age_rs,
            domain_rs,
            path_rs,
            same_site_rs,
        )
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_response_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::Response::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `RouteBuilder` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::RouteBuilder>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_new(method: i32, path: *const std::ffi::c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        let method_rs = match method_from_i32_rs(method) {
            Some(v) => v,
            None => {
                set_last_error(1, "invalid discriminant for Method");
                return 0;
            }
        };
        let path_rs = if path.is_null() {
            String::new()
        } else {
            // SAFETY: null check above guarantees path is a valid pointer.
            unsafe { std::ffi::CStr::from_ptr(path) }.to_string_lossy().into_owned()
        };
        let result = spikard::RouteBuilder::new(method_rs, path_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Assign an explicit handler name.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_handler_name(
    this: AlefHandle,
    name: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = obj.handler_name(name_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Provide a raw JSON schema for the request body.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_request_schema_json(
    this: AlefHandle,
    schema: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if schema.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'schema'");
            return 0;
        }
        // SAFETY: null check above guarantees schema is a valid pointer; string is valid UTF-8 from caller.
        let schema_rs_str = match unsafe { CStr::from_ptr(schema) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'schema'");
                return 0;
            }
        };
        let schema_rs = match serde_json::from_str(schema_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = obj.request_schema_json(schema_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Provide a raw JSON schema for the response body.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_response_schema_json(
    this: AlefHandle,
    schema: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if schema.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'schema'");
            return 0;
        }
        // SAFETY: null check above guarantees schema is a valid pointer; string is valid UTF-8 from caller.
        let schema_rs_str = match unsafe { CStr::from_ptr(schema) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'schema'");
                return 0;
            }
        };
        let schema_rs = match serde_json::from_str(schema_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = obj.response_schema_json(schema_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Provide a raw JSON schema for request parameters.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_params_schema_json(
    this: AlefHandle,
    schema: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if schema.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'schema'");
            return 0;
        }
        // SAFETY: null check above guarantees schema is a valid pointer; string is valid UTF-8 from caller.
        let schema_rs_str = match unsafe { CStr::from_ptr(schema) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'schema'");
                return 0;
            }
        };
        let schema_rs = match serde_json::from_str(schema_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = obj.params_schema_json(schema_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Provide multipart file parameter configuration.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_file_params_json(
    this: AlefHandle,
    schema: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if schema.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'schema'");
            return 0;
        }
        // SAFETY: null check above guarantees schema is a valid pointer; string is valid UTF-8 from caller.
        let schema_rs_str = match unsafe { CStr::from_ptr(schema) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'schema'");
                return 0;
            }
        };
        let schema_rs = match serde_json::from_str(schema_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = obj.file_params_json(schema_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a CORS configuration for this route.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_cors(this: AlefHandle, cors: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: cors,
            expected_type: std::any::TypeId::of::<spikard::CorsConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if cors == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'cors'",
            );
            return 0;
        }
        let cors_rs_ptr = match locked_handle_ptr::<spikard::CorsConfig>(&mut __alef_guards, cors) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let cors_rs = unsafe { &*cors_rs_ptr }.clone();

        let result = obj.cors(cors_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a compression configuration for this route.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_compression(this: AlefHandle, compression: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: compression,
            expected_type: std::any::TypeId::of::<spikard::CompressionConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if compression == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'compression'",
            );
            return 0;
        }
        let compression_rs_ptr = match locked_handle_ptr::<spikard::CompressionConfig>(&mut __alef_guards, compression)
        {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let compression_rs = unsafe { &*compression_rs_ptr }.clone();

        let result = obj.compression(compression_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a per-route maximum request body size in bytes, overriding the server-global default.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_body_limit(this: AlefHandle, max_bytes: usize) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let max_bytes_rs = max_bytes;

        let result = obj.body_limit(max_bytes_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a per-route request timeout in seconds, overriding the server-global default.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_request_timeout(this: AlefHandle, seconds: u64) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let seconds_rs = seconds;

        let result = obj.request_timeout(seconds_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a per-route rate limiting configuration, overriding the server-global default.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_rate_limit(this: AlefHandle, rate_limit: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: rate_limit,
            expected_type: std::any::TypeId::of::<spikard::RateLimitConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if rate_limit == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'rate_limit'",
            );
            return 0;
        }
        let rate_limit_rs_ptr = match locked_handle_ptr::<spikard::RateLimitConfig>(&mut __alef_guards, rate_limit) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let rate_limit_rs = unsafe { &*rate_limit_rs_ptr }.clone();

        let result = obj.rate_limit(rate_limit_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Force per-route request-id generation on or off, overriding the server-global default.
///
/// Takes a plain `bool` rather than `RequestIdConfig` directly: the wire/metadata type had
/// to become a struct to represent `{"enabled": false}` (see `RequestIdConfig`'s docs), but
/// this builder is the ergonomic call site (`.request_id(true)`), so it keeps accepting a bool
/// and wraps it into `RequestIdConfig` internally in `Self.into_metadata`.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_request_id(this: AlefHandle, enabled: i32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let enabled_rs = enabled != 0;

        let result = obj.request_id(enabled_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Require JWT authentication for this route.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_jwt_auth(this: AlefHandle, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<spikard::JwtAuthConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<spikard::JwtAuthConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr }.clone();

        let result = obj.jwt_auth(config_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Require API key authentication for this route.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_api_key_auth(this: AlefHandle, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<spikard::ApiKeyAuthConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<spikard::ApiKeyAuthConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr }.clone();

        let result = obj.api_key_auth(config_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a roles/scopes/permissions authorization requirement for this route.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_authorization(this: AlefHandle, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<spikard::AuthorizationConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<spikard::AuthorizationConfig>(&mut __alef_guards, config) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr }.clone();

        let result = obj.authorization(config_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Select registered lifecycle hooks to run for this route.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_lifecycle_hooks(this: AlefHandle, hooks: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: hooks,
            expected_type: std::any::TypeId::of::<spikard::LifecycleHooksConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if hooks == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'hooks'",
            );
            return 0;
        }
        let hooks_rs_ptr = match locked_handle_ptr::<spikard::LifecycleHooksConfig>(&mut __alef_guards, hooks) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let hooks_rs = unsafe { &*hooks_rs_ptr }.clone();

        let result = obj.lifecycle_hooks(hooks_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Expose this route as a JSON-RPC method.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_jsonrpc_method(this: AlefHandle, info: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: info,
            expected_type: std::any::TypeId::of::<spikard_core::JsonRpcMethodInfo>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if info == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'info'",
            );
            return 0;
        }
        let info_rs_ptr = match locked_handle_ptr::<spikard_core::JsonRpcMethodInfo>(&mut __alef_guards, info) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let info_rs = unsafe { &*info_rs_ptr }.clone();

        let result = obj.jsonrpc_method(info_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Attach a literal `OpenRPC` method spec document for this route, overriding
/// auto-derivation from the `Self.jsonrpc_method` metadata when present.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_openrpc_spec(
    this: AlefHandle,
    spec: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if spec.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'spec'");
            return 0;
        }
        // SAFETY: null check above guarantees spec is a valid pointer; string is valid UTF-8 from caller.
        let spec_rs_str = match unsafe { CStr::from_ptr(spec) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'spec'");
                return 0;
            }
        };
        let spec_rs = match serde_json::from_str(spec_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = obj.openrpc_spec(spec_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Mark the route as synchronous.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_sync(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let result = obj.sync();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Declare the dependency keys that must be resolved before this handler runs.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "di")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_route_builder_handler_dependencies(
    this: AlefHandle,
    dependencies: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::RouteBuilder>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if dependencies.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'dependencies'");
            return 0;
        }
        // SAFETY: null check above guarantees dependencies is a valid pointer; string is valid UTF-8 from caller.
        let dependencies_rs_str = match unsafe { CStr::from_ptr(dependencies) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'dependencies'");
                return 0;
            }
        };
        let dependencies_rs = match serde_json::from_str::<Vec<String>>(dependencies_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = obj.handler_dependencies(dependencies_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `SchemaConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_schema_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::SchemaConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SchemaConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::SchemaConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SchemaConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::SchemaConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `introspection_enabled` field from a `SchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_introspection_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::SchemaConfig, _>(handle, |obj| obj.introspection_enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `complexity_limit` field from a `SchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_complexity_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::SchemaConfig, _>(handle, |obj| match &obj.complexity_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `complexity_limit` field on a `SchemaConfig` is `Some`.
///
/// `spikard_schema_config_complexity_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_has_complexity_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::SchemaConfig, _>(handle, |obj| obj.complexity_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `depth_limit` field from a `SchemaConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_depth_limit(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::SchemaConfig, _>(handle, |obj| match &obj.depth_limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `depth_limit` field on a `SchemaConfig` is `Some`.
///
/// `spikard_schema_config_depth_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_has_depth_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::SchemaConfig, _>(handle, |obj| obj.depth_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::SchemaConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `ServerConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_server_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::ServerConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ServerConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::ServerConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ServerConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::ServerConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `host` field from a `ServerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_host(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match CString::new(obj.host.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `port` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_port(handle: AlefHandle) -> u16 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.port);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `workers` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_workers(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.workers);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enable_request_id` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_enable_request_id(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.enable_request_id as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_body_size` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_max_body_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.max_body_size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_body_size` field on a `ServerConfig` is `Some`.
///
/// `spikard_server_config_max_body_size` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_has_max_body_size(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.max_body_size.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `request_timeout` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_request_timeout(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.request_timeout {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `request_timeout` field on a `ServerConfig` is `Some`.
///
/// `spikard_server_config_request_timeout` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_has_request_timeout(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.request_timeout.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `compression` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_compression_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_compression(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.compression {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `rate_limit` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_rate_limit_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_rate_limit(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.rate_limit {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `jwt_auth` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_jwt_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_jwt_auth(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.jwt_auth {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `api_key_auth` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_api_key_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_api_key_auth(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.api_key_auth {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `static_files` field from a `ServerConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_static_files(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::ServerConfig, _>(handle, |obj| match serde_json::to_string(&obj.static_files) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `graceful_shutdown` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_graceful_shutdown(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.graceful_shutdown as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `shutdown_timeout` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_shutdown_timeout(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.shutdown_timeout);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `asyncapi` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_async_api_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_asyncapi(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.asyncapi {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `openapi` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_open_api_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_openapi(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.openapi {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `jsonrpc` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_json_rpc_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_jsonrpc(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.jsonrpc {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `grpc` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_grpc_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_grpc(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| match &obj.grpc {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `background_tasks` field from a `ServerConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `spikard_background_task_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_background_tasks(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<spikard::ServerConfig, _>(handle, |obj| match insert_handle(obj.background_tasks.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `enable_http_trace` field from a `ServerConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_enable_http_trace(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::ServerConfig, _>(handle, |obj| obj.enable_http_trace as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::ServerConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `ServerInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_server_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard_http::ServerInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ServerInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard_http::ServerInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ServerInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ServerInfo>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `url` field from a `ServerInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_info_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ServerInfo, _>(handle, |obj| match CString::new(obj.url.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `ServerInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_server_info_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ServerInfo, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `SseEvent` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_sse_event_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::SseEvent>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SseEvent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::SseEvent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SseEvent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::SseEvent>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `event_type` field from a `SseEvent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_event_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::SseEvent, _>(handle, |obj| match &obj.event_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `SseEvent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::SseEvent, _>(handle, |obj| match serde_json::to_string(&obj.data) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `id` field from a `SseEvent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::SseEvent, _>(handle, |obj| match &obj.id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `retry` field from a `SseEvent`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_retry(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::SseEvent, _>(handle, |obj| match &obj.retry {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `retry` field on a `SseEvent` is `Some`.
///
/// `spikard_sse_event_retry` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_has_retry(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::SseEvent, _>(handle, |obj| obj.retry.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Set the event ID for client-side reconnection support
///
/// Sets an ID that clients can use to resume from this point if they disconnect.
/// The client sends this ID back in the `Last-Event-ID` header when reconnecting.
/// \param id Unique identifier for this event
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use serde_json::json;
/// use spikard_http::sse::SseEvent;
///
/// let event = SseEvent::new(json!({"count": 1}))
///     .with_id("event-1");
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_with_id(this: AlefHandle, id: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::SseEvent>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        if id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'id'");
            return 0;
        }
        // SAFETY: null check above guarantees id is a valid pointer; string is valid UTF-8 from caller.
        let id_rs = match unsafe { CStr::from_ptr(id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'id'");
                return 0;
            }
        };

        let result = obj.with_id(id_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Set the retry timeout for client reconnection
///
/// Sets the time in milliseconds clients should wait before attempting to reconnect
/// if the connection is lost. The client browser will automatically handle reconnection.
/// \param retry_ms Retry timeout in milliseconds
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use serde_json::json;
/// use spikard_http::sse::SseEvent;
///
/// let event = SseEvent::new(json!({"data": "value"}))
///     .with_retry(5000); // Reconnect after 5 seconds
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_sse_event_with_retry(this: AlefHandle, retry_ms: u64) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<spikard::SseEvent>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let retry_ms_rs = retry_ms;

        let result = obj.with_retry(retry_ms_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `StaticFilesConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_static_files_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::StaticFilesConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StaticFilesConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::StaticFilesConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StaticFilesConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::StaticFilesConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `directory` field from a `StaticFilesConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_directory(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::StaticFilesConfig, _>(handle, |obj| match CString::new(obj.directory.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `route_prefix` field from a `StaticFilesConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_route_prefix(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::StaticFilesConfig, _>(handle, |obj| {
            match CString::new(obj.route_prefix.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `index_file` field from a `StaticFilesConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_index_file(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::StaticFilesConfig, _>(handle, |obj| obj.index_file as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_control` field from a `StaticFilesConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_static_files_config_cache_control(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::StaticFilesConfig, _>(handle, |obj| match &obj.cache_control {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(not(target_arch = "wasm32"))]
/// Free a `TestingSseEvent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_testing_sse_event_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::testing::SseEvent>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(not(target_arch = "wasm32"))]
/// Get the `data` field from a `TestingSseEvent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_testing_sse_event_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::testing::SseEvent, _>(handle, |obj| match CString::new(obj.data.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `UploadFile` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_upload_file_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::UploadFile>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `UploadFile` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `spikard` function.
/// The returned string must be freed with `spikard_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<spikard::UploadFile, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `UploadFile` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::UploadFile>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `filename` field from a `UploadFile`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard::UploadFile, _>(handle, |obj| match CString::new(obj.filename.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content_type` field from a `UploadFile`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_content_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::UploadFile, _>(handle, |obj| match &obj.content_type {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `size` field from a `UploadFile`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard::UploadFile, _>(handle, |obj| match &obj.size {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `size` field on a `UploadFile` is `Some`.
///
/// `spikard_upload_file_size` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_has_size(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<spikard::UploadFile, _>(handle, |obj| obj.size.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `content_encoding` field from a `UploadFile`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_content_encoding(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard::UploadFile, _>(handle, |obj| match &obj.content_encoding {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the raw file content as bytes.
///
/// This provides zero-copy access to the underlying buffer.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_as_bytes(
    this: AlefHandle,
    out_ptr: *mut *mut u8,
    out_len: *mut usize,
    out_cap: *mut usize,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if out_ptr.is_null() || out_len.is_null() || out_cap.is_null() {
            set_last_error(1, "Null out-param pointer for byte buffer return");
            return -1;
        }
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::UploadFile>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<spikard::UploadFile>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.as_bytes();
        // Copy through a `&[u8]` slice so the path works for every byte-ish return shape:
        // an owned `Vec<u8>` or `bytes::Bytes`, and equally a *borrowed* `&Bytes`/`&[u8]`
        // from an accessor like `fn as_bytes(&self) -> &Bytes`. `Vec::<u8>::from(result)`
        // handled only the owned cases -- there is no `From<&Bytes> for Vec<u8>`, so a
        // borrowing accessor failed to compile. Indexing with `[..]` derefs uniformly. The
        // slice is redundant for the owned shapes and load-bearing for the borrowed ones,
        // and this template cannot tell them apart, so the lint is allowed rather than the
        // expression narrowed.
        #[allow(clippy::redundant_slicing)]
        let buffer = Vec::<u8>::from(&result[..]).into_boxed_slice();
        let len = buffer.len();
        let ptr = Box::into_raw(buffer).cast::<u8>();
        // SAFETY: out_ptr/out_len/out_cap were null-checked above.
        unsafe {
            *out_ptr = ptr;
            *out_len = len;
            *out_cap = len;
        }
        0
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Read the file content as a UTF-8 string.
/// \note Returns an error if the content is not valid UTF-8.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_read_to_string(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::UploadFile>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::UploadFile>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.read_to_string();
        match result {
            Ok(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Get the content type, defaulting to "application/octet-stream".
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_upload_file_content_type_or_default(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<spikard::UploadFile>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<spikard::UploadFile>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.content_type_or_default();
        let result = result.to_owned();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Free a `ValidateRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validate_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ValidateRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `spec` field from a `ValidateRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validate_request_spec(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ValidateRequest, _>(handle, |obj| match serde_json::to_string(&obj.spec) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `channel` field from a `ValidateRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validate_request_channel(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ValidateRequest, _>(handle, |obj| {
            match CString::new(obj.channel.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `message` field from a `ValidateRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validate_request_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ValidateRequest, _>(handle, |obj| {
            match CString::new(obj.message.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `payload` field from a `ValidateRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validate_request_payload(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<spikard_http::ValidateRequest, _>(handle, |obj| match serde_json::to_string(&obj.payload) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a `ValidationResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validation_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard_http::ValidationResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `valid` field from a `ValidationResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validation_response_valid(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<spikard_http::ValidationResponse, _>(handle, |obj| obj.valid as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `errors` field from a `ValidationResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `spikard_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_validation_response_errors(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<spikard_http::ValidationResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.errors) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Convert an integer to a `LifecycleHookPhase` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_phase_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // OnRequest
            1 => 1, // PreValidation
            2 => 2, // PreHandler
            3 => 3, // OnResponse
            4 => 4, // OnError
            _ => {
                set_last_error(1, "Invalid LifecycleHookPhase variant");
                -1
            }
        }
    })
}

/// Convert a `LifecycleHookPhase` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_lifecycle_hook_phase_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "OnRequest" => 0,
            "PreValidation" => 1,
            "PreHandler" => 2,
            "OnResponse" => 3,
            "OnError" => 4,
            _ => {
                set_last_error(1, "Unknown LifecycleHookPhase variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `Method` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_method_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Get
            1 => 1, // Post
            2 => 2, // Put
            3 => 3, // Patch
            4 => 4, // Delete
            5 => 5, // Head
            6 => 6, // Options
            7 => 7, // Connect
            8 => 8, // Trace
            _ => {
                set_last_error(1, "Invalid Method variant");
                -1
            }
        }
    })
}

/// Convert a `Method` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_method_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Get" => 0,
            "Post" => 1,
            "Put" => 2,
            "Patch" => 3,
            "Delete" => 4,
            "Head" => 5,
            "Options" => 6,
            "Connect" => 7,
            "Trace" => 8,
            _ => {
                set_last_error(1, "Unknown Method variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `SecuritySchemeInfo` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_security_scheme_info_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Http
            1 => 1, // ApiKey
            _ => {
                set_last_error(1, "Invalid SecuritySchemeInfo variant");
                -1
            }
        }
    })
}

/// Convert a `SecuritySchemeInfo` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_security_scheme_info_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "http" => 0,
            "apiKey" => 1,
            _ => {
                set_last_error(1, "Unknown SecuritySchemeInfo variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `SnapshotError` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_snapshot_error_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // InvalidHeader
            1 => 1, // Decompression
            _ => {
                set_last_error(1, "Invalid SnapshotError variant");
                -1
            }
        }
    })
}

/// Convert a `SnapshotError` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_snapshot_error_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "InvalidHeader" => 0,
            "Decompression" => 1,
            _ => {
                set_last_error(1, "Unknown SnapshotError variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `WebSocketMessage` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_web_socket_message_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Binary
            2 => 2, // Close
            3 => 3, // Ping
            4 => 4, // Pong
            _ => {
                set_last_error(1, "Invalid WebSocketMessage variant");
                -1
            }
        }
    })
}

/// Convert a `WebSocketMessage` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_web_socket_message_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Text" => 0,
            "Binary" => 1,
            "Close" => 2,
            "Ping" => 3,
            "Pong" => 4,
            _ => {
                set_last_error(1, "Unknown WebSocketMessage variant");
                -1
            }
        }
    })
}

/// Create a `Method` from a JSON string. Returns 0 on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `spikard_method_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_method_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<spikard::Method>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Free a `Method` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_method_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<spikard::Method>(handle)
        {
            set_handle_error(&error);
        }
    })
}

// 16 MiB: tokio's ~2 MB default worker stack can overflow on a deep extraction
// future (a nested archive member, a multi-stage OCR pipeline), and a stack overflow
// aborts the process with SIGBUS instead of raising a catchable panic.
const FFI_RUNTIME_STACK_SIZE_BYTES: usize = 16 * 1024 * 1024;

fn get_ffi_runtime() -> &'static tokio::runtime::Runtime {
    use std::sync::OnceLock;
    static RUNTIME: OnceLock<tokio::runtime::Runtime> = OnceLock::new();
    RUNTIME.get_or_init(|| {
        tokio::runtime::Builder::new_multi_thread()
            .enable_all()
            .thread_stack_size(FFI_RUNTIME_STACK_SIZE_BYTES)
            .build()
            .expect("Failed to create tokio runtime")
    })
}

/// Create a schema configuration with all three root types.
///
/// This is a convenience function for fully-featured schemas.
/// \return A `FullSchemaConfig` with default settings
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_full() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::schema_full();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a schema configuration with Query and Mutation types.
///
/// This is a convenience function for schemas with queries and mutations but no subscriptions.
/// \return A `QueryMutationConfig` with default settings
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_query_mutation() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::schema_query_mutation();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a simple schema configuration with only Query type.
///
/// This is a convenience function for schemas that only have queries.
/// \return A `QueryOnlyConfig` with default settings
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_schema_query_only() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = spikard::schema_query_only();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the HTTP status code for the error pointed to by `err`.
/// Returns `0` if `err` is null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_error_status_code(err: *const spikard_graphql::error::GraphQLError) -> u16 {
    if err.is_null() {
        return 0;
    }
    // SAFETY: caller guarantees `err` points to a live `spikard_graphql::error::GraphQLError` value
    // allocated by this library, or is null (checked above).
    unsafe { (*err).status_code() }
}

/// Return whether the error pointed to by `err` is transient.
/// Returns `false` if `err` is null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_error_is_transient(err: *const spikard_graphql::error::GraphQLError) -> bool {
    if err.is_null() {
        return false;
    }
    // SAFETY: caller guarantees `err` points to a live `spikard_graphql::error::GraphQLError` value
    // allocated by this library, or is null (checked above).
    unsafe { (*err).is_transient() }
}

/// Return the machine-readable error category string for the error pointed
/// to by `err` as a heap-allocated, NUL-terminated C string.
/// The caller must free the returned pointer with `spikard_graph_ql_error_error_type_free`.
/// Returns a null pointer if `err` is null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_error_error_type(
    err: *const spikard_graphql::error::GraphQLError,
) -> *mut std::ffi::c_char {
    if err.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: caller guarantees `err` points to a live `spikard_graphql::error::GraphQLError` value
    // allocated by this library, or is null (checked above).
    let s = unsafe { (*err).error_type() };
    // SAFETY: `error_type()` returns a `'static str` containing no NUL bytes.
    std::ffi::CString::new(s)
        .map(|c| c.into_raw())
        .unwrap_or(std::ptr::null_mut())
}

/// Free a string previously returned by `spikard_graph_ql_error_error_type`.
/// Passing a null pointer is a no-op.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn spikard_graph_ql_error_error_type_free(ptr: *mut std::ffi::c_char) {
    if !ptr.is_null() {
        // SAFETY: `ptr` was allocated by `CString::into_raw` inside
        // `spikard_graph_ql_error_error_type` and is now being reclaimed by the matching
        // `CString::from_raw`.  Passing null is explicitly allowed.
        unsafe { drop(std::ffi::CString::from_raw(ptr)) };
    }
}
