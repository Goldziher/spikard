[package]
name = "spikard-php"
version = "0.15.6-rc.2"
edition = "2024"
license = "MIT"
description = "Rust-centric multi-language HTTP framework with polyglot bindings"
readme = false
keywords = ["http", "web", "framework", "polyglot", "rust"]
categories = ["text-processing"]

# `serde_json` and `tokio` are emitted unconditionally above so the manifest
# is stable across regens, but for umbrella crates with no async fns and no
# JSON-marshalled return types they are genuinely unused. The conditional
# `async-trait` / `futures-util` deps are similarly flagged when the
# umbrella has trait-bridge / streaming adapters configured but no actual
# async-trait callsite in the generated PHP shim.
[package.metadata.cargo-machete]
ignored = ["serde_json", "tokio"]

[lib]
crate-type = ["cdylib"]

[features]
extension-module = []

[dependencies]
ext-php-rs = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
spikard = { version = "0.15.6-rc.2", path = "../spikard", features = ["di"] }
spikard-core = { path = "../spikard-core", version = "0.15.6-rc.2" }
spikard-graphql = { path = "../spikard-graphql", version = "0.15.6-rc.2" }
spikard-http = { path = "../spikard-http", version = "0.15.6-rc.2" }
tokio = { version = "1", features = ["full"] }
