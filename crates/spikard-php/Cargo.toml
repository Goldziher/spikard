# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:271cdb3312774b316be0be3d7facce3549bf279f42338d77e5dc33aceb0d4546
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "spikard-php"
version = "0.17.0"
edition = "2024"
license = "MIT"
description = "Codegen-first polyglot web toolkit with a Rust core and bindings for 14 languages"
readme = false
keywords = ["framework", "http", "polyglot", "rust", "web"]
categories = []

# `ahash` and `futures-util` are conditionally included but not directly used in PHP code.
[package.metadata.cargo-machete]
ignored = ["tokio", "ahash"]

[lib]
crate-type = ["cdylib"]

[features]
extension-module = []
default = ["di"]
di = ["spikard/di"]

[dependencies]
axum = { default-features = false, version = "0.8" }
ext-php-rs = "0.15.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
spikard = { version = "0.17.0", path = "../spikard", features = ["di"] }
spikard-core = { path = "../spikard-core", version = "0.17.0" }
spikard-graphql = { path = "../spikard-graphql", version = "0.17.0" }
spikard-http = { path = "../spikard-http", version = "0.17.0" }
tokio = { version = "1", features = ["full"] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
