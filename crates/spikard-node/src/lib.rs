// This file is auto-generated by skif. DO NOT EDIT.
// Re-generate with: skif generate

use napi::*;
use napi_derive::napi;
use std::collections::HashMap;
use serde_json;
use spikard;
use std::sync::Arc;

static WORKER_POOL: std::sync::LazyLock<tokio::runtime::Runtime> = std::sync::LazyLock::new(|| {
    tokio::runtime::Builder::new_multi_thread()
        .enable_all()
        .build()
        .expect("Failed to create Tokio runtime")
});

#[derive(Clone)]
#[napi]
pub struct JsUploadFile {
    pub filename: String,
    #[napi(js_name = "contentType")]
    pub content_type: Option<String>,
    pub size: Option<i64>,
}

#[napi]
impl JsUploadFile {
    #[napi(constructor)]
    pub fn new(filename: String, content_type: Option<String>, size: Option<i64>) -> Self {
        Self { filename, content_type, size }
    }

    #[napi, js_name = "asBytes"]
    pub fn as_bytes(&self, ) -> Vec<u8> {
        todo!("call into core")
    }

    #[napi, js_name = "readToString"]
    pub fn read_to_string(&self, ) -> String {
        todo!("call into core")
    }

    #[napi, js_name = "contentTypeOrDefault"]
    pub fn content_type_or_default(&self, ) -> String {
        todo!("call into core")
    }

    #[napi]
    pub fn read(&self, buf: Vec<u8>) -> i64 {
        todo!("call into core")
    }

    #[napi]
    pub fn seek(&self, pos: String) -> i64 {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsCorsConfig {
    #[napi(js_name = "allowedOrigins")]
    pub allowed_origins: Vec<String>,
    #[napi(js_name = "allowedMethods")]
    pub allowed_methods: Vec<String>,
    #[napi(js_name = "allowedHeaders")]
    pub allowed_headers: Vec<String>,
    #[napi(js_name = "exposeHeaders")]
    pub expose_headers: Option<Vec<String>>,
    #[napi(js_name = "maxAge")]
    pub max_age: Option<u32>,
    #[napi(js_name = "allowCredentials")]
    pub allow_credentials: Option<bool>,
    #[napi(js_name = "methodsJoinedCache")]
    pub methods_joined_cache: String,
    #[napi(js_name = "headersJoinedCache")]
    pub headers_joined_cache: String,
}

#[napi]
impl JsCorsConfig {
    #[napi(constructor)]
    pub fn new(allowed_origins: Vec<String>, allowed_methods: Vec<String>, allowed_headers: Vec<String>, expose_headers: Option<Vec<String>>, max_age: Option<u32>, allow_credentials: Option<bool>, methods_joined_cache: String, headers_joined_cache: String) -> Self {
        Self { allowed_origins, allowed_methods, allowed_headers, expose_headers, max_age, allow_credentials, methods_joined_cache, headers_joined_cache }
    }

    #[napi, js_name = "allowedMethodsJoined"]
    pub fn allowed_methods_joined(&self, ) -> String {
        todo!("call into core")
    }

    #[napi, js_name = "allowedHeadersJoined"]
    pub fn allowed_headers_joined(&self, ) -> String {
        todo!("call into core")
    }

    #[napi, js_name = "isOriginAllowed"]
    pub fn is_origin_allowed(&self, origin: String) -> bool {
        todo!("call into core")
    }

    #[napi, js_name = "isMethodAllowed"]
    pub fn is_method_allowed(&self, method: String) -> bool {
        todo!("call into core")
    }

    #[napi, js_name = "areHeadersAllowed"]
    pub fn are_headers_allowed(&self, requested: Vec<String>) -> bool {
        todo!("call into core")
    }

    #[napi]
    pub fn default() -> String {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsCompressionConfig {
    pub gzip: bool,
    pub brotli: bool,
    #[napi(js_name = "minSize")]
    pub min_size: i64,
    pub quality: u32,
}

#[napi]
impl JsCompressionConfig {
    #[napi(constructor)]
    pub fn new(gzip: bool, brotli: bool, min_size: i64, quality: u32) -> Self {
        Self { gzip, brotli, min_size, quality }
    }

    #[napi]
    pub fn default() -> String {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsRateLimitConfig {
    #[napi(js_name = "perSecond")]
    pub per_second: i64,
    pub burst: u32,
    #[napi(js_name = "ipBased")]
    pub ip_based: bool,
}

#[napi]
impl JsRateLimitConfig {
    #[napi(constructor)]
    pub fn new(per_second: i64, burst: u32, ip_based: bool) -> Self {
        Self { per_second, burst, ip_based }
    }

    #[napi]
    pub fn default() -> String {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsLifecycleHooks {
    inner: std::sync::Arc<spikard::LifecycleHooks>,
}

#[napi]
impl JsLifecycleHooks {
    #[napi, js_name = "isEmpty"]
    pub fn is_empty(&self, ) -> bool {
        let result = self.inner.is_empty();
        Ok(bool::from(result))
    }

    #[napi, js_name = "addOnRequest"]
    pub fn add_on_request(&self, hook: String) -> () {
        let result = self.inner.add_on_request(hook);
        Ok(()::from(result))
    }

    #[napi, js_name = "addPreValidation"]
    pub fn add_pre_validation(&self, hook: String) -> () {
        let result = self.inner.add_pre_validation(hook);
        Ok(()::from(result))
    }

    #[napi, js_name = "addPreHandler"]
    pub fn add_pre_handler(&self, hook: String) -> () {
        let result = self.inner.add_pre_handler(hook);
        Ok(()::from(result))
    }

    #[napi, js_name = "addOnResponse"]
    pub fn add_on_response(&self, hook: String) -> () {
        let result = self.inner.add_on_response(hook);
        Ok(()::from(result))
    }

    #[napi, js_name = "addOnError"]
    pub fn add_on_error(&self, hook: String) -> () {
        let result = self.inner.add_on_error(hook);
        Ok(()::from(result))
    }

    #[napi, js_name = "executeOnRequest"]
    pub async fn execute_on_request(&self, req: String) -> Result<String> {
        let result = self.inner.execute_on_request(req).await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi, js_name = "executePreValidation"]
    pub async fn execute_pre_validation(&self, req: String) -> Result<String> {
        let result = self.inner.execute_pre_validation(req).await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi, js_name = "executePreHandler"]
    pub async fn execute_pre_handler(&self, req: String) -> Result<String> {
        let result = self.inner.execute_pre_handler(req).await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi, js_name = "executeOnResponse"]
    pub async fn execute_on_response(&self, resp: String) -> Result<String> {
        let result = self.inner.execute_on_response(resp).await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi, js_name = "executeOnError"]
    pub async fn execute_on_error(&self, resp: String) -> Result<String> {
        let result = self.inner.execute_on_error(resp).await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi]
    pub fn builder() -> JsLifecycleHooksBuilder {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsLifecycleHooksBuilder {
    inner: std::sync::Arc<spikard::LifecycleHooksBuilder>,
}

#[napi]
impl JsLifecycleHooksBuilder {
    #[napi, js_name = "onRequest"]
    pub fn on_request(&self, hook: String) -> String {
        let result = self.inner.on_request(hook);
        Ok(String::from(result))
    }

    #[napi, js_name = "preValidation"]
    pub fn pre_validation(&self, hook: String) -> String {
        let result = self.inner.pre_validation(hook);
        Ok(String::from(result))
    }

    #[napi, js_name = "preHandler"]
    pub fn pre_handler(&self, hook: String) -> String {
        let result = self.inner.pre_handler(hook);
        Ok(String::from(result))
    }

    #[napi, js_name = "onResponse"]
    pub fn on_response(&self, hook: String) -> String {
        let result = self.inner.on_response(hook);
        Ok(String::from(result))
    }

    #[napi, js_name = "onError"]
    pub fn on_error(&self, hook: String) -> String {
        let result = self.inner.on_error(hook);
        Ok(String::from(result))
    }

    #[napi]
    pub fn build(&self, ) -> JsLifecycleHooks {
        let result = self.inner.build();
        Ok(JsLifecycleHooks::from(result))
    }
}

#[derive(Clone)]
#[napi]
pub struct JsHandlerResult {
    inner: std::sync::Arc<spikard::HandlerResult>,
}

#[napi]
impl JsHandlerResult {
}

#[derive(Clone)]
#[napi]
pub struct JsSseEventProducer {
    inner: std::sync::Arc<spikard::SseEventProducer>,
}

#[napi]
impl JsSseEventProducer {
    #[napi, js_name = "nextEvent"]
    pub fn next_event(&self, ) -> String {
        let result = self.inner.next_event();
        Ok(String::from(result))
    }

    #[napi, js_name = "onConnect"]
    pub fn on_connect(&self, ) -> String {
        let result = self.inner.on_connect();
        Ok(String::from(result))
    }

    #[napi, js_name = "onDisconnect"]
    pub fn on_disconnect(&self, ) -> String {
        let result = self.inner.on_disconnect();
        Ok(String::from(result))
    }
}

#[derive(Clone)]
#[napi]
pub struct JsSseEvent {
    #[napi(js_name = "eventType")]
    pub event_type: Option<String>,
    pub data: String,
    pub id: Option<String>,
    pub retry: Option<i64>,
}

#[napi]
impl JsSseEvent {
    #[napi(constructor)]
    pub fn new(event_type: Option<String>, data: String, id: Option<String>, retry: Option<i64>) -> Self {
        Self { event_type, data, id, retry }
    }

    #[napi, js_name = "withId"]
    pub fn with_id(&self, id: String) -> String {
        todo!("call into core")
    }

    #[napi, js_name = "withRetry"]
    pub fn with_retry(&self, retry_ms: i64) -> String {
        todo!("call into core")
    }

    #[napi, js_name = "withType"]
    pub fn with_type(event_type: String, data: String) -> String {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsWebSocketHandler {
    inner: std::sync::Arc<spikard::WebSocketHandler>,
}

#[napi]
impl JsWebSocketHandler {
    #[napi, js_name = "handleMessage"]
    pub fn handle_message(&self, message: String) -> String {
        let result = self.inner.handle_message(message);
        Ok(String::from(result))
    }

    #[napi, js_name = "onConnect"]
    pub fn on_connect(&self, ) -> String {
        let result = self.inner.on_connect();
        Ok(String::from(result))
    }

    #[napi, js_name = "onDisconnect"]
    pub fn on_disconnect(&self, ) -> String {
        let result = self.inner.on_disconnect();
        Ok(String::from(result))
    }
}

#[derive(Clone)]
#[napi]
pub struct JsStaticFilesConfig {
    pub directory: String,
    #[napi(js_name = "routePrefix")]
    pub route_prefix: String,
    #[napi(js_name = "indexFile")]
    pub index_file: bool,
    #[napi(js_name = "cacheControl")]
    pub cache_control: Option<String>,
}

#[napi]
impl JsStaticFilesConfig {
    #[napi(constructor)]
    pub fn new(directory: String, route_prefix: String, index_file: bool, cache_control: Option<String>) -> Self {
        Self { directory, route_prefix, index_file, cache_control }
    }
}

#[derive(Clone)]
#[napi]
pub struct JsServerConfig {
    pub host: String,
    pub port: u16,
    pub workers: i64,
    #[napi(js_name = "enableRequestId")]
    pub enable_request_id: bool,
    #[napi(js_name = "maxBodySize")]
    pub max_body_size: Option<i64>,
    #[napi(js_name = "requestTimeout")]
    pub request_timeout: Option<i64>,
    pub compression: Option<JsCompressionConfig>,
    #[napi(js_name = "rateLimit")]
    pub rate_limit: Option<JsRateLimitConfig>,
    #[napi(js_name = "jwtAuth")]
    pub jwt_auth: Option<String>,
    #[napi(js_name = "apiKeyAuth")]
    pub api_key_auth: Option<String>,
    #[napi(js_name = "staticFiles")]
    pub static_files: Vec<JsStaticFilesConfig>,
    #[napi(js_name = "gracefulShutdown")]
    pub graceful_shutdown: bool,
    #[napi(js_name = "shutdownTimeout")]
    pub shutdown_timeout: i64,
    pub openapi: Option<String>,
    pub jsonrpc: Option<String>,
    pub grpc: Option<String>,
    #[napi(js_name = "lifecycleHooks")]
    pub lifecycle_hooks: Option<JsLifecycleHooks>,
    #[napi(js_name = "backgroundTasks")]
    pub background_tasks: String,
    #[napi(js_name = "enableHttpTrace")]
    pub enable_http_trace: bool,
    #[napi(js_name = "diContainer")]
    pub di_container: Option<String>,
}

#[napi]
impl JsServerConfig {
    #[napi(constructor)]
    pub fn new(host: String, port: u16, workers: i64, enable_request_id: bool, max_body_size: Option<i64>, request_timeout: Option<i64>, compression: Option<JsCompressionConfig>, rate_limit: Option<JsRateLimitConfig>, jwt_auth: Option<String>, api_key_auth: Option<String>, static_files: Vec<JsStaticFilesConfig>, graceful_shutdown: bool, shutdown_timeout: i64, openapi: Option<String>, jsonrpc: Option<String>, grpc: Option<String>, lifecycle_hooks: Option<JsLifecycleHooks>, background_tasks: String, enable_http_trace: bool, di_container: Option<String>) -> Self {
        Self { host, port, workers, enable_request_id, max_body_size, request_timeout, compression, rate_limit, jwt_auth, api_key_auth, static_files, graceful_shutdown, shutdown_timeout, openapi, jsonrpc, grpc, lifecycle_hooks, background_tasks, enable_http_trace, di_container }
    }

    #[napi]
    pub fn default() -> String {
        todo!("call into core")
    }

    #[napi]
    pub fn builder() -> String {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsTestServer {
    inner: std::sync::Arc<spikard::testing::TestServer>,
}

#[napi]
impl JsTestServer {
    #[napi]
    pub async fn call(&self, request: String) -> Result<String> {
        let result = self.inner.call(request).await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi, js_name = "connectWebsocket"]
    pub async fn connect_websocket(&self, path: String) -> String {
        let result = self.inner.connect_websocket(path).await;
        Ok(String::from(result))
    }

    #[napi, js_name = "fromApp"]
    pub fn from_app(app: JsApp) -> Result<String> {
        todo!("call into core")
    }

    #[napi, js_name = "fromRouter"]
    pub fn from_router(router: String) -> Result<String> {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsApp {
    inner: std::sync::Arc<spikard::App>,
}

#[napi]
impl JsApp {
    #[napi]
    pub fn config(&self, config: JsServerConfig) -> String {
        let result = self.inner.config(config.into());
        Ok(String::from(result))
    }

    #[napi, js_name = "mergeAxumRouter"]
    pub fn merge_axum_router(&self, router: String) -> String {
        let result = self.inner.merge_axum_router(router);
        Ok(String::from(result))
    }

    #[napi, js_name = "attachAxumRouter"]
    pub fn attach_axum_router(&self, router: String) -> String {
        let result = self.inner.attach_axum_router(router);
        Ok(String::from(result))
    }

    #[napi, js_name = "intoRouter"]
    pub fn into_router(&self, ) -> Result<String> {
        let result = self.inner.into_router().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(String::from(result))
    }

    #[napi]
    pub async fn run(&self, ) -> Result<()> {
        let result = self.inner.run().await
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(()::from(result))
    }

    #[napi]
    pub fn default() -> String {
        todo!("call into core")
    }
}

#[derive(Clone)]
#[napi]
pub struct JsRouteBuilder {
    inner: std::sync::Arc<spikard::RouteBuilder>,
}

#[napi]
impl JsRouteBuilder {
    #[napi, js_name = "handlerName"]
    pub fn handler_name(&self, name: String) -> String {
        let result = self.inner.handler_name(name);
        Ok(String::from(result))
    }

    #[napi, js_name = "requestSchemaJson"]
    pub fn request_schema_json(&self, schema: String) -> String {
        let result = self.inner.request_schema_json(schema);
        Ok(String::from(result))
    }

    #[napi, js_name = "responseSchemaJson"]
    pub fn response_schema_json(&self, schema: String) -> String {
        let result = self.inner.response_schema_json(schema);
        Ok(String::from(result))
    }

    #[napi, js_name = "paramsSchemaJson"]
    pub fn params_schema_json(&self, schema: String) -> String {
        let result = self.inner.params_schema_json(schema);
        Ok(String::from(result))
    }

    #[napi, js_name = "fileParamsJson"]
    pub fn file_params_json(&self, schema: String) -> String {
        let result = self.inner.file_params_json(schema);
        Ok(String::from(result))
    }

    #[napi]
    pub fn cors(&self, cors: JsCorsConfig) -> String {
        let result = self.inner.cors(cors.into());
        Ok(String::from(result))
    }

    #[napi]
    pub fn sync(&self, ) -> String {
        let result = self.inner.sync();
        Ok(String::from(result))
    }

    #[napi, js_name = "handlerDependencies"]
    pub fn handler_dependencies(&self, dependencies: Vec<String>) -> String {
        let result = self.inner.handler_dependencies(dependencies);
        Ok(String::from(result))
    }
}

#[derive(Clone)]
#[napi]
pub struct JsRequestContext {
    inner: std::sync::Arc<spikard::RequestContext>,
}

#[napi]
impl JsRequestContext {
    #[napi]
    pub fn request(&self, ) -> String {
        let result = self.inner.request();
        Ok(String::from(result))
    }

    #[napi, js_name = "queryValue"]
    pub fn query_value(&self, ) -> String {
        let result = self.inner.query_value();
        Ok(String::from(result))
    }

    #[napi, js_name = "rawQueryParams"]
    pub fn raw_query_params(&self, ) -> std::collections::HashMap<String, Vec<String>> {
        let result = self.inner.raw_query_params();
        Ok(std::collections::HashMap<String, Vec<String>>::from(result))
    }

    #[napi, js_name = "pathParams"]
    pub fn path_params(&self, ) -> std::collections::HashMap<String, String> {
        let result = self.inner.path_params();
        Ok(std::collections::HashMap<String, String>::from(result))
    }

    #[napi, js_name = "pathParam"]
    pub fn path_param(&self, name: String) -> Option<String> {
        let result = self.inner.path_param(name);
        Ok(Option<String>::from(result))
    }

    #[napi]
    pub fn header(&self, name: String) -> Option<String> {
        let result = self.inner.header(name);
        Ok(Option<String>::from(result))
    }

    #[napi, js_name = "headersMap"]
    pub fn headers_map(&self, ) -> std::collections::HashMap<String, String> {
        let result = self.inner.headers_map();
        Ok(std::collections::HashMap<String, String>::from(result))
    }

    #[napi]
    pub fn cookie(&self, name: String) -> Option<String> {
        let result = self.inner.cookie(name);
        Ok(Option<String>::from(result))
    }

    #[napi, js_name = "cookiesMap"]
    pub fn cookies_map(&self, ) -> std::collections::HashMap<String, String> {
        let result = self.inner.cookies_map();
        Ok(std::collections::HashMap<String, String>::from(result))
    }

    #[napi, js_name = "bodyValue"]
    pub fn body_value(&self, ) -> String {
        let result = self.inner.body_value();
        Ok(String::from(result))
    }

    #[napi]
    pub fn dependencies(&self, ) -> Option<String> {
        let result = self.inner.dependencies();
        Ok(Option<String>::from(result))
    }

    #[napi]
    pub fn method(&self, ) -> String {
        let result = self.inner.method();
        Ok(String::from(result))
    }

    #[napi, js_name = "pathStr"]
    pub fn path_str(&self, ) -> String {
        let result = self.inner.path_str();
        Ok(String::from(result))
    }
}

#[derive(Clone)]
#[napi]
pub struct JsIntoHandler {
    inner: std::sync::Arc<spikard::IntoHandler>,
}

#[napi]
impl JsIntoHandler {
    #[napi, js_name = "intoHandler"]
    pub fn into_handler(&self, ) -> String {
        let result = self.inner.into_handler();
        Ok(String::from(result))
    }
}

#[napi(string_enum)]
#[derive(Clone)]
pub enum JsMethod {
    Get,
    Post,
    Put,
    Patch,
    Delete,
    Head,
    Options,
    Trace,
}

#[napi(string_enum)]
#[derive(Clone)]
pub enum JsHandlerResponse {
    Response,
    Stream,
}

#[napi(string_enum)]
#[derive(Clone)]
pub enum JsAppError {
    Route,
    Server,
    Decode,
}

#[napi, js_name = "validateJsonrpcMethodName"]
pub fn validate_jsonrpc_method_name(name: String) -> Result<()> {
    todo!("call into core")
}

#[napi, js_name = "handlePreflight"]
pub fn handle_preflight(headers: String, cors_config: JsCorsConfig) -> Result<String> {
    todo!("call into core")
}

#[napi, js_name = "addCorsHeaders"]
pub fn add_cors_headers(response: String, origin: String, cors_config: JsCorsConfig) -> () {
    todo!("call into core")
}

#[napi, js_name = "validateCorsRequest"]
pub fn validate_cors_request(headers: String, cors_config: JsCorsConfig) -> Result<()> {
    todo!("call into core")
}

impl From<JsUploadFile> for spikard::UploadFile {
    fn from(val: JsUploadFile) -> Self {
        Self {
            filename: val.filename,
            content_type: val.content_type,
            size: val.size,
        }
    }
}

impl From<spikard::UploadFile> for JsUploadFile {
    fn from(val: spikard::UploadFile) -> Self {
        Self {
            filename: val.filename,
            content_type: val.content_type,
            size: val.size,
        }
    }
}

impl From<JsCompressionConfig> for spikard::CompressionConfig {
    fn from(val: JsCompressionConfig) -> Self {
        Self {
            gzip: val.gzip,
            brotli: val.brotli,
            min_size: val.min_size,
            quality: val.quality,
        }
    }
}

impl From<spikard::CompressionConfig> for JsCompressionConfig {
    fn from(val: spikard::CompressionConfig) -> Self {
        Self {
            gzip: val.gzip,
            brotli: val.brotli,
            min_size: val.min_size,
            quality: val.quality,
        }
    }
}

impl From<JsRateLimitConfig> for spikard::RateLimitConfig {
    fn from(val: JsRateLimitConfig) -> Self {
        Self {
            per_second: val.per_second,
            burst: val.burst,
            ip_based: val.ip_based,
        }
    }
}

impl From<spikard::RateLimitConfig> for JsRateLimitConfig {
    fn from(val: spikard::RateLimitConfig) -> Self {
        Self {
            per_second: val.per_second,
            burst: val.burst,
            ip_based: val.ip_based,
        }
    }
}

impl From<JsStaticFilesConfig> for spikard::StaticFilesConfig {
    fn from(val: JsStaticFilesConfig) -> Self {
        Self {
            directory: val.directory,
            route_prefix: val.route_prefix,
            index_file: val.index_file,
            cache_control: val.cache_control,
        }
    }
}

impl From<spikard::StaticFilesConfig> for JsStaticFilesConfig {
    fn from(val: spikard::StaticFilesConfig) -> Self {
        Self {
            directory: val.directory,
            route_prefix: val.route_prefix,
            index_file: val.index_file,
            cache_control: val.cache_control,
        }
    }
}

impl From<JsMethod> for spikard::Method {
    fn from(val: JsMethod) -> Self {
        match val {
            JsMethod::Get => Self::Get,
            JsMethod::Post => Self::Post,
            JsMethod::Put => Self::Put,
            JsMethod::Patch => Self::Patch,
            JsMethod::Delete => Self::Delete,
            JsMethod::Head => Self::Head,
            JsMethod::Options => Self::Options,
            JsMethod::Trace => Self::Trace,
        }
    }
}

impl From<spikard::Method> for JsMethod {
    fn from(val: spikard::Method) -> Self {
        match val {
            spikard::Method::Get => Self::Get,
            spikard::Method::Post => Self::Post,
            spikard::Method::Put => Self::Put,
            spikard::Method::Patch => Self::Patch,
            spikard::Method::Delete => Self::Delete,
            spikard::Method::Head => Self::Head,
            spikard::Method::Options => Self::Options,
            spikard::Method::Trace => Self::Trace,
        }
    }
}
