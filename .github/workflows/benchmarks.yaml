name: Benchmarks

on:
  workflow_dispatch:
    inputs:
      suite:
        description: 'Workload suite to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - simple
          - json
          - headers
          - cookies
      duration:
        description: 'Duration per workload (seconds)'
        required: false
        default: '30'
        type: string
      concurrency:
        description: 'Concurrent connections'
        required: false
        default: '100'
        type: string

concurrency:
  group: benchmarks-${{ github.ref }}
  cancel-in-progress: true

env:
  RUST_BACKTRACE: short
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_RELEASE_DEBUG: 0

jobs:
  build-harness:
    name: Build Benchmark Harness
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: benchmark-harness

      - name: Build benchmark harness
        run: cargo build --manifest-path tools/benchmark-harness/Cargo.toml --release

      - name: Upload benchmark harness
        uses: actions/upload-artifact@v5
        with:
          name: benchmark-harness
          path: target/release/benchmark-harness
          retention-days: 1

  bench-spikard-rust:
    name: spikard-rust
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-rust
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-spikard-python:
    name: spikard-python
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-python
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-spikard-node:
    name: spikard-node
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-node
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-spikard-ruby:
    name: spikard-ruby
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-ruby
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-spikard-wasm:
    name: spikard-wasm
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-wasm
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-fastapi-uvicorn-dto:
    name: fastapi-uvicorn-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: fastapi-uvicorn-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-fastapi-granian-dto:
    name: fastapi-granian-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: fastapi-granian-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-robyn-dto:
    name: robyn-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: robyn-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-spikard-raw:
    name: spikard-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-fastapi-uvicorn-raw:
    name: fastapi-uvicorn-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: fastapi-uvicorn-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-fastapi-granian-raw:
    name: fastapi-granian-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: fastapi-granian-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-robyn-raw:
    name: robyn-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: robyn-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-fastify-dto:
    name: fastify-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: fastify-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-fastify-raw:
    name: fastify-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: fastify-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-hono-dto:
    name: hono-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: hono-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-elysia-dto:
    name: elysia-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: elysia-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-hono-raw:
    name: hono-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: hono-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-express-dto:
    name: express-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: express-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-express-raw:
    name: express-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: express-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-morojs-dto:
    name: morojs-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: morojs-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-hanami-api-dto:
    name: hanami-api-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: hanami-api-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-hanami-api-raw:
    name: hanami-api-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: hanami-api-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-roda-dto:
    name: roda-dto
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: roda-dto
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-roda-raw:
    name: roda-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: roda-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-phalcon-raw:
    name: phalcon-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: phalcon-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-spikard-php:
    name: spikard-php
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: spikard-php
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}

  bench-trongate-raw:
    name: trongate-raw
    needs: build-harness
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v7
        with:
          name: benchmark-harness
          path: ~/.cargo/bin
      - run: chmod +x ~/.cargo/bin/benchmark-harness
      - uses: ./.github/actions/benchmark-framework
        with:
          framework: trongate-raw
          suite: ${{ inputs.suite || 'all' }}
          duration: ${{ inputs.duration || '30' }}
          concurrency: ${{ inputs.concurrency || '100' }}
