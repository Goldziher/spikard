name: CI Python

on:
  push:
    branches: [main]
    paths:
      - 'crates/spikard-py/**'
      - 'packages/python/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'crates/spikard/**/*.rs'
      - 'crates/spikard-http/**/*.rs'
      - 'testing_data/**'
      - '.github/workflows/ci-python.yaml'
      - '.github/actions/setup-python-env/**'
      - '.github/actions/setup-rust/**'
  pull_request:
    branches: [main]
    paths:
      - 'crates/spikard-py/**'
      - 'packages/python/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'crates/spikard/**/*.rs'
      - 'crates/spikard-http/**/*.rs'
      - 'testing_data/**'
      - '.github/workflows/ci-python.yaml'
      - '.github/actions/setup-python-env/**'
      - '.github/actions/setup-rust/**'

concurrency:
  group: ci-python-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  RUST_BACKTRACE: short
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0

defaults:
  run:
    shell: bash

jobs:
  build-python-wheels:
    name: Build Python Wheels (${{ matrix.os }} | Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: build-wheels-${{ matrix.os }}-py${{ matrix.python-version }}

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: ${{ matrix.python-version }}
          cache-prefix: build-wheels-${{ matrix.os }}-py${{ matrix.python-version }}

      - name: Build Python wheels (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: uv tool run maturin build --release --features extension-module -i ${{ matrix.python-version }} --manifest-path crates/spikard-py/Cargo.toml

      - name: Build Python wheels (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: uv tool run maturin build --release --features extension-module -i ${{ matrix.python-version }} --manifest-path crates/spikard-py/Cargo.toml

      - name: Upload wheels artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}-py${{ matrix.python-version }}
          path: target/wheels/*.whl
          if-no-files-found: error
          retention-days: 7

  test-python:
    name: Test Python (${{ matrix.python-version }} | ${{ matrix.os }})
    needs: build-python-wheels
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: test-python-${{ matrix.os }}-${{ matrix.python-version }}

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: ${{ matrix.python-version }}
          cache-prefix: test-python-${{ matrix.os }}-${{ matrix.python-version }}

      - name: Download wheels artifact
        uses: actions/download-artifact@v6
        with:
          name: wheels-${{ matrix.os }}-py${{ matrix.python-version }}
          path: dist/

      - name: Install wheel (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: uv pip install dist/*.whl

      - name: Install wheel (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: uv pip install (Get-Item dist\*.whl).FullName

      - name: Run pytest (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: cd packages/python && uv run pytest tests/ -v

      - name: Run pytest (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: cd packages/python; uv run pytest tests/ -v

  build-python-sdist:
    name: Build Python Source Distribution
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: python-sdist

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.13"
          cache-prefix: python-sdist

      - name: Build source distribution
        run: uv tool run maturin sdist --manifest-path crates/spikard-py/Cargo.toml

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: python-sdist
          path: target/wheels/*.tar.gz
          if-no-files-found: error
          retention-days: 7
