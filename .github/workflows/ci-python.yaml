name: CI Python

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'crates/spikard-py/**'
      - 'packages/python/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'crates/spikard/**/*.rs'
      - 'crates/spikard-http/**/*.rs'
      - 'testing_data/**'
      - '.cargo/config.toml'
      - '.github/workflows/ci-python.yaml'
      - '.github/actions/setup-python-env/**'
      - '.github/actions/setup-rust/**'
  pull_request:
    branches: [main]
    paths:
      - 'crates/spikard-py/**'
      - 'packages/python/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - 'crates/spikard/**/*.rs'
      - 'crates/spikard-http/**/*.rs'
      - 'testing_data/**'
      - '.cargo/config.toml'
      - '.github/workflows/ci-python.yaml'
      - '.github/actions/setup-python-env/**'
      - '.github/actions/setup-rust/**'

concurrency:
  group: ci-spikard-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  RUST_BACKTRACE: short
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0

defaults:
  run:
    shell: bash

jobs:
  build-python-wheels:
    name: Build Python Wheels
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: build-wheels

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.14"
          cache-prefix: build-wheels

      - name: Build Python wheels
        shell: bash
        run: bash scripts/ci/python/build-wheels.sh "3.14"

      - name: Upload wheels artifact
        uses: actions/upload-artifact@v6
        with:
          name: wheels
          path: target/wheels/*.whl
          if-no-files-found: error
          retention-days: 7

  test-python:
    name: Test Python
    needs: build-python-wheels
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: test-python

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.14"
          cache-prefix: test-python

      - name: Download wheels artifact
        uses: actions/download-artifact@v7
        with:
          name: wheels
          path: dist/

      - name: Select and install wheel
        shell: bash
        run: |
          wheel=$(python scripts/ci/python/select-and-install-wheel.py --dist-dir dist/)
          uv pip install "$wheel"

      - name: Sync dev dependencies
        run: uv sync --all-extras

      - name: Run pytest
        shell: bash
        run: uv run pytest packages/python/tests/ -v

      - name: Run Python e2e tests
        working-directory: e2e/python
        run: PYTHONPATH=../../packages/python:. ../../.venv/bin/python -m pytest tests/ -v

  build-python-sdist:
    name: Build Python Source Distribution
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: python-sdist

      - name: Setup Python
        uses: ./.github/actions/setup-python-env
        with:
          python-version: "3.14"
          cache-prefix: python-sdist

      - name: Sync workspace deps
        run: uv sync --all-packages --all-extras

      - name: Build source distribution
        run: bash scripts/ci/python/build-sdist.sh

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v6
        with:
          name: python-sdist
          path: target/wheels/*.tar.gz
          if-no-files-found: error
          retention-days: 7

  coverage-python:
    name: Python Code Coverage (80% minimum)
    needs: [test-python]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          cache-key-prefix: coverage-python

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Setup uv
        uses: astral-sh/setup-uv@v7

      - name: Run Python tests with coverage
        run: bash scripts/ci/python/run-coverage.sh

      - name: Upload coverage to Codecov
        if: github.actor != 'dependabot[bot]'
        uses: codecov/codecov-action@v5
        with:
          files: ./packages/python/coverage.lcov
          flags: python
          name: python-coverage
          fail_ci_if_error: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Archive coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-coverage-report
          path: packages/python/htmlcov/
          retention-days: 30
