// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5f684838fd97dd756f5222172e52338d8e7e329b935b9feee8a65355911b2753
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: compression

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_compression_brotli_only() {
    // Tests Brotli compression when enabled and client supports it
    let expected_body = r#"{"data":"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa","message":"Brotli compressed payload"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/compression/brotli"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .get("/compression/brotli")
        .add_header(r#"Accept-Encoding"#, r#"br"#)
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_compression_gzip_applied() {
    // Serves a JSON payload compressed with gzip when the client advertises support.
    let expected_body = r#"{"message":"Compressed payload","payload":"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/compression/gzip"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .get("/compression/gzip")
        .add_header(r#"Accept-Encoding"#, r#"gzip"#)
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_compression_min_size_threshold_exact_boundary() {
    // Tests compression respects exact minimum size boundary
    let expected_body =
        r#"{"message":"Exact size payload that is exactly 100 bytes or more to test boundary"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/compression/boundary"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .get("/compression/boundary")
        .add_header(r#"Accept-Encoding"#, r#"gzip"#)
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_compression_payload_below_min_size_is_not_compressed() {
    // Ensures responses smaller than the configured min_size are sent uncompressed even when the client sends Accept-Encoding.
    let expected_body = r#"{"message":"Small payload","payload":"tiny"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/compression/skip"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .get("/compression/skip")
        .add_header(r#"Accept-Encoding"#, r#"gzip"#)
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_compression_quality_level_9() {
    // Tests compression with high quality level setting
    let expected_body = r#"{"message":"High quality compression","payload":"x"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/compression/high_quality"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .get("/compression/high_quality")
        .add_header(r#"Accept-Encoding"#, r#"gzip"#)
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}
