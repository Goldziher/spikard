// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f2d876ff7b9b0cfb15a124c1bff7c0469d8a5a4dc43b6330629747de2e009cf0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: upload

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_upload_file_as_bytes() {
    // Tests file upload read as byte array
    let expected_body = r#"{"as_bytes":true,"filename":"binary.bin","size":11}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload/bytes"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/upload/bytes")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"file":{"content":"binary data","filename":"binary.bin"}}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_file_basic_success() {
    // Tests basic file upload with single file
    let expected_body = r#"{"filename":"test.txt","size":13}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/upload")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"file":{"content":"Hello, World!","filename":"test.txt"}}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_file_content_type_or_default() {
    // Tests file upload uses provided content_type or defaults
    let expected_body = r#"{"content_type":"application/octet-stream","filename":"unknown.xyz"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/upload/auto-type"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/upload/auto-type")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"file":{"content":"Some content","filename":"unknown.xyz"}}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_file_read_to_string() {
    // Tests file upload read as string
    let expected_body = r#"{"as_string":true,"content":"Hello, World!","filename":"text.txt"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload/string"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/upload/string")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"file":{"content":"Hello, World!","filename":"text.txt"}}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_file_with_custom_content_type() {
    // Tests file upload preserves custom Content-Type
    let expected_body = r#"{"content_type":"application/json","filename":"data.json","size":16}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload/typed"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/upload/typed")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"file":{"content":"{\"key\": \"value\"}","content_type":"application/json","filename":"data.json"}}"#
                .as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_file_with_form_fields() {
    // Tests file upload with additional form fields
    let expected_body = r#"{"description":"Important document","filename":"document.pdf","tags_count":2}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/upload/with-fields"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload/with-fields")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"description":"Important document","file":{"content":"PDF content","filename":"document.pdf"},"tags":["important","archive"]}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_file_with_gzip_content_encoding() {
    // Tests file upload with gzip content encoding
    let expected_body = r#"{"content_encoding":"gzip","filename":"data.gz"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/upload/compressed"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/upload/compressed")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"file":{"content":"compressed data","content_encoding":"gzip","filename":"data.gz"}}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_upload_multiple_files() {
    // Tests uploading multiple files at once
    let expected_body = r#"{"total_size":18,"uploaded_files":2}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/upload/multiple"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload/multiple")
        .add_header(r#"Content-Type"#, r#"multipart/form-data"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"files":[{"content":"Content 1","filename":"file1.txt"},{"content":"Content 2","filename":"file2.txt"}]}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}
