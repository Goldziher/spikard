// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bb216a5bd333306783e3eb7d042577ff84a8ceb4aefe9cc4dfa3995ed66d104b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: multipart

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_file_magic_number_png_success() {
    // File with correct PNG magic number and matching MIME type should be accepted
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(201u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload").await;
    assert_eq!(response.status_code().as_u16(), 201u16);
}

#[tokio::test]
async fn test_file_magic_number_jpeg_success() {
    // File with correct JPEG magic number and matching MIME type should be accepted
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(201u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload").await;
    assert_eq!(response.status_code().as_u16(), 201u16);
}

#[tokio::test]
async fn test_file_mime_spoofing_png_as_jpeg() {
    // File with PNG magic number but JPEG MIME type should be rejected (spoofing detection)
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"ctx":{"declared_mime":"image/jpeg","detected_type":"image/png","magic_bytes":"89504e470d0a1a0a"},"loc":["files","image"],"msg":"File type mismatch: MIME type is image/jpeg but magic numbers indicate image/png","type":"validation_error"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload").await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_file_mime_spoofing_jpeg_as_png() {
    // File with JPEG magic number but PNG MIME type should be rejected (spoofing detection)
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"ctx":{"declared_mime":"image/png","detected_type":"image/jpeg","magic_bytes":"ffd8ffe0"},"loc":["files","image"],"msg":"File type mismatch: MIME type is image/png but magic numbers indicate image/jpeg","type":"validation_error"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload").await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_file_pdf_magic_number_success() {
    // File with correct PDF magic number should be accepted
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(201u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload").await;
    assert_eq!(response.status_code().as_u16(), 201u16);
}

#[tokio::test]
async fn test_file_empty_buffer() {
    // File with empty buffer should fail validation
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"ctx":{"buffer_size":0},"loc":["files","file"],"msg":"File buffer is empty","type":"validation_error"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/upload").await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_content_type_validation_invalid_type() {
    // Tests file upload with disallowed content type
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"loc":["files","file"],"msg":"Invalid content type 'application/x-sh'. Allowed types: image/jpeg, image/png, image/gif","type":"validation_error"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/files/images-only"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(422u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/images-only")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"file\"; filename=\"script.sh\"\r\nContent-Type: application/x-sh\r\n\r\n#!/bin/bash\necho hello\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_empty_file_upload() {
    // Tests uploading a file with zero bytes
    let expected_body = r#"{"filename":"empty.txt","size":0}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/files/upload"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/upload")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"file\"; filename=\"empty.txt\"\r\nContent-Type: text/plain\r\n\r\n\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_file_list_upload_array_of_files() {
    // Tests uploading multiple files as a list parameter
    let expected_body = r#"{"filenames":["file1.txt","file2.txt"],"total_size":35}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/files/list"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/list")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"files\"; filename=\"file1.txt\"\r\nContent-Type: text/plain\r\n\r\nfirst file here\r\n--alef-boundary\r\nContent-Disposition: form-data; name=\"files\"; filename=\"file2.txt\"\r\nContent-Type: text/plain\r\n\r\nsecond file content here\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_file_size_validation_too_large() {
    // Tests file upload exceeding max size limit
    let expected_body = r#"{"detail":"File too large. Maximum size is 1MB"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/files/validated"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(413u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/validated")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"file\"; filename=\"large.txt\"\r\nContent-Type: text/plain\r\n\r\nplaceholder content\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 413u16);
}

#[tokio::test]
async fn test_file_upload_with_custom_headers() {
    // File upload with additional custom headers in the multipart section
    let expected_body = r#"{"test2":{"content":"<file2 content>","content_type":"text/plain","filename":"test2.txt","headers":[["content-disposition","form-data; name=\"test2\"; filename=\"test2.txt\""],["content-type","text/plain"],["x-custom","f2"]],"size":15}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_file_upload_without_filename() {
    // Upload file content without providing a filename
    let expected_body = r#"{"test1":"<file1 content>"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_form_data_without_files() {
    // Multipart form with only text fields, no file uploads
    let expected_body = r#"{"some":"data"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_image_file_upload() {
    // Tests uploading an image file (JPEG)
    let expected_body = r#"{"content_type":"image/jpeg","filename":"photo.jpg","size":22}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/files/image"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/image")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"image\"; filename=\"photo.jpg\"\r\nContent-Type: image/jpeg\r\n\r\nJPEG placeholder content here\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_mixed_files_and_form_data() {
    // Multipart request with both file uploads and regular form fields
    let expected_body = r#"{"active":"true","age":"25","file":{"content":"file data here","content_type":"text/plain","filename":"upload.txt","size":14},"username":"testuser"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_multiple_file_uploads() {
    // Upload multiple files in a single multipart request
    let expected_body = r#"{"test1":{"content":"<file1 content>","content_type":"text/plain","filename":"test1.txt","size":15},"test2":{"content":"<file2 content>","content_type":"text/plain","filename":"test2.txt","size":15}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_multiple_values_for_same_field_name() {
    // Multiple files uploaded with the same field name (array-like behavior)
    let expected_body = r#"{"files":[{"content":"first file","content_type":"text/plain","filename":"file1.txt","size":10},{"content":"second file","content_type":"text/plain","filename":"file2.txt","size":11}],"tags":["python","rust","web"]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"files\"; filename=\"file1.txt\"\r\nContent-Type: text/plain\r\n\r\nfirst file\r\n--alef-boundary\r\nContent-Disposition: form-data; name=\"files\"; filename=\"file2.txt\"\r\nContent-Type: text/plain\r\n\r\nsecond file\r\n--alef-boundary\r\nContent-Disposition: form-data; name=\"tags\"\r\n\r\npython\r\n--alef-boundary\r\nContent-Disposition: form-data; name=\"tags\"\r\n\r\nrust\r\n--alef-boundary\r\nContent-Disposition: form-data; name=\"tags\"\r\n\r\nweb\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_optional_file_upload_missing() {
    // Tests optional file parameter when no file is provided
    let expected_body = r#"{"file":null}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/files/optional"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/files/optional")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_optional_file_upload_provided() {
    // Tests optional file parameter when file is provided
    let expected_body = r#"{"content_type":"text/plain","filename":"optional.txt","size":21}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/files/optional"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/optional")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"file\"; filename=\"optional.txt\"\r\nContent-Type: text/plain\r\n\r\noptional file content here\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_pdf_file_upload() {
    // Tests uploading a PDF document
    let expected_body = r#"{"content_type":"application/pdf","filename":"report.pdf","size":16}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/files/document"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/files/document")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary\r\nContent-Disposition: form-data; name=\"document\"; filename=\"report.pdf\"\r\nContent-Type: application/pdf\r\n\r\nPDF placeholder here\r\n--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_required_file_upload_missing() {
    // Tests required file parameter when no file is provided
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"input":{},"loc":["body","file"],"msg":"Field required","type":"missing"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::post("/files/required"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(422u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/files/required")
        .bytes(bytes::Bytes::copy_from_slice(r#""--alef-boundary--\r\n""#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_simple_file_upload() {
    // Single file upload with text/plain content type
    let expected_body =
        r#"{"test":{"content":"<file content>","content_type":"text/plain","filename":"test.txt","size":14}}"#
            .to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.post("/").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}
