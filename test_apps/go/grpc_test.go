// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5f684838fd97dd756f5222172e52338d8e7e329b935b9feee8a65355911b2753
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: grpc
package e2e_test

import (
	"encoding/json"
	"io"
	"net/http"
	"os"
	"reflect"
	"strings"
	"testing"
)

func Test_GrpcCallWithMetadata(t *testing.T) {
	// Tests gRPC call with request metadata headers
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"id":"resource-1"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_call_with_metadata/api.Service/GetData", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`authorization`, `Bearer token123`)
	req.Header.Set(`te`, `trailers`)
	req.Header.Set(`x-custom-metadata`, `value123`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	if !strings.Contains(resp.Header.Get(`x-response-metadata`), `processed`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `x-response-metadata`, resp.Header.Get(`x-response-metadata`), `processed`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"data":"result","id":"resource-1"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcConfigCompressionEnabled(t *testing.T) {
	// Tests gRPC compression is enabled for responses
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"request":"get_data"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_config_compression_enabled/api.Service/GetCompressedData", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`grpc-encoding`, `gzip`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-encoding`), `gzip`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-encoding`, resp.Header.Get(`grpc-encoding`), `gzip`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
}

func Test_GrpcConfigKeepaliveSettings(t *testing.T) {
	// Tests gRPC keepalive ping configuration
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"task":"background_job"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_config_keepalive_settings/api.Service/LongRunning", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
}

func Test_GrpcConfigMaxMessageSize(t *testing.T) {
	// Tests gRPC server respects maximum message size configuration
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"data":"large_payload_data"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_config_max_message_size/api.Service/SendLargeMessage", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"received_size":19}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcConfigRequestTimeout(t *testing.T) {
	// Tests gRPC request timeout configuration
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"operation":"process"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_config_request_timeout/api.Service/TimedOperation", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`grpc-timeout`, `5000m`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
}

func Test_GrpcDeadlineExceeded(t *testing.T) {
	// Tests gRPC DEADLINE_EXCEEDED error when timeout occurs
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"duration_ms":60000}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_deadline_exceeded/api.Service/SlowOperation", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`grpc-timeout`, `100m`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `4`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `4`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"code":"DEADLINE_EXCEEDED","message":"Operation exceeded deadline"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcErrorInvalidArgument(t *testing.T) {
	// Tests gRPC INVALID_ARGUMENT error status response
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"value":-100}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_error_invalid_argument/api.Service/ValidateInput", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `3`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `3`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"code":"INVALID_ARGUMENT","message":"Value must be non-negative"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcErrorNotFound(t *testing.T) {
	// Tests gRPC NOT_FOUND error status response
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"item_id":"nonexistent"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_error_not_found/api.Service/GetItem", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `5`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `5`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"code":"NOT_FOUND","message":"Item not found"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcRequestMethodNameField(t *testing.T) {
	// Tests gRPC request includes method_name field
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"id":"resource-1"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_request_method_name_field/api.Service/GetData", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"data":"result","method":"GetData"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcRequestServiceNameField(t *testing.T) {
	// Tests gRPC request includes service_name field
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"name":"World"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_request_service_name_field/helloworld.Greeter/SayHello", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"message":"Hello World","service":"helloworld.Greeter"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcRequestWithCustomMetadata(t *testing.T) {
	// Tests gRPC request includes custom metadata in trailers
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"data":{"key":"value"},"request_id":"req-12345"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_request_with_custom_metadata/api.Service/ProcessWithMetadata", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	req.Header.Set(`x-request-id`, `req-12345`)
	req.Header.Set(`x-trace-id`, `trace-abc123`)
	req.Header.Set(`x-user-id`, `user-42`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	if !strings.Contains(resp.Header.Get(`x-response-trace-id`), `trace-abc123`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `x-response-trace-id`, resp.Header.Get(`x-response-trace-id`), `trace-abc123`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"processed":true,"request_id":"req-12345"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcResponseWithMetadataTrailers(t *testing.T) {
	// Tests gRPC response includes metadata in trailers
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"request":"test"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_response_with_metadata_trailers/api.Service/MetadataResponse", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	if !strings.Contains(resp.Header.Get(`x-metadata-key`), `metadata-value`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `x-metadata-key`, resp.Header.Get(`x-metadata-key`), `metadata-value`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"result":"success"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_GrpcUnaryCallSuccess(t *testing.T) {
	// Tests unary gRPC call with successful response
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader(`{"name":"World"}`)
	req, err := http.NewRequest("POST", baseURL+"/fixtures/grpc_unary_call_success/helloworld.Greeter/SayHello", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set(`Content-Type`, `application/grpc`)
	req.Header.Set(`te`, `trailers`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`content-type`), `application/grpc`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `content-type`, resp.Header.Get(`content-type`), `application/grpc`)
	}
	if !strings.Contains(resp.Header.Get(`grpc-status`), `0`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `grpc-status`, resp.Header.Get(`grpc-status`), `0`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"message":"Hello World"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}
