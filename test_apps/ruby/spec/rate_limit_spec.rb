# This file is auto-generated by alef. DO NOT EDIT.
# frozen_string_literal: true

require 'spikard'
require 'json'
require 'faraday'

RSpec.describe 'rate_limit' do
  let(:base_url) { ENV.fetch('TEST_SERVER_URL', 'http://localhost:8080') }
  let(:client) do
    Faraday.new(url: base_url) do |f|
      f.request :json
      f.response :json, content_type: /\bjson$/
    end
  end

  describe 'GET /rate-limit/basic' do
    it 'Verifies that requests below the configured rate limit are served normally.' do
      response = client.get('/rate-limit/basic')
      expect(response.status).to eq(200)
      expect(response.body).to eq({ 'request' => 'under-limit', 'status' => 'ok' })
    end
  end

  describe 'GET /rate-limit/exceeded' do
    it 'Sends sequential requests until the configured limit is exceeded and validates the 429 response.' do
      response = client.get('/rate-limit/exceeded')
      expect(response.status).to eq(429)
    end
  end
end
