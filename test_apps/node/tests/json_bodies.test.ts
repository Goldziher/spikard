// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7269e6e312bf735d3a9c38d8643dff0c99b6ab695f463715fb0eed106cfea31e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("json_bodies", () => {

  it("nested_object_validation_success: Nested object in JSON body should validate correctly", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/29_nested_object_validation_success/users`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ profile: { email: "john@example.com", name: "John Doe" } }),
    });
    expect(response.status).toBe(201);  });

  it("nested_object_missing_field: Nested object missing required field should fail validation", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/30_nested_object_missing_field/users`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ profile: { name: "John Doe" } }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { name: "John Doe" }, loc: ["body", "profile", "email"], msg: "Field required", type: "missing" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("nullable_property_null_value: Nullable property with null value should be accepted", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/31_nullable_property_null_value/users`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ description: null, name: "Test User" }),
    });
    expect(response.status).toBe(201);  });

  it("schema_ref_definitions: JSON Schema $ref with definitions should validate correctly", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/32_schema_ref_definitions/products`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ product: { name: "Widget", price: 9.99 } }),
    });
    expect(response.status).toBe(201);  });

  it("allof_schema_composition: JSON Schema allOf composition should validate all schemas", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/33_allof_schema_composition/items`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Product", price: 29.99 }),
    });
    expect(response.status).toBe(201);  });

  it("additional_properties_false: Schema with additionalProperties false should reject extra fields", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/34_additional_properties_false/users`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ email: "john@example.com", extra_field: "should fail", name: "John" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { additional_properties: false, unexpected_field: "extra_field" }, input: { email: "john@example.com", extra_field: "should fail", name: "John" }, loc: ["body", "extra_field"], msg: "Additional properties are not allowed", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("oneof_schema_success: oneOf schema composition - exactly one schema must match", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/35_oneof_schema_success/payment`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ credit_card: "1234567812345678" }),
    });
    expect(response.status).toBe(201);  });

  it("oneof_schema_multiple_match_failure: oneOf schema composition - fails when multiple schemas match", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/36_oneof_schema_multiple_match_failure/payment`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ credit_card: "1234567812345678", paypal_email: "user@example.com" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { credit_card: "1234567812345678", paypal_email: "user@example.com" }, loc: ["body"], msg: "{\"credit_card\":\"1234567812345678\",\"paypal_email\":\"user@example.com\"} is valid under more than one of the schemas listed in the 'oneOf' keyword", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("oneof_schema_no_match_failure: oneOf schema composition - fails when no schemas match", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/37_oneof_schema_no_match_failure/payment`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ bitcoin_address: "1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { bitcoin_address: "1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa" }, loc: ["body"], msg: "{\"bitcoin_address\":\"1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa\"} is not valid under any of the schemas listed in the 'oneOf' keyword", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("anyof_schema_success: anyOf schema composition - at least one schema must match", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/38_anyof_schema_success/contact`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ email: "john@example.com", name: "John Doe" }),
    });
    expect(response.status).toBe(201);  });

  it("anyof_schema_multiple_match_success: anyOf schema composition - succeeds when multiple schemas match", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/39_anyof_schema_multiple_match_success/contact`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ email: "john@example.com", name: "John Doe", phone: "+1-555-0100" }),
    });
    expect(response.status).toBe(201);  });

  it("anyof_schema_failure: anyOf schema composition - fails when no schemas match", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/40_anyof_schema_failure/contact`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "John Doe" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { name: "John Doe" }, loc: ["body"], msg: "{\"name\":\"John Doe\"} is not valid under any of the schemas listed in the 'anyOf' keyword", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("not_schema_success: not schema - value must not match the schema", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/41_not_schema_success/users`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ username: "john_doe" }),
    });
    expect(response.status).toBe(201);  });

  it("not_schema_failure: not schema - fails when value matches the prohibited schema", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/42_not_schema_failure/users`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ username: "admin" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: "admin", loc: ["body", "username"], msg: "{\"enum\":[\"admin\",\"root\",\"system\"]} is not allowed for \"admin\"", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("const_validation_success: Field with const constraint matching exact value should succeed", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/43_const_validation_success/api/v1/data`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ data: "test", version: "1.0" }),
    });
    expect(response.status).toBe(201);  });

  it("const_validation_failure: Field with const constraint not matching exact value should fail", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/44_const_validation_failure/api/v1/data`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ data: "test", version: "2.0" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: "2.0", loc: ["body", "version"], msg: "\"1.0\" was expected", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("minproperties_validation_success: Object with properties meeting minProperties constraint should succeed", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/45_minproperties_validation_success/config`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ host: "localhost", port: 8080 }),
    });
    expect(response.status).toBe(201);  });

  it("minproperties_validation_failure: Object with fewer properties than minProperties should fail", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/46_minproperties_validation_failure/config`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ host: "localhost" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { host: "localhost" }, loc: ["body"], msg: "{\"host\":\"localhost\"} has less than 2 properties", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("maxproperties_validation_failure: Object with more properties than maxProperties should fail", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/47_maxproperties_validation_failure/config`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ debug: false, host: "localhost", port: 8080, ssl: true }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { debug: false, host: "localhost", port: 8080, ssl: true }, loc: ["body"], msg: "{\"debug\":false,\"host\":\"localhost\",\"port\":8080,\"ssl\":true} has more than 3 properties", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("dependencies_validation_success: Dependencies constraint - when A present, B is required and provided", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/48_dependencies_validation_success/billing`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ billing_address: "123 Main St", credit_card: "1234567812345678", name: "John Doe" }),
    });
    expect(response.status).toBe(201);  });

  it("dependencies_validation_failure: Dependencies constraint - when A present, B is required but missing", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/49_dependencies_validation_failure/billing`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ credit_card: "1234567812345678", name: "John Doe" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { credit_card: "1234567812345678", name: "John Doe" }, loc: ["body"], msg: "\"billing_address\" is a required property", type: "validation_error" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("deep_nesting_4_levels: Deeply nested object with 4+ levels should validate correctly", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/50_deep_nesting_4_levels/data`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ user: { profile: { contact: { address: { street: "123 Main St" } } } } }),
    });
    expect(response.status).toBe(201);  });

  it("array_of_objects_success: Tests array field containing objects", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/array_of_objects_success/items/list`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ images: [{ name: "Front", url: "https://example.com/img1.jpg" }, { name: "Back", url: "https://example.com/img2.jpg" }], name: "Product Bundle", tags: ["electronics", "gadget"] }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      images: [{ name: "Front", url: "https://example.com/img1.jpg" }, { name: "Back", url: "https://example.com/img2.jpg" }],
      name: "Product Bundle",
      tags: ["electronics", "gadget"],
    });  });

  it("array_of_primitive_values: Tests array field containing primitive values (strings, numbers)", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/array_of_primitive_values/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Product", ratings: [4.5, 4.8, 5.0, 4.2], tags: ["electronics", "gadget", "new"] }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      name: "Product",
      ratings: [4.5, 4.8, 5.0, 4.2],
      tags: ["electronics", "gadget", "new"],
    });  });

  it("body_with_query_parameters: Tests JSON body combined with query parameters", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/body_with_query_parameters/items/?limit=10`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Item", price: 42.0 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      item: {
        name: "Item",
        price: 42.0,
      },
      limit: 10,
    });  });

  it("boolean_field_success: Tests JSON object with boolean field", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/boolean_field_success/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ in_stock: true, name: "Item", price: 42.0 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      in_stock: true,
      name: "Item",
      price: 42.0,
    });  });

  it("date_field_success: Tests date field with ISO date format", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/date_field_success/events/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ event_date: "2024-03-15", name: "Conference" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      event_date: "2024-03-15",
      name: "Conference",
    });  });

  it("datetime_field_success: Tests datetime field with ISO datetime format", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/datetime_field_success/events/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ created_at: "2024-03-15T10:30:00Z", name: "Meeting" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      created_at: "2024-03-15T10:30:00Z",
      name: "Meeting",
    });  });

  it("deeply_nested_objects: Tests deeply nested JSON structure (3+ levels)", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/deeply_nested_objects/items/nested`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Product", price: 100.0, seller: { address: { city: "Springfield", country: { code: "US", name: "USA" }, street: "123 Main St" }, name: "John Doe" } }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      name: "Product",
      price: 100.0,
      seller: {
        address: {
          city: "Springfield",
          country: {
            code: "US",
            name: "USA",
          },
          street: "123 Main St",
        },
        name: "John Doe",
      },
    });  });

  it("empty_array_validation_fail: Tests array field with min_items constraint failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/empty_array_validation_fail/items/list-validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Product", tags: [] }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { min_length: 1 }, input: [], loc: ["body", "tags"], msg: "List should have at least 1 item after validation", type: "too_short" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("empty_json_object: Tests empty JSON object when all fields are optional", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/empty_json_object/items/optional-all`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({  }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      description: null,
      name: null,
      price: null,
      tax: null,
    });  });

  it("enum_field_invalid_value: Tests enum field with value not in enum", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/enum_field_invalid_value/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ category: "furniture", name: "Item" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { expected: "'electronics', 'clothing' or 'books'" }, input: "furniture", loc: ["body", "category"], msg: "Input should be 'electronics', 'clothing' or 'books'", type: "enum" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("enum_field_success: Tests enum field with valid enum value", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/enum_field_success/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ category: "electronics", name: "Item" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      category: "electronics",
      name: "Item",
    });  });

  it("extra_fields_ignored_no_additionalproperties: Tests that extra fields not in model are ignored", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/extra_fields_ignored_no_additionalproperties/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ another_extra: 123, extra_field: "this should be ignored", name: "Item", price: 42.0 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      name: "Item",
      price: 42.0,
    });  });

  it("field_type_validation_invalid_type: Tests type validation error when field has wrong type", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/field_type_validation_invalid_type/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ description: "A very nice Item", name: "Foo", price: "not a number", tax: 3.2 }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: "not a number", loc: ["body", "price"], msg: "Input should be a valid number, unable to parse string as a number", type: "float_parsing" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("nested_object_success: Tests nested JSON objects", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/nested_object_success/items/nested`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ image: { name: "Product Image", url: "https://example.com/image.jpg" }, name: "Foo", price: 42.0 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      image: {
        name: "Product Image",
        url: "https://example.com/image.jpg",
      },
      name: "Foo",
      price: 42.0,
    });  });

  it("null_value_for_optional_field: Tests explicitly setting optional field to null", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/null_value_for_optional_field/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ description: null, name: "Item", price: 42.0, tax: null }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      description: null,
      name: "Item",
      price: 42.0,
      tax: null,
    });  });

  it("numeric_ge_validation_fail: Tests numeric field with ge (greater than or equal) constraint failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/numeric_ge_validation_fail/items/validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Item", price: 0.5 }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { ge: 1 }, input: 0.5, loc: ["body", "price"], msg: "Input should be greater than or equal to 1", type: "greater_than_equal" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("numeric_le_validation_success: Tests numeric field with le (less than or equal) constraint at boundary", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/numeric_le_validation_success/items/validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Item", price: 100.0 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      name: "Item",
      price: 100.0,
    });  });

  it("optional_fields_omitted: Tests object with optional fields omitted", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/optional_fields_omitted/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Foo", price: 35.4 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      description: null,
      name: "Foo",
      price: 35.4,
      tax: null,
    });  });

  it("patch_partial_update_2: Tests PATCH request with partial object update", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/patch_partial_update_2/items/1`;
    const response = await fetch(url, {
      method: "PATCH",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ price: 45.0 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      description: "Original description",
      name: "Original Item",
      price: 45.0,
    });  });

  it("required_field_missing_validation_error: Tests validation error when required field is missing", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/required_field_missing_validation_error/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ description: "A very nice Item", price: 35.4 }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: { description: "A very nice Item", price: 35.4 }, loc: ["body", "name"], msg: "Field required", type: "missing" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("simple_json_object_success: Tests simple JSON object with all required fields", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/simple_json_object_success/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ description: "A very nice Item", name: "Foo", price: 35.4, tax: 3.2 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      description: "A very nice Item",
      name: "Foo",
      price: 35.4,
      tax: 3.2,
    });  });

  it("string_max_length_validation_fail: Tests string field with max_length constraint failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/string_max_length_validation_fail/items/validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "This is a very long name that exceeds the maximum length", price: 35.4 }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { max_length: 50 }, input: "This is a very long name that exceeds the maximum length", loc: ["body", "name"], msg: "String should have at most 50 characters", type: "string_too_long" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("string_min_length_validation_fail: Tests string field with min_length constraint failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/string_min_length_validation_fail/items/validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "ab", price: 35.4 }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { min_length: 3 }, input: "ab", loc: ["body", "name"], msg: "String should have at least 3 characters", type: "string_too_short" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("string_pattern_validation_fail: Tests string field with regex pattern constraint failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/string_pattern_validation_fail/items/validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Item", sku: "ABC-123" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ ctx: { pattern: "^[A-Z]{3}[0-9]{4}$" }, input: "ABC-123", loc: ["body", "sku"], msg: "String should match pattern '^[A-Z]{3}[0-9]{4}$'", type: "string_pattern_mismatch" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("string_pattern_validation_success: Tests string field with regex pattern constraint success", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/string_pattern_validation_success/items/validated`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ name: "Item", sku: "ABC1234" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      name: "Item",
      sku: "ABC1234",
    });  });

  it("uuid_field_invalid_format: Tests UUID field with invalid UUID format", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/uuid_field_invalid_format/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ item_id: "not-a-valid-uuid", name: "Item" }),
    });
    expect(response.status).toBe(422);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: "not-a-valid-uuid", loc: ["body", "item_id"], msg: "Input should be a valid UUID", type: "uuid_parsing" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });  });

  it("uuid_field_success: Tests UUID field with valid UUID format", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/uuid_field_success/items/`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ item_id: "c892496f-b1fd-4b91-bdb8-b46f92df1716", name: "Item" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      item_id: "c892496f-b1fd-4b91-bdb8-b46f92df1716",
      name: "Item",
    });  });

});
