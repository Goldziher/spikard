# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:0fdebc102fb44f9e2c6b2c86d89a5f7b4b0813772118b76816bed4084da3cd76
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: background
defmodule E2e.BackgroundTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "background_event_logging" do
    test "Enqueues a background job that appends the posted event to shared state" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/background_event_logging", finch: AlefE2EFinch, json: %{"event" => "alpha"})
      assert response.status == 202
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end

  describe "background_event_logging_second_payload" do
    test "Ensures background jobs handle different payloads" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/background_event_logging_second_payload", finch: AlefE2EFinch, json: %{"event" => "beta"})
      assert response.status == 202
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end
end
