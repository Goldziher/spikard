# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:9bde8e8158f8579f0802673f8558e588e6654bbc87df86e8c015ff3decce3080
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: content_types
defmodule E2e.ContentTypesTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "json_with_charset_utf16" do
    test "JSON with UTF-16 charset should be rejected (UTF-8 only)" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/13_json_with_charset_utf16", finch: AlefE2EFinch, json: %{"value" => "test"}, headers: [{"Content-Type", "application/json; charset=utf-16"}])
      assert response.status == 415
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Unsupported charset 'utf-16' for JSON. Only UTF-8 is supported.", "status" => 415, "title" => "Unsupported Charset", "type" => "https://spikard.dev/errors/unsupported-charset"}
    end
  end

  describe "content_type_case_insensitive" do
    test "Content-Type header should be case-insensitive" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/14_content_type_case_insensitive", finch: AlefE2EFinch, json: %{"name" => "test"}, headers: [{"Content-Type", "APPLICATION/JSON"}])
      assert response.status == 201
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"name" => "test"}
    end
  end

  describe "multipart_boundary_required" do
    test "Multipart content-type without boundary parameter should fail" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/15_multipart_boundary_required", finch: AlefE2EFinch, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 400
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"error" => "multipart/form-data requires 'boundary' parameter"}
    end
  end

  describe "text_plain_not_accepted" do
    test "text/plain content-type should be rejected when JSON is expected" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/16_text_plain_not_accepted", finch: AlefE2EFinch, json: "{\"data\": \"value\"}", headers: [{"Content-Type", "text/plain"}])
      assert response.status == 415
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Unsupported media type", "status" => 415, "title" => "Unsupported Media Type", "type" => "https://spikard.dev/errors/unsupported-media-type"}
    end
  end

  describe "vendor_json_accepted" do
    test "Vendor-specific JSON content-type should be accepted" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/17_vendor_json_accepted", finch: AlefE2EFinch, json: %{"data" => "value"}, headers: [{"Content-Type", "application/vnd.api+json"}])
      assert response.status == 201
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => "value"}
    end
  end

  describe "content_type_with_multiple_params" do
    test "Content-Type with multiple parameters should be parsed correctly" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/18_content_type_with_multiple_params", finch: AlefE2EFinch, json: %{"value" => "test"}, headers: [{"Content-Type", "application/json; charset=utf-8; boundary=something"}])
      assert response.status == 201
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"value" => "test"}
    end
  end

  describe "missing_content_type_default_json" do
    test "Missing Content-Type header should default to JSON when body is present" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/19_missing_content_type_default_json", finch: AlefE2EFinch, json: %{"name" => "test"})
      assert response.status == 201
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"name" => "test"}
    end
  end

  describe "content_length_mismatch" do
    test "Content-Length header mismatch with actual body size should fail" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/20_content_length_mismatch", finch: AlefE2EFinch, json: %{"value" => "short"}, headers: [{"Content-Length", "100"}, {"Content-Type", "application/json"}])
      assert response.status == 400
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Content-Length header does not match actual body size", "status" => 400, "title" => "Content-Length header mismatch", "type" => "https://spikard.dev/errors/content-length-mismatch"}
    end
  end

  describe "unsupported_media_type" do
    test "Tests rejection of unsupported content type" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/415_unsupported_media_type", finch: AlefE2EFinch, json: "<?xml version=\"1.0\"?><item><name>Item</name></item>", headers: [{"Content-Type", "application/xml"}])
      assert response.status == 415
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Unsupported media type", "status" => 415, "title" => "Unsupported Media Type", "type" => "https://spikard.dev/errors/unsupported-media-type"}
    end
  end

  describe "content_negotiation_accept_header" do
    test "Tests content negotiation based on Accept header" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/content_negotiation_accept_header", finch: AlefE2EFinch, headers: [{"Accept", "application/json"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"id" => 1, "name" => "Item"}
    end
  end
end
