// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fa06b403270e3c3da4cd204fb21e3c1a52cfd79563d9c34ce969edd497378eee
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
  const contentEncoding = response.headers.get("content-encoding");
  let buffer = await response.arrayBuffer();

  if (contentEncoding === "gzip") {
    const { gunzipSync } = await import("zlib");
    buffer = gunzipSync(new Uint8Array(buffer)).buffer;
  } else if (contentEncoding === "br") {
    const { brotliDecompressSync } = await import("zlib");
    buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
  }

  const text = new TextDecoder().decode(buffer);
  return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("cookies", () => {
  it("cookie_samesite_strict: Cookie with SameSite=Strict attribute should be validated", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/24_cookie_samesite_strict/secure`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
  });

  it("cookie_samesite_lax: Cookie with SameSite=Lax attribute should be validated", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/25_cookie_samesite_lax/data`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
  });

  it("cookie_secure_flag: Cookie with Secure flag should be validated for HTTPS", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/26_cookie_secure_flag/secure`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
  });

  it("cookie_httponly_flag: Cookie with HttpOnly flag should prevent JavaScript access", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/27_cookie_httponly_flag/secure`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
  });

  it("apikey_cookie_authentication_missing: Tests APIKeyCookie authentication returns 403 when cookie missing", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/apikey_cookie_authentication_missing/users/me/auth`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(422);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: null, loc: ["cookie", "key"], msg: "Field required", type: "missing" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });
  });

  it("apikey_cookie_authentication_success: Tests APIKeyCookie authentication with valid cookie", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/apikey_cookie_authentication_success/users/me`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      username: "secret",
    });
  });

  it("cookie_regex_pattern_validation_fail: Tests cookie with regex pattern validation failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/cookie_regex_pattern_validation_fail/cookies/pattern`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(422);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [
        {
          ctx: { pattern: "^[A-Z0-9]{8}$" },
          input: "invalid-format",
          loc: ["cookie", "tracking_id"],
          msg: "String should match pattern '^[A-Z0-9]{8}$'",
          type: "string_pattern_mismatch",
        },
      ],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });
  });

  it("cookie_regex_pattern_validation_success: Tests cookie with regex pattern validation success", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/cookie_regex_pattern_validation_success/cookies/pattern`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      tracking_id: "ABC12345",
    });
  });

  it("cookie_validation_max_length_constraint_fail: Tests cookie validation with max_length constraint failure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/cookie_validation_max_length_constraint_fail/cookies/validated`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(422);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [
        {
          ctx: { max_length: 20 },
          input: "this_cookie_value_is_way_too_long",
          loc: ["cookie", "session_id"],
          msg: "String should have at most 20 characters",
          type: "string_too_long",
        },
      ],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });
  });

  it("cookie_validation_min_length_constraint_success: Tests cookie validation with min_length constraint at boundary", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/cookie_validation_min_length_constraint_success/cookies/min-length`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      token: "abc",
    });
  });

  it("cookie_validation_min_length_failure: Tests cookie parameter with min_length constraint returns 422 when too short", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/cookie_validation_min_length_failure/items/`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(422);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [
        {
          input: "",
          loc: ["cookie", "tracking_id"],
          msg: "String should have at least 3 characters",
          type: "string_too_short",
        },
      ],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });
  });

  it("multiple_cookies_success: Tests multiple cookie parameters in a single request", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/multiple_cookies_success/items/`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      fatebook_tracker: "tracker456",
      googall_tracker: "ga789",
      session_id: "session123",
    });
  });

  it("optional_apikey_cookie_missing: Tests optional APIKeyCookie (auto_error=False) returns None without 403", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/optional_apikey_cookie_missing/users/me`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      msg: "Create an account first",
    });
  });

  it("optional_cookie_parameter_missing: Tests optional cookie parameter returns None when not provided", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/optional_cookie_parameter_missing/items/`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      ads_id: null,
    });
  });

  it("optional_cookie_parameter_success: Tests optional cookie parameter with value provided", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/optional_cookie_parameter_success/items/`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      ads_id: "abc123",
    });
  });

  it("required_cookie_missing: Tests validation error when required cookie is missing", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/required_cookie_missing/items/cookies`;
    const response = await fetch(url, { method: "GET", redirect: "manual" });
    expect(response.status).toBe(422);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      detail: "1 validation error in request",
      errors: [{ input: "", loc: ["cookie", "session_id"], msg: "Field required", type: "missing" }],
      status: 422,
      title: "Request Validation Failed",
      type: "https://spikard.dev/errors/validation-error",
    });
  });

  it("response_cookie_with_domain_attribute: Tests setting cookie with specific domain", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_cookie_with_domain_attribute/cookies/set-with-domain`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ value: "domain_test" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Cookie set with domain",
    });
  });

  it("response_cookie_with_path_attribute: Tests setting cookie with specific path", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_cookie_with_path_attribute/cookies/set-with-path`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ value: "path_test" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Cookie set with path",
    });
  });

  it("response_cookie_with_samesite_lax: Tests setting cookie with SameSite lax attribute", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_cookie_with_samesite_lax/cookies/samesite-lax`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ value: "lax_cookie" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Cookie set with SameSite=Lax",
    });
  });

  it("response_cookie_with_samesite_none: Tests setting cookie with SameSite none (requires Secure)", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_cookie_with_samesite_none/cookies/samesite-none`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ value: "none_cookie" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Cookie set with SameSite=None",
    });
  });

  it("response_cookie_with_samesite_strict: Tests setting cookie with SameSite strict attribute", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_cookie_with_samesite_strict/cookies/samesite-strict`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ value: "strict_cookie" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Cookie set with SameSite=Strict",
    });
  });

  it("response_delete_cookie: Tests deleting a cookie by setting max_age to 0", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_delete_cookie/cookies/delete`;
    const response = await fetch(url, { method: "POST", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Cookie deleted",
    });
  });

  it("response_multiple_cookies: Tests setting multiple cookies in single response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_multiple_cookies/cookies/multiple`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ session: "session123", user: "john" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Multiple cookies set",
    });
  });

  it("response_session_cookie_no_max_age: Tests setting session cookie without max_age (expires with browser)", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_session_cookie_no_max_age/cookies/session`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ value: "session_abc123" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Session cookie set",
    });
  });

  it("response_set_cookie_basic: Tests setting a cookie in the response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/response_set_cookie_basic/cookie/`;
    const response = await fetch(url, { method: "POST", redirect: "manual" });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Come to the dark side, we have cookies",
    });
  });
});
