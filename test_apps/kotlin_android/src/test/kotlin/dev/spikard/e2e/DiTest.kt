// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2a5a90b766eca1206c1cbd4e3f5c8797895d4a5a3411c48af1270b3074c9ed23
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.spikard.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule

/** E2e tests for category: di. */
class DiTest {

    companion object {
        init {
            try {
                System.loadLibrary("spikard_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load spikard_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testAsyncFactoryDependencySuccess() {
        // Tests async factory that creates database pool asynchronously
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/async_factory_dependency_success/api/db-status")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"max_size\":10,\"pool_status\":\"connected\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testCircularDependencyDetectionError() {
        // Tests that circular dependencies (A depends on B, B depends on A) are detected and rejected at registration time
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/circular_dependency_detection_error/api/circular")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(500, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"detail\":\"Circular dependency detected\",\"errors\":[{\"cycle\":[\"service_a\",\"service_b\",\"service_a\"],\"msg\":\"Circular dependency detected in dependency graph\",\"type\":\"circular_dependency\"}],\"status\":500,\"title\":\"Dependency Resolution Failed\",\"type\":\"https://spikard.dev/errors/dependency-error\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testDependencyInjectionInLifecycleHooksSuccess() {
        // Tests accessing dependencies in lifecycle hooks (onRequest, preHandler) for auth, logging, etc.
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/dependency_injection_in_lifecycle_hooks_success/api/hook-di-test")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
            .header("authorization", "Bearer valid_token")
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        assertTrue(response.headers().firstValue("X-Auth-Mode").orElse("").contains("strict"), "header X-Auth-Mode mismatch")
        assertTrue(response.headers().firstValue("X-Log-Level").orElse("").contains("debug"), "header X-Log-Level mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"authenticated\":true,\"logged\":true}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testFactoryDependencySuccess() {
        // Tests factory dependency that creates instances on-demand
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/factory_dependency_success/api/timestamp")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"timestamp\":\"<<present>>\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testMissingDependencyError() {
        // Tests error when handler requires a dependency that was never registered
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/missing_dependency_error/api/missing-dep")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(500, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"detail\":\"Required dependency not found\",\"errors\":[{\"dependency_key\":\"non_existent_service\",\"msg\":\"Dependency 'non_existent_service' is not registered\",\"type\":\"missing_dependency\"}],\"status\":500,\"title\":\"Dependency Resolution Failed\",\"type\":\"https://spikard.dev/errors/dependency-error\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testMixedSingletonAndPerRequestCachingSuccess() {
        // Tests mixing singleton dependencies (shared across requests) with per-request dependencies (cached within request)
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/mixed_singleton_and_per_request_caching_success/api/mixed-caching")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"app_name\":\"MyApp\",\"context_id\":\"<<uuid>>\",\"pool_id\":\"<<uuid>>\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testMultipleDependenciesWithCleanupSuccess() {
        // Tests cleanup for multiple dependencies with generator pattern, ensuring all are cleaned up in reverse resolution order
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/multiple_dependencies_with_cleanup_success/api/multi-cleanup-test")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"session_active\":true}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testNestedDependencies3LevelsSuccess() {
        // Tests dependency resolution where auth depends on cache and db, which both depend on config
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/nested_dependencies_3_levels_success/api/auth-status")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"auth_enabled\":true,\"has_cache\":true,\"has_db\":true}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testNodeJsObjectDestructuringInjectionSuccess() {
        // Tests Node.js/TypeScript-specific object destructuring pattern for dependency injection
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/node_js_object_destructuring_injection_success/api/node-destructure")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"db_name\":\"PostgreSQL\",\"log_level\":\"info\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testPerRequestDependencyCachingSuccess() {
        // Tests per-request caching where dependency is created once per request but shared by multiple usages within that request
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/per_request_dependency_caching_success/api/request-id")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"first_id\":\"<<uuid>>\",\"second_id\":\"<<same_as:first_id>>\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testPythonParameterNameBasedInjectionSuccess() {
        // Tests Python-specific parameter name-based dependency injection where dependencies are matched by parameter names
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/python_parameter_name_based_injection_success/api/python-name-inject")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"cache_status\":\"ready\",\"db_status\":\"connected\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testPythonTypeAnnotationBasedInjectionSuccess() {
        // Tests Python-specific type annotation-based dependency injection where dependencies are matched by type hints
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/python_type_annotation_based_injection_success/api/python-type-inject")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"cache_type\":\"Redis\",\"pool_type\":\"PostgreSQL\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testResourceCleanupAfterRequestSuccess() {
        // Tests generator pattern cleanup where dependency resources are cleaned up after handler completes
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/resource_cleanup_after_request_success/api/cleanup-test")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"session_id\":\"<<uuid>>\",\"status\":\"completed\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testRouteLevelDependencyOverrideSuccess() {
        // Tests route-level dependency override of app-level dependency for testing or special cases
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/route_level_dependency_override_success/api/override-test")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"mode\":\"test\",\"strict\":false}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testRubyKeywordArgumentInjectionSuccess() {
        // Tests Ruby-specific keyword argument pattern for dependency injection
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/ruby_keyword_argument_injection_success/api/ruby-kwargs")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"adapter\":\"postgresql\",\"user_id\":42}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testSingletonDependencyCachingSuccess() {
        // Tests singleton dependency that is created once and shared across all requests
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/singleton_dependency_caching_success/api/app-counter")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"count\":1,\"counter_id\":\"<<uuid>>\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testTypeMismatchInDependencyResolutionError() {
        // Tests error when handler expects a different type than what the dependency provides
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/type_mismatch_in_dependency_resolution_error/api/type-mismatch")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(500, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"detail\":\"Dependency type mismatch\",\"errors\":[{\"actual_type\":\"string\",\"dependency_key\":\"config\",\"expected_type\":\"object\",\"msg\":\"Dependency 'config' type mismatch: expected object, got string\",\"type\":\"type_mismatch\"}],\"status\":500,\"title\":\"Dependency Resolution Failed\",\"type\":\"https://spikard.dev/errors/dependency-error\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

    @Test
    fun testValueDependencyInjectionSuccess() {
        // Tests simple value injection (config, constants) into a handler
        val baseUrl = System.getenv("SUT_URL") ?: "http://127.0.0.1:8007"
        val uri = java.net.URI.create("$baseUrl/fixtures/value_dependency_injection_success/api/config")
        val builder = java.net.http.HttpRequest.newBuilder(uri)
            .method("GET", java.net.http.HttpRequest.BodyPublishers.noBody())
        val response = java.net.http.HttpClient.newHttpClient()
            .send(builder.build(), java.net.http.HttpResponse.BodyHandlers.ofString())
        assertEquals(200, response.statusCode(), "status code mismatch")
        val bodyJson = MAPPER.readTree(response.body())
        val expectedJson = MAPPER.readTree("{\"app_name\":\"SpikardApp\",\"max_connections\":100,\"version\":\"1.0.0\"}")
        assertEquals(expectedJson, bodyJson, "body mismatch")
    }

}
