# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:35eddf0c1c2d5026810d7c6764f4a07eb6f6a4842f16e260e28a28acfe53d607
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "spikard-rb"
version = "0.17.1"
edition = "2024"
license = "MIT"
description = "Codegen-first polyglot web toolkit with a Rust core and bindings for 14 languages"
readme = false
keywords = ["framework", "http", "polyglot", "rust", "web"]
categories = []

[package.metadata.cargo-machete]
ignored = ["rb-sys"]

[lib]
name = "spikard_rb"
path = "../src/lib.rs"
crate-type = ["cdylib"]

[features]
default = ["di"]
di = ["spikard/di"]

[dependencies]
magnus = "0.8"
rb-sys = ">=0.9.130, <0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
spikard = { version = "0.17.1", path = "../../../../../crates/spikard", features = ["di"] }
spikard-core = { path = "../../../../../crates/spikard-core", version = "0.17.1" }
spikard-graphql = { path = "../../../../../crates/spikard-graphql", version = "0.17.1" }
spikard-http = { path = "../../../../../crates/spikard-http", version = "0.17.1" }
tokio = { version = "1", features = ["rt-multi-thread"] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
