// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:17d845fc7038350c31884e1dc9ca25e643d4426359502ca9b4fb0a0318440b1d
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Spikard;

/// <summary>
/// Per-route roles/scopes/permissions authorization requirement.
///
/// `spikard_http.auth.Claims` does not yet carry roles, scopes, or permissions, so nothing can
/// enforce this today. This type only defines the requirement shape; a later phase must extend
/// `Claims` (or an equivalent claims-decoding path) to populate them before enforcement is
/// possible.
///
/// Deserialization goes through `AuthorizationConfigRepr` rather than a derive so the fixture's
/// singular `{"required_role": "admin"}` shape (`fixtures/problem_details.json`'s
/// `problem_details_403_forbidden`) populates `required_roles` instead of being silently dropped
/// as an unrecognized field — a config that parses to "no constraint" from real authorization
/// data is a vacuous-pass bug, not a compatibility shim. `deny_unknown_fields` on the repr means
/// any other unrecognized key is a loud deserialize error instead.
/// </summary>
public sealed record AuthorizationConfig
{
    /// <summary>
    /// Roles the authenticated caller must have
    /// </summary>
    [JsonPropertyName("required_roles")]
    public List<string> RequiredRoles { get; init; } = [];

    /// <summary>
    /// OAuth-style scopes the authenticated caller must have
    /// </summary>
    [JsonPropertyName("required_scopes")]
    public List<string> RequiredScopes { get; init; } = [];

    /// <summary>
    /// Fine-grained permissions the authenticated caller must have
    /// </summary>
    [JsonPropertyName("required_permissions")]
    public List<string> RequiredPermissions { get; init; } = [];

    /// <summary>
    /// When true, the caller must satisfy every listed requirement (AND); when false, any single
    /// listed requirement is sufficient (OR)
    /// </summary>
    [JsonPropertyName("require_all")]
    public bool RequireAll { get; init; } = true;


    /// <summary>
    /// Parse a <see cref="AuthorizationConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="SpikardException">When the JSON cannot be deserialised.</exception>
    public static AuthorizationConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<AuthorizationConfig>(json, JsonOptions)
                ?? throw new SpikardException($"Failed to parse AuthorizationConfig from JSON: deserializer returned null");
        }
        catch (SpikardException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new SpikardException($"Failed to parse AuthorizationConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static AuthorizationConfig Default()
    {
        var nativeResult = NativeMethods.AuthorizationConfigDefault();
        var jsonPtr = NativeMethods.AuthorizationConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.AuthorizationConfigFree(nativeResult);
        return JsonSerializer.Deserialize<AuthorizationConfig>(json ?? "null", JsonOptions)!;
    }
}
