// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8553f599215311e8fdd84f95bd460bc52bbf72ed1e01d52e452a5c628aa3823e
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Spikard;

/// <summary>
/// Per-route API key authentication requirement.
///
/// Mirrors `spikard_http.ApiKeyConfig` for the same reason `JwtAuthConfig` mirrors
/// `spikard_http.JwtConfig`: `spikard-core` cannot depend on `spikard-http`.
/// </summary>
public sealed record ApiKeyAuthConfig
{
    /// <summary>
    /// Whether this per-route API key auth requirement is active. Present on 10/10 `api_key_auth`
    /// fixture payloads in the corpus; defaults to `true` for the same reason as
    /// `JwtAuthConfig.enabled`.
    /// </summary>
    [JsonPropertyName("enabled")]
    public bool Enabled { get; init; } = true;

    /// <summary>
    /// Valid API keys. Defaults to empty (rather than being a required field) so that
    /// `fixtures/server_config.json`'s `server_jwt_and_api_key_auth_combined` payload
    /// (`{"enabled": true, "header": "X-API-Key"}`, no `keys` at all) deserializes instead of
    /// hard-failing with "missing field `keys`". An empty list is NOT "allow everyone": a later
    /// enforcement phase MUST treat an empty `keys` list as a hard misconfiguration error, never
    /// as an open gate.
    /// </summary>
    [JsonPropertyName("keys")]
    public List<string> Keys { get; init; } = [];

    /// <summary>
    /// Header name to check (e.g., "X-API-Key")
    /// </summary>
    [JsonPropertyName("header_name")]
    public string HeaderName { get; init; } = "X-API-Key";


    /// <summary>
    /// Parse a <see cref="ApiKeyAuthConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="SpikardException">When the JSON cannot be deserialised.</exception>
    public static ApiKeyAuthConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<ApiKeyAuthConfig>(json, JsonOptions)
                ?? throw new SpikardException($"Failed to parse ApiKeyAuthConfig from JSON: deserializer returned null");
        }
        catch (SpikardException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new SpikardException($"Failed to parse ApiKeyAuthConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
