// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:7848510f5ff8e5e016683275d9821fedc47b8ce6bf475eb0f78d36e8b491ca27
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Spikard;

// The native handle is the scalar `AlefHandle` (`uint64_t`) registry index, not a pointer.
// `SafeHandle`'s only storage slot is a pointer-width `IntPtr`, so the `ulong` is packed into
// that slot as a raw bit pattern (never dereferenced) to keep `SafeHandle`'s guaranteed
// finalization and `DangerousAddRef`/`DangerousRelease` ref-counting, which the borrow/lease/
// transfer machinery below builds on. This is lossless on every currently supported .NET
// target (IntPtr is 8 bytes on all 64-bit platforms .NET 5+ ships on); on a true 32-bit
// process a handle value above `uint.MaxValue` would truncate, but that requires over four
// billion concurrently live handles of this type, and 32-bit .NET targets are legacy-only.
internal sealed class GraphQLRouteConfigSafeHandle : SafeHandle
{
    internal GraphQLRouteConfigSafeHandle(ulong handle) : base(IntPtr.Zero, true)
    {
        SetHandle(Pack(handle));
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    internal static IntPtr Pack(ulong value) => unchecked((IntPtr)(long)value);

    internal static ulong Unpack(IntPtr value) => unchecked((ulong)(long)value);

    protected override bool ReleaseHandle()
    {
        NativeMethods.GraphQLRouteConfigFree(Unpack(handle));
        return true;
    }
}
/// <summary>/// Configuration for GraphQL routes/// /// Provides a builder pattern for configuring GraphQL route parameters/// for the Spikard HTTP server's routing system./// </summary>
public sealed class GraphQLRouteConfig : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private GraphQLRouteConfigSafeHandle _safeHandle;
    private readonly object _handleLock = new();
    private int _activeBorrows;
    private bool _handleUnavailable;
    private bool _transferPending;

    internal sealed class HandleLease : IDisposable
    {
        private GraphQLRouteConfig? _owner;

        internal HandleLease(GraphQLRouteConfig owner, ulong handle)
        {
            _owner = owner;
            Handle = handle;
        }

        internal ulong Handle { get; }

        public void Dispose()
        {
            var owner = global::System.Threading.Interlocked.Exchange(ref _owner, null);
            owner?.ReleaseBorrow();
        }
    }

    private sealed class HandleTransfer : IDisposable
    {
        private GraphQLRouteConfig? _owner;
        private GraphQLRouteConfigSafeHandle? _transferredHandle;

        internal HandleTransfer(GraphQLRouteConfig owner, ulong handle)
        {
            _owner = owner;
            _transferredHandle = new GraphQLRouteConfigSafeHandle(handle);
        }

        internal ulong Handle => _transferredHandle is { } transferredHandle
            ? GraphQLRouteConfigSafeHandle.Unpack(transferredHandle.DangerousGetHandle())
            : throw new InvalidOperationException("Handle transfer is no longer active");

        internal void Commit()
        {
            var transferredHandle = _transferredHandle
                ?? throw new InvalidOperationException("Handle transfer is no longer active");
            transferredHandle.SetHandleAsInvalid();
            _transferredHandle = null;
            var owner = _owner;
            _owner = null;
            owner?.CommitTransfer();
        }

        public void Dispose()
        {
            var transferredHandle = global::System.Threading.Interlocked.Exchange(ref _transferredHandle, null);
            var owner = global::System.Threading.Interlocked.Exchange(ref _owner, null);
            if (transferredHandle is null) return;
            if (owner is null)
            {
                transferredHandle.Dispose();
                return;
            }
            var handle = GraphQLRouteConfigSafeHandle.Unpack(transferredHandle.DangerousGetHandle());
            transferredHandle.SetHandleAsInvalid();
            owner.RollbackTransfer(handle);
        }
    }

    internal GraphQLRouteConfig(ulong handle)
    {
        _safeHandle = new GraphQLRouteConfigSafeHandle(handle);
    }

    internal ulong Handle => GetHandle();

    private ulong GetHandle()
    {
        lock (_handleLock)
        {
            ThrowIfHandleUnavailable();
            return GraphQLRouteConfigSafeHandle.Unpack(_safeHandle.DangerousGetHandle());
        }
    }

    internal HandleLease BorrowHandle()
    {
        lock (_handleLock)
        {
            ThrowIfHandleUnavailable();
            var lease = new HandleLease(this, GraphQLRouteConfigSafeHandle.Unpack(_safeHandle.DangerousGetHandle()));
            var addRefSucceeded = false;
            _safeHandle.DangerousAddRef(ref addRefSucceeded);
            if (!addRefSucceeded) throw new ObjectDisposedException(nameof(GraphQLRouteConfig));
            _activeBorrows++;
            return lease;
        }
    }

    private void ReleaseBorrow()
    {
        lock (_handleLock)
        {
            _safeHandle.DangerousRelease();
            _activeBorrows--;
            if (_activeBorrows == 0) global::System.Threading.Monitor.PulseAll(_handleLock);
        }
    }

    private HandleTransfer TakeHandle()
    {
        lock (_handleLock)
        {
            ThrowIfHandleUnavailable();
            WaitForBorrowedCalls();
            var transfer = new HandleTransfer(this, GraphQLRouteConfigSafeHandle.Unpack(_safeHandle.DangerousGetHandle()));
            _handleUnavailable = true;
            _safeHandle.SetHandleAsInvalid();
            _transferPending = true;
            return transfer;
        }
    }

    private void RollbackTransfer(ulong handle)
    {
        lock (_handleLock)
        {
            _transferPending = false;
            _safeHandle = new GraphQLRouteConfigSafeHandle(handle);
            _handleUnavailable = false;
            global::System.Threading.Monitor.PulseAll(_handleLock);
        }
    }

    private void CommitTransfer()
    {
        lock (_handleLock)
        {
            _transferPending = false;
            global::System.Threading.Monitor.PulseAll(_handleLock);
        }
    }

    private void WaitForBorrowedCalls()
    {
        var interrupted = false;
        while (_activeBorrows != 0)
        {
            try
            {
                global::System.Threading.Monitor.Wait(_handleLock);
            }
            catch (global::System.Threading.ThreadInterruptedException)
            {
                interrupted = true;
            }
        }
        if (interrupted) global::System.Threading.Thread.CurrentThread.Interrupt();
    }

    private void WaitForPendingTransfer()
    {
        var interrupted = false;
        while (_transferPending)
        {
            try
            {
                global::System.Threading.Monitor.Wait(_handleLock);
            }
            catch (global::System.Threading.ThreadInterruptedException)
            {
                interrupted = true;
            }
        }
        if (interrupted) global::System.Threading.Thread.CurrentThread.Interrupt();
    }

    private void ThrowIfHandleUnavailable()
    {
        if (_handleUnavailable || _safeHandle.IsClosed || _safeHandle.IsInvalid)
        {
            throw new ObjectDisposedException(nameof(GraphQLRouteConfig));
        }
    }

    public void Dispose()
    {
        lock (_handleLock)
        {
            if (_transferPending)
            {
                WaitForPendingTransfer();
                if (_handleUnavailable) return;
            }
            if (_handleUnavailable) return;
            _handleUnavailable = true;
            WaitForBorrowedCalls();
            _safeHandle.Dispose();
        }
    }


    /// <summary>
    /// Create a new GraphQL route configuration with defaults
    ///
    /// Default values:
    /// - path: "/graphql"
    /// - method: "POST"
    /// - `enable_playground`: false
    /// </summary>
    public static GraphQLRouteConfig New()
    {
        var nativeResult = NativeMethods.GraphQLRouteConfigNew(

        );
        if (nativeResult == 0)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigNew failed");
        }
        var returnValue = new GraphQLRouteConfig(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Set the HTTP path for the GraphQL endpoint
    /// </summary>
    public GraphQLRouteConfig Path(string path)
    {
        using var handleTransfer = TakeHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigPath(
            handleTransfer.Handle,
            path
        );
        handleTransfer.Commit();
        if (nativeResult == 0)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigPath failed");
        }
        var returnValue = new GraphQLRouteConfig(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Set the HTTP method for the GraphQL endpoint
    /// </summary>
    public GraphQLRouteConfig Method(string method)
    {
        using var handleTransfer = TakeHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigMethod(
            handleTransfer.Handle,
            method
        );
        handleTransfer.Commit();
        if (nativeResult == 0)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigMethod failed");
        }
        var returnValue = new GraphQLRouteConfig(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Enable or disable the GraphQL Playground UI
    /// </summary>
    public GraphQLRouteConfig EnablePlayground(bool enable)
    {
        using var handleTransfer = TakeHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigEnablePlayground(
            handleTransfer.Handle,
            (enable ? 1 : 0)
        );
        handleTransfer.Commit();
        if (nativeResult == 0)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigEnablePlayground failed");
        }
        var returnValue = new GraphQLRouteConfig(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Set a custom description for documentation
    /// </summary>
    public GraphQLRouteConfig Description(string description)
    {
        using var handleTransfer = TakeHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigDescription(
            handleTransfer.Handle,
            description
        );
        handleTransfer.Commit();
        if (nativeResult == 0)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigDescription failed");
        }
        var returnValue = new GraphQLRouteConfig(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the configured path
    /// </summary>
    public string GetPath()
    {
        using var handleLease = BorrowHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigGetPath(
            handleLease.Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigGetPath failed");
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Get the configured method
    /// </summary>
    public string GetMethod()
    {
        using var handleLease = BorrowHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigGetMethod(
            handleLease.Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigGetMethod failed");
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Check if playground is enabled
    /// </summary>
    public bool IsPlaygroundEnabled()
    {
        using var handleLease = BorrowHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigIsPlaygroundEnabled(
            handleLease.Handle
        );
        var returnValue = nativeResult != 0;
        return returnValue;
    }

    /// <summary>
    /// Get the description if set
    /// </summary>
    public string? GetDescription()
    {
        using var handleLease = BorrowHandle();
        var nativeResult = NativeMethods.GraphQLRouteConfigGetDescription(
            handleLease.Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    public static GraphQLRouteConfig Default()
    {
        var nativeResult = NativeMethods.GraphQLRouteConfigDefault(

        );
        if (nativeResult == 0)
        {
            throw SpikardException.FromLastError("GraphQLRouteConfigDefault failed");
        }
        var returnValue = new GraphQLRouteConfig(nativeResult);
        return returnValue;
    }
}
