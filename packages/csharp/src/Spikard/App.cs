// Auto-generated by alef — DO NOT EDIT
#nullable enable

namespace Spikard {

using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices;

/// <summary>
/// Service wrapper for App.
/// </summary>
public class App {

    private IntPtr _handle;
    private static readonly Dictionary<IntPtr, GCHandle> _registeredCallbacks = new();
    private static readonly NativeMethods.HandlerCallback _handlerCallback = HandlerTrampoline;

    /// <summary>
    /// Create a new App.
    /// </summary>
    public App() {
        _handle = NativeMethods.spikard_app_new();
    }

    /// <summary>
    /// Configure config.
    /// </summary>
    public App config(string config) {
        // Store configuration if needed
        return this;
    }

    /// <summary>
    /// Register a handler for route.
    /// </summary>
    public int route(RouteBuilder builder, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_register_route(
            _handle,
            _handlerCallback,
            ctx,
            builder.Handle
        );

        if (result == 0) {
            // Keep the GCHandle alive for the lifetime of the registration
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a GET route at the given path.
    /// </summary>
    public int Get(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_get(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a POST route at the given path.
    /// </summary>
    public int Post(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_post(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a PUT route at the given path.
    /// </summary>
    public int Put(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_put(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a PATCH route at the given path.
    /// </summary>
    public int Patch(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_patch(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a DELETE route at the given path.
    /// </summary>
    public int Delete(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_delete(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a HEAD route at the given path.
    /// </summary>
    public int Head(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_head(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register an OPTIONS route at the given path.
    /// </summary>
    public int Options(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_options(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a CONNECT route at the given path.
    /// </summary>
    public int Connect(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_connect(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// Register a TRACE route at the given path.
    /// </summary>
    public int Trace(string path, Delegate handler) {
        var handle = GCHandle.Alloc(handler, GCHandleType.Normal);
        IntPtr ctx = GCHandle.ToIntPtr(handle);

        int result = NativeMethods.spikard_app_trace(
            _handle,
            _handlerCallback,
            ctx,
            path
        );

        if (result == 0) {
            lock (_registeredCallbacks) {
                _registeredCallbacks[ctx] = handle;
            }
        } else {
            handle.Free();
        }
        return result;
    }

    /// <summary>
    /// run.
    /// </summary>
    public int run() {
        return NativeMethods.spikard_app_ep_run(
            _handle
        );
    }

    /// <summary>
    /// into_router.
    /// </summary>
    public int into_router() {
        return NativeMethods.spikard_app_ep_into_router(
            _handle
        );
    }

    public void Dispose() {
        if (_handle != IntPtr.Zero) {
            NativeMethods.spikard_app_free(_handle);
            _handle = IntPtr.Zero;
        }
        // Clean up all registered callbacks for this instance
        lock (_registeredCallbacks) {
            var keys = _registeredCallbacks.Keys.ToList();
            foreach (var key in keys) {
                var handle = _registeredCallbacks[key];
                handle.Free();
                _registeredCallbacks.Remove(key);
            }
        }
    }

    /// <summary>
    /// Unmanaged callback trampoline that recovers the GC handle and invokes the handler.
    /// </summary>
    public static IntPtr HandlerTrampoline(IntPtr ctx, IntPtr requestJson) {
        try {
            // Recover the GCHandle and invoke the delegate
            var handle = GCHandle.FromIntPtr(ctx);
            if (handle.Target is Func<string, string> handler) {
                // Unmarshal the request JSON
                string requestStr = Marshal.PtrToStringUTF8(requestJson) ?? "{}";
                // Invoke the handler and get the response
                string responseStr = handler(requestStr);
                // Allocate response string in native memory (malloc-backed on Unix; the
                // native side takes ownership and frees it).
                return Marshal.StringToCoTaskMemUTF8(responseStr);
            }
        } catch {
            // Return null on error
        }
        return IntPtr.Zero;
    }

}

}
