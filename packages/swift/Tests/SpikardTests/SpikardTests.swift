import XCTest
@testable import Spikard

final class SpikardTests: XCTestCase {
  func testPlaceholder() throws {
    // Placeholder test so `swift test` has a target to run.
    // Replace or extend with real tests against the Spikard module.
    XCTAssertTrue(true)
  }
}
