# Generated by alef. Do not edit by hand.
[package]
name = "spikard-swift"
version = "0.15.6-rc.4"
edition = "2024"
license = "MIT"

# `ahash` is conditionally referenced by alef-emitted code: only when the
# umbrella crate exposes `AHashMap<Cow<str>, _>` parameters (the conditional
# `__*_ahash` shim rebuilds). It is listed unconditionally in `[dependencies]`
# so the manifest is stable across regens, and ignored here so cargo-machete
# does not flag the downstream (no-ahash-maps) case as unused.
[package.metadata.cargo-machete]
ignored = ["ahash"]

[lib]
crate-type = ["cdylib", "staticlib"]
# The `extern "Swift"` block emits linker references that are only resolvable
# when the crate is linked into a Swift target. `cargo test --workspace` on
# pure-Rust runners (e.g. windows-latest) would otherwise fail with
# undefined `__swift_bridge__$*$alef_visit_*` symbols.
test = false
doctest = false
bench = false

[dependencies]
ahash = "0.8"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
spikard = { version = "0.15.6-rc.4", path = "../../../crates/spikard", features = ["di"] }
spikard-core = { path = "../../../crates/spikard-core", version = "0.15.6-rc.4" }
spikard-graphql = { path = "../../../crates/spikard-graphql", version = "0.15.6-rc.4" }
spikard-http = { path = "../../../crates/spikard-http", version = "0.15.6-rc.4" }
swift-bridge = "0.1.59"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }

[build-dependencies]
swift-bridge-build = "0.1.59"
