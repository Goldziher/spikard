// Generated by alef. Do not edit by hand.

#![allow(unused_variables, unreachable_code)]
#![allow(
    clippy::collapsible_if,
    clippy::collapsible_match,
    clippy::needless_borrows_for_generic_args,
    clippy::field_reassign_with_default,
    clippy::too_many_arguments,
    clippy::clone_on_copy,
    clippy::unnecessary_cast,
    clippy::manual_flatten,
    clippy::match_single_binding,
    clippy::redundant_closure,
    clippy::useless_conversion,
    clippy::inherent_to_string
)]

#[swift_bridge::bridge]
mod ffi {
    extern "Rust" {
        type CorsConfig;
        #[swift_bridge(init)]
        fn new(
            allowed_origins: Vec<String>,
            allowed_methods: Vec<String>,
            allowed_headers: Vec<String>,
            expose_headers: Option<Vec<String>>,
            max_age: Option<u32>,
            allow_credentials: Option<bool>,
        ) -> CorsConfig;
        fn allowed_origins(&self) -> Vec<String>;
        fn allowed_methods(&self) -> Vec<String>;
        fn allowed_headers(&self) -> Vec<String>;
        fn expose_headers(&self) -> Option<Vec<String>>;
        fn max_age(&self) -> Option<u32>;
        fn allow_credentials(&self) -> Option<bool>;
    }

    extern "Rust" {
        type CompressionConfig;
        #[swift_bridge(init)]
        fn new(gzip: bool, brotli: bool, min_size: usize, quality: u32) -> CompressionConfig;
        fn gzip(&self) -> bool;
        fn brotli(&self) -> bool;
        fn min_size(&self) -> usize;
        fn quality(&self) -> u32;
    }

    extern "Rust" {
        type RateLimitConfig;
        #[swift_bridge(init)]
        fn new(per_second: u64, burst: u32, ip_based: bool) -> RateLimitConfig;
        fn per_second(&self) -> u64;
        fn burst(&self) -> u32;
        fn ip_based(&self) -> bool;
    }

    extern "Rust" {
        type JsonRpcMethodInfo;
        fn method_name(&self) -> String;
        fn description(&self) -> Option<String>;
        fn params_schema(&self) -> Option<String>;
        fn result_schema(&self) -> Option<String>;
        fn deprecated(&self) -> bool;
        fn tags(&self) -> Vec<String>;
    }

    extern "Rust" {
        type ProblemDetails;
        fn type_uri(&self) -> String;
        fn title(&self) -> String;
        fn status(&self) -> u16;
        fn detail(&self) -> Option<String>;
        fn instance(&self) -> Option<String>;
        fn extensions(&self) -> String;
    }

    extern "Rust" {
        type GraphQLRouteConfig;
    }

    extern "Rust" {
        #[swift_bridge(swift_name = "graphQlRouteConfigPath")]
        fn graph_ql_route_config_path(client: &GraphQLRouteConfig, path: String) -> GraphQLRouteConfig;
        #[swift_bridge(swift_name = "graphQlRouteConfigMethod")]
        fn graph_ql_route_config_method(client: &GraphQLRouteConfig, method: String) -> GraphQLRouteConfig;
        #[swift_bridge(swift_name = "graphQlRouteConfigEnablePlayground")]
        fn graph_ql_route_config_enable_playground(client: &GraphQLRouteConfig, enable: bool) -> GraphQLRouteConfig;
        #[swift_bridge(swift_name = "graphQlRouteConfigDescription")]
        fn graph_ql_route_config_description(client: &GraphQLRouteConfig, description: String) -> GraphQLRouteConfig;
        #[swift_bridge(swift_name = "graphQlRouteConfigGetPath")]
        fn graph_ql_route_config_get_path(client: &GraphQLRouteConfig) -> String;
        #[swift_bridge(swift_name = "graphQlRouteConfigGetMethod")]
        fn graph_ql_route_config_get_method(client: &GraphQLRouteConfig) -> String;
        #[swift_bridge(swift_name = "graphQlRouteConfigIsPlaygroundEnabled")]
        fn graph_ql_route_config_is_playground_enabled(client: &GraphQLRouteConfig) -> bool;
        #[swift_bridge(swift_name = "graphQlRouteConfigGetDescription")]
        fn graph_ql_route_config_get_description(client: &GraphQLRouteConfig) -> String;
    }

    extern "Rust" {
        type SchemaConfig;
        #[swift_bridge(init)]
        fn new(
            introspection_enabled: bool,
            complexity_limit: Option<usize>,
            depth_limit: Option<usize>,
        ) -> SchemaConfig;
        fn introspection_enabled(&self) -> bool;
        fn complexity_limit(&self) -> Option<usize>;
        fn depth_limit(&self) -> Option<usize>;
    }

    extern "Rust" {
        type QueryOnlyConfig;
        #[swift_bridge(init)]
        fn new(
            introspection_enabled: bool,
            complexity_limit: Option<usize>,
            depth_limit: Option<usize>,
        ) -> QueryOnlyConfig;
        fn introspection_enabled(&self) -> bool;
        fn complexity_limit(&self) -> Option<usize>;
        fn depth_limit(&self) -> Option<usize>;
    }

    extern "Rust" {
        type QueryMutationConfig;
        #[swift_bridge(init)]
        fn new(
            introspection_enabled: bool,
            complexity_limit: Option<usize>,
            depth_limit: Option<usize>,
        ) -> QueryMutationConfig;
        fn introspection_enabled(&self) -> bool;
        fn complexity_limit(&self) -> Option<usize>;
        fn depth_limit(&self) -> Option<usize>;
    }

    extern "Rust" {
        type FullSchemaConfig;
        #[swift_bridge(init)]
        fn new(
            introspection_enabled: bool,
            complexity_limit: Option<usize>,
            depth_limit: Option<usize>,
        ) -> FullSchemaConfig;
        fn introspection_enabled(&self) -> bool;
        fn complexity_limit(&self) -> Option<usize>;
        fn depth_limit(&self) -> Option<usize>;
    }

    extern "Rust" {
        type AsyncApiConfig;
        #[swift_bridge(init)]
        fn new(enabled: bool, spec: Option<String>) -> AsyncApiConfig;
        fn enabled(&self) -> bool;
        fn spec(&self) -> Option<String>;
    }

    extern "Rust" {
        type ParsedChannel;
        fn name(&self) -> String;
        fn address(&self) -> String;
        fn messages(&self) -> Vec<String>;
        fn bindings(&self) -> Option<String>;
    }

    extern "Rust" {
        type ParsedOperation;
        fn name(&self) -> String;
        fn action(&self) -> String;
        fn channel(&self) -> String;
    }

    extern "Rust" {
        type ParsedMessage;
        fn name(&self) -> String;
        fn schema(&self) -> Option<String>;
    }

    extern "Rust" {
        type ParseResult;
        fn spec_version(&self) -> String;
        fn title(&self) -> String;
        fn api_version(&self) -> String;
        fn channels(&self) -> Vec<ParsedChannel>;
        fn operations(&self) -> Vec<ParsedOperation>;
        fn messages(&self) -> Vec<ParsedMessage>;
    }

    extern "Rust" {
        type ParseRequest;
        fn spec(&self) -> String;
    }

    extern "Rust" {
        type ValidationResponse;
        fn valid(&self) -> bool;
    }

    extern "Rust" {
        type ValidateRequest;
        fn spec(&self) -> String;
        fn channel(&self) -> String;
        fn message(&self) -> String;
        fn payload(&self) -> String;
    }

    extern "Rust" {
        type BackgroundTaskConfig;
        #[swift_bridge(init)]
        fn new(max_queue_size: usize, max_concurrent_tasks: usize, drain_timeout_secs: u64) -> BackgroundTaskConfig;
        fn max_queue_size(&self) -> usize;
        fn max_concurrent_tasks(&self) -> usize;
        fn drain_timeout_secs(&self) -> u64;
    }

    extern "Rust" {
        type BackgroundJobMetadata;
        #[swift_bridge(init)]
        fn new(name: String, request_id: Option<String>) -> BackgroundJobMetadata;
        fn name(&self) -> String;
        fn request_id(&self) -> Option<String>;
    }

    extern "Rust" {
        type GrpcConfig;
        #[swift_bridge(init)]
        fn new(
            enabled: bool,
            max_message_size: usize,
            enable_compression: bool,
            request_timeout: Option<u64>,
            max_concurrent_streams: u32,
            enable_keepalive: bool,
            keepalive_interval: u64,
            keepalive_timeout: u64,
            max_stream_response_bytes: Option<usize>,
        ) -> GrpcConfig;
        fn enabled(&self) -> bool;
        fn max_message_size(&self) -> usize;
        fn enable_compression(&self) -> bool;
        fn request_timeout(&self) -> Option<u64>;
        fn max_concurrent_streams(&self) -> u32;
        fn enable_keepalive(&self) -> bool;
        fn keepalive_interval(&self) -> u64;
        fn keepalive_timeout(&self) -> u64;
        fn max_stream_response_bytes(&self) -> Option<usize>;
    }

    extern "Rust" {
        type JsonRpcConfig;
        #[swift_bridge(init)]
        fn new(enabled: bool, endpoint_path: String, enable_batch: bool, max_batch_size: usize) -> JsonRpcConfig;
        fn enabled(&self) -> bool;
        fn endpoint_path(&self) -> String;
        fn enable_batch(&self) -> bool;
        fn max_batch_size(&self) -> usize;
    }

    extern "Rust" {
        type OpenApiConfig;
        #[swift_bridge(init)]
        fn new(
            enabled: bool,
            title: String,
            version: String,
            description: Option<String>,
            swagger_ui_path: String,
            redoc_path: String,
            openapi_json_path: String,
            contact: Option<ContactInfo>,
            license: Option<LicenseInfo>,
            servers: Vec<ServerInfo>,
            security_schemes: String,
        ) -> OpenApiConfig;
        fn enabled(&self) -> bool;
        fn title(&self) -> String;
        fn version(&self) -> String;
        fn description(&self) -> Option<String>;
        fn swagger_ui_path(&self) -> String;
        fn redoc_path(&self) -> String;
        fn openapi_json_path(&self) -> String;
        fn contact(&self) -> Option<ContactInfo>;
        fn license(&self) -> Option<LicenseInfo>;
        fn servers(&self) -> Vec<ServerInfo>;
        fn security_schemes(&self) -> String;
    }

    extern "Rust" {
        type ContactInfo;
        fn name(&self) -> Option<String>;
        fn email(&self) -> Option<String>;
        fn url(&self) -> Option<String>;
    }

    extern "Rust" {
        type LicenseInfo;
        fn name(&self) -> String;
        fn url(&self) -> Option<String>;
    }

    extern "Rust" {
        type ServerInfo;
        fn url(&self) -> String;
        fn description(&self) -> Option<String>;
    }

    extern "Rust" {
        type Response;
        #[swift_bridge(init)]
        fn new(content: Option<String>, status_code: u16, headers: String) -> Response;
        fn content(&self) -> Option<String>;
        fn status_code(&self) -> u16;
        fn headers(&self) -> String;
    }

    extern "Rust" {
        type SseEvent;
        fn event_type(&self) -> Option<String>;
        fn data(&self) -> String;
        fn id(&self) -> Option<String>;
        fn retry(&self) -> Option<u64>;
    }

    extern "Rust" {
        type JwtConfig;
        fn secret(&self) -> String;
        fn algorithm(&self) -> String;
        fn audience(&self) -> Option<Vec<String>>;
        fn issuer(&self) -> Option<String>;
        fn leeway(&self) -> u64;
    }

    extern "Rust" {
        type ApiKeyConfig;
        fn keys(&self) -> Vec<String>;
        fn header_name(&self) -> String;
    }

    extern "Rust" {
        type StaticFilesConfig;
        fn directory(&self) -> String;
        fn route_prefix(&self) -> String;
        fn index_file(&self) -> bool;
        fn cache_control(&self) -> Option<String>;
    }

    extern "Rust" {
        type ServerConfig;
        #[swift_bridge(init)]
        fn new(
            host: String,
            port: u16,
            workers: usize,
            enable_request_id: bool,
            max_body_size: Option<usize>,
            request_timeout: Option<u64>,
            compression: Option<CompressionConfig>,
            rate_limit: Option<RateLimitConfig>,
            jwt_auth: Option<JwtConfig>,
            api_key_auth: Option<ApiKeyConfig>,
            static_files: Vec<StaticFilesConfig>,
            graceful_shutdown: bool,
            shutdown_timeout: u64,
            asyncapi: Option<AsyncApiConfig>,
            openapi: Option<OpenApiConfig>,
            jsonrpc: Option<JsonRpcConfig>,
            grpc: Option<GrpcConfig>,
            background_tasks: BackgroundTaskConfig,
            enable_http_trace: bool,
        ) -> ServerConfig;
        fn host(&self) -> String;
        fn port(&self) -> u16;
        fn workers(&self) -> usize;
        fn enable_request_id(&self) -> bool;
        fn max_body_size(&self) -> Option<usize>;
        fn request_timeout(&self) -> Option<u64>;
        fn compression(&self) -> Option<CompressionConfig>;
        fn rate_limit(&self) -> Option<RateLimitConfig>;
        fn jwt_auth(&self) -> Option<JwtConfig>;
        fn api_key_auth(&self) -> Option<ApiKeyConfig>;
        fn static_files(&self) -> Vec<StaticFilesConfig>;
        fn graceful_shutdown(&self) -> bool;
        fn shutdown_timeout(&self) -> u64;
        fn asyncapi(&self) -> Option<AsyncApiConfig>;
        fn openapi(&self) -> Option<OpenApiConfig>;
        fn jsonrpc(&self) -> Option<JsonRpcConfig>;
        fn grpc(&self) -> Option<GrpcConfig>;
        fn background_tasks(&self) -> BackgroundTaskConfig;
        fn enable_http_trace(&self) -> bool;
    }

    extern "Rust" {
        type Method;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        type SecuritySchemeInfo;
        fn to_string(&self) -> String;
    }

    extern "Rust" {
        #[swift_bridge(swift_name = "schemaQueryOnly")]
        fn schema_query_only() -> QueryOnlyConfig;
        #[swift_bridge(swift_name = "schemaQueryMutation")]
        fn schema_query_mutation() -> QueryMutationConfig;
        #[swift_bridge(swift_name = "schemaFull")]
        fn schema_full() -> FullSchemaConfig;
    }
}

pub struct CorsConfig(pub spikard_core::CorsConfig);
impl CorsConfig {
    pub fn new(
        allowed_origins: Vec<String>,
        allowed_methods: Vec<String>,
        allowed_headers: Vec<String>,
        expose_headers: Option<Vec<String>>,
        max_age: Option<u32>,
        allow_credentials: Option<bool>,
    ) -> CorsConfig {
        let mut __target: spikard_core::CorsConfig = ::std::default::Default::default();
        if let Ok(__v) = ::serde_json::to_value(allowed_origins) {
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.allowed_origins = t;
            }
        }
        if let Ok(__v) = ::serde_json::to_value(allowed_methods) {
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.allowed_methods = t;
            }
        }
        if let Ok(__v) = ::serde_json::to_value(allowed_headers) {
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.allowed_headers = t;
            }
        }
        if let Ok(__v) = ::serde_json::to_value(expose_headers) {
            if let Ok(t) = ::serde_json::from_value(__v) {
                __target.expose_headers = t;
            }
        }
        __target.max_age = max_age;
        __target.allow_credentials = allow_credentials;
        CorsConfig(__target)
    }
    pub fn allowed_origins(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.allowed_origins)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn allowed_methods(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.allowed_methods)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn allowed_headers(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.allowed_headers)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn expose_headers(&self) -> Option<Vec<String>> {
        self.0.expose_headers.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn max_age(&self) -> Option<u32> {
        self.0.max_age.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn allow_credentials(&self) -> Option<bool> {
        self.0.allow_credentials.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct CompressionConfig(pub spikard_core::CompressionConfig);
impl CompressionConfig {
    pub fn new(gzip: bool, brotli: bool, min_size: usize, quality: u32) -> CompressionConfig {
        let mut __target: spikard_core::CompressionConfig = ::std::default::Default::default();
        __target.gzip = gzip;
        __target.brotli = brotli;
        __target.min_size = min_size;
        __target.quality = quality;
        CompressionConfig(__target)
    }
    pub fn gzip(&self) -> bool {
        ::serde_json::to_value(&self.0.gzip)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn brotli(&self) -> bool {
        ::serde_json::to_value(&self.0.brotli)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn min_size(&self) -> usize {
        ::serde_json::to_value(&self.0.min_size)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn quality(&self) -> u32 {
        ::serde_json::to_value(&self.0.quality)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct RateLimitConfig(pub spikard_core::RateLimitConfig);
impl RateLimitConfig {
    pub fn new(per_second: u64, burst: u32, ip_based: bool) -> RateLimitConfig {
        let mut __target: spikard_core::RateLimitConfig = ::std::default::Default::default();
        __target.per_second = per_second;
        __target.burst = burst;
        __target.ip_based = ip_based;
        RateLimitConfig(__target)
    }
    pub fn per_second(&self) -> u64 {
        ::serde_json::to_value(&self.0.per_second)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn burst(&self) -> u32 {
        ::serde_json::to_value(&self.0.burst)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn ip_based(&self) -> bool {
        ::serde_json::to_value(&self.0.ip_based)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct JsonRpcMethodInfo(pub spikard_core::JsonRpcMethodInfo);
impl JsonRpcMethodInfo {
    pub fn method_name(&self) -> String {
        self.0.method_name.clone()
    }
    pub fn description(&self) -> Option<String> {
        self.0.description.clone()
    }
    pub fn params_schema(&self) -> Option<String> {
        self.0
            .params_schema
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }
    pub fn result_schema(&self) -> Option<String> {
        self.0
            .result_schema
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }
    pub fn deprecated(&self) -> bool {
        ::serde_json::to_value(&self.0.deprecated)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn tags(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.tags)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ProblemDetails(pub spikard_core::ProblemDetails);
impl ProblemDetails {
    pub fn type_uri(&self) -> String {
        self.0.type_uri.clone()
    }
    pub fn title(&self) -> String {
        self.0.title.clone()
    }
    pub fn status(&self) -> u16 {
        ::serde_json::to_value(&self.0.status)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn detail(&self) -> Option<String> {
        self.0.detail.clone()
    }
    pub fn instance(&self) -> Option<String> {
        self.0.instance.clone()
    }
    pub fn extensions(&self) -> String {
        serde_json::to_string(&self.0.extensions).expect("serializable extensions")
    }
}

pub struct GraphQLRouteConfig(pub spikard_graphql::GraphQLRouteConfig);

pub fn graph_ql_route_config_path(client: &GraphQLRouteConfig, path: String) -> GraphQLRouteConfig {
    GraphQLRouteConfig(client.0.clone().path(&path))
}
pub fn graph_ql_route_config_method(client: &GraphQLRouteConfig, method: String) -> GraphQLRouteConfig {
    GraphQLRouteConfig(client.0.clone().method(&method))
}
pub fn graph_ql_route_config_enable_playground(client: &GraphQLRouteConfig, enable: bool) -> GraphQLRouteConfig {
    GraphQLRouteConfig(client.0.clone().enable_playground(enable))
}
pub fn graph_ql_route_config_description(client: &GraphQLRouteConfig, description: String) -> GraphQLRouteConfig {
    GraphQLRouteConfig(client.0.clone().description(&description))
}
pub fn graph_ql_route_config_get_path(client: &GraphQLRouteConfig) -> String {
    client.0.get_path().to_string()
}
pub fn graph_ql_route_config_get_method(client: &GraphQLRouteConfig) -> String {
    client.0.get_method().to_string()
}
pub fn graph_ql_route_config_is_playground_enabled(client: &GraphQLRouteConfig) -> bool {
    client.0.is_playground_enabled()
}
pub fn graph_ql_route_config_get_description(client: &GraphQLRouteConfig) -> String {
    serde_json::to_string(&(client.0.get_description())).expect("serializable return")
}

pub struct SchemaConfig(pub spikard_graphql::SchemaConfig);
impl SchemaConfig {
    pub fn new(
        introspection_enabled: bool,
        complexity_limit: Option<usize>,
        depth_limit: Option<usize>,
    ) -> SchemaConfig {
        let mut __target: spikard_graphql::SchemaConfig = ::std::default::Default::default();
        __target.introspection_enabled = introspection_enabled;
        __target.complexity_limit = complexity_limit;
        __target.depth_limit = depth_limit;
        SchemaConfig(__target)
    }
    pub fn introspection_enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.introspection_enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn complexity_limit(&self) -> Option<usize> {
        self.0.complexity_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn depth_limit(&self) -> Option<usize> {
        self.0.depth_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct QueryOnlyConfig(pub spikard_graphql::QueryOnlyConfig);
impl QueryOnlyConfig {
    pub fn new(
        introspection_enabled: bool,
        complexity_limit: Option<usize>,
        depth_limit: Option<usize>,
    ) -> QueryOnlyConfig {
        let mut __target: spikard_graphql::QueryOnlyConfig = ::std::default::Default::default();
        __target.introspection_enabled = introspection_enabled;
        __target.complexity_limit = complexity_limit;
        __target.depth_limit = depth_limit;
        QueryOnlyConfig(__target)
    }
    pub fn introspection_enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.introspection_enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn complexity_limit(&self) -> Option<usize> {
        self.0.complexity_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn depth_limit(&self) -> Option<usize> {
        self.0.depth_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct QueryMutationConfig(pub spikard_graphql::QueryMutationConfig);
impl QueryMutationConfig {
    pub fn new(
        introspection_enabled: bool,
        complexity_limit: Option<usize>,
        depth_limit: Option<usize>,
    ) -> QueryMutationConfig {
        let mut __target: spikard_graphql::QueryMutationConfig = ::std::default::Default::default();
        __target.introspection_enabled = introspection_enabled;
        __target.complexity_limit = complexity_limit;
        __target.depth_limit = depth_limit;
        QueryMutationConfig(__target)
    }
    pub fn introspection_enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.introspection_enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn complexity_limit(&self) -> Option<usize> {
        self.0.complexity_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn depth_limit(&self) -> Option<usize> {
        self.0.depth_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct FullSchemaConfig(pub spikard_graphql::FullSchemaConfig);
impl FullSchemaConfig {
    pub fn new(
        introspection_enabled: bool,
        complexity_limit: Option<usize>,
        depth_limit: Option<usize>,
    ) -> FullSchemaConfig {
        let mut __target: spikard_graphql::FullSchemaConfig = ::std::default::Default::default();
        __target.introspection_enabled = introspection_enabled;
        __target.complexity_limit = complexity_limit;
        __target.depth_limit = depth_limit;
        FullSchemaConfig(__target)
    }
    pub fn introspection_enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.introspection_enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn complexity_limit(&self) -> Option<usize> {
        self.0.complexity_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn depth_limit(&self) -> Option<usize> {
        self.0.depth_limit.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct AsyncApiConfig(pub spikard_http::AsyncApiConfig);
impl AsyncApiConfig {
    pub fn new(enabled: bool, spec: Option<String>) -> AsyncApiConfig {
        let mut __target: spikard_http::AsyncApiConfig = ::std::default::Default::default();
        __target.enabled = enabled;
        if let Some(s) = spec {
            if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&s) {
                if let Ok(t) = ::serde_json::from_value(v) {
                    __target.spec = Some(t);
                }
            }
        }
        AsyncApiConfig(__target)
    }
    pub fn enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn spec(&self) -> Option<String> {
        self.0.spec.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct ParsedChannel(pub spikard_http::ParsedChannel);
impl ParsedChannel {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn address(&self) -> String {
        self.0.address.clone()
    }
    pub fn messages(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.messages)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn bindings(&self) -> Option<String> {
        self.0.bindings.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct ParsedOperation(pub spikard_http::ParsedOperation);
impl ParsedOperation {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn action(&self) -> String {
        self.0.action.clone()
    }
    pub fn channel(&self) -> String {
        self.0.channel.clone()
    }
}

pub struct ParsedMessage(pub spikard_http::ParsedMessage);
impl ParsedMessage {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn schema(&self) -> Option<String> {
        self.0.schema.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
}

pub struct ParseResult(pub spikard_http::ParseResult);
impl ParseResult {
    pub fn spec_version(&self) -> String {
        self.0.spec_version.clone()
    }
    pub fn title(&self) -> String {
        self.0.title.clone()
    }
    pub fn api_version(&self) -> String {
        self.0.api_version.clone()
    }
    pub fn channels(&self) -> Vec<ParsedChannel> {
        self.0.channels.iter().map(|elem| ParsedChannel(elem.clone())).collect()
    }
    pub fn operations(&self) -> Vec<ParsedOperation> {
        self.0
            .operations
            .iter()
            .map(|elem| ParsedOperation(elem.clone()))
            .collect()
    }
    pub fn messages(&self) -> Vec<ParsedMessage> {
        self.0.messages.iter().map(|elem| ParsedMessage(elem.clone())).collect()
    }
}

pub struct ParseRequest(pub spikard_http::asyncapi::ParseRequest);
impl ParseRequest {
    pub fn spec(&self) -> String {
        format!("{:?}", &self.0.spec)
    }
}

pub struct ValidationResponse(pub spikard_http::ValidationResponse);
impl ValidationResponse {
    pub fn valid(&self) -> bool {
        self.0.valid.clone()
    }
    // alef: skipped getter `errors` — type cannot be bridged through swift-bridge
}

pub struct ValidateRequest(pub spikard_http::ValidateRequest);
impl ValidateRequest {
    pub fn spec(&self) -> String {
        format!("{:?}", &self.0.spec)
    }
    pub fn channel(&self) -> String {
        format!("{:?}", &self.0.channel)
    }
    pub fn message(&self) -> String {
        format!("{:?}", &self.0.message)
    }
    pub fn payload(&self) -> String {
        format!("{:?}", &self.0.payload)
    }
}

pub struct BackgroundTaskConfig(pub spikard_http::BackgroundTaskConfig);
impl BackgroundTaskConfig {
    pub fn new(max_queue_size: usize, max_concurrent_tasks: usize, drain_timeout_secs: u64) -> BackgroundTaskConfig {
        let mut __target: spikard_http::BackgroundTaskConfig = ::std::default::Default::default();
        __target.max_queue_size = max_queue_size;
        __target.max_concurrent_tasks = max_concurrent_tasks;
        __target.drain_timeout_secs = drain_timeout_secs;
        BackgroundTaskConfig(__target)
    }
    pub fn max_queue_size(&self) -> usize {
        ::serde_json::to_value(&self.0.max_queue_size)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn max_concurrent_tasks(&self) -> usize {
        ::serde_json::to_value(&self.0.max_concurrent_tasks)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn drain_timeout_secs(&self) -> u64 {
        ::serde_json::to_value(&self.0.drain_timeout_secs)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct BackgroundJobMetadata(pub spikard_http::BackgroundJobMetadata);
impl BackgroundJobMetadata {
    pub fn new(name: String, request_id: Option<String>) -> BackgroundJobMetadata {
        let mut __target: spikard_http::BackgroundJobMetadata = ::std::default::Default::default();
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&name) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.name = t;
            }
        }
        if let Some(s) = request_id {
            if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&s) {
                if let Ok(t) = ::serde_json::from_value(v) {
                    __target.request_id = Some(t);
                }
            }
        }
        BackgroundJobMetadata(__target)
    }
    pub fn name(&self) -> String {
        self.0.name.to_string()
    }
    pub fn request_id(&self) -> Option<String> {
        self.0.request_id.clone()
    }
}

pub struct GrpcConfig(pub spikard_http::GrpcConfig);
impl GrpcConfig {
    pub fn new(
        enabled: bool,
        max_message_size: usize,
        enable_compression: bool,
        request_timeout: Option<u64>,
        max_concurrent_streams: u32,
        enable_keepalive: bool,
        keepalive_interval: u64,
        keepalive_timeout: u64,
        max_stream_response_bytes: Option<usize>,
    ) -> GrpcConfig {
        let mut __target: spikard_http::GrpcConfig = ::std::default::Default::default();
        __target.enabled = enabled;
        __target.max_message_size = max_message_size;
        __target.enable_compression = enable_compression;
        __target.request_timeout = request_timeout;
        __target.max_concurrent_streams = max_concurrent_streams;
        __target.enable_keepalive = enable_keepalive;
        __target.keepalive_interval = keepalive_interval;
        __target.keepalive_timeout = keepalive_timeout;
        __target.max_stream_response_bytes = max_stream_response_bytes;
        GrpcConfig(__target)
    }
    pub fn enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn max_message_size(&self) -> usize {
        ::serde_json::to_value(&self.0.max_message_size)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn enable_compression(&self) -> bool {
        ::serde_json::to_value(&self.0.enable_compression)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn request_timeout(&self) -> Option<u64> {
        self.0.request_timeout.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn max_concurrent_streams(&self) -> u32 {
        ::serde_json::to_value(&self.0.max_concurrent_streams)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn enable_keepalive(&self) -> bool {
        ::serde_json::to_value(&self.0.enable_keepalive)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn keepalive_interval(&self) -> u64 {
        ::serde_json::to_value(&self.0.keepalive_interval)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn keepalive_timeout(&self) -> u64 {
        ::serde_json::to_value(&self.0.keepalive_timeout)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn max_stream_response_bytes(&self) -> Option<usize> {
        self.0.max_stream_response_bytes.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct JsonRpcConfig(pub spikard_http::JsonRpcConfig);
impl JsonRpcConfig {
    pub fn new(enabled: bool, endpoint_path: String, enable_batch: bool, max_batch_size: usize) -> JsonRpcConfig {
        let mut __target: spikard_http::JsonRpcConfig = ::std::default::Default::default();
        __target.enabled = enabled;
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&endpoint_path) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.endpoint_path = t;
            }
        }
        __target.enable_batch = enable_batch;
        __target.max_batch_size = max_batch_size;
        JsonRpcConfig(__target)
    }
    pub fn enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn endpoint_path(&self) -> String {
        self.0.endpoint_path.clone()
    }
    pub fn enable_batch(&self) -> bool {
        ::serde_json::to_value(&self.0.enable_batch)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn max_batch_size(&self) -> usize {
        ::serde_json::to_value(&self.0.max_batch_size)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct OpenApiConfig(pub spikard_http::OpenApiConfig);
impl OpenApiConfig {
    pub fn new(
        enabled: bool,
        title: String,
        version: String,
        description: Option<String>,
        swagger_ui_path: String,
        redoc_path: String,
        openapi_json_path: String,
        contact: Option<ContactInfo>,
        license: Option<LicenseInfo>,
        servers: Vec<ServerInfo>,
        security_schemes: String,
    ) -> OpenApiConfig {
        let mut __target: spikard_http::OpenApiConfig = ::std::default::Default::default();
        __target.enabled = enabled;
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&title) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.title = t;
            }
        }
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&version) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.version = t;
            }
        }
        if let Some(s) = description {
            if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&s) {
                if let Ok(t) = ::serde_json::from_value(v) {
                    __target.description = Some(t);
                }
            }
        }
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&swagger_ui_path) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.swagger_ui_path = t;
            }
        }
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&redoc_path) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.redoc_path = t;
            }
        }
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&openapi_json_path) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.openapi_json_path = t;
            }
        }
        if let Some(w) = contact {
            __target.contact = Some(w.0);
        }
        if let Some(w) = license {
            __target.license = Some(w.0);
        }
        __target.servers = servers.into_iter().map(|w| w.0).collect();
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&security_schemes) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.security_schemes = t;
            }
        }
        OpenApiConfig(__target)
    }
    pub fn enabled(&self) -> bool {
        ::serde_json::to_value(&self.0.enabled)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn title(&self) -> String {
        self.0.title.clone()
    }
    pub fn version(&self) -> String {
        self.0.version.clone()
    }
    pub fn description(&self) -> Option<String> {
        self.0.description.clone()
    }
    pub fn swagger_ui_path(&self) -> String {
        self.0.swagger_ui_path.clone()
    }
    pub fn redoc_path(&self) -> String {
        self.0.redoc_path.clone()
    }
    pub fn openapi_json_path(&self) -> String {
        self.0.openapi_json_path.clone()
    }
    pub fn contact(&self) -> Option<ContactInfo> {
        self.0.contact.clone().map(ContactInfo)
    }
    pub fn license(&self) -> Option<LicenseInfo> {
        self.0.license.clone().map(LicenseInfo)
    }
    pub fn servers(&self) -> Vec<ServerInfo> {
        self.0.servers.iter().map(|elem| ServerInfo(elem.clone())).collect()
    }
    pub fn security_schemes(&self) -> String {
        serde_json::to_string(&self.0.security_schemes).expect("serializable security_schemes")
    }
}

pub struct ContactInfo(pub spikard_http::ContactInfo);
impl ContactInfo {
    pub fn name(&self) -> Option<String> {
        self.0.name.clone()
    }
    pub fn email(&self) -> Option<String> {
        self.0.email.clone()
    }
    pub fn url(&self) -> Option<String> {
        self.0.url.clone()
    }
}

pub struct LicenseInfo(pub spikard_http::LicenseInfo);
impl LicenseInfo {
    pub fn name(&self) -> String {
        self.0.name.clone()
    }
    pub fn url(&self) -> Option<String> {
        self.0.url.clone()
    }
}

pub struct ServerInfo(pub spikard_http::ServerInfo);
impl ServerInfo {
    pub fn url(&self) -> String {
        self.0.url.clone()
    }
    pub fn description(&self) -> Option<String> {
        self.0.description.clone()
    }
}

pub struct Response(pub spikard_http::Response);
impl Response {
    pub fn new(content: Option<String>, status_code: u16, headers: String) -> Response {
        let mut __target: spikard_http::Response = ::std::default::Default::default();
        if let Some(s) = content {
            if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&s) {
                if let Ok(t) = ::serde_json::from_value(v) {
                    __target.content = Some(t);
                }
            }
        }
        __target.status_code = status_code;
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&headers) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.headers = t;
            }
        }
        Response(__target)
    }
    pub fn content(&self) -> Option<String> {
        self.0.content.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }
    pub fn status_code(&self) -> u16 {
        ::serde_json::to_value(&self.0.status_code)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn headers(&self) -> String {
        serde_json::to_string(&self.0.headers).expect("serializable headers")
    }
}

pub struct SseEvent(pub spikard_http::SseEvent);
impl SseEvent {
    pub fn event_type(&self) -> Option<String> {
        self.0.event_type.clone()
    }
    pub fn data(&self) -> String {
        serde_json::to_string(&self.0.data).unwrap_or_default()
    }
    pub fn id(&self) -> Option<String> {
        self.0.id.clone()
    }
    pub fn retry(&self) -> Option<u64> {
        self.0.retry.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
}

pub struct JwtConfig(pub spikard_http::JwtConfig);
impl JwtConfig {
    pub fn secret(&self) -> String {
        self.0.secret.clone()
    }
    pub fn algorithm(&self) -> String {
        self.0.algorithm.clone()
    }
    pub fn audience(&self) -> Option<Vec<String>> {
        self.0.audience.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn issuer(&self) -> Option<String> {
        self.0.issuer.clone()
    }
    pub fn leeway(&self) -> u64 {
        ::serde_json::to_value(&self.0.leeway)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub struct ApiKeyConfig(pub spikard_http::ApiKeyConfig);
impl ApiKeyConfig {
    pub fn keys(&self) -> Vec<String> {
        ::serde_json::to_value(&self.0.keys)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn header_name(&self) -> String {
        self.0.header_name.clone()
    }
}

pub struct StaticFilesConfig(pub spikard_http::StaticFilesConfig);
impl StaticFilesConfig {
    pub fn directory(&self) -> String {
        self.0.directory.clone()
    }
    pub fn route_prefix(&self) -> String {
        self.0.route_prefix.clone()
    }
    pub fn index_file(&self) -> bool {
        ::serde_json::to_value(&self.0.index_file)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn cache_control(&self) -> Option<String> {
        self.0.cache_control.clone()
    }
}

pub struct ServerConfig(pub spikard_http::ServerConfig);
impl ServerConfig {
    pub fn new(
        host: String,
        port: u16,
        workers: usize,
        enable_request_id: bool,
        max_body_size: Option<usize>,
        request_timeout: Option<u64>,
        compression: Option<CompressionConfig>,
        rate_limit: Option<RateLimitConfig>,
        jwt_auth: Option<JwtConfig>,
        api_key_auth: Option<ApiKeyConfig>,
        static_files: Vec<StaticFilesConfig>,
        graceful_shutdown: bool,
        shutdown_timeout: u64,
        asyncapi: Option<AsyncApiConfig>,
        openapi: Option<OpenApiConfig>,
        jsonrpc: Option<JsonRpcConfig>,
        grpc: Option<GrpcConfig>,
        background_tasks: BackgroundTaskConfig,
        enable_http_trace: bool,
    ) -> ServerConfig {
        let mut __target: spikard_http::ServerConfig = ::std::default::Default::default();
        if let Ok(v) = ::serde_json::from_str::<::serde_json::Value>(&host) {
            if let Ok(t) = ::serde_json::from_value(v) {
                __target.host = t;
            }
        }
        __target.port = port;
        __target.workers = workers;
        __target.enable_request_id = enable_request_id;
        __target.max_body_size = max_body_size;
        __target.request_timeout = request_timeout;
        if let Some(w) = compression {
            __target.compression = Some(w.0);
        }
        if let Some(w) = rate_limit {
            __target.rate_limit = Some(w.0);
        }
        if let Some(w) = jwt_auth {
            __target.jwt_auth = Some(w.0);
        }
        if let Some(w) = api_key_auth {
            __target.api_key_auth = Some(w.0);
        }
        __target.static_files = static_files.into_iter().map(|w| w.0).collect();
        __target.graceful_shutdown = graceful_shutdown;
        __target.shutdown_timeout = shutdown_timeout;
        if let Some(w) = asyncapi {
            __target.asyncapi = Some(w.0);
        }
        if let Some(w) = openapi {
            __target.openapi = Some(w.0);
        }
        if let Some(w) = jsonrpc {
            __target.jsonrpc = Some(w.0);
        }
        if let Some(w) = grpc {
            __target.grpc = Some(w.0);
        }
        __target.background_tasks = background_tasks.0;
        __target.enable_http_trace = enable_http_trace;
        ServerConfig(__target)
    }
    pub fn host(&self) -> String {
        self.0.host.clone()
    }
    pub fn port(&self) -> u16 {
        ::serde_json::to_value(&self.0.port)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn workers(&self) -> usize {
        ::serde_json::to_value(&self.0.workers)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn enable_request_id(&self) -> bool {
        ::serde_json::to_value(&self.0.enable_request_id)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn max_body_size(&self) -> Option<usize> {
        self.0.max_body_size.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn request_timeout(&self) -> Option<u64> {
        self.0.request_timeout.as_ref().and_then(|v| {
            ::serde_json::to_value(v)
                .ok()
                .and_then(|j| ::serde_json::from_value(j).ok())
        })
    }
    pub fn compression(&self) -> Option<CompressionConfig> {
        self.0.compression.clone().map(CompressionConfig)
    }
    pub fn rate_limit(&self) -> Option<RateLimitConfig> {
        self.0.rate_limit.clone().map(RateLimitConfig)
    }
    pub fn jwt_auth(&self) -> Option<JwtConfig> {
        self.0.jwt_auth.clone().map(JwtConfig)
    }
    pub fn api_key_auth(&self) -> Option<ApiKeyConfig> {
        self.0.api_key_auth.clone().map(ApiKeyConfig)
    }
    pub fn static_files(&self) -> Vec<StaticFilesConfig> {
        self.0
            .static_files
            .iter()
            .map(|elem| StaticFilesConfig(elem.clone()))
            .collect()
    }
    pub fn graceful_shutdown(&self) -> bool {
        ::serde_json::to_value(&self.0.graceful_shutdown)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn shutdown_timeout(&self) -> u64 {
        ::serde_json::to_value(&self.0.shutdown_timeout)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
    pub fn asyncapi(&self) -> Option<AsyncApiConfig> {
        self.0.asyncapi.clone().map(AsyncApiConfig)
    }
    pub fn openapi(&self) -> Option<OpenApiConfig> {
        self.0.openapi.clone().map(OpenApiConfig)
    }
    pub fn jsonrpc(&self) -> Option<JsonRpcConfig> {
        self.0.jsonrpc.clone().map(JsonRpcConfig)
    }
    pub fn grpc(&self) -> Option<GrpcConfig> {
        self.0.grpc.clone().map(GrpcConfig)
    }
    pub fn background_tasks(&self) -> BackgroundTaskConfig {
        BackgroundTaskConfig(self.0.background_tasks.clone())
    }
    pub fn enable_http_trace(&self) -> bool {
        ::serde_json::to_value(&self.0.enable_http_trace)
            .ok()
            .and_then(|j| ::serde_json::from_value(j).ok())
            .unwrap_or_default()
    }
}

pub enum Method {
    Get,
    Post,
    Put,
    Patch,
    Delete,
    Head,
    Options,
    Trace,
}

impl From<spikard_core::Method> for Method {
    fn from(val: spikard_core::Method) -> Self {
        match val {
            spikard_core::Method::Get => Self::Get,
            spikard_core::Method::Post => Self::Post,
            spikard_core::Method::Put => Self::Put,
            spikard_core::Method::Patch => Self::Patch,
            spikard_core::Method::Delete => Self::Delete,
            spikard_core::Method::Head => Self::Head,
            spikard_core::Method::Options => Self::Options,
            spikard_core::Method::Trace => Self::Trace,
        }
    }
}

impl Method {
    pub fn to_string(&self) -> String {
        match self {
            Self::Get => "Get".to_string(),
            Self::Post => "Post".to_string(),
            Self::Put => "Put".to_string(),
            Self::Patch => "Patch".to_string(),
            Self::Delete => "Delete".to_string(),
            Self::Head => "Head".to_string(),
            Self::Options => "Options".to_string(),
            Self::Trace => "Trace".to_string(),
        }
    }
}

pub enum SecuritySchemeInfo {
    /// Data variants not directly bridgeable — represented as Unknown.
    Unknown,
}

impl From<spikard_http::SecuritySchemeInfo> for SecuritySchemeInfo {
    fn from(val: spikard_http::SecuritySchemeInfo) -> Self {
        match val {
            _ => Self::Unknown,
        }
    }
}

impl SecuritySchemeInfo {
    pub fn to_string(&self) -> String {
        match self {
            Self::Unknown => "unknown".to_string(),
        }
    }
}

pub fn schema_query_only() -> QueryOnlyConfig {
    QueryOnlyConfig(spikard_graphql::schema_query_only())
}

pub fn schema_query_mutation() -> QueryMutationConfig {
    QueryMutationConfig(spikard_graphql::schema_query_mutation())
}

pub fn schema_full() -> FullSchemaConfig {
    FullSchemaConfig(spikard_graphql::schema_full())
}
