# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d38401ef11d2fe981b47d9e69ad0d12cc6edd23c1d71d40d86f186a5fc102af0
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Spikard.RouteBuilder do
  @moduledoc "Builder for defining a route."

  alias Spikard.Native
  defstruct [:ref]

  @typedoc "Builder for defining a route."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Create a new builder for the provided HTTP method and path."
  def new(method, path) do
    ref = Native.routebuilder_new(method, path)
    %__MODULE__{ref: ref}
  end

  @doc "Assign an explicit handler name."
  def handler_name(obj, name) do
    ref = Native.routebuilder_handler_name(obj.ref, name)
    %__MODULE__{ref: ref}
  end

  @doc "Provide a raw JSON schema for the request body."
  def request_schema_json(obj, schema) do
    ref = Native.routebuilder_request_schema_json(obj.ref, schema)
    %__MODULE__{ref: ref}
  end

  @doc "Provide a raw JSON schema for the response body."
  def response_schema_json(obj, schema) do
    ref = Native.routebuilder_response_schema_json(obj.ref, schema)
    %__MODULE__{ref: ref}
  end

  @doc "Provide a raw JSON schema for request parameters."
  def params_schema_json(obj, schema) do
    ref = Native.routebuilder_params_schema_json(obj.ref, schema)
    %__MODULE__{ref: ref}
  end

  @doc "Provide multipart file parameter configuration."
  def file_params_json(obj, schema) do
    ref = Native.routebuilder_file_params_json(obj.ref, schema)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a CORS configuration for this route."
  def cors(obj, cors) do
    ref = Native.routebuilder_cors(obj.ref, cors)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a compression configuration for this route."
  def compression(obj, compression) do
    ref = Native.routebuilder_compression(obj.ref, compression)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a per-route maximum request body size in bytes, overriding the server-global default."
  def body_limit(obj, max_bytes) do
    ref = Native.routebuilder_body_limit(obj.ref, max_bytes)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a per-route request timeout in seconds, overriding the server-global default."
  def request_timeout(obj, seconds) do
    ref = Native.routebuilder_request_timeout(obj.ref, seconds)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a per-route rate limiting configuration, overriding the server-global default."
  def rate_limit(obj, rate_limit) do
    ref = Native.routebuilder_rate_limit(obj.ref, rate_limit)
    %__MODULE__{ref: ref}
  end

  @doc "Force per-route request-id generation on or off, overriding the server-global default."
  def request_id(obj, enabled) do
    ref = Native.routebuilder_request_id(obj.ref, enabled)
    %__MODULE__{ref: ref}
  end

  @doc "Require JWT authentication for this route."
  def jwt_auth(obj, config) do
    ref = Native.routebuilder_jwt_auth(obj.ref, config)
    %__MODULE__{ref: ref}
  end

  @doc "Require API key authentication for this route."
  def api_key_auth(obj, config) do
    ref = Native.routebuilder_api_key_auth(obj.ref, config)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a roles/scopes/permissions authorization requirement for this route."
  def authorization(obj, config) do
    ref = Native.routebuilder_authorization(obj.ref, config)
    %__MODULE__{ref: ref}
  end

  @doc "Select registered lifecycle hooks to run for this route."
  def lifecycle_hooks(obj, hooks) do
    ref = Native.routebuilder_lifecycle_hooks(obj.ref, hooks)
    %__MODULE__{ref: ref}
  end

  @doc "Expose this route as a JSON-RPC method."
  def jsonrpc_method(obj, info) do
    ref = Native.routebuilder_jsonrpc_method(obj.ref, info)
    %__MODULE__{ref: ref}
  end

  @doc "Attach a literal `OpenRPC` method spec document for this route, overriding"
  def openrpc_spec(obj, spec) do
    ref = Native.routebuilder_openrpc_spec(obj.ref, spec)
    %__MODULE__{ref: ref}
  end

  @doc "Mark the route as synchronous."
  def sync(obj) do
    ref = Native.routebuilder_sync(obj.ref)
    %__MODULE__{ref: ref}
  end

  @doc "Declare the dependency keys that must be resolved before this handler runs."
  def handler_dependencies(obj, dependencies) do
    ref = Native.routebuilder_handler_dependencies(obj.ref, dependencies)
    %__MODULE__{ref: ref}
  end
end
