# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:766cdd87c899d8e9979fcdd42b50846577f0e2ffa4853d0de1d981121dde78da
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "spikard_nif"
version = "0.17.0"
edition = "2024"
license = "MIT"
description = "Codegen-first polyglot web toolkit with a Rust core and bindings for 14 languages"
readme = false
keywords = ["framework", "http", "polyglot", "rust", "web"]
categories = []

[package.metadata.cargo-machete]
ignored = ["tokio", "alloc-no-stdlib", "alloc-stdlib", "brotli-decompressor"]

[workspace]

[lib]
name = "spikard_nif"
path = "../../../../packages/elixir/lib/lib.rs"

crate-type = ["cdylib"]

[features]
default = ["di"]
di = ["spikard/di"]

[dependencies]
alloc-no-stdlib = "=3.0.0"
alloc-stdlib = "=0.3.0"
brotli-decompressor = "=6.0.0"
rustler = "0.38"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
spikard = { version = "0.17.0", path = "../../../../crates/spikard", features = ["di"] }
spikard-core = { path = "../../../../crates/spikard-core", version = "0.17.0" }
spikard-graphql = { path = "../../../../crates/spikard-graphql", version = "0.17.0" }
spikard-http = { path = "../../../../crates/spikard-http", version = "0.17.0" }
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("di"))'] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
