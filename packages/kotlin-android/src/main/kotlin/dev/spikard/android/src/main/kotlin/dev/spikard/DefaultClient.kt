// Generated by alef. Do not edit by hand.
// alef:hash:3dec354cb3bb66f6d2f900e557d4862ffd4d4b5dac26e5b3858b95f93ade783b
@file:Suppress(
    "ktlint:standard:max-line-length",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:annotation",
    "MaxLineLength",
    "TooManyFunctions",
    "LongParameterList",
    "LongMethod",
)

package dev.spikard

@Suppress("TooManyFunctions")
class GraphQLRouteConfig internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
        .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
        .findAndRegisterModules()
        .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Set the HTTP path for the GraphQL endpoint
    //
    // # Arguments
    //
    // * `path` - The URL path (e.g., "/graphql", "/api/graphql")
    fun path(path: String): GraphQLRouteConfig {
        val handle = SpikardBridge.nativeGraphQlRouteConfigPath(handle, MAPPER.writeValueAsString(path))
        return GraphQLRouteConfig(handle)
    }

    // Set the HTTP method for the GraphQL endpoint
    //
    // # Arguments
    //
    // * `method` - The HTTP method (typically "POST")
    fun method(method: String): GraphQLRouteConfig {
        val handle = SpikardBridge.nativeGraphQlRouteConfigMethod(handle, MAPPER.writeValueAsString(method))
        return GraphQLRouteConfig(handle)
    }

    // Enable or disable the GraphQL Playground UI
    //
    // # Arguments
    //
    // * `enable` - Whether to enable playground
    fun enablePlayground(enable: Boolean): GraphQLRouteConfig {
        val handle = SpikardBridge.nativeGraphQlRouteConfigEnablePlayground(handle, MAPPER.writeValueAsString(enable))
        return GraphQLRouteConfig(handle)
    }

    // Set a custom description for documentation
    //
    // # Arguments
    //
    // * `description` - Documentation string
    fun description(description: String): GraphQLRouteConfig {
        val handle = SpikardBridge.nativeGraphQlRouteConfigDescription(handle, MAPPER.writeValueAsString(description))
        return GraphQLRouteConfig(handle)
    }

    // Get the configured path
    fun getPath(): String {
        return SpikardBridge.nativeGraphQlRouteConfigGetPath(handle)
    }

    // Get the configured method
    fun getMethod(): String {
        return SpikardBridge.nativeGraphQlRouteConfigGetMethod(handle)
    }

    // Check if playground is enabled
    fun isPlaygroundEnabled(): Boolean {
        return SpikardBridge.nativeGraphQlRouteConfigIsPlaygroundEnabled(handle)
    }

    // Get the description if set
    fun getDescription(): String? {
        return SpikardBridge.nativeGraphQlRouteConfigGetDescription(handle)
    }

    override fun close() { SpikardBridge.nativeFreeGraphQLRouteConfig(handle) }
}
@Suppress("TooManyFunctions")
class App internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
        .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
        .findAndRegisterModules()
        .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Register a `/graphql`-style route backed by one of Spikard's built-in
    // async-graphql test schemas.
    //
    // This is a binding-friendly entry point: unlike [`spikard_graphql::GraphQLHandler`],
    // which is generic over the GraphQL root types and therefore cannot cross an FFI
    // boundary, this method is monomorphic. Callers select a schema by name and the
    // concrete `GraphQLHandler<Query, Mutation, Subscription>` is constructed internally
    // and registered as an `Arc<dyn Handler>`.
    //
    // The built-in schemas exist to exercise Spikard's GraphQL execution path end to
    // end (see `fixtures/graphql_schema.json`); they are not meant to be extended with
    // application-specific fields. Hosts that need a custom GraphQL schema should build
    // their own `GraphQLHandler` in Rust and register it via [`App::route`].
    //
    // # Arguments
    //
    // * `path` - The HTTP path to serve GraphQL requests on (e.g. `/graphql`).
    // * `method` - The HTTP method to accept (fixtures always use `POST`).
    // * `schema_type` - One of `"query_only"`, `"query_mutation"`, or `"full"`.
    // * `config` - Introspection/complexity/depth limits applied to the selected schema.
    //
    // # Errors
    //
    // Returns [`AppError::GraphQL`] if `schema_type` is not a recognized built-in schema
    // name, and [`AppError::Route`] if route construction fails.
    fun registerGraphqlRoute(path: String, method: Method, schemaType: String, config: SchemaConfig): App {
        val handle = SpikardBridge.nativeAppRegisterGraphqlRoute(handle, MAPPER.writeValueAsString(mapOf("path" to path, "method" to method, "schemaType" to schemaType, "config" to config)))
        return App(handle)
    }

    // Register a `/graphql`-style route backed by an arbitrary SDL schema string, with field
    // resolution driven by a static `response_data` JSON tree instead of hand-written Rust
    // resolvers.
    //
    // This is the dynamic-schema counterpart to [`App::register_graphql_route`]: instead of
    // selecting one of Spikard's built-in test schemas by name, callers supply their own SDL
    // and a JSON tree shaped like the successful `data` payload each query should produce. Every
    // field resolver is generic — it navigates `response_data` by field name — so
    // argument-dependent results (e.g. `user(id: "user-42")`) must already be baked into
    // `response_data` at the correct path. See [`spikard_graphql::dynamic`] for the full
    // resolver-data encoding and field-level-error support via `config.field_errors`.
    //
    // # Arguments
    //
    // * `path` - The HTTP path to serve GraphQL requests on (e.g. `/graphql`).
    // * `method` - The HTTP method to accept (fixtures always use `POST`).
    // * `sdl` - The GraphQL SDL schema string (object and input-object types only).
    // * `response_data` - JSON tree resolvers navigate by field name to produce query results.
    // * `config` - Introspection/complexity/depth limits and field-level error injection.
    //
    // # Errors
    //
    // Returns [`AppError::GraphQL`] if the SDL cannot be parsed or fails dynamic-schema
    // validation (e.g. an unsupported type kind, duplicate field names), and
    // [`AppError::Route`] if route construction fails.
    fun registerGraphqlSdlRoute(path: String, method: Method, sdl: String, responseData: Any, config: DynamicSchemaConfig): App {
        val handle = SpikardBridge.nativeAppRegisterGraphqlSdlRoute(handle, MAPPER.writeValueAsString(mapOf("path" to path, "method" to method, "sdl" to sdl, "responseData" to responseData, "config" to config)))
        return App(handle)
    }

    override fun close() { SpikardBridge.nativeFreeApp(handle) }
}
@Suppress("TooManyFunctions")
class RouteBuilder internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
        .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
        .findAndRegisterModules()
        .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Assign an explicit handler name.
    fun handlerName(name: String): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderHandlerName(handle, MAPPER.writeValueAsString(name))
        return RouteBuilder(handle)
    }

    // Provide a raw JSON schema for the request body.
    fun requestSchemaJson(schema: Any): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderRequestSchemaJson(handle, MAPPER.writeValueAsString(schema))
        return RouteBuilder(handle)
    }

    // Provide a raw JSON schema for the response body.
    fun responseSchemaJson(schema: Any): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderResponseSchemaJson(handle, MAPPER.writeValueAsString(schema))
        return RouteBuilder(handle)
    }

    // Provide a raw JSON schema for request parameters.
    fun paramsSchemaJson(schema: Any): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderParamsSchemaJson(handle, MAPPER.writeValueAsString(schema))
        return RouteBuilder(handle)
    }

    // Provide multipart file parameter configuration.
    fun fileParamsJson(schema: Any): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderFileParamsJson(handle, MAPPER.writeValueAsString(schema))
        return RouteBuilder(handle)
    }

    // Attach a CORS configuration for this route.
    fun cors(cors: CorsConfig): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderCors(handle, MAPPER.writeValueAsString(cors))
        return RouteBuilder(handle)
    }

    // Attach a compression configuration for this route.
    fun compression(compression: CompressionConfig): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderCompression(handle, MAPPER.writeValueAsString(compression))
        return RouteBuilder(handle)
    }

    // Attach a per-route maximum request body size in bytes, overriding the server-global default.
    fun bodyLimit(maxBytes: Long): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderBodyLimit(handle, MAPPER.writeValueAsString(maxBytes))
        return RouteBuilder(handle)
    }

    // Attach a per-route request timeout in seconds, overriding the server-global default.
    fun requestTimeout(seconds: Long): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderRequestTimeout(handle, MAPPER.writeValueAsString(seconds))
        return RouteBuilder(handle)
    }

    // Mark the route as synchronous.
    fun sync(): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderSync(handle)
        return RouteBuilder(handle)
    }

    // Declare the dependency keys that must be resolved before this handler runs.
    fun handlerDependencies(dependencies: List<String>): RouteBuilder {
        val handle = SpikardBridge.nativeRouteBuilderHandlerDependencies(handle, MAPPER.writeValueAsString(dependencies))
        return RouteBuilder(handle)
    }

    override fun close() { SpikardBridge.nativeFreeRouteBuilder(handle) }
}
