// Generated by alef. Do not edit by hand.
// alef:hash:2ffd50061ac8b0f103cf99d225ac09a4a80e24bb03f6524e64d16788fed03f31
@file:Suppress(
    "ktlint:standard:max-line-length",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:annotation",
    "MaxLineLength",
    "TooManyFunctions",
    "LongParameterList",
    "LongMethod",
)

package dev.spikard

@Suppress("TooManyFunctions")
class GraphQLRouteConfig internal constructor(handle: Long) : AutoCloseable {
    private val handleLock = Any()
    private var nativeHandle: Long = handle

    private inline fun <T> withHandle(block: (Long) -> T): T = synchronized(handleLock) {
        check(nativeHandle != 0L) { "GraphQLRouteConfig is closed" }
        block(nativeHandle)
    }
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Set the HTTP path for the GraphQL endpoint
    //
    // # Arguments
    //
    // * `path` - The URL path (e.g., "/graphql", "/api/graphql")
    fun path(path: String): GraphQLRouteConfig {
        val handle = withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigPath(handle, MAPPER.writeValueAsString(path)) }
        return GraphQLRouteConfig(handle)
    }

    // Set the HTTP method for the GraphQL endpoint
    //
    // # Arguments
    //
    // * `method` - The HTTP method (typically "POST")
    fun method(method: String): GraphQLRouteConfig {
        val handle = withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigMethod(handle, MAPPER.writeValueAsString(method)) }
        return GraphQLRouteConfig(handle)
    }

    // Enable or disable the GraphQL Playground UI
    //
    // # Arguments
    //
    // * `enable` - Whether to enable playground
    fun enablePlayground(enable: Boolean): GraphQLRouteConfig {
        val handle = withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigEnablePlayground(handle, MAPPER.writeValueAsString(enable)) }
        return GraphQLRouteConfig(handle)
    }

    // Set a custom description for documentation
    //
    // # Arguments
    //
    // * `description` - Documentation string
    fun description(description: String): GraphQLRouteConfig {
        val handle = withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigDescription(handle, MAPPER.writeValueAsString(description)) }
        return GraphQLRouteConfig(handle)
    }

    // Get the configured path
    fun getPath(): String {
        return withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigGetPath(handle) }
    }

    // Get the configured method
    fun getMethod(): String {
        return withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigGetMethod(handle) }
    }

    // Check if playground is enabled
    fun isPlaygroundEnabled(): Boolean {
        return withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigIsPlaygroundEnabled(handle) }
    }

    // Get the description if set
    fun getDescription(): String? {
        return withHandle { handle -> SpikardBridge.nativeGraphQlRouteConfigGetDescription(handle) }
    }

    override fun close() = synchronized(handleLock) {
        if (nativeHandle != 0L) {
            val handle = nativeHandle
            nativeHandle = 0L
            SpikardBridge.nativeFreeGraphQlRouteConfig(handle)
        }
    }
}
@Suppress("TooManyFunctions")
class App internal constructor(handle: Long) : AutoCloseable {
    private val handleLock = Any()
    private var nativeHandle: Long = handle

    private inline fun <T> withHandle(block: (Long) -> T): T = synchronized(handleLock) {
        check(nativeHandle != 0L) { "App is closed" }
        block(nativeHandle)
    }
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Register a `/graphql`-style route backed by one of Spikard's built-in
    // async-graphql test schemas.
    //
    // This is a binding-friendly entry point: unlike [`spikard_graphql::GraphQLHandler`],
    // which is generic over the GraphQL root types and therefore cannot cross an FFI
    // boundary, this method is monomorphic. Callers select a schema by name and the
    // concrete `GraphQLHandler<Query, Mutation, Subscription>` is constructed internally
    // and registered as an `Arc<dyn Handler>`.
    //
    // The built-in schemas exist to exercise Spikard's GraphQL execution path end to
    // end (see `fixtures/graphql_schema.json`); they are not meant to be extended with
    // application-specific fields. Hosts that need a custom GraphQL schema should build
    // their own `GraphQLHandler` in Rust and register it via [`App::route`].
    //
    // # Arguments
    //
    // * `path` - The HTTP path to serve GraphQL requests on (e.g. `/graphql`).
    // * `method` - The HTTP method to accept (fixtures always use `POST`).
    // * `schema_type` - One of `"query_only"`, `"query_mutation"`, or `"full"`.
    // * `config` - Introspection/complexity/depth limits applied to the selected schema.
    //
    // # Errors
    //
    // Returns [`AppError::GraphQL`] if `schema_type` is not a recognized built-in schema
    // name, and [`AppError::Route`] if route construction fails.
    fun registerGraphqlRoute(path: String, method: Method, schemaType: String, config: SchemaConfig): App {
        val handle = withHandle { handle -> SpikardBridge.nativeAppRegisterGraphqlRoute(handle, MAPPER.writeValueAsString(mapOf("path" to path, "method" to method, "schemaType" to schemaType, "config" to config))) }
        return App(handle)
    }

    // Register a `/graphql`-style route backed by an arbitrary SDL schema string, with field
    // resolution driven by a static `response_data` JSON tree instead of hand-written Rust
    // resolvers.
    //
    // This is the dynamic-schema counterpart to [`App::register_graphql_route`]: instead of
    // selecting one of Spikard's built-in test schemas by name, callers supply their own SDL
    // and a JSON tree shaped like the successful `data` payload each query should produce. Every
    // field resolver is generic — it navigates `response_data` by field name — so
    // argument-dependent results (e.g. `user(id: "user-42")`) must already be baked into
    // `response_data` at the correct path. See [`spikard_graphql::dynamic`] for the full
    // resolver-data encoding and field-level-error support via `config.field_errors`.
    //
    // # Arguments
    //
    // * `path` - The HTTP path to serve GraphQL requests on (e.g. `/graphql`).
    // * `method` - The HTTP method to accept (fixtures always use `POST`).
    // * `sdl` - The GraphQL SDL schema string (object and input-object types only).
    // * `response_data` - JSON tree resolvers navigate by field name to produce query results.
    // * `config` - Introspection/complexity/depth limits and field-level error injection.
    //
    // # Errors
    //
    // Returns [`AppError::GraphQL`] if the SDL cannot be parsed or fails dynamic-schema
    // validation (e.g. an unsupported type kind, duplicate field names), and
    // [`AppError::Route`] if route construction fails.
    fun registerGraphqlSdlRoute(path: String, method: Method, sdl: String, responseData: Any, config: DynamicSchemaConfig): App {
        val handle = withHandle { handle -> SpikardBridge.nativeAppRegisterGraphqlSdlRoute(handle, MAPPER.writeValueAsString(mapOf("path" to path, "method" to method, "sdl" to sdl, "responseData" to responseData, "config" to config))) }
        return App(handle)
    }

    override fun close() = synchronized(handleLock) {
        if (nativeHandle != 0L) {
            val handle = nativeHandle
            nativeHandle = 0L
            SpikardBridge.nativeFreeApp(handle)
        }
    }
}
@Suppress("TooManyFunctions")
class RouteBuilder internal constructor(handle: Long) : AutoCloseable {
    private val handleLock = Any()
    private var nativeHandle: Long = handle

    private inline fun <T> withHandle(block: (Long) -> T): T = synchronized(handleLock) {
        check(nativeHandle != 0L) { "RouteBuilder is closed" }
        block(nativeHandle)
    }
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Assign an explicit handler name.
    fun handlerName(name: String): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderHandlerName(handle, MAPPER.writeValueAsString(name)) }
        return RouteBuilder(handle)
    }

    // Provide a raw JSON schema for the request body.
    fun requestSchemaJson(schema: Any): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderRequestSchemaJson(handle, MAPPER.writeValueAsString(schema)) }
        return RouteBuilder(handle)
    }

    // Provide a raw JSON schema for the response body.
    fun responseSchemaJson(schema: Any): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderResponseSchemaJson(handle, MAPPER.writeValueAsString(schema)) }
        return RouteBuilder(handle)
    }

    // Provide a raw JSON schema for request parameters.
    fun paramsSchemaJson(schema: Any): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderParamsSchemaJson(handle, MAPPER.writeValueAsString(schema)) }
        return RouteBuilder(handle)
    }

    // Provide multipart file parameter configuration.
    fun fileParamsJson(schema: Any): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderFileParamsJson(handle, MAPPER.writeValueAsString(schema)) }
        return RouteBuilder(handle)
    }

    // Attach a CORS configuration for this route.
    fun cors(cors: CorsConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderCors(handle, MAPPER.writeValueAsString(cors)) }
        return RouteBuilder(handle)
    }

    // Attach a compression configuration for this route.
    fun compression(compression: CompressionConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderCompression(handle, MAPPER.writeValueAsString(compression)) }
        return RouteBuilder(handle)
    }

    // Attach a per-route maximum request body size in bytes, overriding the server-global default.
    fun bodyLimit(maxBytes: Long): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderBodyLimit(handle, MAPPER.writeValueAsString(maxBytes)) }
        return RouteBuilder(handle)
    }

    // Attach a per-route request timeout in seconds, overriding the server-global default.
    fun requestTimeout(seconds: Long): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderRequestTimeout(handle, MAPPER.writeValueAsString(seconds)) }
        return RouteBuilder(handle)
    }

    // Attach a per-route rate limiting configuration, overriding the server-global default.
    fun rateLimit(rateLimit: RateLimitConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderRateLimit(handle, MAPPER.writeValueAsString(rateLimit)) }
        return RouteBuilder(handle)
    }

    // Force per-route request-id generation on or off, overriding the server-global default.
    //
    // Takes a plain `bool` rather than [`RequestIdConfig`] directly: the wire/metadata type had
    // to become a struct to represent `{"enabled": false}` (see `RequestIdConfig`'s docs), but
    // this builder is the ergonomic call site (`.request_id(true)`), so it keeps accepting a bool
    // and wraps it into `RequestIdConfig` internally in [`Self::into_metadata`].
    fun requestId(enabled: Boolean): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderRequestId(handle, MAPPER.writeValueAsString(enabled)) }
        return RouteBuilder(handle)
    }

    // Require JWT authentication for this route.
    fun jwtAuth(config: JwtAuthConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderJwtAuth(handle, MAPPER.writeValueAsString(config)) }
        return RouteBuilder(handle)
    }

    // Require API key authentication for this route.
    fun apiKeyAuth(config: ApiKeyAuthConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderApiKeyAuth(handle, MAPPER.writeValueAsString(config)) }
        return RouteBuilder(handle)
    }

    // Attach a roles/scopes/permissions authorization requirement for this route.
    fun authorization(config: AuthorizationConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderAuthorization(handle, MAPPER.writeValueAsString(config)) }
        return RouteBuilder(handle)
    }

    // Select registered lifecycle hooks to run for this route.
    fun lifecycleHooks(hooks: LifecycleHooksConfig): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderLifecycleHooks(handle, MAPPER.writeValueAsString(hooks)) }
        return RouteBuilder(handle)
    }

    // Expose this route as a JSON-RPC method.
    fun jsonrpcMethod(info: JsonRpcMethodInfo): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderJsonrpcMethod(handle, MAPPER.writeValueAsString(info)) }
        return RouteBuilder(handle)
    }

    // Attach a literal `OpenRPC` method spec document for this route, overriding
    // auto-derivation from the [`Self::jsonrpc_method`] metadata when present.
    fun openrpcSpec(spec: Any): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderOpenrpcSpec(handle, MAPPER.writeValueAsString(spec)) }
        return RouteBuilder(handle)
    }

    // Mark the route as synchronous.
    fun sync(): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderSync(handle) }
        return RouteBuilder(handle)
    }

    // Declare the dependency keys that must be resolved before this handler runs.
    fun handlerDependencies(dependencies: List<String>): RouteBuilder {
        val handle = withHandle { handle -> SpikardBridge.nativeRouteBuilderHandlerDependencies(handle, MAPPER.writeValueAsString(dependencies)) }
        return RouteBuilder(handle)
    }

    override fun close() = synchronized(handleLock) {
        if (nativeHandle != 0L) {
            val handle = nativeHandle
            nativeHandle = 0L
            SpikardBridge.nativeFreeRouteBuilder(handle)
        }
    }
}
