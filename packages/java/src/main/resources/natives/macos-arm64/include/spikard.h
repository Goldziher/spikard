#ifndef SPIKARD_H
#define SPIKARD_H

/* Cargo features enabled in this build. */
#define SPIKARD_FEATURE_DEFAULT 1
#define SPIKARD_FEATURE_DI 1

#pragma once

/* This file is auto-generated by cbindgen. DO NOT EDIT. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
/* Opaque type forward declarations */
/**
 * Per-route API key authentication requirement.
 *
 * Mirrors `spikard_http::ApiKeyConfig` for the same reason `JwtAuthConfig`
 * mirrors `spikard_http::JwtConfig`: `spikard-core` cannot depend on
 * `spikard-http`.
 */
typedef struct SPIKARDApiKeyAuthConfig SPIKARDApiKeyAuthConfig;
/**
 * API Key authentication configuration
 */
typedef struct SPIKARDApiKeyConfig SPIKARDApiKeyConfig;
/**
 * Spikard application builder.
 */
typedef struct SPIKARDApp SPIKARDApp;
/**
 * AsyncAPI HTTP endpoint configuration
 */
typedef struct SPIKARDAsyncApiConfig SPIKARDAsyncApiConfig;
/**
 * Per-route roles/scopes/permissions authorization requirement.
 *
 * `spikard_http::auth::Claims` does not yet carry roles, scopes, or
 * permissions, so nothing can enforce this today. This type only defines the
 * requirement shape; a later phase must extend `Claims` (or an equivalent
 * claims-decoding path) to populate them before enforcement is possible.
 *
 * Deserialization goes through `AuthorizationConfigRepr` rather than a derive
 * so the fixture's singular `{"required_role": "admin"}` shape
 * (`fixtures/problem_details.json`'s `problem_details_403_forbidden`) populates
 * `required_roles` instead of being silently dropped as an unrecognized field
 * â a config that parses to "no constraint" from real authorization data
 * is a vacuous-pass bug, not a compatibility shim. `deny_unknown_fields` on the
 * repr means any other unrecognized key is a loud deserialize error instead.
 */
typedef struct SPIKARDAuthorizationConfig SPIKARDAuthorizationConfig;
typedef struct SPIKARDBackgroundJobMetadata SPIKARDBackgroundJobMetadata;
/**
 * Configuration for in-process background task execution.
 */
typedef struct SPIKARDBackgroundTaskConfig SPIKARDBackgroundTaskConfig;
/**
 * Compression configuration shared across runtimes
 */
typedef struct SPIKARDCompressionConfig SPIKARDCompressionConfig;
/**
 * Contact information
 */
typedef struct SPIKARDContactInfo SPIKARDContactInfo;
/**
 * CORS configuration for a route
 */
typedef struct SPIKARDCorsConfig SPIKARDCorsConfig;
/**
 * Configuration for building and executing a dynamic-SDL schema.
 */
typedef struct SPIKARDDynamicSchemaConfig SPIKARDDynamicSchemaConfig;
/**
 * A field-level error to inject at a specific response path.
 *
 * `path` is the dot-separated sequence of field names from the operation root
 * to the field that should fail, e.g. `"user"` for a top-level field or
 * `"order.customer"` for a nested one.
 */
typedef struct SPIKARDFieldErrorSpec SPIKARDFieldErrorSpec;
/**
 * Configuration for fully-featured schemas with Query, Mutation, and
 * Subscription types
 */
typedef struct SPIKARDFullSchemaConfig SPIKARDFullSchemaConfig;
/**
 * Errors that can occur during GraphQL operations
 *
 * These errors are compatible with async-graphql error handling and can be
 * converted to structured HTTP responses matching the project's error fixtures.
 */
typedef struct SPIKARDGraphQLError SPIKARDGraphQLError;
/**
 * Configuration for GraphQL routes
 *
 * Provides a builder pattern for configuring GraphQL route parameters
 * for the Spikard HTTP server's routing system.
 * \code
 * use spikard_graphql::routes::GraphQLRouteConfig;
 *
 * let config = GraphQLRouteConfig::new()
 *     .path("/graphql")
 *     .method("POST")
 *     .enable_playground(true);
 *
 * assert_eq!(config.get_path(), "/graphql");
 * assert_eq!(config.get_method(), "POST");
 * \endcode
 */
typedef uint64_t SPIKARDGraphQLRouteConfig;
/**
 * Configuration for gRPC support
 *
 * Controls how the server handles gRPC requests, including compression,
 * timeouts, and protocol settings.
 *
 * # Stream Limits
 *
 * This configuration enforces message-level size limits but delegates
 * concurrent stream limiting to the HTTP/2 transport layer:
 *
 * - **Message Size Limits**: The `max_message_size` field is enforced per
 *   individual message (request or response) in both unary and streaming RPCs.
 *   When a single message exceeds this limit, the request is rejected with
 *   `PAYLOAD_TOO_LARGE` (HTTP 413).
 *
 * - **Concurrent Stream Limits**: The `max_concurrent_streams` is an advisory
 *   configuration passed to the HTTP/2 layer for connection-level stream
 *   negotiation. The HTTP/2 transport automatically enforces this limit and
 *   returns GOAWAY frames when exceeded. Applications should not rely on
 *   custom enforcement of this limit.
 *
 * - **Stream Response Size Limits**: The `max_stream_response_bytes` field caps
 * the total encoded bytes emitted across a server-streaming or bidi-streaming
 * response. When the cumulative size exceeds the limit, the stream is
 * terminated with `tonic::Status::resource_exhausted`. Defaults to `None`
 * (unbounded).
 * \code
 * let mut config = GrpcConfig::default();
 * config.max_message_size = 10 * 1024 * 1024; // 10MB per message
 * config.max_concurrent_streams = 50; // Advised to HTTP/2 layer
 * \endcode
 */
typedef struct SPIKARDGrpcConfig SPIKARDGrpcConfig;
typedef uint64_t SPIKARDHandlerResult;
/**
 * Convert user-facing handler functions into the low-level `Handler` trait.
 */
typedef uint64_t SPIKARDIntoHandler;
/**
 * JSON-RPC server configuration
 */
typedef struct SPIKARDJsonRpcConfig SPIKARDJsonRpcConfig;
/**
 * JSON-RPC method metadata for routes that support JSON-RPC
 *
 * This struct captures the metadata needed to expose HTTP routes as JSON-RPC
 * methods, enabling discovery and documentation of RPC-compatible endpoints.
 * \code
 * use spikard_core::router::JsonRpcMethodInfo;
 * use serde_json::json;
 *
 * let rpc_info = JsonRpcMethodInfo {
 *     method_name: "user.create".to_string(),
 *     description: Some("Creates a new user".to_string()),
 *     params_schema: Some(json!({
 *         "type": "object",
 *         "properties": {
 *             "name": {"type": "string"}
 *         }
 *     })),
 *     result_schema: Some(json!({
 *         "type": "object",
 *         "properties": {
 *             "id": {"type": "integer"}
 *         }
 *     })),
 *     deprecated: false,
 *     tags: vec!["users".to_string()],
 * };
 * \endcode
 */
typedef struct SPIKARDJsonRpcMethodInfo SPIKARDJsonRpcMethodInfo;
/**
 * Per-route JWT authentication requirement.
 *
 * `spikard-http` defines the canonical `JwtConfig` used by
 * `ServerConfig.jwt_auth`, but `spikard-core` cannot depend on `spikard-http`
 * (the dependency runs the other way), so that type cannot be reused here. This
 * mirrors its fields so a later enforcement phase in `spikard-http` can convert
 * between the two without losing information.
 *
 * `secret` and `public_key` are both optional because asymmetric algorithms
 * (RS256, ES256, ...) verify against a public key rather than a shared secret;
 * see `fixtures/auth.json`'s `jwt_config_algorithm_rs256`, which carries
 * `public_key` and no `secret` at all. Exactly one is expected to be populated
 * for a given `algorithm`, but that cross-field invariant is left to a later
 * enforcement phase rather than the type itself.
 */
typedef struct SPIKARDJwtAuthConfig SPIKARDJwtAuthConfig;
/**
 * JWT authentication configuration
 */
typedef struct SPIKARDJwtConfig SPIKARDJwtConfig;
/**
 * License information
 */
typedef struct SPIKARDLicenseInfo SPIKARDLicenseInfo;
/**
 * The five lifecycle phases a hook can be registered against.
 *
 * Used to resolve a per-route named hook selection (`LifecycleHooksConfig` in
 * `spikard-core::http`) against the hooks actually registered on the server: a
 * name is looked up within one specific phase's registered hooks, not across
 * all five, so a hook registered for `on_response` can never be silently picked
 * up by a route asking for `on_request`.
 */
typedef struct SPIKARDLifecycleHookPhase SPIKARDLifecycleHookPhase;
/**
 * A single lifecycle hook reference within a `LifecycleHooksConfig` phase.
 *
 * Matches the fixture shape exactly (`fixtures/lifecycle_hooks.json`,
 * `fixtures/di.json`): each entry is an object with a required `name` and
 * `handler`, an optional list of dependency keys, and an optional free-form
 * `config` blob (e.g. `{"max_requests": 10, "window_seconds": 60}` for a
 * rate-limiting hook). `deny_unknown_fields` turns future fixture drift into a
 * loud error.
 */
typedef struct SPIKARDLifecycleHookRef SPIKARDLifecycleHookRef;
/**
 * Per-route selection of registered lifecycle hooks.
 *
 * `ServerConfig.lifecycle_hooks` holds `Arc<dyn LifecycleHook>` function
 * pointers and is marked
 * `#`serde(skip)`` / `#`alef(skip)`` because closures cannot be serialized or
 * cross the FFI boundary. A per-route field with that same shape would be
 * invisible to alef, and therefore invisible to every binding â defeating
 * the purpose of exposing it here. This descriptor carries `LifecycleHookRef`
 * entries instead: each one names a registered hook (plus its declared
 * dependencies and optional config), so a later enforcement phase can resolve
 * those names against the server's registered `LifecycleHooks` and run the
 * matches for this route. The five fields mirror the five hook phases
 * documented in the `tower-middleware-and-lifecycle` project convention
 * (onRequest, preValidation, preHandler, onResponse, onError).
 */
typedef struct SPIKARDLifecycleHooksConfig SPIKARDLifecycleHooksConfig;
/**
 * HTTP method
 */
typedef struct SPIKARDMethod SPIKARDMethod;
/**
 * OpenAPI configuration
 */
typedef struct SPIKARDOpenApiConfig SPIKARDOpenApiConfig;
/**
 * Request body for `POST /asyncapi/parse`
 */
typedef struct SPIKARDParseRequest SPIKARDParseRequest;
/**
 * Full parse result returned by `POST /asyncapi/parse`
 */
typedef struct SPIKARDParseResult SPIKARDParseResult;
/**
 * A single channel extracted from an AsyncAPI spec
 */
typedef struct SPIKARDParsedChannel SPIKARDParsedChannel;
/**
 * A resolved message (name + JSON Schema)
 */
typedef struct SPIKARDParsedMessage SPIKARDParsedMessage;
/**
 * A single operation extracted from an AsyncAPI spec
 */
typedef struct SPIKARDParsedOperation SPIKARDParsedOperation;
/**
 * RFC 9457 Problem Details for HTTP APIs
 *
 * A machine-readable format for specifying errors in HTTP API responses.
 * Per RFC 9457, all fields are optional. The `type` field defaults to
 * "about:blank" if not specified.
 *
 * # Content-Type
 * Responses using this struct should set:
 * ```text
 * Content-Type: application/problem+json
 * ```
 *
 * ```json
 * {
 *   "type": "https://spikard.dev/errors/validation-error",
 *   "title": "Request Validation Failed",
 *   "status": 422,
 *   "detail": "2 validation errors in request body",
 *   "errors": [...]
 * }
 * ```
 */
typedef struct SPIKARDProblemDetails SPIKARDProblemDetails;
/**
 * Configuration for schemas with Query and Mutation types
 */
typedef struct SPIKARDQueryMutationConfig SPIKARDQueryMutationConfig;
/**
 * Configuration for schemas with only Query type
 */
typedef struct SPIKARDQueryOnlyConfig SPIKARDQueryOnlyConfig;
/**
 * Rate limiting configuration shared across runtimes
 */
typedef struct SPIKARDRateLimitConfig SPIKARDRateLimitConfig;
typedef uint64_t SPIKARDRequest;
typedef uint64_t SPIKARDRequestData;
/**
 * Per-route request-id generation/propagation override.
 *
 * Modeled as a struct rather than `Option<bool>` on `RouteMetadata` because the
 * wire shape is an object, not a bare boolean: `fixtures/request_id.json`'s
 * `request_id_middleware_can_be_disabled` sends `{"enabled": false}`, which
 * `Option<bool>` cannot deserialize at all ("invalid type: map, expected a
 * boolean") â the very fixture whose purpose is proving the middleware can
 * be disabled was the one that failed to parse.
 */
typedef struct SPIKARDRequestIdConfig SPIKARDRequestIdConfig;
/**
 * HTTP Response with custom status code, headers, and content
 */
typedef struct SPIKARDResponse SPIKARDResponse;
/**
 * Builder for defining a route.
 */
typedef uint64_t SPIKARDRouteBuilder;
/**
 * Configuration for GraphQL schema building.
 *
 * Encapsulates all schema-level configuration options including
 * introspection control, complexity limits, and depth limits.
 */
typedef struct SPIKARDSchemaConfig SPIKARDSchemaConfig;
/**
 * Security scheme types
 */
typedef struct SPIKARDSecuritySchemeInfo SPIKARDSecuritySchemeInfo;
/**
 * Server configuration
 */
typedef struct SPIKARDServerConfig SPIKARDServerConfig;
/**
 * Server information
 */
typedef struct SPIKARDServerInfo SPIKARDServerInfo;
/**
 * An individual SSE event
 *
 * Represents a single Server-Sent Event to be sent to a connected client.
 * Events can have an optional type, ID, and retry timeout for advanced
 * scenarios.
 *
 * # Fields
 *
 * * `event_type` - Optional event type string (used for client-side event
 * filtering)
 * * `data` - JSON data payload to send to the client
 * * `id` - Optional event ID (clients can use this to resume after disconnect)
 * * `retry` - Optional retry timeout in milliseconds (tells client when to
 * reconnect)
 *
 * # SSE Format
 *
 * Events are serialized to the following text format:
 * ```text
 * event: event_type
 * data: {"json":"value"}
 * id: event-123
 * retry: 3000
 * ```
 */
typedef struct SPIKARDSseEvent SPIKARDSseEvent;
/**
 * Static file serving configuration
 */
typedef struct SPIKARDStaticFilesConfig SPIKARDStaticFilesConfig;
/**
 * A single Server-Sent Event.
 */
typedef struct SPIKARDTestingSseEvent SPIKARDTestingSseEvent;
/**
 * Represents an uploaded file from multipart/form-data requests.
 *
 * This struct provides efficient access to file content with automatic
 * base64 decoding and implements standard I/O traits for compatibility.
 * \code
 * use spikard::UploadFile;
 * use serde::Deserialize;
 *
 * #`derive(Deserialize)`
 * struct UploadRequest {
 *     file: UploadFile,
 *     description: String,
 * }
 *
 * // In a handler:
 * // let body: UploadRequest = ctx.json()?;
 * // let content = body.file.as_bytes();
 * // let filename = &body.file.filename;
 * \endcode
 */
typedef struct SPIKARDUploadFile SPIKARDUploadFile;
/**
 * Request body for `POST /asyncapi/validate`
 */
typedef struct SPIKARDValidateRequest SPIKARDValidateRequest;
/**
 * Response body for `POST /asyncapi/validate`
 */
typedef struct SPIKARDValidationResponse SPIKARDValidationResponse;

enum SPIKARDAlefFfiErrorCode
#if __STDC_VERSION__ >= 202311L
    : int32_t
#endif // __STDC_VERSION__ >= 202311L
{
  SpikardAlefNone = 0,
  SpikardAlefConversion = 1,
  SpikardAlefUnknown = 2,
  SpikardAlefPanic = 3,
  SpikardAlefInvalidHandle = 4,
};
#if __STDC_VERSION__ >= 202311L
typedef enum SPIKARDAlefFfiErrorCode SPIKARDAlefFfiErrorCode;
#else
typedef int32_t SPIKARDAlefFfiErrorCode;
#endif // __STDC_VERSION__ >= 202311L

typedef uint64_t SPIKARDAlefHandle;

/**
 * Return the last error code (0 means no error).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * This function does not allocate and returns no owned pointer.
 */
int32_t spikard_last_error_code(void);

/**
 * Return the last error message. The pointer is borrowed and valid until the
 * next FFI call on this thread. # Safety Caller must ensure all pointer
 * arguments are valid or null. The returned pointer is borrowed from
 * thread-local storage and must NOT be freed.
 */
const char *spikard_last_error_context(void);

/**
 * Free a string previously returned by this library.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void spikard_free_string(char *ptr);

/**
 * Free a byte buffer previously returned by this library via out-params.
 * `ptr`, `len`, and `cap` must match the values written by the library
 * function, or the call must pass `ptr = null` (in which case it is a no-op).
 * # Safety
 * Pointer must have been returned by this library (via out_ptr / out_len /
 * out_cap out-params), and ownership must not already have been released.
 */
void spikard_free_bytes(uint8_t *ptr, uintptr_t len, uintptr_t cap);

/**
 * Return the library version string. The pointer is static and must NOT be
 * freed. # Safety Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *spikard_version(void);

/**
 * Create a `ApiKeyAuthConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_api_key_auth_config_free`.
 */
SPIKARDAlefHandle spikard_api_key_auth_config_from_json(const char *json);

/**
 * Serialize a `ApiKeyAuthConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_api_key_auth_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ApiKeyAuthConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_api_key_auth_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `ApiKeyAuthConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_api_key_auth_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `keys` field from a `ApiKeyAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_api_key_auth_config_keys(SPIKARDAlefHandle handle);

/**
 * Get the `header_name` field from a `ApiKeyAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_api_key_auth_config_header_name(SPIKARDAlefHandle handle);

/**
 * Create a `ApiKeyConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_api_key_config_free`.
 */
SPIKARDAlefHandle spikard_api_key_config_from_json(const char *json);

/**
 * Serialize a `ApiKeyConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_api_key_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ApiKeyConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_api_key_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `keys` field from a `ApiKeyConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_api_key_config_keys(SPIKARDAlefHandle handle);

/**
 * Get the `header_name` field from a `ApiKeyConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_api_key_config_header_name(SPIKARDAlefHandle handle);

/**
 * Create a `AsyncApiConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_async_api_config_free`.
 */
SPIKARDAlefHandle spikard_async_api_config_from_json(const char *json);

/**
 * Serialize a `AsyncApiConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_async_api_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `AsyncApiConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_async_api_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `AsyncApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_async_api_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `spec` field from a `AsyncApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_async_api_config_spec(SPIKARDAlefHandle handle);

/**
 * Create a `AuthorizationConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_authorization_config_free`.
 */
SPIKARDAlefHandle spikard_authorization_config_from_json(const char *json);

/**
 * Serialize a `AuthorizationConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_authorization_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `AuthorizationConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_authorization_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `required_roles` field from a `AuthorizationConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_authorization_config_required_roles(SPIKARDAlefHandle handle);

/**
 * Get the `required_scopes` field from a `AuthorizationConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_authorization_config_required_scopes(SPIKARDAlefHandle handle);

/**
 * Get the `required_permissions` field from a `AuthorizationConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *
spikard_authorization_config_required_permissions(SPIKARDAlefHandle handle);

/**
 * Get the `require_all` field from a `AuthorizationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_authorization_config_require_all(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_authorization_config_default(void);

/**
 * Create a `BackgroundJobMetadata` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_background_job_metadata_free`.
 */
SPIKARDAlefHandle spikard_background_job_metadata_from_json(const char *json);

/**
 * Serialize a `BackgroundJobMetadata` to a JSON string. Returns null on
 * failure. # Safety `handle` must be a valid, non-zero handle returned by a
 * `spikard` function. The returned string must be freed with
 * `spikard_free_string`.
 */
char *spikard_background_job_metadata_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `BackgroundJobMetadata` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_background_job_metadata_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `BackgroundJobMetadata`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_background_job_metadata_name(SPIKARDAlefHandle handle);

/**
 * Get the `request_id` field from a `BackgroundJobMetadata`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_background_job_metadata_request_id(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_background_job_metadata_default(void);

/**
 * Create a `BackgroundTaskConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_background_task_config_free`.
 */
SPIKARDAlefHandle spikard_background_task_config_from_json(const char *json);

/**
 * Serialize a `BackgroundTaskConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_background_task_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `BackgroundTaskConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_background_task_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_background_task_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `max_queue_size` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t
spikard_background_task_config_max_queue_size(SPIKARDAlefHandle handle);

/**
 * Get the `max_concurrent_tasks` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t
spikard_background_task_config_max_concurrent_tasks(SPIKARDAlefHandle handle);

/**
 * Get the `drain_timeout_secs` field from a `BackgroundTaskConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t
spikard_background_task_config_drain_timeout_secs(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_background_task_config_default(void);

/**
 * Create a `CompressionConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_compression_config_free`.
 */
SPIKARDAlefHandle spikard_compression_config_from_json(const char *json);

/**
 * Serialize a `CompressionConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_compression_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `CompressionConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_compression_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `gzip` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_compression_config_gzip(SPIKARDAlefHandle handle);

/**
 * Get the `brotli` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_compression_config_brotli(SPIKARDAlefHandle handle);

/**
 * Get the `min_size` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_compression_config_min_size(SPIKARDAlefHandle handle);

/**
 * Get the `quality` field from a `CompressionConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_compression_config_quality(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_compression_config_default(void);

/**
 * Create a `ContactInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_contact_info_free`.
 */
SPIKARDAlefHandle spikard_contact_info_from_json(const char *json);

/**
 * Serialize a `ContactInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_contact_info_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ContactInfo` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_contact_info_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `ContactInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_contact_info_name(SPIKARDAlefHandle handle);

/**
 * Get the `email` field from a `ContactInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_contact_info_email(SPIKARDAlefHandle handle);

/**
 * Get the `url` field from a `ContactInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_contact_info_url(SPIKARDAlefHandle handle);

/**
 * Create a `CorsConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_cors_config_free`.
 */
SPIKARDAlefHandle spikard_cors_config_from_json(const char *json);

/**
 * Serialize a `CorsConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_cors_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `CorsConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_cors_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `allowed_origins` field from a `CorsConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_allowed_origins(SPIKARDAlefHandle handle);

/**
 * Get the `allowed_methods` field from a `CorsConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_allowed_methods(SPIKARDAlefHandle handle);

/**
 * Get the `allowed_headers` field from a `CorsConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_allowed_headers(SPIKARDAlefHandle handle);

/**
 * Get the `expose_headers` field from a `CorsConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_cors_config_expose_headers(SPIKARDAlefHandle handle);

/**
 * Get the `max_age` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_cors_config_max_age(SPIKARDAlefHandle handle);

/**
 * Report whether the `max_age` field on a `CorsConfig` is `Some`.
 *
 * `spikard_cors_config_max_age` cannot distinguish a `None` field from a
 * legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_cors_config_has_max_age(SPIKARDAlefHandle handle);

/**
 * Get the `allow_credentials` field from a `CorsConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_cors_config_allow_credentials(SPIKARDAlefHandle handle);

/**
 * Report whether the `allow_credentials` field on a `CorsConfig` is `Some`.
 *
 * `spikard_cors_config_allow_credentials` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_cors_config_has_allow_credentials(SPIKARDAlefHandle handle);

/**
 * Get the cached joined methods string for preflight responses
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_cors_config_allowed_methods_joined(SPIKARDAlefHandle this_);

/**
 * Get the cached joined headers string for preflight responses
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_cors_config_allowed_headers_joined(SPIKARDAlefHandle this_);

/**
 * Check if an origin is allowed (O(1) with wildcard, O(n) for exact match)
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_cors_config_is_origin_allowed(SPIKARDAlefHandle this_,
                                              const char *origin);

/**
 * Check if a method is allowed (O(1) with wildcard, O(n) for exact match)
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_cors_config_is_method_allowed(SPIKARDAlefHandle this_,
                                              const char *method);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_cors_config_default(void);

/**
 * Create a `DynamicSchemaConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_dynamic_schema_config_free`.
 */
SPIKARDAlefHandle spikard_dynamic_schema_config_from_json(const char *json);

/**
 * Serialize a `DynamicSchemaConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_dynamic_schema_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `DynamicSchemaConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_dynamic_schema_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `introspection_enabled` field from a `DynamicSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_dynamic_schema_config_introspection_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `max_complexity` field from a `DynamicSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t
spikard_dynamic_schema_config_max_complexity(SPIKARDAlefHandle handle);

/**
 * Report whether the `max_complexity` field on a `DynamicSchemaConfig` is
 * `Some`.
 *
 * `spikard_dynamic_schema_config_max_complexity` cannot distinguish a `None`
 * field from a legitimate zero-valued `Some` at the C ABI boundary -- there is
 * no null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_dynamic_schema_config_has_max_complexity(SPIKARDAlefHandle handle);

/**
 * Get the `max_depth` field from a `DynamicSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_dynamic_schema_config_max_depth(SPIKARDAlefHandle handle);

/**
 * Report whether the `max_depth` field on a `DynamicSchemaConfig` is `Some`.
 *
 * `spikard_dynamic_schema_config_max_depth` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_dynamic_schema_config_has_max_depth(SPIKARDAlefHandle handle);

/**
 * Get the `field_errors` field from a `DynamicSchemaConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_dynamic_schema_config_field_errors(SPIKARDAlefHandle handle);

/**
 * Create a `FieldErrorSpec` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_field_error_spec_free`.
 */
SPIKARDAlefHandle spikard_field_error_spec_from_json(const char *json);

/**
 * Serialize a `FieldErrorSpec` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_field_error_spec_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `FieldErrorSpec` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_field_error_spec_free(SPIKARDAlefHandle handle);

/**
 * Get the `path` field from a `FieldErrorSpec`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_field_error_spec_path(SPIKARDAlefHandle handle);

/**
 * Get the `message` field from a `FieldErrorSpec`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_field_error_spec_message(SPIKARDAlefHandle handle);

/**
 * Create a `FullSchemaConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_full_schema_config_free`.
 */
SPIKARDAlefHandle spikard_full_schema_config_from_json(const char *json);

/**
 * Serialize a `FullSchemaConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_full_schema_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `FullSchemaConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_full_schema_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `introspection_enabled` field from a `FullSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_full_schema_config_introspection_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `complexity_limit` field from a `FullSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_full_schema_config_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `complexity_limit` field on a `FullSchemaConfig` is
 * `Some`.
 *
 * `spikard_full_schema_config_complexity_limit` cannot distinguish a `None`
 * field from a legitimate zero-valued `Some` at the C ABI boundary -- there is
 * no null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_full_schema_config_has_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Get the `depth_limit` field from a `FullSchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_full_schema_config_depth_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `depth_limit` field on a `FullSchemaConfig` is `Some`.
 *
 * `spikard_full_schema_config_depth_limit` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_full_schema_config_has_depth_limit(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_full_schema_config_default(void);

/**
 * Free a `GraphQLRouteConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_graph_ql_route_config_free(SPIKARDAlefHandle handle);

SPIKARDAlefHandle spikard_graph_ql_route_config_new(void);

/**
 * Set the HTTP path for the GraphQL endpoint
 * \param path The URL path (e.g., "/graphql", "/api/graphql")
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_graph_ql_route_config_path(SPIKARDAlefHandle this_,
                                                     const char *path);

/**
 * Set the HTTP method for the GraphQL endpoint
 * \param method The HTTP method (typically "POST")
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_graph_ql_route_config_method(SPIKARDAlefHandle this_,
                                                       const char *method);

/**
 * Enable or disable the GraphQL Playground UI
 * \param enable Whether to enable playground
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_graph_ql_route_config_enable_playground(SPIKARDAlefHandle this_,
                                                int32_t enable);

/**
 * Set a custom description for documentation
 * \param description Documentation string
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_graph_ql_route_config_description(SPIKARDAlefHandle this_,
                                          const char *description);

/**
 * Get the configured path
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_graph_ql_route_config_get_path(SPIKARDAlefHandle this_);

/**
 * Get the configured method
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_graph_ql_route_config_get_method(SPIKARDAlefHandle this_);

/**
 * Check if playground is enabled
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t
spikard_graph_ql_route_config_is_playground_enabled(SPIKARDAlefHandle this_);

/**
 * Get the description if set
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_graph_ql_route_config_get_description(SPIKARDAlefHandle this_);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_graph_ql_route_config_default(void);

/**
 * Create a `GrpcConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_grpc_config_free`.
 */
SPIKARDAlefHandle spikard_grpc_config_from_json(const char *json);

/**
 * Serialize a `GrpcConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_grpc_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `GrpcConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_grpc_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `max_message_size` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_grpc_config_max_message_size(SPIKARDAlefHandle handle);

/**
 * Get the `enable_compression` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_enable_compression(SPIKARDAlefHandle handle);

/**
 * Get the `request_timeout` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_grpc_config_request_timeout(SPIKARDAlefHandle handle);

/**
 * Report whether the `request_timeout` field on a `GrpcConfig` is `Some`.
 *
 * `spikard_grpc_config_request_timeout` cannot distinguish a `None` field from
 * a legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_has_request_timeout(SPIKARDAlefHandle handle);

/**
 * Get the `max_concurrent_streams` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_grpc_config_max_concurrent_streams(SPIKARDAlefHandle handle);

/**
 * Get the `enable_keepalive` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_grpc_config_enable_keepalive(SPIKARDAlefHandle handle);

/**
 * Get the `keepalive_interval` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_grpc_config_keepalive_interval(SPIKARDAlefHandle handle);

/**
 * Get the `keepalive_timeout` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_grpc_config_keepalive_timeout(SPIKARDAlefHandle handle);

/**
 * Get the `max_stream_response_bytes` field from a `GrpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t
spikard_grpc_config_max_stream_response_bytes(SPIKARDAlefHandle handle);

/**
 * Report whether the `max_stream_response_bytes` field on a `GrpcConfig` is
 * `Some`.
 *
 * `spikard_grpc_config_max_stream_response_bytes` cannot distinguish a `None`
 * field from a legitimate zero-valued `Some` at the C ABI boundary -- there is
 * no null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_grpc_config_has_max_stream_response_bytes(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_grpc_config_default(void);

/**
 * Free a `HandlerResult` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_handler_result_free(SPIKARDAlefHandle handle);

/**
 * Create a `JsonRpcConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_json_rpc_config_free`.
 */
SPIKARDAlefHandle spikard_json_rpc_config_from_json(const char *json);

/**
 * Serialize a `JsonRpcConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_json_rpc_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `JsonRpcConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_json_rpc_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_json_rpc_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `endpoint_path` field from a `JsonRpcConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_config_endpoint_path(SPIKARDAlefHandle handle);

/**
 * Get the `enable_batch` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_json_rpc_config_enable_batch(SPIKARDAlefHandle handle);

/**
 * Get the `max_batch_size` field from a `JsonRpcConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_json_rpc_config_max_batch_size(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_json_rpc_config_default(void);

/**
 * Create a `JsonRpcMethodInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_json_rpc_method_info_free`.
 */
SPIKARDAlefHandle spikard_json_rpc_method_info_from_json(const char *json);

/**
 * Serialize a `JsonRpcMethodInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_json_rpc_method_info_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `JsonRpcMethodInfo` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_json_rpc_method_info_free(SPIKARDAlefHandle handle);

/**
 * Get the `method_name` field from a `JsonRpcMethodInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_method_name(SPIKARDAlefHandle handle);

/**
 * Get the `description` field from a `JsonRpcMethodInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_description(SPIKARDAlefHandle handle);

/**
 * Get the `params_schema` field from a `JsonRpcMethodInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_params_schema(SPIKARDAlefHandle handle);

/**
 * Get the `result_schema` field from a `JsonRpcMethodInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_result_schema(SPIKARDAlefHandle handle);

/**
 * Get the `deprecated` field from a `JsonRpcMethodInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_json_rpc_method_info_deprecated(SPIKARDAlefHandle handle);

/**
 * Get the `tags` field from a `JsonRpcMethodInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_json_rpc_method_info_tags(SPIKARDAlefHandle handle);

/**
 * Create a `JwtAuthConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_jwt_auth_config_free`.
 */
SPIKARDAlefHandle spikard_jwt_auth_config_from_json(const char *json);

/**
 * Serialize a `JwtAuthConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_jwt_auth_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `JwtAuthConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_jwt_auth_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `JwtAuthConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_jwt_auth_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `secret` field from a `JwtAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_auth_config_secret(SPIKARDAlefHandle handle);

/**
 * Get the `public_key` field from a `JwtAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_auth_config_public_key(SPIKARDAlefHandle handle);

/**
 * Get the `algorithm` field from a `JwtAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_auth_config_algorithm(SPIKARDAlefHandle handle);

/**
 * Get the `audience` field from a `JwtAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_auth_config_audience(SPIKARDAlefHandle handle);

/**
 * Get the `issuer` field from a `JwtAuthConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_auth_config_issuer(SPIKARDAlefHandle handle);

/**
 * Get the `leeway` field from a `JwtAuthConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_jwt_auth_config_leeway(SPIKARDAlefHandle handle);

/**
 * Create a `JwtConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_jwt_config_free`.
 */
SPIKARDAlefHandle spikard_jwt_config_from_json(const char *json);

/**
 * Serialize a `JwtConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_jwt_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `JwtConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_jwt_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `secret` field from a `JwtConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_secret(SPIKARDAlefHandle handle);

/**
 * Get the `algorithm` field from a `JwtConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_algorithm(SPIKARDAlefHandle handle);

/**
 * Get the `audience` field from a `JwtConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_audience(SPIKARDAlefHandle handle);

/**
 * Get the `issuer` field from a `JwtConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_jwt_config_issuer(SPIKARDAlefHandle handle);

/**
 * Get the `leeway` field from a `JwtConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_jwt_config_leeway(SPIKARDAlefHandle handle);

/**
 * Create a `LicenseInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_license_info_free`.
 */
SPIKARDAlefHandle spikard_license_info_from_json(const char *json);

/**
 * Serialize a `LicenseInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_license_info_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `LicenseInfo` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_license_info_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `LicenseInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_license_info_name(SPIKARDAlefHandle handle);

/**
 * Get the `url` field from a `LicenseInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_license_info_url(SPIKARDAlefHandle handle);

/**
 * Create a `LifecycleHookRef` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_lifecycle_hook_ref_free`.
 */
SPIKARDAlefHandle spikard_lifecycle_hook_ref_from_json(const char *json);

/**
 * Serialize a `LifecycleHookRef` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_lifecycle_hook_ref_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `LifecycleHookRef` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_lifecycle_hook_ref_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `LifecycleHookRef`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hook_ref_name(SPIKARDAlefHandle handle);

/**
 * Get the `handler` field from a `LifecycleHookRef`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hook_ref_handler(SPIKARDAlefHandle handle);

/**
 * Get the `dependencies` field from a `LifecycleHookRef`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hook_ref_dependencies(SPIKARDAlefHandle handle);

/**
 * Get the `config` field from a `LifecycleHookRef`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hook_ref_config(SPIKARDAlefHandle handle);

/**
 * Get the `order` field from a `LifecycleHookRef`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_lifecycle_hook_ref_order(SPIKARDAlefHandle handle);

/**
 * Report whether the `order` field on a `LifecycleHookRef` is `Some`.
 *
 * `spikard_lifecycle_hook_ref_order` cannot distinguish a `None` field from a
 * legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_lifecycle_hook_ref_has_order(SPIKARDAlefHandle handle);

/**
 * Create a `LifecycleHooksConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_lifecycle_hooks_config_free`.
 */
SPIKARDAlefHandle spikard_lifecycle_hooks_config_from_json(const char *json);

/**
 * Serialize a `LifecycleHooksConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_lifecycle_hooks_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `LifecycleHooksConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_lifecycle_hooks_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `on_request` field from a `LifecycleHooksConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hooks_config_on_request(SPIKARDAlefHandle handle);

/**
 * Get the `pre_validation` field from a `LifecycleHooksConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hooks_config_pre_validation(SPIKARDAlefHandle handle);

/**
 * Get the `pre_handler` field from a `LifecycleHooksConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hooks_config_pre_handler(SPIKARDAlefHandle handle);

/**
 * Get the `on_response` field from a `LifecycleHooksConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hooks_config_on_response(SPIKARDAlefHandle handle);

/**
 * Get the `on_error` field from a `LifecycleHooksConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_lifecycle_hooks_config_on_error(SPIKARDAlefHandle handle);

/**
 * Create a `OpenApiConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_open_api_config_free`.
 */
SPIKARDAlefHandle spikard_open_api_config_from_json(const char *json);

/**
 * Serialize a `OpenApiConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_open_api_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `OpenApiConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_open_api_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `OpenApiConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_open_api_config_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `title` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_title(SPIKARDAlefHandle handle);

/**
 * Get the `version` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_version(SPIKARDAlefHandle handle);

/**
 * Get the `description` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_description(SPIKARDAlefHandle handle);

/**
 * Get the `swagger_ui_path` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_swagger_ui_path(SPIKARDAlefHandle handle);

/**
 * Get the `redoc_path` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_redoc_path(SPIKARDAlefHandle handle);

/**
 * Get the `openapi_json_path` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_openapi_json_path(SPIKARDAlefHandle handle);

/**
 * Get the `contact` field from a `OpenApiConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_contact_info_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_open_api_config_contact(SPIKARDAlefHandle handle);

/**
 * Get the `license` field from a `OpenApiConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_license_info_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_open_api_config_license(SPIKARDAlefHandle handle);

/**
 * Get the `servers` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_servers(SPIKARDAlefHandle handle);

/**
 * Get the `security_schemes` field from a `OpenApiConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_open_api_config_security_schemes(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_open_api_config_default(void);

/**
 * Free a `ParseRequest` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_parse_request_free(SPIKARDAlefHandle handle);

/**
 * Get the `spec` field from a `ParseRequest`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_request_spec(SPIKARDAlefHandle handle);

/**
 * Create a `ParseResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_parse_result_free`.
 */
SPIKARDAlefHandle spikard_parse_result_from_json(const char *json);

/**
 * Serialize a `ParseResult` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_parse_result_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ParseResult` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_parse_result_free(SPIKARDAlefHandle handle);

/**
 * Get the `spec_version` field from a `ParseResult`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_result_spec_version(SPIKARDAlefHandle handle);

/**
 * Get the `title` field from a `ParseResult`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_result_title(SPIKARDAlefHandle handle);

/**
 * Get the `api_version` field from a `ParseResult`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_result_api_version(SPIKARDAlefHandle handle);

/**
 * Get the `channels` field from a `ParseResult`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_result_channels(SPIKARDAlefHandle handle);

/**
 * Get the `operations` field from a `ParseResult`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_result_operations(SPIKARDAlefHandle handle);

/**
 * Get the `messages` field from a `ParseResult`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parse_result_messages(SPIKARDAlefHandle handle);

/**
 * Create a `ParsedChannel` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_parsed_channel_free`.
 */
SPIKARDAlefHandle spikard_parsed_channel_from_json(const char *json);

/**
 * Serialize a `ParsedChannel` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_parsed_channel_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ParsedChannel` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_parsed_channel_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `ParsedChannel`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_channel_name(SPIKARDAlefHandle handle);

/**
 * Get the `address` field from a `ParsedChannel`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_channel_address(SPIKARDAlefHandle handle);

/**
 * Get the `messages` field from a `ParsedChannel`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_channel_messages(SPIKARDAlefHandle handle);

/**
 * Get the `bindings` field from a `ParsedChannel`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_channel_bindings(SPIKARDAlefHandle handle);

/**
 * Create a `ParsedMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_parsed_message_free`.
 */
SPIKARDAlefHandle spikard_parsed_message_from_json(const char *json);

/**
 * Serialize a `ParsedMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_parsed_message_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ParsedMessage` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_parsed_message_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `ParsedMessage`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_message_name(SPIKARDAlefHandle handle);

/**
 * Get the `schema` field from a `ParsedMessage`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_message_schema(SPIKARDAlefHandle handle);

/**
 * Create a `ParsedOperation` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_parsed_operation_free`.
 */
SPIKARDAlefHandle spikard_parsed_operation_from_json(const char *json);

/**
 * Serialize a `ParsedOperation` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_parsed_operation_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ParsedOperation` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_parsed_operation_free(SPIKARDAlefHandle handle);

/**
 * Get the `name` field from a `ParsedOperation`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_operation_name(SPIKARDAlefHandle handle);

/**
 * Get the `action` field from a `ParsedOperation`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_operation_action(SPIKARDAlefHandle handle);

/**
 * Get the `channel` field from a `ParsedOperation`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_parsed_operation_channel(SPIKARDAlefHandle handle);

/**
 * Create a `ProblemDetails` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_problem_details_free`.
 */
SPIKARDAlefHandle spikard_problem_details_from_json(const char *json);

/**
 * Free a `ProblemDetails` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_problem_details_free(SPIKARDAlefHandle handle);

/**
 * Get the `type_uri` field from a `ProblemDetails`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_type_uri(SPIKARDAlefHandle handle);

/**
 * Get the `title` field from a `ProblemDetails`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_title(SPIKARDAlefHandle handle);

/**
 * Get the `status` field from a `ProblemDetails`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t spikard_problem_details_status(SPIKARDAlefHandle handle);

/**
 * Get the `detail` field from a `ProblemDetails`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_detail(SPIKARDAlefHandle handle);

/**
 * Get the `instance` field from a `ProblemDetails`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_instance(SPIKARDAlefHandle handle);

/**
 * Get the `extensions` field from a `ProblemDetails`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_problem_details_extensions(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_problem_details_default(void);

/**
 * Set the detail field
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_problem_details_with_detail(SPIKARDAlefHandle this_,
                                                      const char *detail);

/**
 * Set the instance field
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_problem_details_with_instance(SPIKARDAlefHandle this_,
                                                        const char *instance);

/**
 * Create a not found error
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_problem_details_not_found(const char *detail);

/**
 * Create a method not allowed error
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_problem_details_method_not_allowed(const char *detail);

/**
 * Create an internal server error
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_problem_details_internal_server_error(const char *detail);

/**
 * Create a bad request error
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_problem_details_bad_request(const char *detail);

/**
 * Serialize to JSON string
 * \note Returns an error if the serialization fails.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_problem_details_to_json(SPIKARDAlefHandle this_);

/**
 * Serialize to pretty JSON string
 * \note Returns an error if the serialization fails.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_problem_details_to_json_pretty(SPIKARDAlefHandle this_);

/**
 * Create a `QueryMutationConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_query_mutation_config_free`.
 */
SPIKARDAlefHandle spikard_query_mutation_config_from_json(const char *json);

/**
 * Serialize a `QueryMutationConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_query_mutation_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `QueryMutationConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_query_mutation_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `introspection_enabled` field from a `QueryMutationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_query_mutation_config_introspection_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `complexity_limit` field from a `QueryMutationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t
spikard_query_mutation_config_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `complexity_limit` field on a `QueryMutationConfig` is
 * `Some`.
 *
 * `spikard_query_mutation_config_complexity_limit` cannot distinguish a `None`
 * field from a legitimate zero-valued `Some` at the C ABI boundary -- there is
 * no null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_query_mutation_config_has_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Get the `depth_limit` field from a `QueryMutationConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_mutation_config_depth_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `depth_limit` field on a `QueryMutationConfig` is `Some`.
 *
 * `spikard_query_mutation_config_depth_limit` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_query_mutation_config_has_depth_limit(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_query_mutation_config_default(void);

/**
 * Create a `QueryOnlyConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_query_only_config_free`.
 */
SPIKARDAlefHandle spikard_query_only_config_from_json(const char *json);

/**
 * Serialize a `QueryOnlyConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_query_only_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `QueryOnlyConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_query_only_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `introspection_enabled` field from a `QueryOnlyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_query_only_config_introspection_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `complexity_limit` field from a `QueryOnlyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_only_config_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `complexity_limit` field on a `QueryOnlyConfig` is `Some`.
 *
 * `spikard_query_only_config_complexity_limit` cannot distinguish a `None`
 * field from a legitimate zero-valued `Some` at the C ABI boundary -- there is
 * no null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t
spikard_query_only_config_has_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Get the `depth_limit` field from a `QueryOnlyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_query_only_config_depth_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `depth_limit` field on a `QueryOnlyConfig` is `Some`.
 *
 * `spikard_query_only_config_depth_limit` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_query_only_config_has_depth_limit(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_query_only_config_default(void);

/**
 * Create a `RateLimitConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_rate_limit_config_free`.
 */
SPIKARDAlefHandle spikard_rate_limit_config_from_json(const char *json);

/**
 * Serialize a `RateLimitConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_rate_limit_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `RateLimitConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_rate_limit_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `per_second` field from a `RateLimitConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_rate_limit_config_per_second(SPIKARDAlefHandle handle);

/**
 * Get the `burst` field from a `RateLimitConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t spikard_rate_limit_config_burst(SPIKARDAlefHandle handle);

/**
 * Get the `ip_based` field from a `RateLimitConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_rate_limit_config_ip_based(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_rate_limit_config_default(void);

/**
 * Free a `RequestData` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_request_data_free(SPIKARDAlefHandle handle);

/**
 * Create a `RequestIdConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_request_id_config_free`.
 */
SPIKARDAlefHandle spikard_request_id_config_from_json(const char *json);

/**
 * Serialize a `RequestIdConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_request_id_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `RequestIdConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_request_id_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `enabled` field from a `RequestIdConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_request_id_config_enabled(SPIKARDAlefHandle handle);

/**
 * Create a `Response` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_response_free`.
 */
SPIKARDAlefHandle spikard_response_from_json(const char *json);

/**
 * Serialize a `Response` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_response_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `Response` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_response_free(SPIKARDAlefHandle handle);

/**
 * Get the `content` field from a `Response`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_response_content(SPIKARDAlefHandle handle);

/**
 * Get the `status_code` field from a `Response`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t spikard_response_status_code(SPIKARDAlefHandle handle);

/**
 * Get the `headers` field from a `Response`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_response_headers(SPIKARDAlefHandle handle);

/**
 * Set a header
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
void spikard_response_set_header(SPIKARDAlefHandle this_, const char *key,
                                 const char *value);

/**
 * Set a cookie in the response
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
void spikard_response_set_cookie(SPIKARDAlefHandle this_, const char *key,
                                 const char *value, int32_t secure,
                                 int32_t http_only, int64_t max_age,
                                 const char *domain, const char *path,
                                 const char *same_site);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_response_default(void);

/**
 * Free a `RouteBuilder` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_route_builder_free(SPIKARDAlefHandle handle);

SPIKARDAlefHandle spikard_route_builder_new(int32_t method, const char *path);

/**
 * Assign an explicit handler name.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_handler_name(SPIKARDAlefHandle this_,
                                                     const char *name);

/**
 * Provide a raw JSON schema for the request body.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_request_schema_json(SPIKARDAlefHandle this_,
                                          const char *schema);

/**
 * Provide a raw JSON schema for the response body.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_response_schema_json(SPIKARDAlefHandle this_,
                                           const char *schema);

/**
 * Provide a raw JSON schema for request parameters.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_params_schema_json(SPIKARDAlefHandle this_,
                                         const char *schema);

/**
 * Provide multipart file parameter configuration.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_file_params_json(SPIKARDAlefHandle this_,
                                       const char *schema);

/**
 * Attach a CORS configuration for this route.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_cors(SPIKARDAlefHandle this_,
                                             SPIKARDAlefHandle cors);

/**
 * Attach a compression configuration for this route.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_compression(SPIKARDAlefHandle this_,
                                  SPIKARDAlefHandle compression);

/**
 * Attach a per-route maximum request body size in bytes, overriding the
 * server-global default.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_body_limit(SPIKARDAlefHandle this_,
                                                   uintptr_t max_bytes);

/**
 * Attach a per-route request timeout in seconds, overriding the server-global
 * default.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_request_timeout(SPIKARDAlefHandle this_,
                                                        uint64_t seconds);

/**
 * Attach a per-route rate limiting configuration, overriding the server-global
 * default.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_rate_limit(SPIKARDAlefHandle this_,
                                 SPIKARDAlefHandle rate_limit);

/**
 * Force per-route request-id generation on or off, overriding the server-global
 * default.
 *
 * Takes a plain `bool` rather than `RequestIdConfig` directly: the
 * wire/metadata type had to become a struct to represent `{"enabled": false}`
 * (see `RequestIdConfig`'s docs), but this builder is the ergonomic call site
 * (`.request_id(true)`), so it keeps accepting a bool and wraps it into
 * `RequestIdConfig` internally in `Self.into_metadata`.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_request_id(SPIKARDAlefHandle this_,
                                                   int32_t enabled);

/**
 * Require JWT authentication for this route.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_jwt_auth(SPIKARDAlefHandle this_,
                                                 SPIKARDAlefHandle config);

/**
 * Require API key authentication for this route.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_api_key_auth(SPIKARDAlefHandle this_,
                                                     SPIKARDAlefHandle config);

/**
 * Attach a roles/scopes/permissions authorization requirement for this route.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_authorization(SPIKARDAlefHandle this_,
                                                      SPIKARDAlefHandle config);

/**
 * Select registered lifecycle hooks to run for this route.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_lifecycle_hooks(SPIKARDAlefHandle this_,
                                      SPIKARDAlefHandle hooks);

/**
 * Expose this route as a JSON-RPC method.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_jsonrpc_method(SPIKARDAlefHandle this_,
                                                       SPIKARDAlefHandle info);

/**
 * Attach a literal `OpenRPC` method spec document for this route, overriding
 * auto-derivation from the `Self.jsonrpc_method` metadata when present.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_openrpc_spec(SPIKARDAlefHandle this_,
                                                     const char *spec);

/**
 * Mark the route as synchronous.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_route_builder_sync(SPIKARDAlefHandle this_);

#if defined(SPIKARD_FEATURE_DI)
/**
 * Declare the dependency keys that must be resolved before this handler runs.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle
spikard_route_builder_handler_dependencies(SPIKARDAlefHandle this_,
                                           const char *dependencies);
#endif

/**
 * Create a `SchemaConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_schema_config_free`.
 */
SPIKARDAlefHandle spikard_schema_config_from_json(const char *json);

/**
 * Serialize a `SchemaConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_schema_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `SchemaConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_schema_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `introspection_enabled` field from a `SchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_schema_config_introspection_enabled(SPIKARDAlefHandle handle);

/**
 * Get the `complexity_limit` field from a `SchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_schema_config_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `complexity_limit` field on a `SchemaConfig` is `Some`.
 *
 * `spikard_schema_config_complexity_limit` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_schema_config_has_complexity_limit(SPIKARDAlefHandle handle);

/**
 * Get the `depth_limit` field from a `SchemaConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_schema_config_depth_limit(SPIKARDAlefHandle handle);

/**
 * Report whether the `depth_limit` field on a `SchemaConfig` is `Some`.
 *
 * `spikard_schema_config_depth_limit` cannot distinguish a `None` field from a
 * legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_schema_config_has_depth_limit(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_schema_config_default(void);

/**
 * Create a `ServerConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_server_config_free`.
 */
SPIKARDAlefHandle spikard_server_config_from_json(const char *json);

/**
 * Serialize a `ServerConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_server_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ServerConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_server_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `host` field from a `ServerConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_config_host(SPIKARDAlefHandle handle);

/**
 * Get the `port` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t spikard_server_config_port(SPIKARDAlefHandle handle);

/**
 * Get the `workers` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_server_config_workers(SPIKARDAlefHandle handle);

/**
 * Get the `enable_request_id` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_enable_request_id(SPIKARDAlefHandle handle);

/**
 * Get the `max_body_size` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_server_config_max_body_size(SPIKARDAlefHandle handle);

/**
 * Report whether the `max_body_size` field on a `ServerConfig` is `Some`.
 *
 * `spikard_server_config_max_body_size` cannot distinguish a `None` field from
 * a legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_has_max_body_size(SPIKARDAlefHandle handle);

/**
 * Get the `request_timeout` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_server_config_request_timeout(SPIKARDAlefHandle handle);

/**
 * Report whether the `request_timeout` field on a `ServerConfig` is `Some`.
 *
 * `spikard_server_config_request_timeout` cannot distinguish a `None` field
 * from a legitimate zero-valued `Some` at the C ABI boundary -- there is no
 * null representation for a numeric return, so both collapse to the same
 * sentinel. Call this function first: `1` means the field getter's return value
 * is meaningful, `0` means the field is absent and the getter's sentinel must
 * be ignored, `-1` reports an invalid handle (see `spikard_last_error_code`).
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_has_request_timeout(SPIKARDAlefHandle handle);

/**
 * Get the `compression` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_compression_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_compression(SPIKARDAlefHandle handle);

/**
 * Get the `rate_limit` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_rate_limit_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_rate_limit(SPIKARDAlefHandle handle);

/**
 * Get the `jwt_auth` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_jwt_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_jwt_auth(SPIKARDAlefHandle handle);

/**
 * Get the `api_key_auth` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_api_key_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_api_key_auth(SPIKARDAlefHandle handle);

/**
 * Get the `static_files` field from a `ServerConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_config_static_files(SPIKARDAlefHandle handle);

/**
 * Get the `graceful_shutdown` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_graceful_shutdown(SPIKARDAlefHandle handle);

/**
 * Get the `shutdown_timeout` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_server_config_shutdown_timeout(SPIKARDAlefHandle handle);

/**
 * Get the `asyncapi` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_async_api_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_asyncapi(SPIKARDAlefHandle handle);

/**
 * Get the `openapi` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_open_api_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_openapi(SPIKARDAlefHandle handle);

/**
 * Get the `jsonrpc` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_json_rpc_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_jsonrpc(SPIKARDAlefHandle handle);

/**
 * Get the `grpc` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_grpc_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle spikard_server_config_grpc(SPIKARDAlefHandle handle);

/**
 * Get the `background_tasks` field from a `ServerConfig`.
 * A non-null returned handle is owned by the caller.
 * It must be freed with `spikard_background_task_config_free`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
SPIKARDAlefHandle
spikard_server_config_background_tasks(SPIKARDAlefHandle handle);

/**
 * Get the `enable_http_trace` field from a `ServerConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_server_config_enable_http_trace(SPIKARDAlefHandle handle);

/**
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_server_config_default(void);

/**
 * Create a `ServerInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_server_info_free`.
 */
SPIKARDAlefHandle spikard_server_info_from_json(const char *json);

/**
 * Serialize a `ServerInfo` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_server_info_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `ServerInfo` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_server_info_free(SPIKARDAlefHandle handle);

/**
 * Get the `url` field from a `ServerInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_info_url(SPIKARDAlefHandle handle);

/**
 * Get the `description` field from a `ServerInfo`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_server_info_description(SPIKARDAlefHandle handle);

/**
 * Create a `SseEvent` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_sse_event_free`.
 */
SPIKARDAlefHandle spikard_sse_event_from_json(const char *json);

/**
 * Serialize a `SseEvent` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_sse_event_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `SseEvent` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_sse_event_free(SPIKARDAlefHandle handle);

/**
 * Get the `event_type` field from a `SseEvent`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_sse_event_event_type(SPIKARDAlefHandle handle);

/**
 * Get the `data` field from a `SseEvent`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_sse_event_data(SPIKARDAlefHandle handle);

/**
 * Get the `id` field from a `SseEvent`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_sse_event_id(SPIKARDAlefHandle handle);

/**
 * Get the `retry` field from a `SseEvent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t spikard_sse_event_retry(SPIKARDAlefHandle handle);

/**
 * Report whether the `retry` field on a `SseEvent` is `Some`.
 *
 * `spikard_sse_event_retry` cannot distinguish a `None` field from a
 * legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_sse_event_has_retry(SPIKARDAlefHandle handle);

/**
 * Set the event ID for client-side reconnection support
 *
 * Sets an ID that clients can use to resume from this point if they disconnect.
 * The client sends this ID back in the `Last-Event-ID` header when
 * reconnecting.
 * \param id Unique identifier for this event
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 * \code
 * use serde_json::json;
 * use spikard_http::sse::SseEvent;
 *
 * let event = SseEvent::new(json!({"count": 1}))
 *     .with_id("event-1");
 * \endcode
 */
SPIKARDAlefHandle spikard_sse_event_with_id(SPIKARDAlefHandle this_,
                                            const char *id);

/**
 * Set the retry timeout for client reconnection
 *
 * Sets the time in milliseconds clients should wait before attempting to
 * reconnect if the connection is lost. The client browser will automatically
 * handle reconnection.
 * \param retry_ms Retry timeout in milliseconds
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 * \code
 * use serde_json::json;
 * use spikard_http::sse::SseEvent;
 *
 * let event = SseEvent::new(json!({"data": "value"}))
 *     .with_retry(5000); // Reconnect after 5 seconds
 * \endcode
 */
SPIKARDAlefHandle spikard_sse_event_with_retry(SPIKARDAlefHandle this_,
                                               uint64_t retry_ms);

/**
 * Create a `StaticFilesConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_static_files_config_free`.
 */
SPIKARDAlefHandle spikard_static_files_config_from_json(const char *json);

/**
 * Serialize a `StaticFilesConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_static_files_config_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `StaticFilesConfig` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_static_files_config_free(SPIKARDAlefHandle handle);

/**
 * Get the `directory` field from a `StaticFilesConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_static_files_config_directory(SPIKARDAlefHandle handle);

/**
 * Get the `route_prefix` field from a `StaticFilesConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_static_files_config_route_prefix(SPIKARDAlefHandle handle);

/**
 * Get the `index_file` field from a `StaticFilesConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_static_files_config_index_file(SPIKARDAlefHandle handle);

/**
 * Get the `cache_control` field from a `StaticFilesConfig`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_static_files_config_cache_control(SPIKARDAlefHandle handle);

/**
 * Free a `TestingSseEvent` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_testing_sse_event_free(SPIKARDAlefHandle handle);

/**
 * Get the `data` field from a `TestingSseEvent`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_testing_sse_event_data(SPIKARDAlefHandle handle);

/**
 * Create a `UploadFile` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_upload_file_free`.
 */
SPIKARDAlefHandle spikard_upload_file_from_json(const char *json);

/**
 * Serialize a `UploadFile` to a JSON string. Returns null on failure.
 * # Safety
 * `handle` must be a valid, non-zero handle returned by a `spikard` function.
 * The returned string must be freed with `spikard_free_string`.
 */
char *spikard_upload_file_to_json(SPIKARDAlefHandle handle);

/**
 * Free a `UploadFile` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_upload_file_free(SPIKARDAlefHandle handle);

/**
 * Get the `filename` field from a `UploadFile`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_upload_file_filename(SPIKARDAlefHandle handle);

/**
 * Get the `content_type` field from a `UploadFile`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_upload_file_content_type(SPIKARDAlefHandle handle);

/**
 * Get the `size` field from a `UploadFile`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t spikard_upload_file_size(SPIKARDAlefHandle handle);

/**
 * Report whether the `size` field on a `UploadFile` is `Some`.
 *
 * `spikard_upload_file_size` cannot distinguish a `None` field from a
 * legitimate zero-valued `Some` at the C ABI boundary -- there is no null
 * representation for a numeric return, so both collapse to the same sentinel.
 * Call this function first: `1` means the field getter's return value is
 * meaningful, `0` means the field is absent and the getter's sentinel must be
 * ignored, `-1` reports an invalid handle (see `spikard_last_error_code`). #
 * Safety Pointer must be a valid handle returned by this library.
 */
int32_t spikard_upload_file_has_size(SPIKARDAlefHandle handle);

/**
 * Get the `content_encoding` field from a `UploadFile`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_upload_file_content_encoding(SPIKARDAlefHandle handle);

/**
 * Get the raw file content as bytes.
 *
 * This provides zero-copy access to the underlying buffer.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_upload_file_as_bytes(SPIKARDAlefHandle this_, uint8_t **out_ptr,
                                     uintptr_t *out_len, uintptr_t *out_cap);

/**
 * Read the file content as a UTF-8 string.
 * \note Returns an error if the content is not valid UTF-8.
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_upload_file_read_to_string(SPIKARDAlefHandle this_);

/**
 * Get the content type, defaulting to "application/octet-stream".
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *spikard_upload_file_content_type_or_default(SPIKARDAlefHandle this_);

/**
 * Free a `ValidateRequest` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_validate_request_free(SPIKARDAlefHandle handle);

/**
 * Get the `spec` field from a `ValidateRequest`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_validate_request_spec(SPIKARDAlefHandle handle);

/**
 * Get the `channel` field from a `ValidateRequest`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_validate_request_channel(SPIKARDAlefHandle handle);

/**
 * Get the `message` field from a `ValidateRequest`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_validate_request_message(SPIKARDAlefHandle handle);

/**
 * Get the `payload` field from a `ValidateRequest`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_validate_request_payload(SPIKARDAlefHandle handle);

/**
 * Free a `ValidationResponse` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_validation_response_free(SPIKARDAlefHandle handle);

/**
 * Get the `valid` field from a `ValidationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t spikard_validation_response_valid(SPIKARDAlefHandle handle);

/**
 * Get the `errors` field from a `ValidationResponse`.
 * A non-null returned pointer is owned by the caller.
 * It must be freed with `spikard_free_string`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *spikard_validation_response_errors(SPIKARDAlefHandle handle);

/**
 * Convert an integer to a `LifecycleHookPhase` variant. Returns -1 on invalid
 * input. # Safety Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_lifecycle_hook_phase_from_i32(int32_t value);

/**
 * Convert a `LifecycleHookPhase` serde wire value (C string) to its integer
 * discriminant. Returns -1 on invalid input. # Safety Caller must ensure `ptr`
 * is a valid pointer to a `c_char` or null.
 */
int32_t spikard_lifecycle_hook_phase_from_str(const char *name);

/**
 * Convert an integer to a `Method` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_method_from_i32(int32_t value);

/**
 * Convert a `Method` serde wire value (C string) to its integer discriminant.
 * Returns -1 on invalid input. # Safety Caller must ensure `ptr` is a valid
 * pointer to a `c_char` or null.
 */
int32_t spikard_method_from_str(const char *name);

/**
 * Convert an integer to a `SecuritySchemeInfo` variant. Returns -1 on invalid
 * input. # Safety Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_security_scheme_info_from_i32(int32_t value);

/**
 * Convert a `SecuritySchemeInfo` serde wire value (C string) to its integer
 * discriminant. Returns -1 on invalid input. # Safety Caller must ensure `ptr`
 * is a valid pointer to a `c_char` or null.
 */
int32_t spikard_security_scheme_info_from_str(const char *name);

/**
 * Convert an integer to a `SnapshotError` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_snapshot_error_from_i32(int32_t value);

/**
 * Convert a `SnapshotError` serde wire value (C string) to its integer
 * discriminant. Returns -1 on invalid input. # Safety Caller must ensure `ptr`
 * is a valid pointer to a `c_char` or null.
 */
int32_t spikard_snapshot_error_from_str(const char *name);

/**
 * Convert an integer to a `WebSocketMessage` variant. Returns -1 on invalid
 * input. # Safety Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t spikard_web_socket_message_from_i32(int32_t value);

/**
 * Convert a `WebSocketMessage` serde wire value (C string) to its integer
 * discriminant. Returns -1 on invalid input. # Safety Caller must ensure `ptr`
 * is a valid pointer to a `c_char` or null.
 */
int32_t spikard_web_socket_message_from_str(const char *name);

/**
 * Create a `Method` from a JSON string. Returns 0 on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `spikard_method_free`.
 */
SPIKARDAlefHandle spikard_method_from_json(const char *json);

/**
 * Free a `Method` handle.
 * # Safety
 * Handle must have been returned by this library, or be zero.
 */
void spikard_method_free(SPIKARDAlefHandle handle);

/**
 * Create a schema configuration with all three root types.
 *
 * This is a convenience function for fully-featured schemas.
 * \return A `FullSchemaConfig` with default settings
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_schema_full(void);

/**
 * Create a schema configuration with Query and Mutation types.
 *
 * This is a convenience function for schemas with queries and mutations but no
 * subscriptions.
 * \return A `QueryMutationConfig` with default settings
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_schema_query_mutation(void);

/**
 * Create a simple schema configuration with only Query type.
 *
 * This is a convenience function for schemas that only have queries.
 * \return A `QueryOnlyConfig` with default settings
 * \note SAFETY: Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
SPIKARDAlefHandle spikard_schema_query_only(void);

/**
 * Return the HTTP status code for the error pointed to by `err`.
 * Returns `0` if `err` is null.
 */
uint16_t spikard_graph_ql_error_status_code(const SPIKARDGraphQLError *err);

/**
 * Return whether the error pointed to by `err` is transient.
 * Returns `false` if `err` is null.
 */
bool spikard_graph_ql_error_is_transient(const SPIKARDGraphQLError *err);

/**
 * Return the machine-readable error category string for the error pointed
 * to by `err` as a heap-allocated, NUL-terminated C string.
 * The caller must free the returned pointer with
 * `spikard_graph_ql_error_error_type_free`. Returns a null pointer if `err` is
 * null.
 */
char *spikard_graph_ql_error_error_type(const SPIKARDGraphQLError *err);

/**
 * Free a string previously returned by `spikard_graph_ql_error_error_type`.
 * Passing a null pointer is a no-op.
 */
void spikard_graph_ql_error_error_type_free(char *ptr);

/**
 * Allocate a new App instance.
 *
 * # Safety
 * The returned pointer must be freed via spikard_app_free().
 * Never access the pointer after freeing it.
 */
SPIKARDAlefHandle spikard_app_new(void);

/**
 * Free a App instance allocated by spikard_app_new().
 *
 * # Safety
 * - `ptr` must have been allocated by spikard_app_new().
 * - After this call, `ptr` is invalid and must not be dereferenced.
 * - Calling this twice on the same pointer causes undefined behavior.
 * - Safe to call even after a `Finalize` entrypoint has emptied `inner`.
 */
void spikard_app_free(SPIKARDAlefHandle handle);

/**
 * Register a handler callback for method 'route'.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_register_route(SPIKARDAlefHandle owner,
                                   char *(*callback)(void *, const char *),
                                   void (*response_free)(char *), void *context,
                                   SPIKARDAlefHandle builder);

/**
 * Register a GET route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_get(SPIKARDAlefHandle owner,
                        char *(*callback)(void *, const char *),
                        void (*response_free)(char *), void *context,
                        const char *path);

/**
 * Register a POST route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_post(SPIKARDAlefHandle owner,
                         char *(*callback)(void *, const char *),
                         void (*response_free)(char *), void *context,
                         const char *path);

/**
 * Register a PUT route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_put(SPIKARDAlefHandle owner,
                        char *(*callback)(void *, const char *),
                        void (*response_free)(char *), void *context,
                        const char *path);

/**
 * Register a PATCH route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_patch(SPIKARDAlefHandle owner,
                          char *(*callback)(void *, const char *),
                          void (*response_free)(char *), void *context,
                          const char *path);

/**
 * Register a DELETE route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_delete(SPIKARDAlefHandle owner,
                           char *(*callback)(void *, const char *),
                           void (*response_free)(char *), void *context,
                           const char *path);

/**
 * Register a HEAD route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_head(SPIKARDAlefHandle owner,
                         char *(*callback)(void *, const char *),
                         void (*response_free)(char *), void *context,
                         const char *path);

/**
 * Register an OPTIONS route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_options(SPIKARDAlefHandle owner,
                            char *(*callback)(void *, const char *),
                            void (*response_free)(char *), void *context,
                            const char *path);

/**
 * Register a CONNECT route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_connect(SPIKARDAlefHandle owner,
                            char *(*callback)(void *, const char *),
                            void (*response_free)(char *), void *context,
                            const char *path);

/**
 * Register a TRACE route at the given path.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - `callback` must be a valid function pointer that remains valid for the
 * lifetime of this service instance.
 * - `response_free` must release pointers returned by `callback` with the same
 *   allocator family and remain valid for the lifetime of this service
 * instance.
 * - `context` is an opaque pointer passed to the callback on each invocation.
 *   The caller is responsible for keeping it valid.
 * Returns 0 on success, non-zero error code on failure.
 */
int32_t spikard_app_trace(SPIKARDAlefHandle owner,
                          char *(*callback)(void *, const char *),
                          void (*response_free)(char *), void *context,
                          const char *path);

/**
 * Configure the service via 'config'.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - The same `owner` pointer is returned on success — the caller does **not**
 *   need to swap the handle they hold. Returns `null` on failure (the original
 *   handle is still valid in that case but should be inspected for usability).
 */
SPIKARDAlefHandle spikard_app_config(SPIKARDAlefHandle owner,
                                     SPIKARDAlefHandle config);

/**
 * Run the service entrypoint 'run'.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - The inner owner value is moved out by this call and the opaque shell is
 * left with `inner = None`. The caller may still invoke `()` afterwards to
 * release the shell; subsequent registration/configurator calls on the same
 * pointer will fail with the null-return error code.
 */
int32_t spikard_app_ep_run(SPIKARDAlefHandle owner);

/**
 * Run the service entrypoint 'into_router'.
 *
 * # Safety
 * - `owner` must be a valid pointer returned by `spikard_app_new()` and not yet
 * freed.
 * - The inner owner value is moved out by this call and the opaque shell is
 * left with `inner = None`. The caller may still invoke `()` afterwards to
 * release the shell; subsequent registration/configurator calls on the same
 * pointer will fail with the null-return error code.
 */
int32_t spikard_app_ep_into_router(SPIKARDAlefHandle owner);

#endif /* SPIKARD_H */
