// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fa1466d9b1e570cd419c00d580b253d3bc112ef6627624f2bc4c2b05d562fa1f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.spikard;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import org.jspecify.annotations.Nullable;

/**
 * Configuration for gRPC support
 *
 * Controls how the server handles gRPC requests, including compression,
 * timeouts, and protocol settings.
 *
 * # Stream Limits
 *
 * This configuration enforces message-level size limits but delegates
 * concurrent stream limiting to the HTTP/2 transport layer:
 *
 * - **Message Size Limits**: The {@code max_message_size} field is enforced per
 *   individual message (request or response) in both unary and streaming RPCs.
 *   When a single message exceeds this limit, the request is rejected with
 *   {@code PAYLOAD_TOO_LARGE} (HTTP 413).
 *
 * - **Concurrent Stream Limits**: The {@code max_concurrent_streams} is an advisory
 *   configuration passed to the HTTP/2 layer for connection-level stream
 *   negotiation. The HTTP/2 transport automatically enforces this limit and
 *   returns GOAWAY frames when exceeded. Applications should not rely on
 *   custom enforcement of this limit.
 *
 * - **Stream Response Size Limits**: The {@code max_stream_response_bytes} field caps the
 *   total encoded bytes emitted across a server-streaming or bidi-streaming response.
 *   When the cumulative size exceeds the limit, the stream is terminated with
 *   {@code tonic::Status::resource_exhausted}. Defaults to {@code None} (unbounded).
 */
@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GrpcConfigBuilder.class)
public record GrpcConfig(
    boolean enabled,
    @JsonProperty("max_message_size") long maxMessageSize,
    @JsonProperty("enable_compression") boolean enableCompression,
    @Nullable @JsonProperty("request_timeout") Long requestTimeout,
    @JsonProperty("max_concurrent_streams") int maxConcurrentStreams,
    @JsonProperty("enable_keepalive") boolean enableKeepalive,
    @JsonProperty("keepalive_interval") long keepaliveInterval,
    @JsonProperty("keepalive_timeout") long keepaliveTimeout,
    @Nullable @JsonProperty("max_stream_response_bytes") Long maxStreamResponseBytes
) {
    public static GrpcConfigBuilder builder() {
        return new GrpcConfigBuilder();
    }

    /**
     * Parse a {@code GrpcConfig} from a JSON string.
     *
     * @param json JSON serialisation matching the Rust-side field names (snake_case).
     * @throws SpikardRsException if the JSON cannot be deserialised.
     */
    public static GrpcConfig fromJson(String json) throws SpikardRsException {
        try {
            return new com.fasterxml.jackson.databind.ObjectMapper()
                .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
                .findAndRegisterModules()
                .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
                .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
                .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true)
                .readValue(json, GrpcConfig.class);
        } catch (Exception e) {
            throw new SpikardRsException("Failed to parse GrpcConfig from JSON: " + e.getMessage(), e);
        }
    }
}
