// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7af34a1d6da5f42d2667add5eb94cf3433a98632a55b7d515dcaa4331c41b3fb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.spikard;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonPOJOBuilder;
import org.jspecify.annotations.Nullable;

/**
 * Configuration for gRPC support
 *
 * Controls how the server handles gRPC requests, including compression,
 * timeouts, and protocol settings.
 *
 * # Stream Limits
 *
 * This configuration enforces message-level size limits but delegates
 * concurrent stream limiting to the HTTP/2 transport layer:
 *
 * - **Message Size Limits**: The {@code max_message_size} field is enforced per
 *   individual message (request or response) in both unary and streaming RPCs.
 *   When a single message exceeds this limit, the request is rejected with
 *   {@code PAYLOAD_TOO_LARGE} (HTTP 413).
 *
 * - **Concurrent Stream Limits**: The {@code max_concurrent_streams} is an advisory
 *   configuration passed to the HTTP/2 layer for connection-level stream
 *   negotiation. The HTTP/2 transport automatically enforces this limit and
 *   returns GOAWAY frames when exceeded. Applications should not rely on
 *   custom enforcement of this limit.
 *
 * - **Stream Response Size Limits**: The {@code max_stream_response_bytes} field caps the
 *   total encoded bytes emitted across a server-streaming or bidi-streaming response.
 *   When the cumulative size exceeds the limit, the stream is terminated with
 *   {@code tonic.Status.resource_exhausted}. Defaults to {@code None} (unbounded).
 */
@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = GrpcConfig.Builder.class)
public record GrpcConfig(
    /**
     * Enable gRPC support
     */
    @Nullable @JsonProperty("enabled") Boolean enabled,
    /**
     * Maximum message size in bytes (for both sending and receiving)
     *
     * This limit applies to individual messages in both unary and streaming RPCs.
     * When a single message exceeds this size, the request is rejected with HTTP 413
     * (Payload Too Large).
     *
     * Default: 4MB (4194304 bytes)
     *
     * # Note
     * This limit does NOT apply to the total response size in streaming RPCs.
     * For multi-message streams, the total response can exceed this limit as long
     * as each individual message stays within the limit.
     */
    @Nullable @JsonProperty("max_message_size") Long maxMessageSize,
    /**
     * Enable gzip compression for gRPC messages
     */
    @Nullable @JsonProperty("enable_compression") Boolean enableCompression,
    /**
     * Timeout for gRPC requests in seconds (null = no timeout)
     */
    @Nullable @JsonProperty("request_timeout") Long requestTimeout,
    /**
     * Maximum number of concurrent streams per connection (HTTP/2 advisory)
     *
     * This value is communicated to HTTP/2 clients as the server's flow control limit.
     * The HTTP/2 transport layer enforces this limit automatically via SETTINGS frames
     * and GOAWAY responses. Applications should NOT implement custom enforcement.
     *
     * Default: 100 streams per connection
     *
     * # Stream Limiting Strategy
     * - **Per Connection**: This limit applies per HTTP/2 connection, not globally
     * - **Transport Enforcement**: HTTP/2 handles all stream limiting; applications
     *   need not implement custom checks
     * - **Streaming Requests**: In server streaming or bidi streaming, each logical
     *   RPC consumes one stream slot. Message ordering within a stream follows
     *   HTTP/2 frame ordering.
     */
    @Nullable @JsonProperty("max_concurrent_streams") Integer maxConcurrentStreams,
    /**
     * Enable HTTP/2 keepalive
     */
    @Nullable @JsonProperty("enable_keepalive") Boolean enableKeepalive,
    /**
     * HTTP/2 keepalive interval in seconds
     */
    @Nullable @JsonProperty("keepalive_interval") Long keepaliveInterval,
    /**
     * HTTP/2 keepalive timeout in seconds
     */
    @Nullable @JsonProperty("keepalive_timeout") Long keepaliveTimeout,
    /**
     * Total byte cap across an entire streaming response.
     *
     * When {@code Some(n)}, the streaming adapter aborts the stream with
     * {@code tonic.Status.resource_exhausted} once the cumulative encoded message
     * bytes exceed {@code n}. The stream yields the error item and then terminates.
     *
     * Per-message cap remains {@code max_message_size}. This limit applies to
     * server-streaming and bidirectional-streaming RPCs only; unary RPCs are
     * governed solely by {@code max_message_size}.
     *
     * Default: {@code None} (unbounded total response size).
     */
    @Nullable @JsonProperty("max_stream_response_bytes") Long maxStreamResponseBytes
) {
    public static Builder builder() {
        return new Builder();
    }

    // CPD-OFF
    @JsonPOJOBuilder(withPrefix = "with", buildMethodName = "build")
    public static final class Builder {

private Boolean enabled = null;
        @JsonProperty("max_message_size")
private Long maxMessageSize = null;
        @JsonProperty("enable_compression")
private Boolean enableCompression = null;
        @JsonProperty("request_timeout")
private Long requestTimeout = null;
        @JsonProperty("max_concurrent_streams")
private Integer maxConcurrentStreams = null;
        @JsonProperty("enable_keepalive")
private Boolean enableKeepalive = null;
        @JsonProperty("keepalive_interval")
private Long keepaliveInterval = null;
        @JsonProperty("keepalive_timeout")
private Long keepaliveTimeout = null;
        @JsonProperty("max_stream_response_bytes")
private Long maxStreamResponseBytes = null;

        /** Sets the enabled field. */
        @JsonProperty("enabled")
        public Builder withEnabled(final @Nullable Boolean value) {
            this.enabled = value;
            return this;
        }

        /** Sets the maxMessageSize field. */
        @JsonProperty("max_message_size")
        public Builder withMaxMessageSize(final @Nullable Long value) {
            this.maxMessageSize = value;
            return this;
        }

        /** Sets the enableCompression field. */
        @JsonProperty("enable_compression")
        public Builder withEnableCompression(final @Nullable Boolean value) {
            this.enableCompression = value;
            return this;
        }

        /** Sets the requestTimeout field. */
        @JsonProperty("request_timeout")
        public Builder withRequestTimeout(final @Nullable Long value) {
            this.requestTimeout = value;
            return this;
        }

        /** Sets the maxConcurrentStreams field. */
        @JsonProperty("max_concurrent_streams")
        public Builder withMaxConcurrentStreams(final @Nullable Integer value) {
            this.maxConcurrentStreams = value;
            return this;
        }

        /** Sets the enableKeepalive field. */
        @JsonProperty("enable_keepalive")
        public Builder withEnableKeepalive(final @Nullable Boolean value) {
            this.enableKeepalive = value;
            return this;
        }

        /** Sets the keepaliveInterval field. */
        @JsonProperty("keepalive_interval")
        public Builder withKeepaliveInterval(final @Nullable Long value) {
            this.keepaliveInterval = value;
            return this;
        }

        /** Sets the keepaliveTimeout field. */
        @JsonProperty("keepalive_timeout")
        public Builder withKeepaliveTimeout(final @Nullable Long value) {
            this.keepaliveTimeout = value;
            return this;
        }

        /** Sets the maxStreamResponseBytes field. */
        @JsonProperty("max_stream_response_bytes")
        public Builder withMaxStreamResponseBytes(final @Nullable Long value) {
            this.maxStreamResponseBytes = value;
            return this;
        }

        /** Builds the GrpcConfig instance. */
        public GrpcConfig build() {
            return new GrpcConfig(
                enabled,
                maxMessageSize,
                enableCompression,
                requestTimeout,
                maxConcurrentStreams,
                enableKeepalive,
                keepaliveInterval,
                keepaliveTimeout,
                maxStreamResponseBytes
            );
        }
    }
    // CPD-ON
    public static GrpcConfig defaultInstance() {
        throw new UnsupportedOperationException("defaultInstance is not yet bridged via JNI; use the Builder instead.");
    }
}
