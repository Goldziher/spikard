// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d8b1643a4cd9a72da237fcc5d854980b8cd2cf2208633997c38c060e3ee88037
// To regenerate: alef generate
// To verify freshness: alef verify

package dev.spikard;

import java.lang.foreign.Arena;
import java.lang.foreign.MemorySegment;
@SuppressWarnings("PMD")
public final class SpikardRs {
    private SpikardRs() { }

    /**
     * Create a schema configuration with all three root types.
     *
     * This is a convenience function for fully-featured schemas.
     * {@literal @}return A {@code FullSchemaConfig} with default settings
     */
    public static FullSchemaConfig schemaFull() throws SpikardRsException {
        try (var arena = Arena.ofShared();
             var nativeResources = new NativeResources()) {
            var resultPtr = (MemorySegment) NativeLib.SPIKARD_SCHEMA_FULL.invoke();
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastError();                return null;            }
            // CPD-OFF
            nativeResources.register(resultPtr, handle -> NativeLib.SPIKARD_FULL_SCHEMA_CONFIG_FREE.invoke(handle));
            var jsonPtr = (MemorySegment) NativeLib.SPIKARD_FULL_SCHEMA_CONFIG_TO_JSON.invoke(resultPtr);
            if (jsonPtr.equals(MemorySegment.NULL)) {
                checkLastError();
                throw new SpikardRsException("schemaFull: failed to serialize response", null);
            }
            nativeResources.register(jsonPtr, handle -> NativeLib.SPIKARD_FREE_STRING.invoke(handle));
            String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
            return MAPPER.readValue(json, FullSchemaConfig.class);
            // CPD-ON
        } catch (SpikardRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new SpikardRsException("FFI call failed", e);
        }
    }

    /**
     * Create a schema configuration with Query and Mutation types.
     *
     * This is a convenience function for schemas with queries and mutations but no subscriptions.
     * {@literal @}return A {@code QueryMutationConfig} with default settings
     */
    public static QueryMutationConfig schemaQueryMutation() throws SpikardRsException {
        try (var arena = Arena.ofShared();
             var nativeResources = new NativeResources()) {
            var resultPtr = (MemorySegment) NativeLib.SPIKARD_SCHEMA_QUERY_MUTATION.invoke();
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastError();                return null;            }
            // CPD-OFF
            nativeResources.register(resultPtr, handle -> NativeLib.SPIKARD_QUERY_MUTATION_CONFIG_FREE.invoke(handle));
            var jsonPtr = (MemorySegment) NativeLib.SPIKARD_QUERY_MUTATION_CONFIG_TO_JSON.invoke(resultPtr);
            if (jsonPtr.equals(MemorySegment.NULL)) {
                checkLastError();
                throw new SpikardRsException("schemaQueryMutation: failed to serialize response", null);
            }
            nativeResources.register(jsonPtr, handle -> NativeLib.SPIKARD_FREE_STRING.invoke(handle));
            String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
            return MAPPER.readValue(json, QueryMutationConfig.class);
            // CPD-ON
        } catch (SpikardRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new SpikardRsException("FFI call failed", e);
        }
    }

    /**
     * Create a simple schema configuration with only Query type.
     *
     * This is a convenience function for schemas that only have queries.
     * {@literal @}return A {@code QueryOnlyConfig} with default settings
     */
    public static QueryOnlyConfig schemaQueryOnly() throws SpikardRsException {
        try (var arena = Arena.ofShared();
             var nativeResources = new NativeResources()) {
            var resultPtr = (MemorySegment) NativeLib.SPIKARD_SCHEMA_QUERY_ONLY.invoke();
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastError();                return null;            }
            // CPD-OFF
            nativeResources.register(resultPtr, handle -> NativeLib.SPIKARD_QUERY_ONLY_CONFIG_FREE.invoke(handle));
            var jsonPtr = (MemorySegment) NativeLib.SPIKARD_QUERY_ONLY_CONFIG_TO_JSON.invoke(resultPtr);
            if (jsonPtr.equals(MemorySegment.NULL)) {
                checkLastError();
                throw new SpikardRsException("schemaQueryOnly: failed to serialize response", null);
            }
            nativeResources.register(jsonPtr, handle -> NativeLib.SPIKARD_FREE_STRING.invoke(handle));
            String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
            return MAPPER.readValue(json, QueryOnlyConfig.class);
            // CPD-ON
        } catch (SpikardRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new SpikardRsException("FFI call failed", e);
        }
    }

    // Helper methods for FFI marshalling

    @FunctionalInterface
    private interface NativeReleaser {
        void release(MemorySegment handle) throws Throwable;
    }
    static final class NativeResources implements AutoCloseable {
        private static final class Entry {
            private final MemorySegment handle;
            private final NativeReleaser releaser;

            private Entry(MemorySegment handle, NativeReleaser releaser) {
                this.handle = handle;
                this.releaser = releaser;
            }
        }

        private final java.util.ArrayDeque<Entry> entries = new java.util.ArrayDeque<>();

        MemorySegment register(MemorySegment handle, NativeReleaser releaser) {
            if (handle != null && !handle.equals(MemorySegment.NULL)) {
                entries.push(new Entry(handle, releaser));
            }
            return handle;
        }

        MemorySegment transfer(MemorySegment handle) {
            var iterator = entries.iterator();
            while (iterator.hasNext()) {
                if (iterator.next().handle.equals(handle)) {
                    iterator.remove();
                    break;
                }
            }
            return handle;
        }

        @Override
        public void close() {
            Throwable failure = null;
            while (!entries.isEmpty()) {
                Entry entry = entries.pop();
                try {
                    entry.releaser.release(entry.handle);
                } catch (Throwable cleanupError) {
                    if (failure == null) {
                        failure = cleanupError;
                    } else {
                        failure.addSuppressed(cleanupError);
                    }
                }
            }
            if (failure != null) {
                throw new RuntimeException("Failed to release native resources", failure);
            }
        }
    }
    private static void checkLastError() throws Throwable {
        int errCode = (int)(long) NativeLib.SPIKARD_LAST_ERROR_CODE.invoke();
        if (errCode != 0) {
            var ctxPtr = (MemorySegment) NativeLib.SPIKARD_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL)
                ? "Native call failed with error code " + errCode
                : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            switch (errCode) {
                case 1 -> throw new ConversionErrorException(msg);
                case 2 -> throw new CoreErrorException(msg);
                case 3 -> throw new PanicException(msg);
                default -> throw new SpikardRsException(errCode, msg);
            }
        }
    }
    private static com.fasterxml.jackson.databind.ObjectMapper createObjectMapper() {
        var mapper = new com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
            .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.ALWAYS)
            .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
        var module = new com.fasterxml.jackson.databind.module.SimpleModule();
        module.addSerializer(byte[].class, new ByteArraySerializer());
        mapper.registerModule(module);
        return mapper;
    }

    private static final com.fasterxml.jackson.databind.ObjectMapper MAPPER = createObjectMapper();
}
