// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fb10df8cfbcaa07c1fc70cbeafbe016d2c5b9fabaa405aa5b5e1e85358d27b58
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.spikard;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.Optional;

/**
 * Configuration for GraphQL routes
 *
 * Provides a builder pattern for configuring GraphQL route parameters
 * for the Spikard HTTP server's routing system.
 */
public class GraphQLRouteConfig implements AutoCloseable {
    private final MemorySegment handle;

    GraphQLRouteConfig(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    /**
     * Set the HTTP path for the GraphQL endpoint
     * {@literal @}param path The URL path (e.g., "/graphql", "/api/graphql")
     */
    public GraphQLRouteConfig path(final String path) throws SpikardRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        try (var arena = Arena.ofShared()) {
            var cPath = arena.allocateFrom(path);
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_PATH.invoke(this.handle, cPath);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new SpikardRsException("path: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.SPIKARD_FREE_STRING.invoke(jsonPtr);
                return STREAM_MAPPER.readValue(json, GraphQLRouteConfig.class);
            } finally {
                NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_FREE.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("path: failed", e);
        }
    }
    /**
     * Set the HTTP method for the GraphQL endpoint
     * {@literal @}param method The HTTP method (typically "POST")
     */
    public GraphQLRouteConfig method(final String method) throws SpikardRsException {
        java.util.Objects.requireNonNull(method, "method must not be null");
        try (var arena = Arena.ofShared()) {
            var cMethod = arena.allocateFrom(method);
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_METHOD.invoke(this.handle, cMethod);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new SpikardRsException("method: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.SPIKARD_FREE_STRING.invoke(jsonPtr);
                return STREAM_MAPPER.readValue(json, GraphQLRouteConfig.class);
            } finally {
                NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_FREE.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("method: failed", e);
        }
    }
    /**
     * Enable or disable the GraphQL Playground UI
     * {@literal @}param enable Whether to enable playground
     */
    public GraphQLRouteConfig enablePlayground(final boolean enable) throws SpikardRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_ENABLE_PLAYGROUND.invoke(this.handle, enable);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new SpikardRsException("enablePlayground: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.SPIKARD_FREE_STRING.invoke(jsonPtr);
                return STREAM_MAPPER.readValue(json, GraphQLRouteConfig.class);
            } finally {
                NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_FREE.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("enablePlayground: failed", e);
        }
    }
    /**
     * Set a custom description for documentation
     * {@literal @}param description Documentation string
     */
    public GraphQLRouteConfig description(final String description) throws SpikardRsException {
        java.util.Objects.requireNonNull(description, "description must not be null");
        try (var arena = Arena.ofShared()) {
            var cDescription = arena.allocateFrom(description);
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_DESCRIPTION.invoke(this.handle, cDescription);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new SpikardRsException("description: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.SPIKARD_FREE_STRING.invoke(jsonPtr);
                return STREAM_MAPPER.readValue(json, GraphQLRouteConfig.class);
            } finally {
                NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_FREE.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("description: failed", e);
        }
    }
    /**
     * Get the configured path
     */
    public String getPath() throws SpikardRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_GET_PATH.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return result;
            } finally {
                NativeLib.SPIKARD_FREE_STRING.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("getPath: failed", e);
        }
    }
    /**
     * Get the configured method
     */
    public String getMethod() throws SpikardRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_GET_METHOD.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return result;
            } finally {
                NativeLib.SPIKARD_FREE_STRING.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("getMethod: failed", e);
        }
    }
    /**
     * Check if playground is enabled
     */
    public boolean isPlaygroundEnabled() throws SpikardRsException {
        try (var arena = Arena.ofShared()) {
            var result = (boolean) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_IS_PLAYGROUND_ENABLED.invoke(this.handle);
            return result;
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("isPlaygroundEnabled: failed", e);
        }
    }
    /**
     * Get the description if set
     */
    public Optional<String> getDescription() throws SpikardRsException {
        try (var arena = Arena.ofShared()) {
            MemorySegment resultPtr = (MemorySegment) NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_GET_DESCRIPTION.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                return java.util.Optional.empty();
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return java.util.Optional.of(result);
            } finally {
                NativeLib.SPIKARD_FREE_STRING.invoke(resultPtr);
            }
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("getDescription: failed", e);
        }
    }
    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.SPIKARD_GRAPH_QL_ROUTE_CONFIG_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free GraphQLRouteConfig: " + e.getMessage(), e);
            }
        }
    }

    private void checkLastFfiError() throws SpikardRsException {
        try {
            int code = (int) NativeLib.SPIKARD_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.SPIKARD_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new SpikardRsException(code, msg);
        } catch (Throwable e) {
            if (e instanceof SpikardRsException ex) { throw ex; }
            throw new SpikardRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = new ObjectMapper()
        .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
        .findAndRegisterModules()
        .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
        .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
        .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
}
