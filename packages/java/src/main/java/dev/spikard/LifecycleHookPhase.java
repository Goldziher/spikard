// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4701f20632d8e956819a265890e60a1f41f80f72162e6e58575e3a7e8a16bc50
// To regenerate: alef generate
// To verify freshness: alef verify
package dev.spikard;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * The five lifecycle phases a hook can be registered against.
 *
 * Used to resolve a per-route named hook selection ({@code LifecycleHooksConfig} in
 * {@code spikard-core.http}) against the hooks actually registered on the server: a name is looked
 * up within one specific phase's registered hooks, not across all five, so a hook registered
 * for {@code on_response} can never be silently picked up by a route asking for {@code on_request}.
 */
@SuppressWarnings("PMD")
public enum LifecycleHookPhase {
    OnRequest("OnRequest"),
    PreValidation("PreValidation"),
    PreHandler("PreHandler"),
    OnResponse("OnResponse"),
    OnError("OnError");


    /** The string value. */
    private final String value;

    LifecycleHookPhase(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static LifecycleHookPhase fromValue(final String value) {
        for (LifecycleHookPhase e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown LifecycleHookPhase value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
