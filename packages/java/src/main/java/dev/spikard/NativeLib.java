// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0a2720dc2aa69dee7188cda3cc501ce0045c76fb40d108f9e6521084ecb99b4b
// alef:handle-abi:1
// To regenerate: alef generate
// To verify freshness: alef verify
package dev.spikard;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.Linker;
import java.lang.foreign.SymbolLookup;
import java.lang.foreign.ValueLayout;
import java.lang.invoke.MethodHandle;
import java.io.File;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.StandardCopyOption;
import java.util.ArrayList;
import java.util.Enumeration;
import java.util.List;
import java.util.jar.JarEntry;
import java.util.jar.JarFile;

@SuppressWarnings({"PMD.AvoidCatchingGenericException", "PMD"})
final class NativeLib {
    private static final System.Logger LOGGER = System.getLogger(NativeLib.class.getName());
    private static final Linker LINKER = Linker.nativeLinker();
    private static SymbolLookup LIB;
    private static final String NATIVES_RESOURCE_ROOT = "/natives";
    private static final String LIBRARY_PATH_PROPERTY = "spikard_ffi.library.path";
    private static final String LIBRARY_DIRECTORY_PROPERTY = "spikard_ffi.library.dir";
    private static final String SKIP_BUNDLED_PROPERTY = "spikard_ffi.skipBundled";
    private static final String LIBRARY_PATH_ENV = "SPIKARD_FFI_LIB_PATH";
    private static final String LIBRARY_DIRECTORY_ENV = "SPIKARD_FFI_LIB_DIR";
    private static final String SKIP_BUNDLED_ENV = "SPIKARD_FFI_SKIP_BUNDLED";
    private static final String[] REQUIRED_SYMBOLS = {
        "spikard_api_key_auth_config_free",
        "spikard_api_key_auth_config_from_json",
        "spikard_api_key_config_free",
        "spikard_api_key_config_from_json",
        "spikard_app_config",
        "spikard_app_connect",
        "spikard_app_delete",
        "spikard_app_ep_into_router",
        "spikard_app_ep_run",
        "spikard_app_free",
        "spikard_app_get",
        "spikard_app_head",
        "spikard_app_new",
        "spikard_app_options",
        "spikard_app_patch",
        "spikard_app_post",
        "spikard_app_put",
        "spikard_app_register_route",
        "spikard_app_trace",
        "spikard_async_api_config_free",
        "spikard_async_api_config_from_json",
        "spikard_authorization_config_free",
        "spikard_authorization_config_from_json",
        "spikard_background_job_metadata_free",
        "spikard_background_job_metadata_from_json",
        "spikard_background_task_config_free",
        "spikard_background_task_config_from_json",
        "spikard_compression_config_free",
        "spikard_compression_config_from_json",
        "spikard_contact_info_free",
        "spikard_contact_info_from_json",
        "spikard_cors_config_free",
        "spikard_cors_config_from_json",
        "spikard_dynamic_schema_config_free",
        "spikard_dynamic_schema_config_from_json",
        "spikard_field_error_spec_free",
        "spikard_field_error_spec_from_json",
        "spikard_free_bytes",
        "spikard_free_string",
        "spikard_full_schema_config_free",
        "spikard_full_schema_config_from_json",
        "spikard_full_schema_config_to_json",
        "spikard_graph_ql_route_config_description",
        "spikard_graph_ql_route_config_enable_playground",
        "spikard_graph_ql_route_config_free",
        "spikard_graph_ql_route_config_get_description",
        "spikard_graph_ql_route_config_get_method",
        "spikard_graph_ql_route_config_get_path",
        "spikard_graph_ql_route_config_is_playground_enabled",
        "spikard_graph_ql_route_config_method",
        "spikard_graph_ql_route_config_new",
        "spikard_graph_ql_route_config_path",
        "spikard_grpc_config_free",
        "spikard_grpc_config_from_json",
        "spikard_handler_result_free",
        "spikard_json_rpc_config_free",
        "spikard_json_rpc_config_from_json",
        "spikard_json_rpc_method_info_free",
        "spikard_json_rpc_method_info_from_json",
        "spikard_jwt_auth_config_free",
        "spikard_jwt_auth_config_from_json",
        "spikard_jwt_config_free",
        "spikard_jwt_config_from_json",
        "spikard_last_error_code",
        "spikard_last_error_context",
        "spikard_license_info_free",
        "spikard_license_info_from_json",
        "spikard_lifecycle_hook_ref_free",
        "spikard_lifecycle_hook_ref_from_json",
        "spikard_lifecycle_hooks_config_free",
        "spikard_lifecycle_hooks_config_from_json",
        "spikard_open_api_config_free",
        "spikard_open_api_config_from_json",
        "spikard_parse_request_free",
        "spikard_parse_result_free",
        "spikard_parse_result_from_json",
        "spikard_parsed_channel_free",
        "spikard_parsed_channel_from_json",
        "spikard_parsed_message_free",
        "spikard_parsed_message_from_json",
        "spikard_parsed_operation_free",
        "spikard_parsed_operation_from_json",
        "spikard_problem_details_free",
        "spikard_problem_details_from_json",
        "spikard_query_mutation_config_free",
        "spikard_query_mutation_config_from_json",
        "spikard_query_mutation_config_to_json",
        "spikard_query_only_config_free",
        "spikard_query_only_config_from_json",
        "spikard_query_only_config_to_json",
        "spikard_rate_limit_config_free",
        "spikard_rate_limit_config_from_json",
        "spikard_request_data_free",
        "spikard_request_id_config_free",
        "spikard_request_id_config_from_json",
        "spikard_response_free",
        "spikard_response_from_json",
        "spikard_route_builder_api_key_auth",
        "spikard_route_builder_authorization",
        "spikard_route_builder_body_limit",
        "spikard_route_builder_compression",
        "spikard_route_builder_cors",
        "spikard_route_builder_file_params_json",
        "spikard_route_builder_free",
        "spikard_route_builder_handler_dependencies",
        "spikard_route_builder_handler_name",
        "spikard_route_builder_jsonrpc_method",
        "spikard_route_builder_jwt_auth",
        "spikard_route_builder_lifecycle_hooks",
        "spikard_route_builder_new",
        "spikard_route_builder_openrpc_spec",
        "spikard_route_builder_params_schema_json",
        "spikard_route_builder_rate_limit",
        "spikard_route_builder_request_id",
        "spikard_route_builder_request_schema_json",
        "spikard_route_builder_request_timeout",
        "spikard_route_builder_response_schema_json",
        "spikard_route_builder_sync",
        "spikard_schema_config_free",
        "spikard_schema_config_from_json",
        "spikard_schema_full",
        "spikard_schema_query_mutation",
        "spikard_schema_query_only",
        "spikard_server_config_free",
        "spikard_server_config_from_json",
        "spikard_server_info_free",
        "spikard_server_info_from_json",
        "spikard_sse_event_free",
        "spikard_sse_event_from_json",
        "spikard_static_files_config_free",
        "spikard_static_files_config_from_json",
        "spikard_testing_sse_event_free",
        "spikard_upload_file_free",
        "spikard_upload_file_from_json",
        "spikard_validate_request_free",
        "spikard_validation_response_free"
    };
    private static final Object NATIVE_EXTRACT_LOCK = new Object();
    private static String cachedExtractKey;
    private static Path cachedExtractDir;

    static {
        Path loadedLibraryPath = loadNativeLibrary();
        try {
            Arena arena = Arena.ofShared();
            // Try the loaded library path (for System.load() with absolute path)
            if (loadedLibraryPath != null) {
                try {
                    LIB = SymbolLookup.libraryLookup(loadedLibraryPath, arena);
                } catch (RuntimeException | Error inner) {
                    // If Path variant fails, fallback to defaultLookup
                    LIB = LINKER.defaultLookup();
                }
            } else {
                LIB = LINKER.defaultLookup();
            }
            validateRequiredSymbols(loadedLibraryPath);
        } catch (RuntimeException | Error e) {
            ExceptionInInitializerError error = new ExceptionInInitializerError(
                "Failed to initialize library symbols: " + e.getMessage());
            error.initCause(e);
            throw error;
        }
    }

    private static Path loadNativeLibrary() {
        Path loadedLibraryPath = null;
        String osName = System.getProperty("os.name", "").toLowerCase(java.util.Locale.ROOT);
        String osArch = System.getProperty("os.arch", "").toLowerCase(java.util.Locale.ROOT);

        String libName;
        String libExt;
        if (osName.contains("mac") || osName.contains("darwin")) {
            libName = "libspikard_ffi";
            libExt = ".dylib";
        } else if (osName.contains("win")) {
            libName = "spikard_ffi";
            libExt = ".dll";
        } else {
            libName = "libspikard_ffi";
            libExt = ".so";
        }

        String nativesRid = resolveNativesRid(osName, osArch);
        String nativesDir = NATIVES_RESOURCE_ROOT + "/" + nativesRid;

        String explicitPath = firstNonBlank(System.getProperty(LIBRARY_PATH_PROPERTY), System.getenv(LIBRARY_PATH_ENV));
        if (explicitPath != null) {
            return loadAbsolutePath(Paths.get(explicitPath));
        }

        String explicitDirectory = firstNonBlank(
                System.getProperty(LIBRARY_DIRECTORY_PROPERTY),
                System.getenv(LIBRARY_DIRECTORY_ENV));
        if (explicitDirectory != null) {
            return loadAbsolutePath(Paths.get(explicitDirectory, libName + libExt));
        }

        String skipBundledValue = firstNonBlank(
                System.getProperty(SKIP_BUNDLED_PROPERTY),
                System.getenv(SKIP_BUNDLED_ENV));
        if (!Boolean.parseBoolean(skipBundledValue)) {
            Path extracted = tryExtractAndLoadFromResources(nativesDir, libName, libExt);
            if (extracted != null) {
                return extracted;
            }
        }

        try {
            System.loadLibrary("spikard_ffi");
            // Find the full path by searching java.library.path
            Path libPath = findLoadedLibraryPath(libName, libExt);
            if (libPath != null) {
                loadedLibraryPath = libPath;
            }
        } catch (UnsatisfiedLinkError e) {
            String msg = "Failed to load spikard_ffi native library. Expected resource: " + nativesDir + "/" + libName
                    + libExt + " (RID: " + nativesRid + "). "
                    + "Ensure the library is bundled in the JAR under natives/{os-arch}/, "
                    + "place it on java.library.path, set -D" + LIBRARY_PATH_PROPERTY
                    + "=/absolute/path, or set " + LIBRARY_PATH_ENV + ".";
            UnsatisfiedLinkError out = new UnsatisfiedLinkError(msg + " Original error: " + e.getMessage());
            out.initCause(e);
            throw out;
        }
        return loadedLibraryPath;
    }

    private static String firstNonBlank(String preferred, String fallback) {
        if (preferred != null && !preferred.isBlank()) {
            return preferred;
        }
        if (fallback != null && !fallback.isBlank()) {
            return fallback;
        }
        return null;
    }

    private static Path loadAbsolutePath(Path configuredPath) {
        Path absolutePath = configuredPath.toAbsolutePath().normalize();
        if (!Files.isRegularFile(absolutePath)) {
            throw new UnsatisfiedLinkError("Configured native library does not exist: " + absolutePath);
        }
        System.load(absolutePath.toString());
        return absolutePath;
    }

    private static void validateRequiredSymbols(Path loadedLibraryPath) {
        List<String> missing = new ArrayList<>();
        for (String symbol : REQUIRED_SYMBOLS) {
            if (LIB.find(symbol).isEmpty() && LIB.find("_" + symbol).isEmpty()) {
                missing.add(symbol);
            }
        }
        if (missing.isEmpty()) {
            return;
        }

        int exportedCount = REQUIRED_SYMBOLS.length - missing.size();
        String loadedFrom = loadedLibraryPath == null
                ? "System.loadLibrary(spikard_ffi) / java.library.path"
                : loadedLibraryPath.toString();
        throw new UnsatisfiedLinkError(
                "The loaded spikard_ffi native library is stale relative to this binding: it exports "
                        + exportedCount + " of " + REQUIRED_SYMBOLS.length + " required symbols. Missing: "
                        + String.join(", ", missing) + ". Loaded from: " + loadedFrom
                        + ". Rebuild the native library, point this binding at a current one with -D"
                        + LIBRARY_PATH_PROPERTY + "=/absolute/path, or bypass bundled resources with -D"
                        + SKIP_BUNDLED_PROPERTY + "=true.");
    }

    private static Path tryExtractAndLoadFromResources(String nativesDir, String libName, String libExt) {
        String resourcePath = nativesDir + "/" + libName + libExt;
        URL resource = NativeLib.class.getResource(resourcePath);
        if (resource == null) {
            return null;
        }

        try {
            Path tempDir = extractOrReuseNativeDirectory(nativesDir);
            Path libPath = tempDir.resolve(libName + libExt);
            if (!Files.exists(libPath)) {
                throw new UnsatisfiedLinkError("Missing extracted native library: " + libPath);
            }
            Path absPath = libPath.toAbsolutePath();
            System.load(absPath.toString());
            return absPath;
        } catch (Exception | Error e) {
            LOGGER.log(System.Logger.Level.WARNING, "Failed to extract and load native library from resources", e);
            return null;
        }
    }

    private static Path extractOrReuseNativeDirectory(String nativesDir) throws Exception {
        URL location = NativeLib.class.getProtectionDomain().getCodeSource().getLocation();
        if (location == null) {
            throw new IllegalStateException("Missing code source location for spikard_ffi JAR");
        }

        Path codePath = Path.of(location.toURI());
        String key = codePath.toAbsolutePath() + "::" + nativesDir;

        synchronized (NATIVE_EXTRACT_LOCK) {
            if (cachedExtractDir != null && key.equals(cachedExtractKey)) {
                return cachedExtractDir;
            }
            Path tempDir = Files.createTempDirectory("spikard_ffi_native");
            tempDir.toFile().deleteOnExit();
            List<Path> extracted = extractNativeDirectory(codePath, nativesDir, tempDir);
            if (extracted.isEmpty()) {
                throw new IllegalStateException("No native files extracted from resources dir: " + nativesDir);
            }
            cachedExtractKey = key;
            cachedExtractDir = tempDir;
            return tempDir;
        }
    }

    private static List<Path> extractNativeDirectory(Path codePath, String nativesDir, Path destDir) throws Exception {
        if (!Files.exists(destDir) || !Files.isDirectory(destDir)) {
            throw new IllegalArgumentException("Destination directory does not exist: " + destDir);
        }

        String prefix = nativesDir.startsWith("/") ? nativesDir.substring(1) : nativesDir;
        if (!prefix.endsWith("/")) {
            prefix = prefix + "/";
        }

        if (Files.isDirectory(codePath)) {
            Path nativesPath = codePath.resolve(prefix);
            if (!Files.exists(nativesPath) || !Files.isDirectory(nativesPath)) {
                return List.of();
            }
            return copyDirectory(nativesPath, destDir);
        }

        List<Path> extracted = new ArrayList<>();
        try (JarFile jar = new JarFile(codePath.toFile())) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (!name.startsWith(prefix) || entry.isDirectory()) {
                    continue;
                }
                String relative = name.substring(prefix.length());
                Path out = safeResolve(destDir, relative);
                Files.createDirectories(out.getParent());
                try (var in = jar.getInputStream(entry)) {
                    Files.copy(in, out, StandardCopyOption.REPLACE_EXISTING);
                }
                out.toFile().deleteOnExit();
                extracted.add(out);
            }
        }
        return extracted;
    }

    private static List<Path> copyDirectory(Path srcDir, Path destDir) throws Exception {
        List<Path> copied = new ArrayList<>();
        try (var paths = Files.walk(srcDir)) {
            for (Path src : (Iterable<Path>) paths::iterator) {
                if (Files.isDirectory(src)) {
                    continue;
                }
                Path relative = srcDir.relativize(src);
                Path out = safeResolve(destDir, relative.toString());
                Files.createDirectories(out.getParent());
                Files.copy(src, out, StandardCopyOption.REPLACE_EXISTING);
                out.toFile().deleteOnExit();
                copied.add(out);
            }
        }
        return copied;
    }

    private static Path safeResolve(Path destDir, String relative) throws Exception {
        Path normalizedDest = destDir.toAbsolutePath().normalize();
        Path out = normalizedDest.resolve(relative).normalize();
        if (!out.startsWith(normalizedDest)) {
            throw new SecurityException("Blocked extracting native file outside destination directory: " + relative);
        }
        return out;
    }

    private static String resolveNativesRid(String osName, String osArch) {
        // Classifier names match go_java_platform(): `linux-aarch64`, `macos-arm64`,
        // `windows-x86_64`, etc. macOS distinguishes arm64 (ARM-based), while other
        // platforms use aarch64. Align with src/publish/platform.rs::go_java_platform().
        boolean isMac = osName.contains("mac") || osName.contains("darwin");
        boolean isWindows = osName.contains("win");

        String arch;
        if (osArch.contains("aarch64") || osArch.contains("arm64")) {
            // macOS uses "arm64" for aarch64; Linux uses "aarch64"
            arch = isMac ? "arm64" : "aarch64";
        } else if (osArch.contains("x86_64") || osArch.contains("amd64")) {
            arch = "x86_64";
        } else {
            arch = osArch.replaceAll("[^a-z0-9_]+", "");
        }

        String os;
        if (isMac) {
            os = "macos";
        } else if (isWindows) {
            os = "windows";
        } else {
            os = "linux";
        }

        return os + "-" + arch;
    }

    private static Path findLoadedLibraryPath(String fullLibName, String libExt) {
        // Search java.library.path for the library file
        String javaLibPath = System.getProperty("java.library.path");
        if (javaLibPath != null) {
            for (String path : javaLibPath.split(File.pathSeparator)) {
                Path libPath = Paths.get(path, fullLibName + libExt);
                if (Files.exists(libPath)) {
                    try {
                        return libPath.toRealPath();
                    } catch (java.io.IOException e) {
                        return libPath.toAbsolutePath();
                    }
                }
            }
        }
        // Library not found in java.library.path
        return null;
    }


    static final MethodHandle SPIKARD_SCHEMA_FULL = LINKER.downcallHandle(
        LIB.find("spikard_schema_full")
            .or(() -> LIB.find("_spikard_schema_full"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_schema_full"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_schema_full"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_schema_full" +
                " (tried: spikard_schema_full, _spikard_schema_full). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SCHEMA_QUERY_MUTATION = LINKER.downcallHandle(
        LIB.find("spikard_schema_query_mutation")
            .or(() -> LIB.find("_spikard_schema_query_mutation"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_schema_query_mutation"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_schema_query_mutation"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_schema_query_mutation" +
                " (tried: spikard_schema_query_mutation, _spikard_schema_query_mutation). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SCHEMA_QUERY_ONLY = LINKER.downcallHandle(
        LIB.find("spikard_schema_query_only")
            .or(() -> LIB.find("_spikard_schema_query_only"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_schema_query_only"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_schema_query_only"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_schema_query_only" +
                " (tried: spikard_schema_query_only, _spikard_schema_query_only). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FREE_STRING = LINKER.downcallHandle(
        LIB.find("spikard_free_string")
            .or(() -> LIB.find("_spikard_free_string"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_free_string"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_free_string"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FREE_BYTES = LINKER.downcallHandle(
        LIB.find("spikard_free_bytes")
            .or(() -> LIB.find("_spikard_free_bytes"))  // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_free_bytes"))  // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_free_bytes"))  // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_NEW = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_new")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_new" +
                " (tried: spikard_graph_ql_route_config_new, _spikard_graph_ql_route_config_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_PATH = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_path")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_path"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_path"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_path"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_path" +
                " (tried: spikard_graph_ql_route_config_path, _spikard_graph_ql_route_config_path). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_METHOD = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_method")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_method"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_method"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_method"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_method" +
                " (tried: spikard_graph_ql_route_config_method, _spikard_graph_ql_route_config_method). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_ENABLE_PLAYGROUND = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_enable_playground")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_enable_playground"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_enable_playground"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_enable_playground"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_enable_playground" +
                " (tried: spikard_graph_ql_route_config_enable_playground, _spikard_graph_ql_route_config_enable_playground). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_DESCRIPTION = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_description")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_description"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_description"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_description"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_description" +
                " (tried: spikard_graph_ql_route_config_description, _spikard_graph_ql_route_config_description). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_GET_PATH = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_get_path")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_get_path"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_get_path"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_get_path"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_get_path" +
                " (tried: spikard_graph_ql_route_config_get_path, _spikard_graph_ql_route_config_get_path). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_GET_METHOD = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_get_method")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_get_method"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_get_method"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_get_method"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_get_method" +
                " (tried: spikard_graph_ql_route_config_get_method, _spikard_graph_ql_route_config_get_method). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_IS_PLAYGROUND_ENABLED = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_is_playground_enabled")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_is_playground_enabled"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_is_playground_enabled"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_is_playground_enabled"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_is_playground_enabled" +
                " (tried: spikard_graph_ql_route_config_is_playground_enabled, _spikard_graph_ql_route_config_is_playground_enabled). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_GET_DESCRIPTION = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_get_description")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_get_description"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_get_description"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_get_description"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_graph_ql_route_config_get_description" +
                " (tried: spikard_graph_ql_route_config_get_description, _spikard_graph_ql_route_config_get_description). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_NEW = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_new")
            .or(() -> LIB.find("_spikard_route_builder_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_new" +
                " (tried: spikard_route_builder_new, _spikard_route_builder_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_HANDLER_NAME = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_handler_name")
            .or(() -> LIB.find("_spikard_route_builder_handler_name"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_handler_name"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_handler_name"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_handler_name" +
                " (tried: spikard_route_builder_handler_name, _spikard_route_builder_handler_name). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_REQUEST_SCHEMA_JSON = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_request_schema_json")
            .or(() -> LIB.find("_spikard_route_builder_request_schema_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_request_schema_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_request_schema_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_request_schema_json" +
                " (tried: spikard_route_builder_request_schema_json, _spikard_route_builder_request_schema_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_RESPONSE_SCHEMA_JSON = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_response_schema_json")
            .or(() -> LIB.find("_spikard_route_builder_response_schema_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_response_schema_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_response_schema_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_response_schema_json" +
                " (tried: spikard_route_builder_response_schema_json, _spikard_route_builder_response_schema_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_PARAMS_SCHEMA_JSON = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_params_schema_json")
            .or(() -> LIB.find("_spikard_route_builder_params_schema_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_params_schema_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_params_schema_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_params_schema_json" +
                " (tried: spikard_route_builder_params_schema_json, _spikard_route_builder_params_schema_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_FILE_PARAMS_JSON = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_file_params_json")
            .or(() -> LIB.find("_spikard_route_builder_file_params_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_file_params_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_file_params_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_file_params_json" +
                " (tried: spikard_route_builder_file_params_json, _spikard_route_builder_file_params_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_CORS = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_cors")
            .or(() -> LIB.find("_spikard_route_builder_cors"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_cors"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_cors"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_cors" +
                " (tried: spikard_route_builder_cors, _spikard_route_builder_cors). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_COMPRESSION = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_compression")
            .or(() -> LIB.find("_spikard_route_builder_compression"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_compression"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_compression"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_compression" +
                " (tried: spikard_route_builder_compression, _spikard_route_builder_compression). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_BODY_LIMIT = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_body_limit")
            .or(() -> LIB.find("_spikard_route_builder_body_limit"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_body_limit"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_body_limit"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_body_limit" +
                " (tried: spikard_route_builder_body_limit, _spikard_route_builder_body_limit). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_REQUEST_TIMEOUT = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_request_timeout")
            .or(() -> LIB.find("_spikard_route_builder_request_timeout"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_request_timeout"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_request_timeout"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_request_timeout" +
                " (tried: spikard_route_builder_request_timeout, _spikard_route_builder_request_timeout). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_RATE_LIMIT = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_rate_limit")
            .or(() -> LIB.find("_spikard_route_builder_rate_limit"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_rate_limit"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_rate_limit"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_rate_limit" +
                " (tried: spikard_route_builder_rate_limit, _spikard_route_builder_rate_limit). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_REQUEST_ID = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_request_id")
            .or(() -> LIB.find("_spikard_route_builder_request_id"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_request_id"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_request_id"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_request_id" +
                " (tried: spikard_route_builder_request_id, _spikard_route_builder_request_id). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_JWT_AUTH = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_jwt_auth")
            .or(() -> LIB.find("_spikard_route_builder_jwt_auth"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_jwt_auth"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_jwt_auth"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_jwt_auth" +
                " (tried: spikard_route_builder_jwt_auth, _spikard_route_builder_jwt_auth). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_API_KEY_AUTH = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_api_key_auth")
            .or(() -> LIB.find("_spikard_route_builder_api_key_auth"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_api_key_auth"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_api_key_auth"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_api_key_auth" +
                " (tried: spikard_route_builder_api_key_auth, _spikard_route_builder_api_key_auth). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_AUTHORIZATION = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_authorization")
            .or(() -> LIB.find("_spikard_route_builder_authorization"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_authorization"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_authorization"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_authorization" +
                " (tried: spikard_route_builder_authorization, _spikard_route_builder_authorization). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_LIFECYCLE_HOOKS = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_lifecycle_hooks")
            .or(() -> LIB.find("_spikard_route_builder_lifecycle_hooks"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_lifecycle_hooks"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_lifecycle_hooks"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_lifecycle_hooks" +
                " (tried: spikard_route_builder_lifecycle_hooks, _spikard_route_builder_lifecycle_hooks). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_JSONRPC_METHOD = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_jsonrpc_method")
            .or(() -> LIB.find("_spikard_route_builder_jsonrpc_method"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_jsonrpc_method"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_jsonrpc_method"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_jsonrpc_method" +
                " (tried: spikard_route_builder_jsonrpc_method, _spikard_route_builder_jsonrpc_method). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_OPENRPC_SPEC = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_openrpc_spec")
            .or(() -> LIB.find("_spikard_route_builder_openrpc_spec"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_openrpc_spec"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_openrpc_spec"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_openrpc_spec" +
                " (tried: spikard_route_builder_openrpc_spec, _spikard_route_builder_openrpc_spec). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_SYNC = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_sync")
            .or(() -> LIB.find("_spikard_route_builder_sync"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_sync"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_sync"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_sync" +
                " (tried: spikard_route_builder_sync, _spikard_route_builder_sync). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_HANDLER_DEPENDENCIES = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_handler_dependencies")
            .or(() -> LIB.find("_spikard_route_builder_handler_dependencies"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_handler_dependencies"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_handler_dependencies"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "spikard_route_builder_handler_dependencies" +
                " (tried: spikard_route_builder_handler_dependencies, _spikard_route_builder_handler_dependencies). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LAST_ERROR_CODE = LINKER.downcallHandle(
        LIB.find("spikard_last_error_code").orElse(null),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );

    static final MethodHandle SPIKARD_LAST_ERROR_CONTEXT = LINKER.downcallHandle(
        LIB.find("spikard_last_error_context").orElse(null),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_API_KEY_AUTH_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_api_key_auth_config_from_json")
            .or(() -> LIB.find("_spikard_api_key_auth_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_api_key_auth_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_api_key_auth_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_API_KEY_AUTH_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_api_key_auth_config_free")
            .or(() -> LIB.find("_spikard_api_key_auth_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_api_key_auth_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_api_key_auth_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_API_KEY_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_api_key_config_from_json")
            .or(() -> LIB.find("_spikard_api_key_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_api_key_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_api_key_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_API_KEY_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_api_key_config_free")
            .or(() -> LIB.find("_spikard_api_key_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_api_key_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_api_key_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ASYNC_API_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_async_api_config_from_json")
            .or(() -> LIB.find("_spikard_async_api_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_async_api_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_async_api_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ASYNC_API_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_async_api_config_free")
            .or(() -> LIB.find("_spikard_async_api_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_async_api_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_async_api_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_AUTHORIZATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_authorization_config_from_json")
            .or(() -> LIB.find("_spikard_authorization_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_authorization_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_authorization_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_AUTHORIZATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_authorization_config_free")
            .or(() -> LIB.find("_spikard_authorization_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_authorization_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_authorization_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_BACKGROUND_JOB_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_background_job_metadata_from_json")
            .or(() -> LIB.find("_spikard_background_job_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_background_job_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_background_job_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_BACKGROUND_JOB_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("spikard_background_job_metadata_free")
            .or(() -> LIB.find("_spikard_background_job_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_background_job_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_background_job_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_BACKGROUND_TASK_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_background_task_config_from_json")
            .or(() -> LIB.find("_spikard_background_task_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_background_task_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_background_task_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_BACKGROUND_TASK_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_background_task_config_free")
            .or(() -> LIB.find("_spikard_background_task_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_background_task_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_background_task_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_COMPRESSION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_compression_config_from_json")
            .or(() -> LIB.find("_spikard_compression_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_compression_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_compression_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_COMPRESSION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_compression_config_free")
            .or(() -> LIB.find("_spikard_compression_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_compression_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_compression_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_CONTACT_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_contact_info_from_json")
            .or(() -> LIB.find("_spikard_contact_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_contact_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_contact_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_CONTACT_INFO_FREE = LINKER.downcallHandle(
        LIB.find("spikard_contact_info_free")
            .or(() -> LIB.find("_spikard_contact_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_contact_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_contact_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_CORS_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_cors_config_from_json")
            .or(() -> LIB.find("_spikard_cors_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_cors_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_cors_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_CORS_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_cors_config_free")
            .or(() -> LIB.find("_spikard_cors_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_cors_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_cors_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_DYNAMIC_SCHEMA_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_dynamic_schema_config_from_json")
            .or(() -> LIB.find("_spikard_dynamic_schema_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_dynamic_schema_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_dynamic_schema_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_DYNAMIC_SCHEMA_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_dynamic_schema_config_free")
            .or(() -> LIB.find("_spikard_dynamic_schema_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_dynamic_schema_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_dynamic_schema_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FIELD_ERROR_SPEC_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_field_error_spec_from_json")
            .or(() -> LIB.find("_spikard_field_error_spec_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_field_error_spec_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_field_error_spec_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FIELD_ERROR_SPEC_FREE = LINKER.downcallHandle(
        LIB.find("spikard_field_error_spec_free")
            .or(() -> LIB.find("_spikard_field_error_spec_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_field_error_spec_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_field_error_spec_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FULL_SCHEMA_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_full_schema_config_from_json")
            .or(() -> LIB.find("_spikard_full_schema_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_full_schema_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_full_schema_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FULL_SCHEMA_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_full_schema_config_free")
            .or(() -> LIB.find("_spikard_full_schema_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_full_schema_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_full_schema_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRAPH_QL_ROUTE_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_graph_ql_route_config_free")
            .or(() -> LIB.find("_spikard_graph_ql_route_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_graph_ql_route_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_graph_ql_route_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRPC_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_grpc_config_from_json")
            .or(() -> LIB.find("_spikard_grpc_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_grpc_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_grpc_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_GRPC_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_grpc_config_free")
            .or(() -> LIB.find("_spikard_grpc_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_grpc_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_grpc_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_HANDLER_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("spikard_handler_result_free")
            .or(() -> LIB.find("_spikard_handler_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_handler_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_handler_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JSON_RPC_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_json_rpc_config_from_json")
            .or(() -> LIB.find("_spikard_json_rpc_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_json_rpc_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_json_rpc_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JSON_RPC_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_json_rpc_config_free")
            .or(() -> LIB.find("_spikard_json_rpc_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_json_rpc_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_json_rpc_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JSON_RPC_METHOD_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_json_rpc_method_info_from_json")
            .or(() -> LIB.find("_spikard_json_rpc_method_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_json_rpc_method_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_json_rpc_method_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JSON_RPC_METHOD_INFO_FREE = LINKER.downcallHandle(
        LIB.find("spikard_json_rpc_method_info_free")
            .or(() -> LIB.find("_spikard_json_rpc_method_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_json_rpc_method_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_json_rpc_method_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JWT_AUTH_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_jwt_auth_config_from_json")
            .or(() -> LIB.find("_spikard_jwt_auth_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_jwt_auth_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_jwt_auth_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JWT_AUTH_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_jwt_auth_config_free")
            .or(() -> LIB.find("_spikard_jwt_auth_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_jwt_auth_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_jwt_auth_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JWT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_jwt_config_from_json")
            .or(() -> LIB.find("_spikard_jwt_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_jwt_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_jwt_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_JWT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_jwt_config_free")
            .or(() -> LIB.find("_spikard_jwt_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_jwt_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_jwt_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LICENSE_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_license_info_from_json")
            .or(() -> LIB.find("_spikard_license_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_license_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_license_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LICENSE_INFO_FREE = LINKER.downcallHandle(
        LIB.find("spikard_license_info_free")
            .or(() -> LIB.find("_spikard_license_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_license_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_license_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LIFECYCLE_HOOK_REF_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_lifecycle_hook_ref_from_json")
            .or(() -> LIB.find("_spikard_lifecycle_hook_ref_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_lifecycle_hook_ref_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_lifecycle_hook_ref_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LIFECYCLE_HOOK_REF_FREE = LINKER.downcallHandle(
        LIB.find("spikard_lifecycle_hook_ref_free")
            .or(() -> LIB.find("_spikard_lifecycle_hook_ref_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_lifecycle_hook_ref_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_lifecycle_hook_ref_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LIFECYCLE_HOOKS_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_lifecycle_hooks_config_from_json")
            .or(() -> LIB.find("_spikard_lifecycle_hooks_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_lifecycle_hooks_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_lifecycle_hooks_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_LIFECYCLE_HOOKS_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_lifecycle_hooks_config_free")
            .or(() -> LIB.find("_spikard_lifecycle_hooks_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_lifecycle_hooks_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_lifecycle_hooks_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_OPEN_API_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_open_api_config_from_json")
            .or(() -> LIB.find("_spikard_open_api_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_open_api_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_open_api_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_OPEN_API_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_open_api_config_free")
            .or(() -> LIB.find("_spikard_open_api_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_open_api_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_open_api_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSE_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("spikard_parse_request_free")
            .or(() -> LIB.find("_spikard_parse_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parse_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parse_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSE_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_parse_result_from_json")
            .or(() -> LIB.find("_spikard_parse_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parse_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parse_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSE_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("spikard_parse_result_free")
            .or(() -> LIB.find("_spikard_parse_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parse_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parse_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSED_CHANNEL_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_parsed_channel_from_json")
            .or(() -> LIB.find("_spikard_parsed_channel_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parsed_channel_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parsed_channel_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSED_CHANNEL_FREE = LINKER.downcallHandle(
        LIB.find("spikard_parsed_channel_free")
            .or(() -> LIB.find("_spikard_parsed_channel_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parsed_channel_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parsed_channel_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSED_MESSAGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_parsed_message_from_json")
            .or(() -> LIB.find("_spikard_parsed_message_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parsed_message_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parsed_message_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSED_MESSAGE_FREE = LINKER.downcallHandle(
        LIB.find("spikard_parsed_message_free")
            .or(() -> LIB.find("_spikard_parsed_message_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parsed_message_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parsed_message_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSED_OPERATION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_parsed_operation_from_json")
            .or(() -> LIB.find("_spikard_parsed_operation_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parsed_operation_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parsed_operation_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PARSED_OPERATION_FREE = LINKER.downcallHandle(
        LIB.find("spikard_parsed_operation_free")
            .or(() -> LIB.find("_spikard_parsed_operation_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_parsed_operation_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_parsed_operation_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PROBLEM_DETAILS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_problem_details_from_json")
            .or(() -> LIB.find("_spikard_problem_details_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_problem_details_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_problem_details_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_PROBLEM_DETAILS_FREE = LINKER.downcallHandle(
        LIB.find("spikard_problem_details_free")
            .or(() -> LIB.find("_spikard_problem_details_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_problem_details_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_problem_details_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_QUERY_MUTATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_query_mutation_config_from_json")
            .or(() -> LIB.find("_spikard_query_mutation_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_query_mutation_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_query_mutation_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_QUERY_MUTATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_query_mutation_config_free")
            .or(() -> LIB.find("_spikard_query_mutation_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_query_mutation_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_query_mutation_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_QUERY_ONLY_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_query_only_config_from_json")
            .or(() -> LIB.find("_spikard_query_only_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_query_only_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_query_only_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_QUERY_ONLY_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_query_only_config_free")
            .or(() -> LIB.find("_spikard_query_only_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_query_only_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_query_only_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_RATE_LIMIT_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_rate_limit_config_from_json")
            .or(() -> LIB.find("_spikard_rate_limit_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_rate_limit_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_rate_limit_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_RATE_LIMIT_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_rate_limit_config_free")
            .or(() -> LIB.find("_spikard_rate_limit_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_rate_limit_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_rate_limit_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_REQUEST_DATA_FREE = LINKER.downcallHandle(
        LIB.find("spikard_request_data_free")
            .or(() -> LIB.find("_spikard_request_data_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_request_data_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_request_data_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_REQUEST_ID_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_request_id_config_from_json")
            .or(() -> LIB.find("_spikard_request_id_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_request_id_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_request_id_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_REQUEST_ID_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_request_id_config_free")
            .or(() -> LIB.find("_spikard_request_id_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_request_id_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_request_id_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_RESPONSE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_response_from_json")
            .or(() -> LIB.find("_spikard_response_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_response_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_response_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("spikard_response_free")
            .or(() -> LIB.find("_spikard_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_ROUTE_BUILDER_FREE = LINKER.downcallHandle(
        LIB.find("spikard_route_builder_free")
            .or(() -> LIB.find("_spikard_route_builder_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_route_builder_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_route_builder_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SCHEMA_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_schema_config_from_json")
            .or(() -> LIB.find("_spikard_schema_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_schema_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_schema_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SCHEMA_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_schema_config_free")
            .or(() -> LIB.find("_spikard_schema_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_schema_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_schema_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SERVER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_server_config_from_json")
            .or(() -> LIB.find("_spikard_server_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_server_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_server_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SERVER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_server_config_free")
            .or(() -> LIB.find("_spikard_server_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_server_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_server_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SERVER_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_server_info_from_json")
            .or(() -> LIB.find("_spikard_server_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_server_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_server_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SERVER_INFO_FREE = LINKER.downcallHandle(
        LIB.find("spikard_server_info_free")
            .or(() -> LIB.find("_spikard_server_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_server_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_server_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SSE_EVENT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_sse_event_from_json")
            .or(() -> LIB.find("_spikard_sse_event_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_sse_event_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_sse_event_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_SSE_EVENT_FREE = LINKER.downcallHandle(
        LIB.find("spikard_sse_event_free")
            .or(() -> LIB.find("_spikard_sse_event_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_sse_event_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_sse_event_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_STATIC_FILES_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_static_files_config_from_json")
            .or(() -> LIB.find("_spikard_static_files_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_static_files_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_static_files_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_STATIC_FILES_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("spikard_static_files_config_free")
            .or(() -> LIB.find("_spikard_static_files_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_static_files_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_static_files_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_TESTING_SSE_EVENT_FREE = LINKER.downcallHandle(
        LIB.find("spikard_testing_sse_event_free")
            .or(() -> LIB.find("_spikard_testing_sse_event_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_testing_sse_event_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_testing_sse_event_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_UPLOAD_FILE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("spikard_upload_file_from_json")
            .or(() -> LIB.find("_spikard_upload_file_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_upload_file_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_upload_file_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_UPLOAD_FILE_FREE = LINKER.downcallHandle(
        LIB.find("spikard_upload_file_free")
            .or(() -> LIB.find("_spikard_upload_file_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_upload_file_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_upload_file_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_VALIDATE_REQUEST_FREE = LINKER.downcallHandle(
        LIB.find("spikard_validate_request_free")
            .or(() -> LIB.find("_spikard_validate_request_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_validate_request_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_validate_request_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_VALIDATION_RESPONSE_FREE = LINKER.downcallHandle(
        LIB.find("spikard_validation_response_free")
            .or(() -> LIB.find("_spikard_validation_response_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("spikard_validation_response_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_spikard_validation_response_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle SPIKARD_FULL_SCHEMA_CONFIG_TO_JSON = LIB.find("spikard_full_schema_config_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle SPIKARD_QUERY_MUTATION_CONFIG_TO_JSON = LIB.find("spikard_query_mutation_config_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle SPIKARD_QUERY_ONLY_CONFIG_TO_JSON = LIB.find("spikard_query_only_config_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


}
