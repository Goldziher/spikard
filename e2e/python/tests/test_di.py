# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: di."""

import pytest


def test_async_factory_dependency_success(client) -> None:
    """Tests async factory that creates database pool asynchronously."""
    response = client.get("/api/db-status")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"max_size": 10, "pool_status": "connected"}  # noqa: S101


def test_circular_dependency_detection_error(client) -> None:
    """Tests that circular dependencies (A depends on B, B depends on A) are detected and rejected at registration time."""
    response = client.get("/api/circular")
    assert response.status_code == 500  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "Circular dependency detected",
        "errors": [
            {
                "cycle": ["service_a", "service_b", "service_a"],
                "msg": "Circular dependency detected in dependency graph",
                "type": "circular_dependency",
            }
        ],
        "status": 500,
        "title": "Dependency Resolution Failed",
        "type": "https://spikard.dev/errors/dependency-error",
    }  # noqa: S101


def test_dependency_injection_in_lifecycle_hooks_success(client) -> None:
    """Tests accessing dependencies in lifecycle hooks (onRequest, preHandler) for auth, logging, etc."""
    response = client.get(
        "/api/hook-di-test",
        headers={
            "authorization": "Bearer valid_token",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"authenticated": True, "logged": True}  # noqa: S101
    assert response.headers["x-auth-mode"] == "strict"  # noqa: S101
    assert response.headers["x-log-level"] == "debug"  # noqa: S101


def test_factory_dependency_success(client) -> None:
    """Tests factory dependency that creates instances on-demand."""
    response = client.get("/api/timestamp")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"timestamp": "<<present>>"}  # noqa: S101


def test_missing_dependency_error(client) -> None:
    """Tests error when handler requires a dependency that was never registered."""
    response = client.get("/api/missing-dep")
    assert response.status_code == 500  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "Required dependency not found",
        "errors": [
            {
                "dependency_key": "non_existent_service",
                "msg": "Dependency 'non_existent_service' is not registered",
                "type": "missing_dependency",
            }
        ],
        "status": 500,
        "title": "Dependency Resolution Failed",
        "type": "https://spikard.dev/errors/dependency-error",
    }  # noqa: S101


def test_mixed_singleton_and_per_request_caching_success(client) -> None:
    """Tests mixing singleton dependencies (shared across requests) with per-request dependencies (cached within request)."""
    response = client.get("/api/mixed-caching")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"app_name": "MyApp", "context_id": "<<uuid>>", "pool_id": "<<uuid>>"}  # noqa: S101


def test_multiple_dependencies_with_cleanup_success(client) -> None:
    """Tests cleanup for multiple dependencies with generator pattern, ensuring all are cleaned up in reverse resolution order."""
    response = client.get("/api/multi-cleanup-test")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"session_active": True}  # noqa: S101


def test_nested_dependencies_3_levels_success(client) -> None:
    """Tests dependency resolution where auth depends on cache and db, which both depend on config."""
    response = client.get("/api/auth-status")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"auth_enabled": True, "has_cache": True, "has_db": True}  # noqa: S101


def test_node_js_object_destructuring_injection_success(client) -> None:
    """Tests Node.js/TypeScript-specific object destructuring pattern for dependency injection."""
    response = client.get("/api/node-destructure")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"db_name": "PostgreSQL", "log_level": "info"}  # noqa: S101


def test_per_request_dependency_caching_success(client) -> None:
    """Tests per-request caching where dependency is created once per request but shared by multiple usages within that request."""
    response = client.get("/api/request-id")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"first_id": "<<uuid>>", "second_id": "<<same_as:first_id>>"}  # noqa: S101


def test_python_parameter_name_based_injection_success(client) -> None:
    """Tests Python-specific parameter name-based dependency injection where dependencies are matched by parameter names."""
    response = client.get("/api/python-name-inject")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"cache_status": "ready", "db_status": "connected"}  # noqa: S101


def test_python_type_annotation_based_injection_success(client) -> None:
    """Tests Python-specific type annotation-based dependency injection where dependencies are matched by type hints."""
    response = client.get("/api/python-type-inject")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"cache_type": "Redis", "pool_type": "PostgreSQL"}  # noqa: S101


def test_resource_cleanup_after_request_success(client) -> None:
    """Tests generator pattern cleanup where dependency resources are cleaned up after handler completes."""
    response = client.get("/api/cleanup-test")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"session_id": "<<uuid>>", "status": "completed"}  # noqa: S101


def test_route_level_dependency_override_success(client) -> None:
    """Tests route-level dependency override of app-level dependency for testing or special cases."""
    response = client.get("/api/override-test")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"mode": "test", "strict": False}  # noqa: S101


def test_ruby_keyword_argument_injection_success(client) -> None:
    """Tests Ruby-specific keyword argument pattern for dependency injection."""
    response = client.get("/api/ruby-kwargs")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"adapter": "postgresql", "user_id": 42}  # noqa: S101


def test_singleton_dependency_caching_success(client) -> None:
    """Tests singleton dependency that is created once and shared across all requests."""
    response = client.get("/api/app-counter")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"count": 1, "counter_id": "<<uuid>>"}  # noqa: S101


def test_type_mismatch_in_dependency_resolution_error(client) -> None:
    """Tests error when handler expects a different type than what the dependency provides."""
    response = client.get("/api/type-mismatch")
    assert response.status_code == 500  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "Dependency type mismatch",
        "errors": [
            {
                "actual_type": "string",
                "dependency_key": "config",
                "expected_type": "object",
                "msg": "Dependency 'config' type mismatch: expected object, got string",
                "type": "type_mismatch",
            }
        ],
        "status": 500,
        "title": "Dependency Resolution Failed",
        "type": "https://spikard.dev/errors/dependency-error",
    }  # noqa: S101


def test_value_dependency_injection_success(client) -> None:
    """Tests simple value injection (config, constants) into a handler."""
    response = client.get("/api/config")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"app_name": "SpikardApp", "max_connections": 100, "version": "1.0.0"}  # noqa: S101
