# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c710538c5fecde3735ae46357d269034d1a39f3b2ff8a5838ed648fcd7bb29b6
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: cors."""

import pytest  # noqa: F401


def test__cors_preflight_method_not_allowed(mock_server: str) -> None:
    """CORS preflight request for non-allowed method should be rejected."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/06_cors_preflight_method_not_allowed"
    _headers = {
        "Access-Control-Request-Method": "DELETE",
        "Access-Control-Request-Headers": "Content-Type",
        "Origin": "https://example.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="OPTIONS")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
    assert status_code == 403  # noqa: S101


def test__cors_preflight_header_not_allowed(mock_server: str) -> None:
    """CORS preflight request with non-allowed header should be rejected."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/07_cors_preflight_header_not_allowed"
    _headers = {
        "Origin": "https://example.com",
        "Access-Control-Request-Method": "POST",
        "Access-Control-Request-Headers": "X-Custom-Header",
    }
    _req = urllib.request.Request(url, headers=_headers, method="OPTIONS")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
    assert status_code == 403  # noqa: S101


def test__cors_max_age(mock_server: str) -> None:
    """CORS preflight response should include Access-Control-Max-Age."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/08_cors_max_age"
    _headers = {
        "Origin": "https://example.com",
        "Access-Control-Request-Method": "POST",
        "Access-Control-Request-Headers": "Content-Type",
    }
    _req = urllib.request.Request(url, headers=_headers, method="OPTIONS")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_headers = dict(response.headers)
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_headers = dict(_exc.headers)
    assert status_code == 204  # noqa: S101
    assert resp_headers["access-control-allow-headers"] == "Content-Type"  # noqa: S101
    assert resp_headers["access-control-allow-methods"] == "POST"  # noqa: S101
    assert resp_headers["access-control-allow-origin"] == "https://example.com"  # noqa: S101
    assert resp_headers["access-control-max-age"] == "3600"  # noqa: S101


def test__cors_expose_headers(mock_server: str) -> None:
    """CORS response should include Access-Control-Expose-Headers for custom headers."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/09_cors_expose_headers"
    _headers = {
        "Origin": "https://example.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="GET")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_headers = dict(response.headers)
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_headers = dict(_exc.headers)
    assert status_code == 200  # noqa: S101
    assert resp_headers["access-control-allow-origin"] == "https://example.com"  # noqa: S101
    assert (
        resp_headers["access-control-expose-headers"] == "X-Total-Count, X-Request-Id"
    )  # noqa: S101
    assert resp_headers["x-total-count"] == "42"  # noqa: S101
    assert resp_headers["x-request-id"] == "abc123"  # noqa: S101


def test__cors_origin_null(mock_server: str) -> None:
    """CORS request with 'null' origin should be handled according to policy."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/10_cors_origin_null"
    _headers = {
        "Origin": "null",
    }
    _req = urllib.request.Request(url, headers=_headers, method="GET")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 403  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"error": "Origin 'null' is not allowed"}  # noqa: S101


def test_cors_allow_credentials_flag(mock_server: str) -> None:
    """Tests CORS response with credentials flag when allowed."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/cors_allow_credentials_flag"
    _headers = {
        "Origin": "https://example.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="GET")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
        resp_headers = dict(response.headers)
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
        resp_headers = dict(_exc.headers)
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"message": "Success"}  # noqa: S101
    assert resp_headers["access-control-allow-credentials"] == "true"  # noqa: S101
    assert resp_headers["access-control-allow-origin"] == "https://example.com"  # noqa: S101


def test_cors_custom_allowed_headers_x_custom(mock_server: str) -> None:
    """Tests CORS allows custom X-Custom-Header in requests."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/cors_custom_allowed_headers_x_custom"
    _headers = {
        "Access-Control-Request-Method": "POST",
        "Access-Control-Request-Headers": "X-Custom-Header",
        "Origin": "https://example.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="OPTIONS")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_headers = dict(response.headers)
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_headers = dict(_exc.headers)
    assert status_code == 204  # noqa: S101
    assert resp_headers["access-control-allow-methods"] == "POST"  # noqa: S101
    assert resp_headers["access-control-allow-origin"] == "https://example.com"  # noqa: S101
    assert resp_headers["access-control-allow-headers"] == "X-Custom-Header"  # noqa: S101


def test_cors_request_blocked(mock_server: str) -> None:
    """Tests CORS request from disallowed origin."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/cors_request_blocked"
    _headers = {
        "Origin": "https://malicious-site.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="GET")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 403  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {
        "detail": "CORS request from origin 'https://malicious-site.com' not allowed"
    }  # noqa: S101


def test_cors_restricted_methods_post_get_only(mock_server: str) -> None:
    """Tests CORS allows only specific HTTP methods (POST, GET)."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/cors_restricted_methods_post_get_only"
    _headers = {
        "Origin": "https://example.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="GET")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
        resp_headers = dict(response.headers)
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
        resp_headers = dict(_exc.headers)
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"message": "Success"}  # noqa: S101
    assert resp_headers["access-control-allow-methods"] == "GET,POST"  # noqa: S101


def test_cors_safelisted_headers_without_preflight(mock_server: str) -> None:
    """Tests that safelisted headers (Content-Type: text/plain, Accept, Accept-Language) don't require preflight."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/cors_safelisted_headers_without_preflight"
    _headers = {
        "Content-Type": "text/plain",
        "Accept": "application/json",
        "Accept-Language": "en-US",
        "Origin": "https://app.example.com",
    }
    _req = urllib.request.Request(url, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
        resp_headers = dict(response.headers)
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
        resp_headers = dict(_exc.headers)
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"message": "Success"}  # noqa: S101
    assert resp_headers["access-control-allow-origin"] == "https://app.example.com"  # noqa: S101
    assert resp_headers["vary"] == "Origin"  # noqa: S101
