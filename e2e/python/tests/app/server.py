#!/usr/bin/env python3
"""Auto-generated Spikard-Python benchmark server.

DO NOT EDIT - Generated by app-generator
"""

from typing import Any
from spikard import Spikard, delete, get, patch, post, put
from spikard.config import ServerConfig, JwtConfig, ApiKeyConfig

config = ServerConfig(
    jwt_auth=JwtConfig(
        secret="test-secret-key-do-not-use-in-production",
        algorithm="HS256",
        audience=["https://api.example.com"],
        issuer="https://auth.example.com",
    ),
    api_key_auth=ApiKeyConfig(keys=["sk_test_123456", "sk_test_789012"], header_name="X-API-Key"),
)

app = Spikard(config=config)


@get("/protected/user")
async def get_protected_user() -> dict[str, Any]:
    """Handler for GET /protected/user."""
    response = {}
    return response


@get("/api/data")
async def get_api_data() -> dict[str, Any]:
    """Handler for GET /api/data."""
    response = {}
    return response


if __name__ == "__main__":
    import sys

    port = int(sys.argv[1]) if len(sys.argv) > 1 else 8000
    print(f"Starting Spikard-Python server on port {port}", file=sys.stderr)
    app.run(host="0.0.0.0", port=port)
