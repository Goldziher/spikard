# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:26a1970bc828f516613e467717c7396775e873c50b451f349694a12b2cc0b5b2
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: upload."""

import pytest  # noqa: F401


def test_upload_file_as_bytes(mock_server: str) -> None:
    """Tests file upload read as byte array."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_as_bytes"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {"file": {"content": "binary data", "filename": "binary.bin"}}
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"as_bytes": True, "filename": "binary.bin", "size": 11}  # noqa: S101


def test_upload_file_basic_success(mock_server: str) -> None:
    """Tests basic file upload with single file."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_basic_success"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {"file": {"content": "Hello, World!", "filename": "test.txt"}}
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"filename": "test.txt", "size": 13}  # noqa: S101


def test_upload_file_content_type_or_default(mock_server: str) -> None:
    """Tests file upload uses provided content_type or defaults."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_content_type_or_default"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {"file": {"content": "Some content", "filename": "unknown.xyz"}}
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {
        "content_type": "application/octet-stream",
        "filename": "unknown.xyz",
    }  # noqa: S101


def test_upload_file_read_to_string(mock_server: str) -> None:
    """Tests file upload read as string."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_read_to_string"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {"file": {"content": "Hello, World!", "filename": "text.txt"}}
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {
        "as_string": True,
        "content": "Hello, World!",
        "filename": "text.txt",
    }  # noqa: S101


def test_upload_file_with_custom_content_type(mock_server: str) -> None:
    """Tests file upload preserves custom Content-Type."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_with_custom_content_type"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {
            "file": {
                "content": '{"key": "value"}',
                "content_type": "application/json",
                "filename": "data.json",
            }
        }
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {
        "content_type": "application/json",
        "filename": "data.json",
        "size": 16,
    }  # noqa: S101


def test_upload_file_with_form_fields(mock_server: str) -> None:
    """Tests file upload with additional form fields."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_with_form_fields"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {
            "description": "Important document",
            "file": {"content": "PDF content", "filename": "document.pdf"},
            "tags": ["important", "archive"],
        }
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {
        "description": "Important document",
        "filename": "document.pdf",
        "tags_count": 2,
    }  # noqa: S101


def test_upload_file_with_gzip_content_encoding(mock_server: str) -> None:
    """Tests file upload with gzip content encoding."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_file_with_gzip_content_encoding"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {
            "file": {
                "content": "compressed data",
                "content_encoding": "gzip",
                "filename": "data.gz",
            }
        }
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"content_encoding": "gzip", "filename": "data.gz"}  # noqa: S101


def test_upload_multiple_files(mock_server: str) -> None:
    """Tests uploading multiple files at once."""
    import os  # noqa: PLC0415
    import urllib.request  # noqa: PLC0415

    base = os.environ.get("MOCK_SERVER_URL", mock_server)
    url = f"{base}/fixtures/upload_multiple_files"
    import json  # noqa: PLC0415

    _headers = {
        "Content-Type": "multipart/form-data",
    }
    _headers.setdefault("Content-Type", "application/json")
    _body = json.dumps(
        {
            "files": [
                {"content": "Content 1", "filename": "file1.txt"},
                {"content": "Content 2", "filename": "file2.txt"},
            ]
        }
    ).encode()
    _req = urllib.request.Request(url, data=_body, headers=_headers, method="POST")
    try:
        response = urllib.request.urlopen(_req)  # noqa: S310
        status_code = response.status
        resp_body = response.read()
    except urllib.error.HTTPError as _exc:
        status_code = _exc.code
        resp_body = _exc.read()
    assert status_code == 200  # noqa: S101
    import json as _json  # noqa: PLC0415

    data = _json.loads(resp_body)
    assert data == {"total_size": 18, "uploaded_files": 2}  # noqa: S101
