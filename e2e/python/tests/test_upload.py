# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: upload."""
import pytest


def test_upload_file_as_bytes(client) -> None:
    """Tests file upload read as byte array."""
    response = client.post(
        "/upload/bytes",
        json={"file": {"content": "binary data", "filename": "binary.bin"}},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"as_bytes": True, "filename": "binary.bin", "size": 11}  # noqa: S101

def test_upload_file_basic_success(client) -> None:
    """Tests basic file upload with single file."""
    response = client.post(
        "/upload",
        json={"file": {"content": "Hello, World!", "filename": "test.txt"}},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"filename": "test.txt", "size": 13}  # noqa: S101

def test_upload_file_content_type_or_default(client) -> None:
    """Tests file upload uses provided content_type or defaults."""
    response = client.post(
        "/upload/auto-type",
        json={"file": {"content": "Some content", "filename": "unknown.xyz"}},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"content_type": "application/octet-stream", "filename": "unknown.xyz"}  # noqa: S101

def test_upload_file_read_to_string(client) -> None:
    """Tests file upload read as string."""
    response = client.post(
        "/upload/string",
        json={"file": {"content": "Hello, World!", "filename": "text.txt"}},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"as_string": True, "content": "Hello, World!", "filename": "text.txt"}  # noqa: S101

def test_upload_file_with_custom_content_type(client) -> None:
    """Tests file upload preserves custom Content-Type."""
    response = client.post(
        "/upload/typed",
        json={"file": {"content": '{"key": "value"}', "content_type": "application/json", "filename": "data.json"}},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"content_type": "application/json", "filename": "data.json", "size": 16}  # noqa: S101

def test_upload_file_with_form_fields(client) -> None:
    """Tests file upload with additional form fields."""
    response = client.post(
        "/upload/with-fields",
        json={"description": "Important document", "file": {"content": "PDF content", "filename": "document.pdf"}, "tags": ["important", "archive"]},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"description": "Important document", "filename": "document.pdf", "tags_count": 2}  # noqa: S101

def test_upload_file_with_gzip_content_encoding(client) -> None:
    """Tests file upload with gzip content encoding."""
    response = client.post(
        "/upload/compressed",
        json={"file": {"content": "compressed data", "content_encoding": "gzip", "filename": "data.gz"}},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"content_encoding": "gzip", "filename": "data.gz"}  # noqa: S101

def test_upload_multiple_files(client) -> None:
    """Tests uploading multiple files at once."""
    response = client.post(
        "/upload/multiple",
        json={"files": [{"content": "Content 1", "filename": "file1.txt"}, {"content": "Content 2", "filename": "file2.txt"}]},
        headers={
            "Content-Type": "multipart/form-data",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"total_size": 18, "uploaded_files": 2}  # noqa: S101

