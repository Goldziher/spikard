# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:f0e46eec48d3bee1b3b12fb732a97fe0212bf382ae6533c62a85800258fb8ae9
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: headers."""

import pytest  # noqa: F401


def test__bearer_token_format_valid(client) -> None:
    """Authorization header with valid Bearer token format should be accepted."""
    response = client.get(
        "/protected",
        headers={
            "Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIn0.dozjgNryP4J3jVmNHl0w5N_XgL0n3I9PlFUP0THsR8U",
        },
    )
    assert response.status_code == 200  # noqa: S101


def test__bearer_token_format_invalid(client) -> None:
    """Authorization header with invalid Bearer token format should fail validation."""
    response = client.get(
        "/protected",
        headers={
            "Authorization": "Bearer invalid token with spaces",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "ctx": {
                    "pattern": "^Bearer [A-Za-z0-9-._~+/]+=*$",
                    "value": "Bearer invalid token with spaces",
                },
                "loc": ["headers", "authorization"],
                "msg": "Invalid Bearer token format",
                "type": "validation_error",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test__bearer_token_missing_prefix(client) -> None:
    """Authorization header without Bearer prefix should fail validation."""
    response = client.get(
        "/protected",
        headers={
            "Authorization": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "ctx": {
                    "pattern": "^Bearer [A-Za-z0-9-._~+/]+=*$",
                    "value": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9",
                },
                "loc": ["headers", "authorization"],
                "msg": "Invalid Bearer token format",
                "type": "validation_error",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test__api_key_header_valid(client) -> None:
    """X-API-Key header with valid format should be accepted."""
    response = client.get(
        "/api/data",
        headers={
            "X-API-Key": "a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
        },
    )
    assert response.status_code == 200  # noqa: S101


def test__api_key_header_invalid(client) -> None:
    """X-API-Key header with invalid format should fail validation."""
    response = client.get(
        "/api/data",
        headers={
            "X-API-Key": "invalid-key",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "ctx": {"pattern": "^[a-f0-9]{32}$", "value": "invalid-key"},
                "loc": ["headers", "x-api-key"],
                "msg": "Invalid API key format",
                "type": "validation_error",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_accept_encoding_header(client) -> None:
    """Tests Accept-Encoding header for compression negotiation."""
    response = client.get(
        "/headers/accept-encoding",
        headers={
            "Accept-Encoding": "gzip, deflate, br",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"accept_encoding": "gzip, deflate, br"}  # noqa: S101


def test_accept_header_json(client) -> None:
    """Tests Accept header for content negotiation."""
    response = client.get(
        "/headers/accept",
        headers={
            "Accept": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"accept": "application/json"}  # noqa: S101


def test_accept_language_header(client) -> None:
    """Tests Accept-Language header for locale/i18n."""
    response = client.get(
        "/headers/accept-language",
        headers={
            "Accept-Language": "en-US,en;q=0.9",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"accept_language": "en-US,en;q=0.9"}  # noqa: S101


def test_authorization_header_missing(client) -> None:
    """Tests missing Authorization header returns 403."""
    response = client.get("/users/me")
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "input": None,
                "loc": ["headers", "authorization"],
                "msg": "Field required",
                "type": "missing",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_authorization_header_success(client) -> None:
    """Tests Authorization header with valid Digest scheme."""
    response = client.get(
        "/users/me",
        headers={
            "Authorization": "Digest foobar",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"credentials": "foobar", "scheme": "Digest"}  # noqa: S101


def test_authorization_header_wrong_scheme(client) -> None:
    """Tests Authorization header with incorrect scheme returns 403."""
    response = client.get(
        "/users/me",
        headers={
            "Authorization": "Other invalidauthorization",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "input": "Other invalidauthorization",
                "loc": ["headers", "authorization"],
                "msg": "String should match pattern '^Digest .+'",
                "type": "string_pattern_mismatch",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_basic_authentication_success(client) -> None:
    """Tests Authorization header with Basic auth scheme."""
    response = client.get(
        "/headers/basic-auth",
        headers={
            "Authorization": "Basic dXNlcm5hbWU6cGFzc3dvcmQ=",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"password": "password", "username": "username"}  # noqa: S101


def test_bearer_token_authentication_missing(client) -> None:
    """Tests missing Bearer token returns 401 Unauthorized."""
    response = client.get("/headers/bearer-auth")
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "input": None,
                "loc": ["headers", "authorization"],
                "msg": "Field required",
                "type": "missing",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_bearer_token_authentication_success(client) -> None:
    """Tests Authorization header with Bearer token scheme."""
    response = client.get(
        "/headers/bearer-auth",
        headers={
            "Authorization": "Bearer valid_token_123",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"token": "valid_token_123"}  # noqa: S101


def test_content_type_header_application_json(client) -> None:
    """Tests Content-Type header with JSON media type."""
    response = client.get(
        "/headers/content-type",
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"content_type": "application/json"}  # noqa: S101


def test_header_case_insensitivity_access(client) -> None:
    """Tests case-insensitive header access (Content-Type vs content-type)."""
    response = client.post(
        "/echo",
        json={"test": "data"},
        headers={
            "content-type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {
        "content_type_lower": "application/json",
        "content_type_mixed": "application/json",
        "content_type_upper": "application/json",
    }  # noqa: S101


def test_header_regex_validation_fail(client) -> None:
    """Tests header with regex pattern validation failure."""
    response = client.get(
        "/headers/pattern",
        headers={
            "X-Request-Id": "invalid-format",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "ctx": {"pattern": "^[0-9]{3,}$"},
                "input": "invalid-format",
                "loc": ["headers", "x-request-id"],
                "msg": "String should match pattern '^[0-9]{3,}$'",
                "type": "string_pattern_mismatch",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_header_regex_validation_success(client) -> None:
    """Tests header with regex pattern validation success."""
    response = client.get(
        "/headers/pattern",
        headers={
            "X-Request-Id": "12345",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"x_request_id": "12345"}  # noqa: S101


def test_header_validation_max_length_constraint_fail(client) -> None:
    """Tests header validation with max_length constraint failure."""
    response = client.get(
        "/headers/max-length",
        headers={
            "X-Session-Id": "this_is_way_too_long_for_validation",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "ctx": {"max_length": 20},
                "input": "this_is_way_too_long_for_validation",
                "loc": ["headers", "x-session-id"],
                "msg": "String should have at most 20 characters",
                "type": "string_too_long",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_header_validation_min_length_constraint(client) -> None:
    """Tests header validation with min_length constraint."""
    response = client.get(
        "/headers/validated",
        headers={
            "X-Token": "ab",
        },
    )
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "ctx": {"min_length": 3},
                "input": "ab",
                "loc": ["headers", "x-token"],
                "msg": "String should have at least 3 characters",
                "type": "string_too_short",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_header_with_underscore_conversion_explicit(client) -> None:
    """Tests X-Token header converted to x_token parameter."""
    response = client.get(
        "/headers/underscore",
        headers={
            "X-Token": "secret123",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"x_token": "secret123"}  # noqa: S101


def test_host_header(client) -> None:
    """Tests Host header (standard HTTP header)."""
    response = client.get(
        "/headers/host",
        headers={
            "Host": "example.com:8080",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"host": "example.com:8080"}  # noqa: S101


def test_multiple_custom_headers(client) -> None:
    """Tests multiple custom headers in single request."""
    response = client.get(
        "/headers/multiple",
        headers={
            "X-Request-Id": "req-12345",
            "X-Trace-Id": "trace-abc",
            "X-Client-Version": "1.2.3",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {
        "x_client_version": "1.2.3",
        "x_request_id": "req-12345",
        "x_trace_id": "trace-abc",
    }  # noqa: S101


def test_multiple_header_values_x_token(client) -> None:
    """Tests multiple values for same header name (X-Token)."""
    response = client.get(
        "/items/",
        headers={
            "x-token": "foo, bar",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"X-Token values": ["foo", "bar"]}  # noqa: S101


def test_optional_header_with_none_default_missing(client) -> None:
    """Tests optional header parameter with None default when not provided."""
    response = client.get("/items/")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"strange_header": None}  # noqa: S101


def test_origin_header(client) -> None:
    """Tests Origin header for CORS."""
    response = client.get(
        "/headers/origin",
        headers={
            "Origin": "https://example.com",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"origin": "https://example.com"}  # noqa: S101


def test_referer_header(client) -> None:
    """Tests Referer header (standard misspelling)."""
    response = client.get(
        "/headers/referer",
        headers={
            "Referer": "https://example.com/page",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"referer": "https://example.com/page"}  # noqa: S101


def test_user_agent_header_custom_value(client) -> None:
    """Tests User-Agent header with custom value."""
    response = client.get(
        "/items/",
        headers={
            "User-Agent": "Mozilla/5.0 Custom Browser",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"User-Agent": "Mozilla/5.0 Custom Browser"}  # noqa: S101


def test_user_agent_header_default_value(client) -> None:
    """Tests optional User-Agent header when not provided, uses testclient default."""
    response = client.get("/items/")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"User-Agent": "testclient"}  # noqa: S101


def test_x_api_key_optional_header_missing(client) -> None:
    """Tests optional X-API-Key header when not provided, returns fallback message."""
    response = client.get("/users/me")
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"msg": "Hello World"}  # noqa: S101


def test_x_api_key_optional_header_success(client) -> None:
    """Tests optional X-API-Key header with valid value."""
    response = client.get(
        "/users/me",
        headers={
            "key": "secret",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"msg": "Hello secret"}  # noqa: S101


def test_x_api_key_required_header_missing(client) -> None:
    """Tests required X-API-Key header when not provided, returns 403."""
    response = client.get("/users/me")
    assert response.status_code == 422  # noqa: S101
    data = response.json()
    assert data == {
        "detail": "1 validation error in request",
        "errors": [
            {
                "input": None,
                "loc": ["headers", "x-api-key"],
                "msg": "Field required",
                "type": "missing",
            }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error",
    }  # noqa: S101


def test_x_api_key_required_header_success(client) -> None:
    """Tests required X-API-Key header with valid value."""
    response = client.get(
        "/users/me",
        headers={
            "key": "secret",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"username": "secret"}  # noqa: S101
