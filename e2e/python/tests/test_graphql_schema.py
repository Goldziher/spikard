# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:5d0363dbfe97bc3a05c57ebf902516ef89af2b091fc249810668fc008f285cab
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
"""E2e tests for category: graphql_schema."""

import pytest  # noqa: F401


def test_graphql_authentication_error_variant(client) -> None:
    """Tests GraphQL authentication error when auth fails."""
    response = client.post(
        "/graphql",
        json={"query": "{ user { id } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 401  # noqa: S101
    data = response.json()
    assert data == {"error": "<<present>>"}  # noqa: S101


def test_graphql_authorization_error_variant(client) -> None:
    """Tests GraphQL authorization error when access denied."""
    response = client.post(
        "/graphql",
        json={"query": "{ admin { id } }"},
        headers={
            "Content-Type": "application/json",
            "Authorization": "Bearer invalid",
        },
    )
    assert response.status_code == 403  # noqa: S101
    data = response.json()
    assert data == {"error": "<<present>>"}  # noqa: S101


def test_graphql_complexity_limit_exceeded(client) -> None:
    """Tests GraphQL query complexity validation rejects overly complex queries."""
    response = client.post(
        "/graphql",
        json={
            "query": "{ user { id name email posts { id title comments { id text } } } }"
        },
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"errors": [{"message": "Query complexity limit exceeded"}]}  # noqa: S101


def test_graphql_custom_endpoint_configuration(client) -> None:
    """Tests GraphQL route configured at custom endpoint."""
    response = client.post(
        "/api/v2/query",
        json={"query": "{ user { id } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": {"user": {"id": "1"}}}  # noqa: S101


def test_graphql_depth_limit_exceeded(client) -> None:
    """Tests GraphQL query depth validation rejects deeply nested queries."""
    response = client.post(
        "/graphql",
        json={
            "query": "{ user { posts { comments { author { posts { title } } } } } }"
        },
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"errors": [{"message": "Query depth limit exceeded"}]}  # noqa: S101


def test_graphql_error_response_format(client) -> None:
    """Tests GraphQL error response format with correct structure."""
    response = client.post(
        "/graphql",
        json={"query": "{ invalidField }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {
        "errors": [
            {
                "extensions": {"code": "GRAPHQL_VALIDATION_FAILED"},
                "message": "Cannot query field 'invalidField' on type 'Query'",
            }
        ]
    }  # noqa: S101


def test_graphql_execution_error_variant(client) -> None:
    """Tests GraphQL execution error response."""
    response = client.post(
        "/graphql",
        json={"query": "{ invalidField }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"errors": [{"message": "<<present>>"}]}  # noqa: S101


def test_graphql_full_schema_with_subscriptions(client) -> None:
    """Tests GraphQL schema with queries, mutations, and subscriptions."""
    response = client.post(
        "/graphql",
        json={"query": "{ __typename }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": {"__typename": "Query"}}  # noqa: S101


def test_graphql_introspection_disabled(client) -> None:
    """Tests GraphQL introspection returns error when disabled."""
    response = client.post(
        "/graphql",
        json={"query": "{ __schema { types { name } } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"errors": [{"message": "GraphQL introspection is disabled"}]}  # noqa: S101


def test_graphql_introspection_enabled(client) -> None:
    """Tests GraphQL introspection is available."""
    response = client.post(
        "/graphql",
        json={"query": "{ __schema { types { name } } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": {"__schema": {"types": None}}}  # noqa: S101


def test_graphql_invalid_input_error_variant(client) -> None:
    """Tests GraphQL invalid input error."""
    response = client.post(
        "/graphql",
        json={"query": "mutation { createUser(age: -5) { id } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"errors": [{"message": "<<present>>"}]}  # noqa: S101


def test_graphql_not_found_error_variant(client) -> None:
    """Tests GraphQL not found error for missing resource."""
    response = client.post(
        "/graphql",
        json={"query": '{ user(id: "nonexistent") { id } }'},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"errors": [{"message": "<<present>>"}]}  # noqa: S101


def test_graphql_query_and_mutation_schema(client) -> None:
    """Tests GraphQL schema with queries and mutations."""
    response = client.post(
        "/graphql",
        json={"query": 'mutation { createUser(name: "New") { id name } }'},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": {"createUser": {"id": "2", "name": "New"}}}  # noqa: S101


def test_graphql_query_only_schema(client) -> None:
    """Tests GraphQL schema configured with queries only."""
    response = client.post(
        "/graphql",
        json={"query": "{ user { id name } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": {"user": {"id": "1", "name": "Test User"}}}  # noqa: S101


def test_graphql_rate_limit_error_variant(client) -> None:
    """Tests GraphQL rate limit exceeded error."""
    response = client.post(
        "/graphql",
        json={"query": "{ user { id } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": "<<present>>"}  # noqa: S101


def test_graphql_schema_build_error_variant(client) -> None:
    """Tests GraphQL schema build error."""
    response = client.post(
        "/graphql",
        json={"query": "{ test }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 500  # noqa: S101
    data = response.json()
    assert data == {"error": "<<present>>"}  # noqa: S101


def test_graphql_serialization_error_variant(client) -> None:
    """Tests GraphQL serialization error for invalid response."""
    response = client.post(
        "/graphql",
        json={"query": "{ user { id } }"},
        headers={
            "Content-Type": "application/json",
        },
    )
    assert response.status_code == 200  # noqa: S101
    data = response.json()
    assert data == {"data": "<<present>>"}  # noqa: S101
