# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: sse."""
import pytest


def test_sse_event_retry_5000_milliseconds(client) -> None:
    """Tests SSE event with 5000ms retry interval."""
    response = client.get(
        "/events/retry-5s",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_event_with_all_fields(client) -> None:
    """Tests SSE event with all optional fields (id, type, retry)."""
    response = client.get(
        "/events/complete",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_event_with_data_and_metadata(client) -> None:
    """Tests SSE event with JSON data and metadata fields."""
    response = client.get(
        "/events/data",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_event_with_id_field(client) -> None:
    """Tests SSE event includes unique ID field."""
    response = client.get(
        "/events/with-id",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_event_with_retry_field(client) -> None:
    """Tests SSE event includes retry interval field."""
    response = client.get(
        "/events/with-retry",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_event_with_type_field(client) -> None:
    """Tests SSE event includes event type field."""
    response = client.get(
        "/events/typed",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_multiple_events_stream(client) -> None:
    """Tests SSE stream with multiple consecutive events."""
    response = client.get(
        "/events/multi",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

def test_sse_simple_event_stream(client) -> None:
    """Tests Server-Sent Events with simple text events."""
    response = client.get(
        "/events",
        headers={
            "Accept": "text/event-stream",
        },
    )
    assert response.status_code == 200  # noqa: S101
    assert response.headers["cache-control"] == "no-cache"  # noqa: S101
    assert response.headers["content-type"] == "text/event-stream"  # noqa: S101

