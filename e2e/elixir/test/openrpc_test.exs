# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:7c7ee205ed67da6580e7ab6a13c5d310daa2bf4570de73c097d26e8a90e383d0
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: openrpc
defmodule E2e.OpenrpcTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "openrpc_batch_mixed_valid_invalid" do
    test "POST /rpc - Tests batch request where some calls are valid and some reference undefined methods" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_batch_mixed_valid_invalid", json: [%{"id" => 1, "jsonrpc" => "2.0", "method" => "add", "params" => %{"a" => 5, "b" => 5}}, %{"id" => 2, "jsonrpc" => "2.0", "method" => "nonexistent", "params" => %{}}], headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == [%{"id" => 1, "jsonrpc" => "2.0", "result" => 10}, %{"error" => %{"code" => -32601, "message" => "Method not found"}, "id" => 2, "jsonrpc" => "2.0"}]
    end
  end

  describe "openrpc_batch_request_all_valid" do
    test "POST /rpc - Tests batch request against OpenRPC-defined methods where all calls are valid" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_batch_request_all_valid", json: [%{"id" => 1, "jsonrpc" => "2.0", "method" => "add", "params" => %{"a" => 1, "b" => 2}}, %{"id" => 2, "jsonrpc" => "2.0", "method" => "add", "params" => %{"a" => 10, "b" => 20}}], headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == [%{"id" => 1, "jsonrpc" => "2.0", "result" => 3}, %{"id" => 2, "jsonrpc" => "2.0", "result" => 30}]
    end
  end

  describe "openrpc_error_mapping_per_spec" do
    test "POST /rpc - Tests that application errors are mapped to the error codes defined in the OpenRPC spec errors array" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_error_mapping_per_spec", json: %{"id" => 7, "jsonrpc" => "2.0", "method" => "resource.get", "params" => %{"id" => 99999}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"error" => %{"code" => -32000, "message" => "Resource not found"}, "id" => 7, "jsonrpc" => "2.0"}
    end
  end

  describe "openrpc_method_handler_basic" do
    test "POST /rpc - Tests calling a method defined in an OpenRPC spec that takes typed params and returns a typed result" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_method_handler_basic", json: %{"id" => 1, "jsonrpc" => "2.0", "method" => "add", "params" => %{"a" => 10, "b" => 5}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 1, "jsonrpc" => "2.0", "result" => 15}
    end
  end

  describe "openrpc_method_namespacing" do
    test "POST /rpc - Tests method with dot-namespaced name such as account.create is dispatched correctly" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_method_namespacing", json: %{"id" => 1, "jsonrpc" => "2.0", "method" => "account.create", "params" => %{"email" => "alice@example.com", "username" => "alice"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 1, "jsonrpc" => "2.0", "result" => %{"id" => 1, "username" => "alice"}}
    end
  end

  describe "openrpc_multi_method_spec" do
    test "POST /rpc - Tests that a spec with multiple methods routes correctly to each registered handler" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_multi_method_spec", json: %{"id" => 10, "jsonrpc" => "2.0", "method" => "echo", "params" => %{"message" => "hello world"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 10, "jsonrpc" => "2.0", "result" => "hello world"}
    end
  end

  describe "openrpc_optional_param_omitted" do
    test "POST /rpc - Tests that a method call omitting an optional parameter succeeds using its default behaviour" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_optional_param_omitted", json: %{"id" => 5, "jsonrpc" => "2.0", "method" => "search", "params" => %{"query" => "spikard"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 5, "jsonrpc" => "2.0", "result" => %{"items" => [], "total" => 0}}
    end
  end

  describe "openrpc_param_schema_ref_resolution" do
    test "POST /rpc - Tests that parameter schemas referenced via $ref in OpenRPC components/schemas are resolved" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_param_schema_ref_resolution", json: %{"id" => 8, "jsonrpc" => "2.0", "method" => "address.validate", "params" => %{"address" => %{"city" => "Springfield", "country" => "US", "street" => "123 Main St"}}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 8, "jsonrpc" => "2.0", "result" => true}
    end
  end

  describe "openrpc_param_validation_missing_required" do
    test "POST /rpc - Tests that a missing required parameter is rejected per the OpenRPC method param schema" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_param_validation_missing_required", json: %{"id" => 3, "jsonrpc" => "2.0", "method" => "user.create", "params" => %{"name" => "Alice"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"error" => %{"code" => -32602, "message" => "Invalid params"}, "id" => 3, "jsonrpc" => "2.0"}
    end
  end

  describe "openrpc_param_validation_wrong_type" do
    test "POST /rpc - Tests that a parameter with wrong type is rejected per the OpenRPC method param schema" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/openrpc_param_validation_wrong_type", json: %{"id" => 2, "jsonrpc" => "2.0", "method" => "multiply", "params" => %{"factor" => "not_a_number"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"error" => %{"code" => -32602, "message" => "Invalid params"}, "id" => 2, "jsonrpc" => "2.0"}
    end
  end
end
