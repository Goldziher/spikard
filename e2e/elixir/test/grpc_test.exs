# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:73303505764dea065bbb66505f9931304c9ede70802389e3f5dea1de08253a4f
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: grpc
defmodule E2e.GrpcTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "grpc_call_with_metadata" do
    test "Tests gRPC call with request metadata headers" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_call_with_metadata", finch: AlefE2EFinch, json: %{"id" => "resource-1"}, headers: [{"Content-Type", "application/grpc"}, {"authorization", "Bearer token123"}, {"te", "trailers"}, {"x-custom-metadata", "value123"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-response-metadata", do: List.first(List.wrap(v)) end) == "processed"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => "result", "id" => "resource-1"}
    end
  end

  describe "grpc_config_compression_enabled" do
    test "Tests gRPC compression is enabled for responses" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_config_compression_enabled", finch: AlefE2EFinch, json: %{"request" => "get_data"}, headers: [{"Content-Type", "application/grpc"}, {"grpc-encoding", "gzip"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-encoding", do: List.first(List.wrap(v)) end) == "gzip"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
    end
  end

  describe "grpc_config_keepalive_settings" do
    test "Tests gRPC keepalive ping configuration" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_config_keepalive_settings", finch: AlefE2EFinch, json: %{"task" => "background_job"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
    end
  end

  describe "grpc_config_max_message_size" do
    test "Tests gRPC server respects maximum message size configuration" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_config_max_message_size", finch: AlefE2EFinch, json: %{"data" => "large_payload_data"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"received_size" => 19}
    end
  end

  describe "grpc_config_request_timeout" do
    test "Tests gRPC request timeout configuration" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_config_request_timeout", finch: AlefE2EFinch, json: %{"operation" => "process"}, headers: [{"Content-Type", "application/grpc"}, {"grpc-timeout", "5000m"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
    end
  end

  describe "grpc_deadline_exceeded" do
    test "Tests gRPC DEADLINE_EXCEEDED error when timeout occurs" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_deadline_exceeded", finch: AlefE2EFinch, json: %{"duration_ms" => 60000}, headers: [{"Content-Type", "application/grpc"}, {"grpc-timeout", "100m"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "4"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"code" => "DEADLINE_EXCEEDED", "message" => "Operation exceeded deadline"}
    end
  end

  describe "grpc_error_invalid_argument" do
    test "Tests gRPC INVALID_ARGUMENT error status response" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_error_invalid_argument", finch: AlefE2EFinch, json: %{"value" => -100}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "3"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"code" => "INVALID_ARGUMENT", "message" => "Value must be non-negative"}
    end
  end

  describe "grpc_error_not_found" do
    test "Tests gRPC NOT_FOUND error status response" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_error_not_found", finch: AlefE2EFinch, json: %{"item_id" => "nonexistent"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "5"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"code" => "NOT_FOUND", "message" => "Item not found"}
    end
  end

  describe "grpc_request_method_name_field" do
    test "Tests gRPC request includes method_name field" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_request_method_name_field", finch: AlefE2EFinch, json: %{"id" => "resource-1"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => "result", "method" => "GetData"}
    end
  end

  describe "grpc_request_service_name_field" do
    test "Tests gRPC request includes service_name field" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_request_service_name_field", finch: AlefE2EFinch, json: %{"name" => "World"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"message" => "Hello World", "service" => "helloworld.Greeter"}
    end
  end

  describe "grpc_request_with_custom_metadata" do
    test "Tests gRPC request includes custom metadata in trailers" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_request_with_custom_metadata", finch: AlefE2EFinch, json: %{"data" => %{"key" => "value"}, "request_id" => "req-12345"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}, {"x-request-id", "req-12345"}, {"x-trace-id", "trace-abc123"}, {"x-user-id", "user-42"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-response-trace-id", do: List.first(List.wrap(v)) end) == "trace-abc123"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"processed" => true, "request_id" => "req-12345"}
    end
  end

  describe "grpc_response_with_metadata_trailers" do
    test "Tests gRPC response includes metadata in trailers" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_response_with_metadata_trailers", finch: AlefE2EFinch, json: %{"request" => "test"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-metadata-key", do: List.first(List.wrap(v)) end) == "metadata-value"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"result" => "success"}
    end
  end

  describe "grpc_unary_call_success" do
    test "Tests unary gRPC call with successful response" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/grpc_unary_call_success", finch: AlefE2EFinch, json: %{"name" => "World"}, headers: [{"Content-Type", "application/grpc"}, {"te", "trailers"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/grpc"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "grpc-status", do: List.first(List.wrap(v)) end) == "0"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"message" => "Hello World"}
    end
  end
end
