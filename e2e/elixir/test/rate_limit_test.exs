# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e795e4f6668b39f4dab9ec8d4aedd564da95692620a6c7e2e7a98809fdaee124
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: rate_limit
defmodule E2e.RateLimitTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "rate_limit_below_threshold_succeeds" do
    test "Verifies that requests below the configured rate limit are served normally." do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/rate_limit_below_threshold_succeeds", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"request" => "under-limit", "status" => "ok"}
    end
  end

  describe "rate_limit_burst_setting_allows_spike" do
    test "Tests burst setting allows request spike beyond per_second rate" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/rate_limit_burst_setting_allows_spike", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"status" => "ok"}
    end
  end

  describe "rate_limit_exceeded_returns_429" do
    test "Sends sequential requests until the configured limit is exceeded and validates the 429 response." do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/rate_limit_exceeded_returns_429", finch: AlefE2EFinch)
      assert response.status == 429
    end
  end

  describe "rate_limit_ip_based_tracking" do
    test "Tests rate limiting is applied per IP address" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/rate_limit_ip_based_tracking", finch: AlefE2EFinch, headers: [{"X-Forwarded-For", "192.168.1.100"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"status" => "ok"}
    end
  end

  describe "rate_limit_per_second_setting_10_requests" do
    test "Tests rate limit allows up to 10 requests per second" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/rate_limit_per_second_setting_10_requests", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"status" => "ok"}
    end
  end
end
