# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:13affc129d0533e7d0d02193d5855daeac05138ed8dd80300e7f1f780b62e287
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: background_tasks
defmodule E2e.BackgroundTasksTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "background_task_cancellation" do
    test "DELETE /background/cancel/task-001 - Tests background task cancellation via request" do
      {:ok, response} = Req.delete(url: "#{mock_server_url()}/fixtures/background_task_cancellation")
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"cancelled" => true, "task_id" => "task-001"}
    end
  end

  describe "background_task_custom_max_concurrent" do
    test "POST /background/process - Tests background task with custom max_concurrent setting" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/background_task_custom_max_concurrent", json: %{"data" => "process this", "task_id" => "task-001"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 202
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"status" => "queued", "task_id" => "task-001"}
    end
  end

  describe "background_task_custom_timeout" do
    test "POST /background/slow-task - Tests background task with custom timeout configuration" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/background_task_custom_timeout", json: %{"duration_ms" => 5000}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 202
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end

  describe "background_task_error_handling" do
    test "POST /background/may-fail - Tests background task error handling and retry behavior" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/background_task_error_handling", json: %{"should_fail" => true}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 202
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"status" => "queued", "will_retry" => true}
    end
  end

  describe "background_task_multiple_concurrent" do
    test "POST /background/batch - Tests multiple concurrent background tasks are queued" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/background_task_multiple_concurrent", json: %{"tasks" => [%{"id" => "task-1", "payload" => "data1"}, %{"id" => "task-2", "payload" => "data2"}, %{"id" => "task-3", "payload" => "data3"}]}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 202
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"queued" => 3, "status" => "all_queued"}
    end
  end
end
