# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:95d009c58747e49824c4344040d85dcad6dbdd2ee2853de81d1564abf7ffd5a6
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: background
defmodule E2e.BackgroundTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "background_event_logging" do
    test "POST /background/events - Enqueues a background job that appends the posted event to shared state" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/background_event_logging", json: %{"event" => "alpha"})
      assert response.status == 202
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end

  describe "background_event_logging_second_payload" do
    test "POST /background/events - Ensures background jobs handle different payloads" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/background_event_logging_second_payload", json: %{"event" => "beta"})
      assert response.status == 202
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end
end
