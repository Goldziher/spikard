# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:1343d5cc8d0baeb72b5ccb387dd56e070453b2547cd6dfa9988d5465f03a8621
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: body_limits
defmodule E2e.BodyLimitsTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "body_over_limit_returns_413" do
    test "POST /body-limit/over - Requests that exceed the configured max body size should be rejected with Payload Too Large." do
      {:ok, response} = Req.post(client(), url: "/body-limit/over", json: %{"note" => "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"})
      assert response.status == 413
    end
  end

  describe "body_under_limit_succeeds" do
    test "POST /body-limit/under - Ensures requests smaller than the configured body limit are accepted." do
      {:ok, response} = Req.post(client(), url: "/body-limit/under", json: %{"note" => "small"})
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"accepted" => true, "note" => "small"}
    end
  end
end
