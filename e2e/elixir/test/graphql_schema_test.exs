# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d3901ccd0562d670c46890c6dfba29ddcc523dd04f549dd746fe451e2d4f19c5
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: graphql_schema
defmodule E2e.GraphqlSchemaTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "graphql_authentication_error_variant" do
    test "Tests GraphQL authentication error when auth fails" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_authentication_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ user { id } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 401
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"error" => "<<present>>"}
    end
  end

  describe "graphql_authorization_error_variant" do
    test "Tests GraphQL authorization error when access denied" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_authorization_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ admin { id } }"}, headers: [{"Authorization", "Bearer invalid"}, {"Content-Type", "application/json"}])
      assert response.status == 403
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"error" => "<<present>>"}
    end
  end

  describe "graphql_complexity_limit_exceeded" do
    test "Tests GraphQL query complexity validation rejects overly complex queries" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_complexity_limit_exceeded", finch: AlefE2EFinch, json: %{"query" => "{ user { id name email posts { id title comments { id text } } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"message" => "Query complexity limit exceeded"}]}
    end
  end

  describe "graphql_custom_endpoint_configuration" do
    test "Tests GraphQL route configured at custom endpoint" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_custom_endpoint_configuration", finch: AlefE2EFinch, json: %{"query" => "{ user { id } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => %{"user" => %{"id" => "1"}}}
    end
  end

  describe "graphql_depth_limit_exceeded" do
    test "Tests GraphQL query depth validation rejects deeply nested queries" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_depth_limit_exceeded", finch: AlefE2EFinch, json: %{"query" => "{ user { posts { comments { author { posts { title } } } } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"message" => "Query depth limit exceeded"}]}
    end
  end

  describe "graphql_error_response_format" do
    test "Tests GraphQL error response format with correct structure" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_error_response_format", finch: AlefE2EFinch, json: %{"query" => "{ invalidField }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"extensions" => %{"code" => "GRAPHQL_VALIDATION_FAILED"}, "message" => "Cannot query field 'invalidField' on type 'Query'"}]}
    end
  end

  describe "graphql_execution_error_variant" do
    test "Tests GraphQL execution error response" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_execution_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ invalidField }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"message" => "<<present>>"}]}
    end
  end

  describe "graphql_full_schema_with_subscriptions" do
    test "Tests GraphQL schema with queries, mutations, and subscriptions" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_full_schema_with_subscriptions", finch: AlefE2EFinch, json: %{"query" => "{ __typename }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => %{"__typename" => "Query"}}
    end
  end

  describe "graphql_introspection_disabled" do
    test "Tests GraphQL introspection returns error when disabled" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_introspection_disabled", finch: AlefE2EFinch, json: %{"query" => "{ __schema { types { name } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"message" => "GraphQL introspection is disabled"}]}
    end
  end

  describe "graphql_introspection_enabled" do
    test "Tests GraphQL introspection is available" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_introspection_enabled", finch: AlefE2EFinch, json: %{"query" => "{ __schema { types { name } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => %{"__schema" => %{"types" => nil}}}
    end
  end

  describe "graphql_invalid_input_error_variant" do
    test "Tests GraphQL invalid input error" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_invalid_input_error_variant", finch: AlefE2EFinch, json: %{"query" => "mutation { createUser(age: -5) { id } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"message" => "<<present>>"}]}
    end
  end

  describe "graphql_not_found_error_variant" do
    test "Tests GraphQL not found error for missing resource" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_not_found_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ user(id: \"nonexistent\") { id } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"errors" => [%{"message" => "<<present>>"}]}
    end
  end

  describe "graphql_query_and_mutation_schema" do
    test "Tests GraphQL schema with queries and mutations" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_query_and_mutation_schema", finch: AlefE2EFinch, json: %{"query" => "mutation { createUser(name: \"New\") { id name } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => %{"createUser" => %{"id" => "2", "name" => "New"}}}
    end
  end

  describe "graphql_query_only_schema" do
    test "Tests GraphQL schema configured with queries only" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_query_only_schema", finch: AlefE2EFinch, json: %{"query" => "{ user { id name } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => %{"user" => %{"id" => "1", "name" => "Test User"}}}
    end
  end

  describe "graphql_rate_limit_error_variant" do
    test "Tests GraphQL rate limit exceeded error" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_rate_limit_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ user { id } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => "<<present>>"}
    end
  end

  describe "graphql_schema_build_error_variant" do
    test "Tests GraphQL schema build error" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_schema_build_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ test }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 500
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"error" => "<<present>>"}
    end
  end

  describe "graphql_serialization_error_variant" do
    test "Tests GraphQL serialization error for invalid response" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/graphql_serialization_error_variant", finch: AlefE2EFinch, json: %{"query" => "{ user { id } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => "<<present>>"}
    end
  end
end
