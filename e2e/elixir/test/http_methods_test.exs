# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:7881afcb68f5ec09df119d04892db2dc65fd0217da73e7ff663bb4dea8b6a71e
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: http_methods
defmodule E2e.HttpMethodsTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "delete_remove_resource" do
    test "DELETE /items/1 - Tests DELETE method to remove a resource" do
      {:ok, response} = Req.delete(client(), url: "/items/1")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{}
    end
  end

  describe "delete_resource_not_found" do
    test "DELETE /items/999 - Tests DELETE on non-existent resource returns 404" do
      {:ok, response} = Req.delete(client(), url: "/items/999")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{}
    end
  end

  describe "delete_with_response_body" do
    test "DELETE /items/1 - Tests DELETE returning deleted resource data" do
      {:ok, response} = Req.delete(client(), url: "/items/1")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 1, "message" => "Item deleted successfully", "name" => "Deleted Item"}
    end
  end

  describe "head_get_metadata_without_body" do
    test "HEAD /items/1 - Tests HEAD method returns headers without response body" do
      {:ok, response} = Req.head(client(), url: "/items/1")
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-length", do: v end) == "85"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/json"
    end
  end

  describe "patch_partial_update" do
    test "PATCH /items/1 - Tests PATCH method for partial resource updates" do
      {:ok, response} = Req.patch(client(), url: "/items/1", json: %{"price" => 79.99}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 1, "in_stock" => true, "name" => "Existing Item", "price" => 79.99}
    end
  end

  describe "patch_update_multiple_fields" do
    test "PATCH /items/1 - Tests PATCH updating multiple fields at once" do
      {:ok, response} = Req.patch(client(), url: "/items/1", json: %{"in_stock" => false, "name" => "Updated Name", "price" => 89.99}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 1, "in_stock" => false, "name" => "Updated Name", "price" => 89.99}
    end
  end

  describe "put_complete_resource_replacement" do
    test "PUT /items/1 - Tests PUT method for complete resource replacement" do
      {:ok, response} = Req.put(client(), url: "/items/1", json: %{"description" => "Completely replaced", "id" => 1, "in_stock" => true, "name" => "Updated Item", "price" => 99.99}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"description" => "Completely replaced", "id" => 1, "in_stock" => true, "name" => "Updated Item", "price" => 99.99}
    end
  end

  describe "put_create_resource_if_doesn_t_exist" do
    test "PUT /items/999 - Tests PUT creating new resource at specific URI" do
      {:ok, response} = Req.put(client(), url: "/items/999", json: %{"id" => 999, "name" => "New Item", "price" => 49.99}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 999, "name" => "New Item", "price" => 49.99}
    end
  end

  describe "put_idempotent_operation" do
    test "PUT /items/1 - Tests PUT idempotency - repeated calls produce same result" do
      {:ok, response} = Req.put(client(), url: "/items/1", json: %{"id" => 1, "name" => "Fixed Name", "price" => 50.0}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"id" => 1, "name" => "Fixed Name", "price" => 50.0}
    end
  end

  describe "put_missing_required_field" do
    test "PUT /items/1 - Tests PUT with missing required fields returns 422" do
      {:ok, response} = Req.put(client(), url: "/items/1", json: %{"id" => 1, "name" => "Item Name"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 422
      assert Jason.decode!(response.body) == %{"detail" => "1 validation error in request", "errors" => [%{"input" => %{"id" => 1, "name" => "Item Name"}, "loc" => ["body", "price"], "msg" => "Field required", "type" => "missing"}], "status" => 422, "title" => "Request Validation Failed", "type" => "https://spikard.dev/errors/validation-error"}
    end
  end

  describe "put_validation_error" do
    test "PUT /items/1 - Tests PUT with invalid data returns 422" do
      {:ok, response} = Req.put(client(), url: "/items/1", json: %{"id" => 1, "name" => "X", "price" => -10}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 422
      assert Jason.decode!(response.body) == %{"detail" => "2 validation errors in request", "errors" => [%{"ctx" => %{"min_length" => 3}, "input" => "X", "loc" => ["body", "name"], "msg" => "String should have at least 3 characters", "type" => "string_too_short"}, %{"ctx" => %{"gt" => 0}, "input" => -10, "loc" => ["body", "price"], "msg" => "Input should be greater than 0", "type" => "greater_than"}], "status" => 422, "title" => "Request Validation Failed", "type" => "https://spikard.dev/errors/validation-error"}
    end
  end
end
