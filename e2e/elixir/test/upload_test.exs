# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2f6c3f90f4a4b8f13b14d0c6cbe9fadc091c336017f8857a60090d28ced717b0
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: upload
defmodule E2e.UploadTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "upload_file_as_bytes" do
    test "Tests file upload read as byte array" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_as_bytes", finch: AlefE2EFinch, json: %{"file" => %{"content" => "binary data", "filename" => "binary.bin"}}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"as_bytes" => true, "filename" => "binary.bin", "size" => 11}
    end
  end

  describe "upload_file_basic_success" do
    test "Tests basic file upload with single file" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_basic_success", finch: AlefE2EFinch, json: %{"file" => %{"content" => "Hello, World!", "filename" => "test.txt"}}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"filename" => "test.txt", "size" => 13}
    end
  end

  describe "upload_file_content_type_or_default" do
    test "Tests file upload uses provided content_type or defaults" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_content_type_or_default", finch: AlefE2EFinch, json: %{"file" => %{"content" => "Some content", "filename" => "unknown.xyz"}}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"content_type" => "application/octet-stream", "filename" => "unknown.xyz"}
    end
  end

  describe "upload_file_read_to_string" do
    test "Tests file upload read as string" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_read_to_string", finch: AlefE2EFinch, json: %{"file" => %{"content" => "Hello, World!", "filename" => "text.txt"}}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"as_string" => true, "content" => "Hello, World!", "filename" => "text.txt"}
    end
  end

  describe "upload_file_with_custom_content_type" do
    test "Tests file upload preserves custom Content-Type" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_with_custom_content_type", finch: AlefE2EFinch, json: %{"file" => %{"content" => "{\"key\": \"value\"}", "content_type" => "application/json", "filename" => "data.json"}}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"content_type" => "application/json", "filename" => "data.json", "size" => 16}
    end
  end

  describe "upload_file_with_form_fields" do
    test "Tests file upload with additional form fields" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_with_form_fields", finch: AlefE2EFinch, json: %{"description" => "Important document", "file" => %{"content" => "PDF content", "filename" => "document.pdf"}, "tags" => ["important", "archive"]}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"description" => "Important document", "filename" => "document.pdf", "tags_count" => 2}
    end
  end

  describe "upload_file_with_gzip_content_encoding" do
    test "Tests file upload with gzip content encoding" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_file_with_gzip_content_encoding", finch: AlefE2EFinch, json: %{"file" => %{"content" => "compressed data", "content_encoding" => "gzip", "filename" => "data.gz"}}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"content_encoding" => "gzip", "filename" => "data.gz"}
    end
  end

  describe "upload_multiple_files" do
    test "Tests uploading multiple files at once" do
      {:ok, response} = Req.post(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/upload_multiple_files", finch: AlefE2EFinch, json: %{"files" => [%{"content" => "Content 1", "filename" => "file1.txt"}, %{"content" => "Content 2", "filename" => "file2.txt"}]}, headers: [{"Content-Type", "multipart/form-data"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"total_size" => 18, "uploaded_files" => 2}
    end
  end
end
