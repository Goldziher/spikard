# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:630f0afe6de5d35ae4be21314fdcafae04f4918bd1cc8e5a3eecbaf2a4422fa1
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: streaming
defmodule E2e.StreamingTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "binary_log_download" do
    test "GET /stream/logfile - Streams binary log segments with control bytes" do
      {:ok, response} = Req.get(client(), url: "/stream/logfile")
      assert response.status == 200
      assert Jason.decode!(response.body) == "LOG:\\u0000\\u0001\\u0002\\u0003|TAIL|\\u0007\\n"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/octet-stream"
    end
  end

  describe "chunked_csv_export" do
    test "GET /stream/csv-report - Streams CSV header and rows as discrete chunks" do
      {:ok, response} = Req.get(client(), url: "/stream/csv-report")
      assert response.status == 200
      assert Jason.decode!(response.body) == "id,name,value\\n1,Alice,42\\n2,Bob,7\\n"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/csv"
    end
  end

  describe "stream_json_lines" do
    test "GET /stream/json-lines - Streams newline-delimited JSON payload in small chunks" do
      {:ok, response} = Req.get(client(), url: "/stream/json-lines")
      assert response.status == 200
      assert Jason.decode!(response.body) == "{\"index\":0,\"payload\":\"alpha\"}\\n{\"index\":1,\"payload\":\"beta\"}\\n{\"index\":2,\"payload\":\"gamma\"}\\n"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/x-ndjson"
    end
  end
end
