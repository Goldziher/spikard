# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: openapi
defmodule E2e.OpenapiTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "openapi_basic_spec_generation" do
    test "GET /openapi.json - Tests OpenAPI specification is generated from route definitions" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"info" => %{"title" => "API"}, "openapi" => "3.0.0"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/json"
    end
  end

  describe "openapi_contact_information" do
    test "GET /openapi.json - Tests OpenAPI spec includes contact information" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"info" => %{"contact" => %{"name" => "API Support", "url" => "https://example.com/support"}}}
    end
  end

  describe "openapi_disabled_returns_404" do
    test "GET /openapi.json - Tests OpenAPI returns 404 when explicitly disabled" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 404
    end
  end

  describe "openapi_enabled_flag_true" do
    test "GET /openapi.json - Tests OpenAPI can be explicitly enabled" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"openapi" => "3.0.0"}
    end
  end

  describe "openapi_json_endpoint_path" do
    test "GET /api/openapi.json - Tests OpenAPI JSON is served at configured endpoint" do
      {:ok, response} = Req.get(client(), url: "/api/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"openapi" => "3.0.0"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/json"
    end
  end

  describe "openapi_license_information" do
    test "GET /openapi.json - Tests OpenAPI spec includes license information" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"info" => %{"license" => %{"name" => "MIT"}}}
    end
  end

  describe "openapi_redoc_path" do
    test "GET /redoc - Tests ReDoc UI is served at configured path" do
      {:ok, response} = Req.get(client(), url: "/redoc")
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/html"
    end
  end

  describe "openapi_security_schemes_definition" do
    test "GET /openapi.json - Tests OpenAPI spec includes security schemes" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"components" => %{"security_schemes" => %{"bearer_auth" => %{"scheme" => "bearer", "type" => "http"}}}}
    end
  end

  describe "openapi_servers_configuration" do
    test "GET /openapi.json - Tests OpenAPI spec includes configured servers" do
      {:ok, response} = Req.get(client(), url: "/openapi.json")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"servers" => [%{"url" => "https://api.example.com"}, %{"url" => "https://staging-api.example.com"}]}
    end
  end

  describe "openapi_swagger_ui_path" do
    test "GET /docs - Tests Swagger UI is served at configured path" do
      {:ok, response} = Req.get(client(), url: "/docs")
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/html"
    end
  end
end
