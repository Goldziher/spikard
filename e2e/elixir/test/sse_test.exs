# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:203c1dc64ca7233a5d9b2a07bfd0f6e0102ad89bf1462b4337ebdf06c383cb66
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: sse
defmodule E2e.SseTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "sse_event_retry_5000_milliseconds" do
    test "GET /events/retry-5s - Tests SSE event with 5000ms retry interval" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_event_retry_5000_milliseconds", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_event_with_all_fields" do
    test "GET /events/complete - Tests SSE event with all optional fields (id, type, retry)" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_event_with_all_fields", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_event_with_data_and_metadata" do
    test "GET /events/data - Tests SSE event with JSON data and metadata fields" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_event_with_data_and_metadata", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_event_with_id_field" do
    test "GET /events/with-id - Tests SSE event includes unique ID field" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_event_with_id_field", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_event_with_retry_field" do
    test "GET /events/with-retry - Tests SSE event includes retry interval field" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_event_with_retry_field", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_event_with_type_field" do
    test "GET /events/typed - Tests SSE event includes event type field" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_event_with_type_field", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_multiple_events_stream" do
    test "GET /events/multi - Tests SSE stream with multiple consecutive events" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_multiple_events_stream", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
    end
  end

  describe "sse_simple_event_stream" do
    test "GET /events - Tests Server-Sent Events with simple text events" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/sse_simple_event_stream", headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/event-stream"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "cache-control", do: v end) == "no-cache"
    end
  end
end
