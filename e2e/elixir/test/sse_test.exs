# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d09a78f4a9bd131bc0158b123a7c58f95c84fb311186c45d9b8640a44534e2dc
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: sse
defmodule E2e.SseTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "sse_event_retry_5000_milliseconds" do
    test "Tests SSE event with 5000ms retry interval" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_event_retry_5000_milliseconds", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_event_with_all_fields" do
    test "Tests SSE event with all optional fields (id, type, retry)" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_event_with_all_fields", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_event_with_data_and_metadata" do
    test "Tests SSE event with JSON data and metadata fields" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_event_with_data_and_metadata", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_event_with_id_field" do
    test "Tests SSE event includes unique ID field" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_event_with_id_field", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_event_with_retry_field" do
    test "Tests SSE event includes retry interval field" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_event_with_retry_field", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_event_with_type_field" do
    test "Tests SSE event includes event type field" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_event_with_type_field", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_multiple_events_stream" do
    test "Tests SSE stream with multiple consecutive events" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_multiple_events_stream", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end

  describe "sse_simple_event_stream" do
    test "Tests Server-Sent Events with simple text events" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/sse_simple_event_stream", connect_options: [protocols: [:http1]], headers: [{"Accept", "text/event-stream"}])
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "cache-control", do: List.first(List.wrap(v)) end) == "no-cache"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/event-stream"
    end
  end
end
