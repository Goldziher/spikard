# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: problem_details
defmodule E2e.ProblemDetailsTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "problem_details_400_bad_request" do
    test "POST /parse - Tests ProblemDetails for 400 Bad Request status" do
      {:ok, response} = Req.post(client(), url: "/parse", json: "invalid json", headers: [{"Content-Type", "application/json"}])
      assert response.status == 400
      assert Jason.decode!(response.body) == %{"detail" => "Invalid JSON in request body", "status" => 400, "title" => "Bad Request", "type" => "https://spikard.dev/errors/bad-request"}
    end
  end

  describe "problem_details_401_unauthorized" do
    test "GET /protected - Tests ProblemDetails for 401 Unauthorized status" do
      {:ok, response} = Req.get(client(), url: "/protected")
      assert response.status == 401
      assert Jason.decode!(response.body) == %{"detail" => "Authentication required", "status" => 401, "title" => "Unauthorized", "type" => "https://spikard.dev/errors/unauthorized"}
    end
  end

  describe "problem_details_403_forbidden" do
    test "GET /admin/config - Tests ProblemDetails for 403 Forbidden status" do
      {:ok, response} = Req.get(client(), url: "/admin/config")
      assert response.status == 403
      assert Jason.decode!(response.body) == %{"detail" => "Insufficient permissions to access this resource", "status" => 403, "title" => "Forbidden", "type" => "https://spikard.dev/errors/forbidden"}
    end
  end

  describe "problem_details_500_internal_error" do
    test "GET /error - Tests ProblemDetails for 500 Internal Server Error status" do
      {:ok, response} = Req.get(client(), url: "/error")
      assert response.status == 500
      assert Jason.decode!(response.body) == %{"detail" => "An unexpected error occurred", "status" => 500, "title" => "Internal Server Error", "type" => "https://spikard.dev/errors/internal-error"}
    end
  end

  describe "problem_details_standard_all_fields" do
    test "GET /items/999 - Tests RFC 9457 ProblemDetails response with all standard fields" do
      {:ok, response} = Req.get(client(), url: "/items/999")
      assert response.status == 404
      assert Jason.decode!(response.body) == %{"detail" => "Item with ID 999 was not found", "instance" => "/items/999", "status" => 404, "title" => "Resource Not Found", "type" => "https://spikard.dev/errors/not-found"}
    end
  end

  describe "problem_details_with_extensions" do
    test "POST /api/resource - Tests ProblemDetails includes custom extensions for additional context" do
      {:ok, response} = Req.post(client(), url: "/api/resource", json: %{"data" => "invalid"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 422
      assert Jason.decode!(response.body) == %{"detail" => "Validation failed", "extensions" => %{"error_code" => "VALIDATION_001", "timestamp" => nil, "trace_id" => nil}, "status" => 422, "title" => "Request Validation Failed", "type" => "https://spikard.dev/errors/validation-error"}
    end
  end

  describe "problem_details_with_instance" do
    test "GET /users/123/posts/456 - Tests ProblemDetails includes instance URI for context" do
      {:ok, response} = Req.get(client(), url: "/users/123/posts/456")
      assert response.status == 404
      assert Jason.decode!(response.body) == %{"detail" => "Post 456 not found for user 123", "instance" => "/users/123/posts/456", "status" => 404, "title" => "Post Not Found", "type" => "https://spikard.dev/errors/not-found"}
    end
  end

  describe "problem_details_with_type_uri" do
    test "POST /validate - Tests ProblemDetails includes type URI for error classification" do
      {:ok, response} = Req.post(client(), url: "/validate", json: %{"value" => -5}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 422
      assert Jason.decode!(response.body) == %{"detail" => "Request validation failed", "status" => 422, "title" => "Validation Failed", "type" => "https://spikard.dev/errors/validation-error"}
    end
  end
end
