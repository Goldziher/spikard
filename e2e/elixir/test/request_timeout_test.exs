# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2bf2af9e19ec39a6723796edacd28d32d9369978df270ade7d51f550dd97d6d0
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: request_timeout
defmodule E2e.RequestTimeoutTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "request_completes_before_timeout" do
    test "GET /timeouts/fast - Simulated handler sleeps briefly and should complete before the timeout middleware fires." do
      {:ok, response} = Req.get(client(), url: "/timeouts/fast")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"duration" => "fast", "status" => "ok"}
    end
  end

  describe "request_exceeds_timeout" do
    test "GET /timeouts/slow - Simulates a handler that sleeps longer than the configured timeout to ensure a 408 response." do
      {:ok, response} = Req.get(client(), url: "/timeouts/slow")
      assert response.status == 408
    end
  end
end
