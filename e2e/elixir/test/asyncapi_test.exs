# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2c0b925a0bb4683442e13eab88c0f9e94714d6d0eb18d1994b8d44800d823323
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: asyncapi
defmodule E2e.AsyncapiTest do
  use ExUnit.Case, async: true

  describe "asyncapi_channel_extraction" do
    test "Tests that channels are correctly extracted from an AsyncAPI 3.0.0 spec" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_invalid_spec_bad_version" do
    test "Tests that a spec with an invalid asyncapi version field is rejected with an error" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_message_schema_validation_fail_missing_required" do
    test "Tests that a message missing required fields is rejected by schema validation" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_message_schema_validation_pass" do
    test "Tests that a valid message passes schema validation against its channel definition" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_multi_channel_spec" do
    test "Tests parsing of a spec with multiple channels each targeting different protocols" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_operation_extraction" do
    test "Tests that send/receive operations are correctly extracted from an AsyncAPI 3.0.0 spec" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_schema_ref_resolution" do
    test "Tests that $ref references to components/messages are resolved during extraction" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_sse_binding_extraction" do
    test "Tests that HTTP SSE binding is extracted from a channel with http bindings block" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_validate_summary_fields" do
    test "Tests that validate_asyncapi_schema returns correct spec_version, title, api_version, and channel_count" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end

  describe "asyncapi_websocket_binding_extraction" do
    test "Tests that WebSocket binding is extracted from a channel with ws bindings block" do
      {:ok, response} = Spikard.handle_request_async(nil)
    end
  end
end
