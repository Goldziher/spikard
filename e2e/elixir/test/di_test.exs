# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:fa06b403270e3c3da4cd204fb21e3c1a52cfd79563d9c34ce969edd497378eee
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: di
defmodule E2e.DiTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "async_factory_dependency_success" do
    test "Tests async factory that creates database pool asynchronously" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/async_factory_dependency_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"max_size" => 10, "pool_status" => "connected"}
    end
  end

  describe "circular_dependency_detection_error" do
    test "Tests that circular dependencies (A depends on B, B depends on A) are detected and rejected at registration time" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/circular_dependency_detection_error", finch: AlefE2EFinch)
      assert response.status == 500
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Circular dependency detected", "errors" => [%{"cycle" => ["service_a", "service_b", "service_a"], "msg" => "Circular dependency detected in dependency graph", "type" => "circular_dependency"}], "status" => 500, "title" => "Dependency Resolution Failed", "type" => "https://spikard.dev/errors/dependency-error"}
    end
  end

  describe "dependency_injection_in_lifecycle_hooks_success" do
    test "Tests accessing dependencies in lifecycle hooks (onRequest, preHandler) for auth, logging, etc." do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/dependency_injection_in_lifecycle_hooks_success", finch: AlefE2EFinch, headers: [{"authorization", "Bearer valid_token"}])
      assert response.status == 200
    assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-auth-mode", do: List.first(List.wrap(v)) end) == "strict"
    assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-log-level", do: List.first(List.wrap(v)) end) == "debug"
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"authenticated" => true, "logged" => true}
    end
  end

  describe "factory_dependency_success" do
    test "Tests factory dependency that creates instances on-demand" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/factory_dependency_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"timestamp" => "<<present>>"}
    end
  end

  describe "missing_dependency_error" do
    test "Tests error when handler requires a dependency that was never registered" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/missing_dependency_error", finch: AlefE2EFinch)
      assert response.status == 500
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Required dependency not found", "errors" => [%{"dependency_key" => "non_existent_service", "msg" => "Dependency 'non_existent_service' is not registered", "type" => "missing_dependency"}], "status" => 500, "title" => "Dependency Resolution Failed", "type" => "https://spikard.dev/errors/dependency-error"}
    end
  end

  describe "mixed_singleton_and_per_request_caching_success" do
    test "Tests mixing singleton dependencies (shared across requests) with per-request dependencies (cached within request)" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/mixed_singleton_and_per_request_caching_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"app_name" => "MyApp", "context_id" => "<<uuid>>", "pool_id" => "<<uuid>>"}
    end
  end

  describe "multiple_dependencies_with_cleanup_success" do
    test "Tests cleanup for multiple dependencies with generator pattern, ensuring all are cleaned up in reverse resolution order" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/multiple_dependencies_with_cleanup_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"session_active" => true}
    end
  end

  describe "nested_dependencies_3_levels_success" do
    test "Tests dependency resolution where auth depends on cache and db, which both depend on config" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/nested_dependencies_3_levels_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"auth_enabled" => true, "has_cache" => true, "has_db" => true}
    end
  end

  describe "node_js_object_destructuring_injection_success" do
    test "Tests Node.js/TypeScript-specific object destructuring pattern for dependency injection" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/node_js_object_destructuring_injection_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"db_name" => "PostgreSQL", "log_level" => "info"}
    end
  end

  describe "per_request_dependency_caching_success" do
    test "Tests per-request caching where dependency is created once per request but shared by multiple usages within that request" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/per_request_dependency_caching_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"first_id" => "<<uuid>>", "second_id" => "<<same_as:first_id>>"}
    end
  end

  describe "python_parameter_name_based_injection_success" do
    test "Tests Python-specific parameter name-based dependency injection where dependencies are matched by parameter names" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/python_parameter_name_based_injection_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"cache_status" => "ready", "db_status" => "connected"}
    end
  end

  describe "python_type_annotation_based_injection_success" do
    test "Tests Python-specific type annotation-based dependency injection where dependencies are matched by type hints" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/python_type_annotation_based_injection_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"cache_type" => "Redis", "pool_type" => "PostgreSQL"}
    end
  end

  describe "resource_cleanup_after_request_success" do
    test "Tests generator pattern cleanup where dependency resources are cleaned up after handler completes" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/resource_cleanup_after_request_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"session_id" => "<<uuid>>", "status" => "completed"}
    end
  end

  describe "route_level_dependency_override_success" do
    test "Tests route-level dependency override of app-level dependency for testing or special cases" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/route_level_dependency_override_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"mode" => "test", "strict" => false}
    end
  end

  describe "ruby_keyword_argument_injection_success" do
    test "Tests Ruby-specific keyword argument pattern for dependency injection" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/ruby_keyword_argument_injection_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"adapter" => "postgresql", "user_id" => 42}
    end
  end

  describe "singleton_dependency_caching_success" do
    test "Tests singleton dependency that is created once and shared across all requests" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/singleton_dependency_caching_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"count" => 1, "counter_id" => "<<uuid>>"}
    end
  end

  describe "type_mismatch_in_dependency_resolution_error" do
    test "Tests error when handler expects a different type than what the dependency provides" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/type_mismatch_in_dependency_resolution_error", finch: AlefE2EFinch)
      assert response.status == 500
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"detail" => "Dependency type mismatch", "errors" => [%{"actual_type" => "string", "dependency_key" => "config", "expected_type" => "object", "msg" => "Dependency 'config' type mismatch: expected object, got string", "type" => "type_mismatch"}], "status" => 500, "title" => "Dependency Resolution Failed", "type" => "https://spikard.dev/errors/dependency-error"}
    end
  end

  describe "value_dependency_injection_success" do
    test "Tests simple value injection (config, constants) into a handler" do
      {:ok, response} = Req.get(url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/value_dependency_injection_success", finch: AlefE2EFinch)
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"app_name" => "SpikardApp", "max_connections" => 100, "version" => "1.0.0"}
    end
  end
end
