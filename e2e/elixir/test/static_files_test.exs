# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c2356e021f1241fae817c4afe328b34929efc2125520d0314f49d9bb2ae88a74
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: static_files
defmodule E2e.StaticFilesTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "static_file_server_returns_text_file" do
    test "Tests serving a plain text file with Cache-Control headers." do
      {:ok, response} =
        Req.get(
          url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/static_file_server_returns_text_file",
          finch: AlefE2EFinch
        )

      assert response.status == 200

      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "cache-control", do: List.first(List.wrap(v)) end) ==
               "public, max-age=60"

      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) ==
               "text/plain"

      assert response.body == "Hello from static storage"
    end
  end

  describe "static_server_returns_index_html_for_directory" do
    test "When index files are enabled the server should serve index.html when the directory root is requested." do
      {:ok, response} =
        Req.get(
          url: (System.get_env("SUT_URL") || mock_server_url()) <> "/fixtures/static_server_returns_index_html_for_directory",
          finch: AlefE2EFinch
        )

      assert response.status == 200

      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) ==
               "text/html"

      assert response.body == "<!doctype html><h1>Welcome</h1>"
    end
  end
end
