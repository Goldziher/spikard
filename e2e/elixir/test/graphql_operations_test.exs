# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:a40ccf910316609aeca571aa486bc54d408ad71612dc9f27026c2b524327089a
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: graphql_operations
defmodule E2e.GraphqlOperationsTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "graphql_field_level_error_partial_data" do
    test "POST /graphql - Tests that a field-level resolver error returns partial data alongside an errors array" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_field_level_error_partial_data", json: %{"query" => "{ user(id: \"missing-user\") { id name avatar } status }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"status" => "ok", "user" => nil}, "errors" => [%{"message" => "User not found", "path" => ["user"]}]}
    end
  end

  describe "graphql_introspection_query" do
    test "POST /graphql - Tests that the __schema introspection query returns the schema type list" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_introspection_query", json: %{"query" => "{ __schema { queryType { name } types { name kind } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"schema" => %{"query_type" => %{"name" => "Query"}, "types" => [%{"kind" => "OBJECT", "name" => "Query"}, %{"kind" => "SCALAR", "name" => "String"}, %{"kind" => "SCALAR", "name" => "Boolean"}]}}}
    end
  end

  describe "graphql_invalid_syntax_rejected" do
    test "POST /graphql - Tests that a query with invalid GraphQL syntax returns a syntax error in the errors array" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_invalid_syntax_rejected", json: %{"query" => "{ hello { this is not valid syntax {{{"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"errors" => [%{"locations" => [%{"column" => 10, "line" => 1}], "message" => "Syntax Error"}]}
    end
  end

  describe "graphql_multi_operation_with_operation_name" do
    test "POST /graphql - Tests executing a specific named operation from a document containing multiple operations" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_multi_operation_with_operation_name", json: %{"operation_name" => "Version", "query" => "query Ping { ping } query Version { version }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"version" => "1.0.0"}}
    end
  end

  describe "graphql_mutation_create_object" do
    test "POST /graphql - Tests GraphQL mutation with an input type that returns the newly created object" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_mutation_create_object", json: %{"query" => "mutation CreateUser($input: CreateUserInput!) { createUser(input: $input) { id name email } }", "variables" => %{"input" => %{"email" => "carol@example.com", "name" => "Carol"}}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"create_user" => %{"email" => "carol@example.com", "id" => "new-user-id", "name" => "Carol"}}}
    end
  end

  describe "graphql_ops_complexity_limit_exceeded" do
    test "POST /graphql - Tests that a query exceeding the configured complexity limit returns an error" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_ops_complexity_limit_exceeded", json: %{"query" => "{ users { id friends { id friends { id friends { id friends { id } } } } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"errors" => [%{"message" => "Query complexity exceeds maximum allowed complexity of 10"}]}
    end
  end

  describe "graphql_ops_depth_limit_exceeded" do
    test "POST /graphql - Tests that a query exceeding the configured depth limit returns an error" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_ops_depth_limit_exceeded", json: %{"query" => "{ a { b { c { d { e } } } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"errors" => [%{"message" => "Query depth exceeds maximum allowed depth of 3"}]}
    end
  end

  describe "graphql_query_nested_fields" do
    test "POST /graphql - Tests GraphQL query selecting nested fields at depth 2-3" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_query_nested_fields", json: %{"query" => "{ order(id: \"ORD-1\") { id customer { name address { city } } items { sku quantity } } }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"order" => %{"customer" => %{"address" => %{"city" => "Springfield"}, "name" => "Bob"}, "id" => "ORD-1", "items" => [%{"quantity" => 2, "sku" => "WIDGET-A"}]}}}
    end
  end

  describe "graphql_query_with_variables" do
    test "POST /graphql - Tests GraphQL query that uses variables passed in the request body" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_query_with_variables", json: %{"query" => "query GetUser($id: ID!) { user(id: $id) { id name } }", "variables" => %{"id" => "user-42"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"user" => %{"id" => "user-42", "name" => "Alice"}}}
    end
  end

  describe "graphql_simple_query" do
    test "POST /graphql - Tests simple GraphQL query execution returning data from the root query type" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_simple_query", json: %{"query" => "{ hello }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"hello" => "world"}}
    end
  end

  describe "graphql_subscription_declaration" do
    test "POST /graphql - Tests that a subscription operation type is declared and the initial response shape matches" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_subscription_declaration", json: %{"query" => "subscription OnMessageAdded($roomId: ID!) { messageAdded(roomId: $roomId) { id text authorId } }", "variables" => %{"room_id" => "room-1"}}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => %{"message_added" => %{"author_id" => "user-1", "id" => "msg-1", "text" => "Hello room"}}}
    end
  end

  describe "graphql_undefined_field_error" do
    test "POST /graphql - Tests that querying a field that does not exist in the schema returns a validation error" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/graphql_undefined_field_error", json: %{"query" => "{ hello nonExistentField }"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"errors" => [%{"message" => "Cannot query field \"nonExistentField\" on type \"Query\"."}]}
    end
  end
end
