# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: response
defmodule E2e.ResponseTest do
  use ExUnit.Case, async: true

  defp base_url do
    System.get_env("TEST_SERVER_URL") || "http://localhost:8080"
  end

  defp client do
    Req.new(base_url: base_url())
  end

  describe "response_cache_control_header" do
    test "GET /static-resource - Tests response includes Cache-Control header" do
      {:ok, response} = Req.get(client(), url: "/static-resource")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"resource" => "data"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "cache-control", do: v end) == "public, max-age=3600"
    end
  end

  describe "response_content_type_application_json" do
    test "GET /api/data - Tests response with Content-Type: application/json" do
      {:ok, response} = Req.get(client(), url: "/api/data")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"key" => "value"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/json"
    end
  end

  describe "response_content_type_text_html" do
    test "GET /html - Tests response with Content-Type: text/html" do
      {:ok, response} = Req.get(client(), url: "/html")
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "text/html"
    end
  end

  describe "response_location_header_redirect" do
    test "GET /old-path - Tests response includes Location header for redirects" do
      {:ok, response} = Req.get(client(), url: "/old-path")
      assert response.status == 301
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "location", do: v end) == "https://example.com/new-path"
    end
  end

  describe "response_multiple_headers_combined" do
    test "GET /complex - Tests response with multiple custom headers combined" do
      {:ok, response} = Req.get(client(), url: "/complex")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"message" => "Complex response"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: v end) == "application/json"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-third-header", do: v end) == "value3"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-another-header", do: v end) == "value2"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-custom-header", do: v end) == "value1"
    end
  end

  describe "response_set_cookie_in_response" do
    test "POST /login - Tests response sets cookie with Set-Cookie header" do
      {:ok, response} = Req.post(client(), url: "/login", json: %{"password" => "pass", "username" => "user"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"message" => "Login successful"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "set-cookie", do: v end) != nil
    end
  end

  describe "response_status_code_200_success" do
    test "GET /success - Tests response with explicit 200 status code" do
      {:ok, response} = Req.get(client(), url: "/success")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"status" => "ok"}
    end
  end

  describe "response_status_code_201_created" do
    test "POST /items - Tests response with 201 Created status code" do
      {:ok, response} = Req.post(client(), url: "/items", json: %{"name" => "New Item"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 201
      assert Jason.decode!(response.body) == %{"id" => "1", "name" => "New Item"}
    end
  end

  describe "response_status_code_204_no_content" do
    test "DELETE /items/1 - Tests response with 204 No Content status code" do
      {:ok, response} = Req.delete(client(), url: "/items/1")
      assert response.status == 204
      assert Jason.decode!(response.body) == ""
    end
  end

  describe "response_with_custom_headers" do
    test "GET /data - Tests response includes custom headers" do
      {:ok, response} = Req.get(client(), url: "/data")
      assert response.status == 200
      assert Jason.decode!(response.body) == %{"data" => "value"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-custom-header", do: v end) == "custom-value"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-total-count", do: v end) == "42"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-request-id", do: v end) != nil
    end
  end
end
