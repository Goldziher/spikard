# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:5a3dcb290422b19baeae42a59d3e1078e0d5f741d962df54de7d0ac2fe749517
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: response
defmodule E2e.ResponseTest do
  use ExUnit.Case, async: true

  defp mock_server_url do
    System.get_env("MOCK_SERVER_URL") || "http://localhost:8080"
  end

  describe "response_cache_control_header" do
    test "GET /static-resource - Tests response includes Cache-Control header" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_cache_control_header")
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"resource" => "data"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "cache-control", do: List.first(List.wrap(v)) end) == "public, max-age=3600"
    end
  end

  describe "response_content_type_application_json" do
    test "GET /api/data - Tests response with Content-Type: application/json" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_content_type_application_json")
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"key" => "value"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end

  describe "response_content_type_text_html" do
    test "GET /html - Tests response with Content-Type: text/html" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_content_type_text_html")
      assert response.status == 200
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "text/html"
    end
  end

  describe "response_location_header_redirect" do
    test "GET /old-path - Tests response includes Location header for redirects" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_location_header_redirect", redirect: false)
      assert response.status == 301
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "location", do: List.first(List.wrap(v)) end) == "https://example.com/new-path"
    end
  end

  describe "response_multiple_headers_combined" do
    test "GET /complex - Tests response with multiple custom headers combined" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_multiple_headers_combined")
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"message" => "Complex response"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-custom-header", do: List.first(List.wrap(v)) end) == "value1"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-third-header", do: List.first(List.wrap(v)) end) == "value3"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-another-header", do: List.first(List.wrap(v)) end) == "value2"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "content-type", do: List.first(List.wrap(v)) end) == "application/json"
    end
  end

  describe "response_set_cookie_in_response" do
    test "POST /login - Tests response sets cookie with Set-Cookie header" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/response_set_cookie_in_response", json: %{"password" => "pass", "username" => "user"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"message" => "Login successful"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "set-cookie", do: List.first(List.wrap(v)) end) != nil
    end
  end

  describe "response_status_code_200_success" do
    test "GET /success - Tests response with explicit 200 status code" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_status_code_200_success")
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"status" => "ok"}
    end
  end

  describe "response_status_code_201_created" do
    test "POST /items - Tests response with 201 Created status code" do
      {:ok, response} = Req.post(url: "#{mock_server_url()}/fixtures/response_status_code_201_created", json: %{"name" => "New Item"}, headers: [{"Content-Type", "application/json"}])
      assert response.status == 201
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"id" => "1", "name" => "New Item"}
    end
  end

  describe "response_status_code_204_no_content" do
    test "DELETE /items/1 - Tests response with 204 No Content status code" do
      {:ok, response} = Req.delete(url: "#{mock_server_url()}/fixtures/response_status_code_204_no_content")
      assert response.status == 204
      assert response.body == ""
    end
  end

  describe "response_with_custom_headers" do
    test "GET /data - Tests response includes custom headers" do
      {:ok, response} = Req.get(url: "#{mock_server_url()}/fixtures/response_with_custom_headers")
      assert response.status == 200
      body_decoded = if is_binary(response.body), do: Jason.decode!(response.body), else: response.body
      assert body_decoded == %{"data" => "value"}
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-request-id", do: List.first(List.wrap(v)) end) != nil
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-custom-header", do: List.first(List.wrap(v)) end) == "custom-value"
      assert Enum.find_value(response.headers, fn {k, v} -> if String.downcase(k) == "x-total-count", do: List.first(List.wrap(v)) end) == "42"
    end
  end
end
