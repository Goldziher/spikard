// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7cc2d07aa9e1432a9f9eef7566eb1ea0c34e82d7e2a009b8936a13901ffa280e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("graphql_schema", () => {

  it("graphql_authentication_error_variant: Tests GraphQL authentication error when auth fails", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_authentication_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { id } }" }),
    });
    expect(response.status).toBe(401);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: "<<present>>",
    });  });

  it("graphql_authorization_error_variant: Tests GraphQL authorization error when access denied", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_authorization_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Authorization": "Bearer invalid",
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ admin { id } }" }),
    });
    expect(response.status).toBe(403);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: "<<present>>",
    });  });

  it("graphql_complexity_limit_exceeded: Tests GraphQL query complexity validation rejects overly complex queries", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_complexity_limit_exceeded/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { id name email posts { id title comments { id text } } } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ message: "Query complexity limit exceeded" }],
    });  });

  it("graphql_custom_endpoint_configuration: Tests GraphQL route configured at custom endpoint", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_custom_endpoint_configuration/api/v2/query`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { id } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: {
        user: {
          id: "1",
        },
      },
    });  });

  it("graphql_depth_limit_exceeded: Tests GraphQL query depth validation rejects deeply nested queries", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_depth_limit_exceeded/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { posts { comments { author { posts { title } } } } } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ message: "Query depth limit exceeded" }],
    });  });

  it("graphql_error_response_format: Tests GraphQL error response format with correct structure", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_error_response_format/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ invalidField }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ extensions: { code: "GRAPHQL_VALIDATION_FAILED" }, message: "Cannot query field 'invalidField' on type 'Query'" }],
    });  });

  it("graphql_execution_error_variant: Tests GraphQL execution error response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_execution_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ invalidField }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ message: "<<present>>" }],
    });  });

  it("graphql_full_schema_with_subscriptions: Tests GraphQL schema with queries, mutations, and subscriptions", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_full_schema_with_subscriptions/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ __typename }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: {
        __typename: "Query",
      },
    });  });

  it("graphql_introspection_disabled: Tests GraphQL introspection returns error when disabled", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_introspection_disabled/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ __schema { types { name } } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ message: "GraphQL introspection is disabled" }],
    });  });

  it("graphql_introspection_enabled: Tests GraphQL introspection is available", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_introspection_enabled/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ __schema { types { name } } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: {
        __schema: {
          types: null,
        },
      },
    });  });

  it("graphql_invalid_input_error_variant: Tests GraphQL invalid input error", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_invalid_input_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "mutation { createUser(age: -5) { id } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ message: "<<present>>" }],
    });  });

  it("graphql_not_found_error_variant: Tests GraphQL not found error for missing resource", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_not_found_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user(id: \"nonexistent\") { id } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      errors: [{ message: "<<present>>" }],
    });  });

  it("graphql_query_and_mutation_schema: Tests GraphQL schema with queries and mutations", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_query_and_mutation_schema/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "mutation { createUser(name: \"New\") { id name } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: {
        createUser: {
          id: "2",
          name: "New",
        },
      },
    });  });

  it("graphql_query_only_schema: Tests GraphQL schema configured with queries only", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_query_only_schema/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { id name } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: {
        user: {
          id: "1",
          name: "Test User",
        },
      },
    });  });

  it("graphql_rate_limit_error_variant: Tests GraphQL rate limit exceeded error", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_rate_limit_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { id } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: "<<present>>",
    });  });

  it("graphql_schema_build_error_variant: Tests GraphQL schema build error", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_schema_build_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ test }" }),
    });
    expect(response.status).toBe(500);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: "<<present>>",
    });  });

  it("graphql_serialization_error_variant: Tests GraphQL serialization error for invalid response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/graphql_serialization_error_variant/graphql`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ query: "{ user { id } }" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: "<<present>>",
    });  });

});
