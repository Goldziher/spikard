// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c3567566f634539f6bff663db33e0a0b6daf57b79454ffe78e372c50fdefd2b7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const spikard = @import("spikard");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

// E2e tests for category: grpc

test "grpc_call_with_metadata" {
    // Tests gRPC call with request metadata headers
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_call_with_metadata/api.Service/GetData", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "authorization", .value = "Bearer token123" },
        .{ .name = "te", .value = "trailers" },
        .{ .name = "x-custom-metadata", .value = "value123" },
    };
    const body_bytes: []const u8 = "{\"id\":\"resource-1\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    // assert header 'x-response-metadata' == "processed" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"data\":\"result\",\"id\":\"resource-1\"}", response_body.written());
}

test "grpc_config_compression_enabled" {
    // Tests gRPC compression is enabled for responses
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_config_compression_enabled/api.Service/GetCompressedData", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "grpc-encoding", .value = "gzip" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"request\":\"get_data\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-encoding' == "gzip" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
}

test "grpc_config_keepalive_settings" {
    // Tests gRPC keepalive ping configuration
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_config_keepalive_settings/api.Service/LongRunning", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"task\":\"background_job\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
}

test "grpc_config_max_message_size" {
    // Tests gRPC server respects maximum message size configuration
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_config_max_message_size/api.Service/SendLargeMessage", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"data\":\"large_payload_data\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"received_size\":19}", response_body.written());
}

test "grpc_config_request_timeout" {
    // Tests gRPC request timeout configuration
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_config_request_timeout/api.Service/TimedOperation", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "grpc-timeout", .value = "5000m" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"operation\":\"process\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
}

test "grpc_deadline_exceeded" {
    // Tests gRPC DEADLINE_EXCEEDED error when timeout occurs
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_deadline_exceeded/api.Service/SlowOperation", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "grpc-timeout", .value = "100m" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"duration_ms\":60000}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "4" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"code\":\"DEADLINE_EXCEEDED\",\"message\":\"Operation exceeded deadline\"}", response_body.written());
}

test "grpc_error_invalid_argument" {
    // Tests gRPC INVALID_ARGUMENT error status response
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_error_invalid_argument/api.Service/ValidateInput", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"value\":-100}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "3" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"code\":\"INVALID_ARGUMENT\",\"message\":\"Value must be non-negative\"}", response_body.written());
}

test "grpc_error_not_found" {
    // Tests gRPC NOT_FOUND error status response
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_error_not_found/api.Service/GetItem", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"item_id\":\"nonexistent\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "5" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"code\":\"NOT_FOUND\",\"message\":\"Item not found\"}", response_body.written());
}

test "grpc_request_method_name_field" {
    // Tests gRPC request includes method_name field
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_request_method_name_field/api.Service/GetData", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"id\":\"resource-1\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"data\":\"result\",\"method\":\"GetData\"}", response_body.written());
}

test "grpc_request_service_name_field" {
    // Tests gRPC request includes service_name field
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_request_service_name_field/helloworld.Greeter/SayHello", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"name\":\"World\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"message\":\"Hello World\",\"service\":\"helloworld.Greeter\"}", response_body.written());
}

test "grpc_request_with_custom_metadata" {
    // Tests gRPC request includes custom metadata in trailers
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_request_with_custom_metadata/api.Service/ProcessWithMetadata", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
        .{ .name = "x-request-id", .value = "req-12345" },
        .{ .name = "x-trace-id", .value = "trace-abc123" },
        .{ .name = "x-user-id", .value = "user-42" },
    };
    const body_bytes: []const u8 = "{\"data\":{\"key\":\"value\"},\"request_id\":\"req-12345\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    // assert header 'x-response-trace-id' == "trace-abc123" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"processed\":true,\"request_id\":\"req-12345\"}", response_body.written());
}

test "grpc_response_with_metadata_trailers" {
    // Tests gRPC response includes metadata in trailers
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_response_with_metadata_trailers/api.Service/MetadataResponse", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"request\":\"test\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    // assert header 'x-metadata-key' == "metadata-value" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"result\":\"success\"}", response_body.written());
}

test "grpc_unary_call_success" {
    // Tests unary gRPC call with successful response
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();
    var url_buf: [512]u8 = undefined;
    const url = try std.fmt.bufPrint(&url_buf, "{s}/fixtures/grpc_unary_call_success/helloworld.Greeter/SayHello", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    const headers = [_]std.http.Header{
        .{ .name = "Content-Type", .value = "application/grpc" },
        .{ .name = "te", .value = "trailers" },
    };
    const body_bytes: []const u8 = "{\"name\":\"World\"}";
    var threaded = std.Io.Threaded.init(allocator, .{});
    defer threaded.deinit();
    const io = threaded.io();
    var http_client = std.http.Client{ .allocator = allocator, .io = io };
    defer http_client.deinit();
    var response_body = std.Io.Writer.Allocating.init(allocator);
    defer response_body.deinit();
    const response = try http_client.fetch(.{ .location = .{ .url = url }, .method = .POST, .extra_headers = &headers, .payload = body_bytes, .keep_alive = false, .redirect_behavior = .unhandled, .response_writer = &response_body.writer });
    try testing.expectEqual(@as(u10, 200), @intFromEnum(response.status));
    // assert header 'content-type' == "application/grpc" (header inspection not yet implemented)
    // assert header 'grpc-status' == "0" (header inspection not yet implemented)
    try testing.expectEqualStrings("{\"message\":\"Hello World\"}", response_body.written());
}
