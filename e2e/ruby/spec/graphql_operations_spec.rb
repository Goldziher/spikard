# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:e795e4f6668b39f4dab9ec8d4aedd564da95692620a6c7e2e7a98809fdaee124
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# frozen_string_literal: true

require "spec_helper"
require "spikard"
require "json"

RSpec.describe "graphql_operations" do
  let(:mock_server_url) { ENV.fetch("MOCK_SERVER_URL", "http://localhost:8080") }
  describe "graphql_field_level_error_partial_data" do
    it "Tests that a field-level resolver error returns partial data alongside an errors array." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_field_level_error_partial_data/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"query" => "{ user(id: \"missing-user\") { id name avatar } status }"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(
        eq(
          {
            "data" => {"status" => "ok", "user" => nil},
            "errors" => [
              {"locations" => [{"column" => 3, "line" => 1}], "message" => "User not found", "path" => ["user"]}
            ]
          }
        )
      )
    end
  end

  describe "graphql_introspection_query" do
    it(
      "Tests that the __schema introspection query returns the schema's query root type name via real introspection execution."
    ) do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_introspection_query/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"query" => "{ __schema { queryType { name } } }"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => {"__schema" => {"queryType" => {"name" => "Query"}}}}))
    end
  end

  describe "graphql_invalid_syntax_rejected" do
    it "Tests that a query with invalid GraphQL syntax returns a syntax error in the errors array." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_invalid_syntax_rejected/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"query" => "{ hello { this is not valid syntax {{{"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => nil, "errors" => [{"message" => "<<present>>"}]}))
    end
  end

  describe "graphql_multi_operation_with_operation_name" do
    it "Tests executing a specific named operation from a document containing multiple operations." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_multi_operation_with_operation_name/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"operationName" => "Version", "query" => "query Ping { ping } query Version { version }"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => {"version" => "1.0.0"}}))
    end
  end

  describe "graphql_mutation_create_object" do
    it "Tests GraphQL mutation with an input type that returns the newly created object." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_mutation_create_object/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump(
        {
          "query" => "mutation CreateUser($input: CreateUserInput!) { createUser(input: $input) { id name email } }",
          "variables" => {"input" => {"email" => "carol@example.com", "name" => "Carol"}}
        }
      )
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(
        eq({"data" => {"createUser" => {"email" => "carol@example.com", "id" => "new-user-id", "name" => "Carol"}}})
      )
    end
  end

  describe "graphql_ops_complexity_limit_exceeded" do
    it "Tests that a query exceeding the configured complexity limit returns an error." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_ops_complexity_limit_exceeded/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump(
        {"query" => "{ users { id friends { id friends { id friends { id friends { id friends { id } } } } } } }"}
      )
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => nil, "errors" => [{"message" => "Query is too complex."}]}))
    end
  end

  describe "graphql_ops_depth_limit_exceeded" do
    it "Tests that a query exceeding the configured depth limit returns an error." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_ops_depth_limit_exceeded/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"query" => "{ a { b { c { d { e } } } } }"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => nil, "errors" => [{"message" => "Query is nested too deep."}]}))
    end
  end

  describe "graphql_query_nested_fields" do
    it "Tests GraphQL query selecting nested fields at depth 2-3." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_query_nested_fields/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump(
        {"query" => "{ order(id: \"ORD-1\") { id customer { name address { city } } items { sku quantity } } }"}
      )
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(
        eq(
          {
            "data" => {
              "order" => {
                "customer" => {"address" => {"city" => "Springfield"}, "name" => "Bob"},
                "id" => "ORD-1",
                "items" => [{"quantity" => 2, "sku" => "WIDGET-A"}]
              }
            }
          }
        )
      )
    end
  end

  describe "graphql_query_with_variables" do
    it "Tests GraphQL query that uses variables passed in the request body." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_query_with_variables/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump(
        {"query" => "query GetUser($id: ID!) { user(id: $id) { id name } }", "variables" => {"id" => "user-42"}}
      )
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => {"user" => {"id" => "user-42", "name" => "Alice"}}}))
    end
  end

  describe "graphql_simple_query" do
    it "Tests simple GraphQL query execution returning data from the root query type." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_simple_query/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"query" => "{ hello }"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(eq({"data" => {"hello" => "world"}}))
    end
  end

  describe "graphql_subscription_declaration" do
    it(
      "Tests that a subscription operation type is accepted by SDL/schema validation, and that executing it over the plain-HTTP (non-streaming) transport is rejected the way real GraphQL engines reject subscriptions on transports that cannot stream events."
    ) do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_subscription_declaration/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump(
        {
          "query" => "subscription OnMessageAdded($roomId: ID!) { messageAdded(roomId: $roomId) { id text authorId } }",
          "variables" => {"roomId" => "room-1"}
        }
      )
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(
        eq({"data" => nil, "errors" => [{"message" => "Subscriptions are not supported on this transport."}]})
      )
    end
  end

  describe "graphql_undefined_field_error" do
    it "Tests that querying a field that does not exist in the schema returns a validation error." do
      require "net/http"
      require "uri"
      require "json"

      base_url = ENV.fetch("SUT_URL", "http://127.0.0.1:8000")
      uri = URI.parse("#{base_url}/fixtures/graphql_undefined_field_error/graphql")
      http = Net::HTTP.new(uri.host, uri.port)
      http.use_ssl = uri.scheme == "https"
      _headers = {
        "Content-Type" => "application/json"
      }.dup
      _headers["Content-Type"] ||= "application/json"
      _req = Net::HTTP::Post.new(uri.request_uri, _headers)
      _req.body = JSON.dump({"query" => "{ hello nonExistentField }"})
      response = http.request(_req)
      expect(response.code.to_i).to(eq(200))
      _body = response.body && !response.body.empty? ? JSON.parse(response.body) : nil
      expect(_body).to(
        eq(
          {
            "data" => nil,
            "errors" => [
              {
                "locations" => [{"column" => 9, "line" => 1}],
                "message" => "Unknown field \"nonExistentField\" on type \"Query\"."
              }
            ]
          }
        )
      )
    end
  end
end
