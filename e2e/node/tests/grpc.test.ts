// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:70d6afb04d5fecb0ab2f4ebe104a87ee02bf617fd393e24a5daaaa0b5ba34da1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("grpc", () => {

  it("grpc_call_with_metadata: Tests gRPC call with request metadata headers", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_call_with_metadata/api.Service/GetData`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "authorization": "Bearer token123",
        "te": "trailers",
        "x-custom-metadata": "value123",
      },
      body: JSON.stringify({ id: "resource-1" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: "result",
      id: "resource-1",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");    expect(response.headers.get("x-response-metadata")).toBe("processed");  });

  it("grpc_config_compression_enabled: Tests gRPC compression is enabled for responses", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_config_compression_enabled/api.Service/GetCompressedData`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "grpc-encoding": "gzip",
        "te": "trailers",
      },
      body: JSON.stringify({ request: "get_data" }),
    });
    expect(response.status).toBe(200);    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-encoding")).toBe("gzip");    expect(response.headers.get("grpc-status")).toBe("0");  });

  it("grpc_config_keepalive_settings: Tests gRPC keepalive ping configuration", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_config_keepalive_settings/api.Service/LongRunning`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ task: "background_job" }),
    });
    expect(response.status).toBe(200);    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");  });

  it("grpc_config_max_message_size: Tests gRPC server respects maximum message size configuration", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_config_max_message_size/api.Service/SendLargeMessage`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ data: "large_payload_data" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      received_size: 19,
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");  });

  it("grpc_config_request_timeout: Tests gRPC request timeout configuration", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_config_request_timeout/api.Service/TimedOperation`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "grpc-timeout": "5000m",
        "te": "trailers",
      },
      body: JSON.stringify({ operation: "process" }),
    });
    expect(response.status).toBe(200);    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");  });

  it("grpc_deadline_exceeded: Tests gRPC DEADLINE_EXCEEDED error when timeout occurs", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_deadline_exceeded/api.Service/SlowOperation`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "grpc-timeout": "100m",
        "te": "trailers",
      },
      body: JSON.stringify({ duration_ms: 60000 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      code: "DEADLINE_EXCEEDED",
      message: "Operation exceeded deadline",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("4");  });

  it("grpc_error_invalid_argument: Tests gRPC INVALID_ARGUMENT error status response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_error_invalid_argument/api.Service/ValidateInput`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ value: -100 }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      code: "INVALID_ARGUMENT",
      message: "Value must be non-negative",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("3");  });

  it("grpc_error_not_found: Tests gRPC NOT_FOUND error status response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_error_not_found/api.Service/GetItem`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ item_id: "nonexistent" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      code: "NOT_FOUND",
      message: "Item not found",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("5");  });

  it("grpc_request_method_name_field: Tests gRPC request includes method_name field", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_request_method_name_field/api.Service/GetData`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ id: "resource-1" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      data: "result",
      method: "GetData",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");  });

  it("grpc_request_service_name_field: Tests gRPC request includes service_name field", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_request_service_name_field/helloworld.Greeter/SayHello`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ name: "World" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Hello World",
      service: "helloworld.Greeter",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");  });

  it("grpc_request_with_custom_metadata: Tests gRPC request includes custom metadata in trailers", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_request_with_custom_metadata/api.Service/ProcessWithMetadata`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
        "x-request-id": "req-12345",
        "x-trace-id": "trace-abc123",
        "x-user-id": "user-42",
      },
      body: JSON.stringify({ data: { key: "value" }, request_id: "req-12345" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      processed: true,
      request_id: "req-12345",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");    expect(response.headers.get("x-response-trace-id")).toBe("trace-abc123");  });

  it("grpc_response_with_metadata_trailers: Tests gRPC response includes metadata in trailers", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_response_with_metadata_trailers/api.Service/MetadataResponse`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ request: "test" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      result: "success",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");    expect(response.headers.get("x-metadata-key")).toBe("metadata-value");  });

  it("grpc_unary_call_success: Tests unary gRPC call with successful response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/grpc_unary_call_success/helloworld.Greeter/SayHello`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/grpc",
        "te": "trailers",
      },
      body: JSON.stringify({ name: "World" }),
    });
    expect(response.status).toBe(200);    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      message: "Hello World",
    });    expect(response.headers.get("content-type")).toBe("application/grpc");    expect(response.headers.get("grpc-status")).toBe("0");  });

});
