// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4ce867295513d72598ed022b33915fb85feedac9e083c29792218922d00f5aa8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';

describe('body_limits', () => {
  it('body_over_limit_returns_413: Requests that exceed the configured max body size should be rejected with Payload Too Large.', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/body_over_limit_returns_413`;
    const response = await fetch(mockUrl, { method: 'POST', body: JSON.stringify({ note: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" }) });
    expect(response.status).toBe(413);
  });

  it('body_under_limit_succeeds: Ensures requests smaller than the configured body limit are accepted.', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/body_under_limit_succeeds`;
    const response = await fetch(mockUrl, { method: 'POST', body: JSON.stringify({ note: "small" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ accepted: true, note: "small" });
  });
});
