// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f2c04bb99fd35788a6ffdc6a115217d6bf1b51843835dada2d360cbe67f496cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
  const contentEncoding = response.headers.get("content-encoding");
  let buffer = await response.arrayBuffer();

  if (contentEncoding === "gzip") {
    const { gunzipSync } = await import("zlib");
    buffer = gunzipSync(new Uint8Array(buffer)).buffer;
  } else if (contentEncoding === "br") {
    const { brotliDecompressSync } = await import("zlib");
    buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
  }

  const text = new TextDecoder().decode(buffer);
  return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("jsonrpc", () => {
  it("jsonrpc_batch_request: Tests JSON-RPC batch request with multiple calls", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_batch_request/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify([
        { id: 1, jsonrpc: "2.0", method: "add", params: [1, 2] },
        { id: 2, jsonrpc: "2.0", method: "subtract", params: [5, 3] },
      ]),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual([
      { id: 1, jsonrpc: "2.0", result: 3 },
      { id: 2, jsonrpc: "2.0", result: 2 },
    ]);
  });

  it("jsonrpc_call_with_positional_params: Tests JSON-RPC call with positional array parameters", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_call_with_positional_params/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ id: 2, jsonrpc: "2.0", method: "multiply", params: [4, 7] }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 2,
      jsonrpc: "2.0",
      result: 28,
    });
  });

  it("jsonrpc_config_batch_disabled: Tests JSON-RPC batch requests disabled returns error", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_config_batch_disabled/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify([
        { id: 1, jsonrpc: "2.0", method: "add", params: [1, 2] },
        { id: 2, jsonrpc: "2.0", method: "subtract", params: [5, 3] },
      ]),
    });
    expect(response.status).toBe(400);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: {
        code: -32600,
        message: "Batch requests not enabled",
      },
      jsonrpc: "2.0",
    });
  });

  it("jsonrpc_config_custom_endpoint_path: Tests JSON-RPC server at custom endpoint path", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_config_custom_endpoint_path/api/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ id: 1, jsonrpc: "2.0", method: "test", params: {} }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 1,
      jsonrpc: "2.0",
      result: "ok",
    });
  });

  it("jsonrpc_config_max_batch_size: Tests JSON-RPC batch request respects maximum batch size", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_config_max_batch_size/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify([
        { id: 1, jsonrpc: "2.0", method: "method1" },
        { id: 2, jsonrpc: "2.0", method: "method2" },
        { id: 3, jsonrpc: "2.0", method: "method3" },
      ]),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual([
      { id: 1, jsonrpc: "2.0", result: "result1" },
      { id: 2, jsonrpc: "2.0", result: "result2" },
      { id: 3, jsonrpc: "2.0", result: "result3" },
    ]);
  });

  it("jsonrpc_deprecated_method_flag: Tests JSON-RPC method marked as deprecated", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_deprecated_method_flag/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ id: 1, jsonrpc: "2.0", method: "oldMethod", params: {} }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 1,
      jsonrpc: "2.0",
      result: "deprecated",
    });
    expect(response.headers.get("deprecation")).toBe("true");
    expect(response.headers.get("sunset")).not.toBeNull();
  });

  it("jsonrpc_error_response: Tests JSON-RPC error response with error object", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_error_response/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        id: 3,
        jsonrpc: "2.0",
        method: "divide",
        params: { denominator: 0, numerator: 10 },
      }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: {
        code: -32600,
        data: "Division by zero",
        message: "Invalid Request",
      },
      id: 3,
      jsonrpc: "2.0",
    });
  });

  it("jsonrpc_invalid_params: Tests JSON-RPC error for invalid parameters", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_invalid_params/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        id: 5,
        jsonrpc: "2.0",
        method: "subtract",
        params: { wrong_field: 42 },
      }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: {
        code: -32602,
        message: "Invalid params",
      },
      id: 5,
      jsonrpc: "2.0",
    });
  });

  it("jsonrpc_method_not_found: Tests JSON-RPC error when method does not exist", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_method_not_found/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ id: 4, jsonrpc: "2.0", method: "nonexistent_method" }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      error: {
        code: -32601,
        message: "Method not found",
      },
      id: 4,
      jsonrpc: "2.0",
    });
  });

  it("jsonrpc_method_parameter_validation: Tests JSON-RPC validates parameter types and values", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_method_parameter_validation/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        id: 6,
        jsonrpc: "2.0",
        method: "update_user",
        params: { email: "john@example.com", id: 123, name: "John Doe" },
      }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 6,
      jsonrpc: "2.0",
      result: {
        email: "john@example.com",
        id: 123,
        name: "John Doe",
        updated: true,
      },
    });
  });

  it("jsonrpc_method_with_params_schema: Tests JSON-RPC method params validated against schema", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_method_with_params_schema/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        id: 1,
        jsonrpc: "2.0",
        method: "createUser",
        params: { age: 30, name: "Alice" },
      }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 1,
      jsonrpc: "2.0",
      result: {
        age: 30,
        id: 1,
        name: "Alice",
      },
    });
  });

  it("jsonrpc_method_with_result_schema: Tests JSON-RPC method result schema validation", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_method_with_result_schema/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ id: 1, jsonrpc: "2.0", method: "getUser", params: { id: 1 } }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 1,
      jsonrpc: "2.0",
      result: {
        email: "john@example.com",
        id: 1,
        name: "John",
      },
    });
  });

  it("jsonrpc_notification_no_id: Tests JSON-RPC notification (no response expected)", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_notification_no_id/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        jsonrpc: "2.0",
        method: "notify_event",
        params: { event: "user_login" },
      }),
    });
    expect(response.status).toBe(204);
  });

  it("jsonrpc_notification_no_response: Tests JSON-RPC notification (no id) doesn't return response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_notification_no_response/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ jsonrpc: "2.0", method: "notify", params: { message: "hello" } }),
    });
    expect(response.status).toBe(204);
  });

  it("jsonrpc_single_call_success: Tests single JSON-RPC 2.0 call with successful response", async () => {
    const sutUrl = process.env.SUT_URL || "http://127.0.0.1:8001";
    const url = `${sutUrl}/fixtures/jsonrpc_single_call_success/rpc`;
    const response = await fetch(url, {
      method: "POST",
      redirect: "manual",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ id: 1, jsonrpc: "2.0", method: "add", params: { a: 5, b: 3 } }),
    });
    expect(response.status).toBe(200);
    const data = await _alefE2eDecompressAndParseJson(response);
    expect(data).toEqual({
      id: 1,
      jsonrpc: "2.0",
      result: 8,
    });
  });
});
