// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c584fc66dd4acd502e11db7f3c9523b7b483f7634e05e1006c53cb2de059e1f0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';

describe('cookies', () => {
  it('_cookie_samesite_strict: Cookie with SameSite=Strict attribute should be validated', async () => {
    const response = await app.request('/secure', { method: 'GET' });
    expect(response.status).toBe(200);
  });

  it('_cookie_samesite_lax: Cookie with SameSite=Lax attribute should be validated', async () => {
    const response = await app.request('/data', { method: 'GET' });
    expect(response.status).toBe(200);
  });

  it('_cookie_secure_flag: Cookie with Secure flag should be validated for HTTPS', async () => {
    const response = await app.request('/secure', { method: 'GET' });
    expect(response.status).toBe(200);
  });

  it('_cookie_httponly_flag: Cookie with HttpOnly flag should prevent JavaScript access', async () => {
    const response = await app.request('/secure', { method: 'GET' });
    expect(response.status).toBe(200);
  });

  it('apikey_cookie_authentication_missing: Tests APIKeyCookie authentication returns 403 when cookie missing', async () => {
    const response = await app.request('/users/me/auth', { method: 'GET' });
    expect(response.status).toBe(422);
    const data = await response.json();
    expect(data).toEqual({ detail: "1 validation error in request", errors: [{ input: null, loc: ["cookie", "key"], msg: "Field required", type: "missing" }], status: 422, title: "Request Validation Failed", type: "https://spikard.dev/errors/validation-error" });
  });

  it('apikey_cookie_authentication_success: Tests APIKeyCookie authentication with valid cookie', async () => {
    const response = await app.request('/users/me', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ username: "secret" });
  });

  it('cookie_regex_pattern_validation_fail: Tests cookie with regex pattern validation failure', async () => {
    const response = await app.request('/cookies/pattern', { method: 'GET' });
    expect(response.status).toBe(422);
    const data = await response.json();
    expect(data).toEqual({ detail: "1 validation error in request", errors: [{ ctx: { pattern: "^[A-Z0-9]{8}$" }, input: "invalid-format", loc: ["cookie", "tracking_id"], msg: "String should match pattern '^[A-Z0-9]{8}$'", type: "string_pattern_mismatch" }], status: 422, title: "Request Validation Failed", type: "https://spikard.dev/errors/validation-error" });
  });

  it('cookie_regex_pattern_validation_success: Tests cookie with regex pattern validation success', async () => {
    const response = await app.request('/cookies/pattern', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ tracking_id: "ABC12345" });
  });

  it('cookie_validation_max_length_constraint_fail: Tests cookie validation with max_length constraint failure', async () => {
    const response = await app.request('/cookies/validated', { method: 'GET' });
    expect(response.status).toBe(422);
    const data = await response.json();
    expect(data).toEqual({ detail: "1 validation error in request", errors: [{ ctx: { max_length: 20 }, input: "this_cookie_value_is_way_too_long", loc: ["cookie", "session_id"], msg: "String should have at most 20 characters", type: "string_too_long" }], status: 422, title: "Request Validation Failed", type: "https://spikard.dev/errors/validation-error" });
  });

  it('cookie_validation_min_length_constraint_success: Tests cookie validation with min_length constraint at boundary', async () => {
    const response = await app.request('/cookies/min-length', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ token: "abc" });
  });

  it('cookie_validation_min_length_failure: Tests cookie parameter with min_length constraint returns 422 when too short', async () => {
    const response = await app.request('/items/', { method: 'GET' });
    expect(response.status).toBe(422);
    const data = await response.json();
    expect(data).toEqual({ detail: "1 validation error in request", errors: [{ input: "", loc: ["cookie", "tracking_id"], msg: "String should have at least 3 characters", type: "string_too_short" }], status: 422, title: "Request Validation Failed", type: "https://spikard.dev/errors/validation-error" });
  });

  it('multiple_cookies_success: Tests multiple cookie parameters in a single request', async () => {
    const response = await app.request('/items/', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ fatebook_tracker: "tracker456", googall_tracker: "ga789", session_id: "session123" });
  });

  it('optional_apikey_cookie_missing: Tests optional APIKeyCookie (auto_error=False) returns None without 403', async () => {
    const response = await app.request('/users/me', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ msg: "Create an account first" });
  });

  it('optional_cookie_parameter_missing: Tests optional cookie parameter returns None when not provided', async () => {
    const response = await app.request('/items/', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ ads_id: null });
  });

  it('optional_cookie_parameter_success: Tests optional cookie parameter with value provided', async () => {
    const response = await app.request('/items/', { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ ads_id: "abc123" });
  });

  it('required_cookie_missing: Tests validation error when required cookie is missing', async () => {
    const response = await app.request('/items/cookies', { method: 'GET' });
    expect(response.status).toBe(422);
    const data = await response.json();
    expect(data).toEqual({ detail: "1 validation error in request", errors: [{ input: "", loc: ["cookie", "session_id"], msg: "Field required", type: "missing" }], status: 422, title: "Request Validation Failed", type: "https://spikard.dev/errors/validation-error" });
  });

  it('response_cookie_with_domain_attribute: Tests setting cookie with specific domain', async () => {
    const response = await app.request('/cookies/set-with-domain', { method: 'POST', body: JSON.stringify({ value: "domain_test" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Cookie set with domain" });
  });

  it('response_cookie_with_path_attribute: Tests setting cookie with specific path', async () => {
    const response = await app.request('/cookies/set-with-path', { method: 'POST', body: JSON.stringify({ value: "path_test" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Cookie set with path" });
  });

  it('response_cookie_with_samesite_lax: Tests setting cookie with SameSite lax attribute', async () => {
    const response = await app.request('/cookies/samesite-lax', { method: 'POST', body: JSON.stringify({ value: "lax_cookie" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Cookie set with SameSite=Lax" });
  });

  it('response_cookie_with_samesite_none: Tests setting cookie with SameSite none (requires Secure)', async () => {
    const response = await app.request('/cookies/samesite-none', { method: 'POST', body: JSON.stringify({ value: "none_cookie" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Cookie set with SameSite=None" });
  });

  it('response_cookie_with_samesite_strict: Tests setting cookie with SameSite strict attribute', async () => {
    const response = await app.request('/cookies/samesite-strict', { method: 'POST', body: JSON.stringify({ value: "strict_cookie" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Cookie set with SameSite=Strict" });
  });

  it('response_delete_cookie: Tests deleting a cookie by setting max_age to 0', async () => {
    const response = await app.request('/cookies/delete', { method: 'POST' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Cookie deleted" });
  });

  it('response_multiple_cookies: Tests setting multiple cookies in single response', async () => {
    const response = await app.request('/cookies/multiple', { method: 'POST', body: JSON.stringify({ session: "session123", user: "john" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Multiple cookies set" });
  });

  it('response_session_cookie_no_max_age: Tests setting session cookie without max_age (expires with browser)', async () => {
    const response = await app.request('/cookies/session', { method: 'POST', body: JSON.stringify({ value: "session_abc123" }) });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Session cookie set" });
  });

  it('response_set_cookie_basic: Tests setting a cookie in the response', async () => {
    const response = await app.request('/cookie/', { method: 'POST' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ message: "Come to the dark side, we have cookies" });
  });
});
