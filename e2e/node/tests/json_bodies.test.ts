// This file is auto-generated by alef. DO NOT EDIT.
import { describe, expect, it } from "vitest";

describe("json_bodies", () => {
	it("_nested_object_validation_success: Nested object in JSON body should validate correctly", async () => {
		const response = await app.request("/users", {
			method: "POST",
			body: JSON.stringify({ profile: { email: "john@example.com", name: "John Doe" } }),
		});
		expect(response.status).toBe(201);
	});

	it("_nested_object_missing_field: Nested object missing required field should fail validation", async () => {
		const response = await app.request("/users", {
			method: "POST",
			body: JSON.stringify({ profile: { name: "John Doe" } }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{ input: { name: "John Doe" }, loc: ["body", "profile", "email"], msg: "Field required", type: "missing" },
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_nullable_property_null_value: Nullable property with null value should be accepted", async () => {
		const response = await app.request("/users", {
			method: "POST",
			body: JSON.stringify({ description: null, name: "Test User" }),
		});
		expect(response.status).toBe(201);
	});

	it("_schema_ref_definitions: JSON Schema $ref with definitions should validate correctly", async () => {
		const response = await app.request("/products", {
			method: "POST",
			body: JSON.stringify({ product: { name: "Widget", price: 9.99 } }),
		});
		expect(response.status).toBe(201);
	});

	it("_allof_schema_composition: JSON Schema allOf composition should validate all schemas", async () => {
		const response = await app.request("/items", {
			method: "POST",
			body: JSON.stringify({ name: "Product", price: 29.99 }),
		});
		expect(response.status).toBe(201);
	});

	it("_additional_properties_false: Schema with additionalProperties false should reject extra fields", async () => {
		const response = await app.request("/users", {
			method: "POST",
			body: JSON.stringify({ email: "john@example.com", extra_field: "should fail", name: "John" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { additional_properties: false, unexpected_field: "extra_field" },
					input: { email: "john@example.com", extra_field: "should fail", name: "John" },
					loc: ["body", "extra_field"],
					msg: "Additional properties are not allowed",
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_oneof_schema_success: oneOf schema composition - exactly one schema must match", async () => {
		const response = await app.request("/payment", {
			method: "POST",
			body: JSON.stringify({ credit_card: "1234567812345678" }),
		});
		expect(response.status).toBe(201);
	});

	it("_oneof_schema_multiple_match_failure: oneOf schema composition - fails when multiple schemas match", async () => {
		const response = await app.request("/payment", {
			method: "POST",
			body: JSON.stringify({ credit_card: "1234567812345678", paypal_email: "user@example.com" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { credit_card: "1234567812345678", paypal_email: "user@example.com" },
					loc: ["body"],
					msg: '{"credit_card":"1234567812345678","paypal_email":"user@example.com"} is valid under more than one of the schemas listed in the \'oneOf\' keyword',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_oneof_schema_no_match_failure: oneOf schema composition - fails when no schemas match", async () => {
		const response = await app.request("/payment", {
			method: "POST",
			body: JSON.stringify({ bitcoin_address: "1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { bitcoin_address: "1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa" },
					loc: ["body"],
					msg: '{"bitcoin_address":"1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa"} is not valid under any of the schemas listed in the \'oneOf\' keyword',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_anyof_schema_success: anyOf schema composition - at least one schema must match", async () => {
		const response = await app.request("/contact", {
			method: "POST",
			body: JSON.stringify({ email: "john@example.com", name: "John Doe" }),
		});
		expect(response.status).toBe(201);
	});

	it("_anyof_schema_multiple_match_success: anyOf schema composition - succeeds when multiple schemas match", async () => {
		const response = await app.request("/contact", {
			method: "POST",
			body: JSON.stringify({ email: "john@example.com", name: "John Doe", phone: "+1-555-0100" }),
		});
		expect(response.status).toBe(201);
	});

	it("_anyof_schema_failure: anyOf schema composition - fails when no schemas match", async () => {
		const response = await app.request("/contact", { method: "POST", body: JSON.stringify({ name: "John Doe" }) });
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { name: "John Doe" },
					loc: ["body"],
					msg: '{"name":"John Doe"} is not valid under any of the schemas listed in the \'anyOf\' keyword',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_not_schema_success: not schema - value must not match the schema", async () => {
		const response = await app.request("/users", { method: "POST", body: JSON.stringify({ username: "john_doe" }) });
		expect(response.status).toBe(201);
	});

	it("_not_schema_failure: not schema - fails when value matches the prohibited schema", async () => {
		const response = await app.request("/users", { method: "POST", body: JSON.stringify({ username: "admin" }) });
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: "admin",
					loc: ["body", "username"],
					msg: '{"enum":["admin","root","system"]} is not allowed for "admin"',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_const_validation_success: Field with const constraint matching exact value should succeed", async () => {
		const response = await app.request("/api/v1/data", {
			method: "POST",
			body: JSON.stringify({ data: "test", version: "1.0" }),
		});
		expect(response.status).toBe(201);
	});

	it("_const_validation_failure: Field with const constraint not matching exact value should fail", async () => {
		const response = await app.request("/api/v1/data", {
			method: "POST",
			body: JSON.stringify({ data: "test", version: "2.0" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [{ input: "2.0", loc: ["body", "version"], msg: '"1.0" was expected', type: "validation_error" }],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_minproperties_validation_success: Object with properties meeting minProperties constraint should succeed", async () => {
		const response = await app.request("/config", {
			method: "POST",
			body: JSON.stringify({ host: "localhost", port: 8080 }),
		});
		expect(response.status).toBe(201);
	});

	it("_minproperties_validation_failure: Object with fewer properties than minProperties should fail", async () => {
		const response = await app.request("/config", { method: "POST", body: JSON.stringify({ host: "localhost" }) });
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { host: "localhost" },
					loc: ["body"],
					msg: '{"host":"localhost"} has less than 2 properties',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_maxproperties_validation_failure: Object with more properties than maxProperties should fail", async () => {
		const response = await app.request("/config", {
			method: "POST",
			body: JSON.stringify({ debug: false, host: "localhost", port: 8080, ssl: true }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { debug: false, host: "localhost", port: 8080, ssl: true },
					loc: ["body"],
					msg: '{"host":"localhost","port":8080,"ssl":true,"debug":false} has more than 3 properties',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_dependencies_validation_success: Dependencies constraint - when A present, B is required and provided", async () => {
		const response = await app.request("/billing", {
			method: "POST",
			body: JSON.stringify({ billing_address: "123 Main St", credit_card: "1234567812345678", name: "John Doe" }),
		});
		expect(response.status).toBe(201);
	});

	it("_dependencies_validation_failure: Dependencies constraint - when A present, B is required but missing", async () => {
		const response = await app.request("/billing", {
			method: "POST",
			body: JSON.stringify({ credit_card: "1234567812345678", name: "John Doe" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { credit_card: "1234567812345678", name: "John Doe" },
					loc: ["body"],
					msg: '"billing_address" is a required property',
					type: "validation_error",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("_deep_nesting_4_levels: Deeply nested object with 4+ levels should validate correctly", async () => {
		const response = await app.request("/data", {
			method: "POST",
			body: JSON.stringify({ user: { profile: { contact: { address: { street: "123 Main St" } } } } }),
		});
		expect(response.status).toBe(201);
	});

	it("array_of_objects_success: Tests array field containing objects", async () => {
		const response = await app.request("/items/list", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({
				images: [
					{ name: "Front", url: "https://example.com/img1.jpg" },
					{ name: "Back", url: "https://example.com/img2.jpg" },
				],
				name: "Product Bundle",
				tags: ["electronics", "gadget"],
			}),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({
			images: [
				{ name: "Front", url: "https://example.com/img1.jpg" },
				{ name: "Back", url: "https://example.com/img2.jpg" },
			],
			name: "Product Bundle",
			tags: ["electronics", "gadget"],
		});
	});

	it("array_of_primitive_values: Tests array field containing primitive values (strings, numbers)", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Product", ratings: [4.5, 4.8, 5.0, 4.2], tags: ["electronics", "gadget", "new"] }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ name: "Product", ratings: [4.5, 4.8, 5.0, 4.2], tags: ["electronics", "gadget", "new"] });
	});

	it("body_with_query_parameters: Tests JSON body combined with query parameters", async () => {
		const response = await app.request("/items/?limit=10?limit=10", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Item", price: 42.0 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ item: { name: "Item", price: 42.0 }, limit: 10 });
	});

	it("boolean_field_success: Tests JSON object with boolean field", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ in_stock: true, name: "Item", price: 42.0 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ in_stock: true, name: "Item", price: 42.0 });
	});

	it("date_field_success: Tests date field with ISO date format", async () => {
		const response = await app.request("/events/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ event_date: "2024-03-15", name: "Conference" }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ event_date: "2024-03-15", name: "Conference" });
	});

	it("datetime_field_success: Tests datetime field with ISO datetime format", async () => {
		const response = await app.request("/events/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ created_at: "2024-03-15T10:30:00Z", name: "Meeting" }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ created_at: "2024-03-15T10:30:00Z", name: "Meeting" });
	});

	it("deeply_nested_objects: Tests deeply nested JSON structure (3+ levels)", async () => {
		const response = await app.request("/items/nested", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({
				name: "Product",
				price: 100.0,
				seller: {
					address: { city: "Springfield", country: { code: "US", name: "USA" }, street: "123 Main St" },
					name: "John Doe",
				},
			}),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({
			name: "Product",
			price: 100.0,
			seller: {
				address: { city: "Springfield", country: { code: "US", name: "USA" }, street: "123 Main St" },
				name: "John Doe",
			},
		});
	});

	it("empty_array_validation_fail: Tests array field with min_items constraint failure", async () => {
		const response = await app.request("/items/list-validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Product", tags: [] }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { min_length: 1 },
					input: [],
					loc: ["body", "tags"],
					msg: "List should have at least 1 item after validation",
					type: "too_short",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("empty_json_object: Tests empty JSON object when all fields are optional", async () => {
		const response = await app.request("/items/optional-all", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({}),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ description: null, name: null, price: null, tax: null });
	});

	it("enum_field_invalid_value: Tests enum field with value not in enum", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ category: "furniture", name: "Item" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { expected: "'electronics', 'clothing' or 'books'" },
					input: "furniture",
					loc: ["body", "category"],
					msg: "Input should be 'electronics', 'clothing' or 'books'",
					type: "enum",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("enum_field_success: Tests enum field with valid enum value", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ category: "electronics", name: "Item" }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ category: "electronics", name: "Item" });
	});

	it("extra_fields_ignored_no_additionalproperties: Tests that extra fields not in model are ignored", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ another_extra: 123, extra_field: "this should be ignored", name: "Item", price: 42.0 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ name: "Item", price: 42.0 });
	});

	it("field_type_validation_invalid_type: Tests type validation error when field has wrong type", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ description: "A very nice Item", name: "Foo", price: "not a number", tax: 3.2 }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: "not a number",
					loc: ["body", "price"],
					msg: "Input should be a valid number, unable to parse string as a number",
					type: "float_parsing",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("nested_object_success: Tests nested JSON objects", async () => {
		const response = await app.request("/items/nested", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({
				image: { name: "Product Image", url: "https://example.com/image.jpg" },
				name: "Foo",
				price: 42.0,
			}),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({
			image: { name: "Product Image", url: "https://example.com/image.jpg" },
			name: "Foo",
			price: 42.0,
		});
	});

	it("null_value_for_optional_field: Tests explicitly setting optional field to null", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ description: null, name: "Item", price: 42.0, tax: null }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ description: null, name: "Item", price: 42.0, tax: null });
	});

	it("numeric_ge_validation_fail: Tests numeric field with ge (greater than or equal) constraint failure", async () => {
		const response = await app.request("/items/validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Item", price: 0.5 }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { ge: 1 },
					input: 0.5,
					loc: ["body", "price"],
					msg: "Input should be greater than or equal to 1",
					type: "greater_than_equal",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("numeric_le_validation_success: Tests numeric field with le (less than or equal) constraint at boundary", async () => {
		const response = await app.request("/items/validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Item", price: 100.0 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ name: "Item", price: 100.0 });
	});

	it("optional_fields_omitted: Tests object with optional fields omitted", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Foo", price: 35.4 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ description: null, name: "Foo", price: 35.4, tax: null });
	});

	it("patch_partial_update_2: Tests PATCH request with partial object update", async () => {
		const response = await app.request("/items/1", {
			method: "PATCH",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ price: 45.0 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ description: "Original description", name: "Original Item", price: 45.0 });
	});

	it("required_field_missing_validation_error: Tests validation error when required field is missing", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ description: "A very nice Item", price: 35.4 }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: { description: "A very nice Item", price: 35.4 },
					loc: ["body", "name"],
					msg: "Field required",
					type: "missing",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("simple_json_object_success: Tests simple JSON object with all required fields", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ description: "A very nice Item", name: "Foo", price: 35.4, tax: 3.2 }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ description: "A very nice Item", name: "Foo", price: 35.4, tax: 3.2 });
	});

	it("string_max_length_validation_fail: Tests string field with max_length constraint failure", async () => {
		const response = await app.request("/items/validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "This is a very long name that exceeds the maximum length", price: 35.4 }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { max_length: 50 },
					input: "This is a very long name that exceeds the maximum length",
					loc: ["body", "name"],
					msg: "String should have at most 50 characters",
					type: "string_too_long",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("string_min_length_validation_fail: Tests string field with min_length constraint failure", async () => {
		const response = await app.request("/items/validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "ab", price: 35.4 }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { min_length: 3 },
					input: "ab",
					loc: ["body", "name"],
					msg: "String should have at least 3 characters",
					type: "string_too_short",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("string_pattern_validation_fail: Tests string field with regex pattern constraint failure", async () => {
		const response = await app.request("/items/validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Item", sku: "ABC-123" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					ctx: { pattern: "^[A-Z]{3}[0-9]{4}$" },
					input: "ABC-123",
					loc: ["body", "sku"],
					msg: "String should match pattern '^[A-Z]{3}[0-9]{4}$'",
					type: "string_pattern_mismatch",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("string_pattern_validation_success: Tests string field with regex pattern constraint success", async () => {
		const response = await app.request("/items/validated", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ name: "Item", sku: "ABC1234" }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ name: "Item", sku: "ABC1234" });
	});

	it("uuid_field_invalid_format: Tests UUID field with invalid UUID format", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ item_id: "not-a-valid-uuid", name: "Item" }),
		});
		expect(response.status).toBe(422);
		const data = await response.json();
		expect(data).toEqual({
			detail: "1 validation error in request",
			errors: [
				{
					input: "not-a-valid-uuid",
					loc: ["body", "item_id"],
					msg: "Input should be a valid UUID",
					type: "uuid_parsing",
				},
			],
			status: 422,
			title: "Request Validation Failed",
			type: "https://spikard.dev/errors/validation-error",
		});
	});

	it("uuid_field_success: Tests UUID field with valid UUID format", async () => {
		const response = await app.request("/items/", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
			},
			body: JSON.stringify({ item_id: "c892496f-b1fd-4b91-bdb8-b46f92df1716", name: "Item" }),
		});
		expect(response.status).toBe(200);
		const data = await response.json();
		expect(data).toEqual({ item_id: "c892496f-b1fd-4b91-bdb8-b46f92df1716", name: "Item" });
	});
});
