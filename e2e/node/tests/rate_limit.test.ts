// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8c671b5a87f251ffe7c6af18333d1d4645bcbe20178a3bf6e1944d9285b2f2db
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';

describe('rate_limit', () => {
  it('rate_limit_below_threshold_succeeds: Verifies that requests below the configured rate limit are served normally.', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_below_threshold_succeeds`;
    const response = await fetch(mockUrl, { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ request: "under-limit", status: "ok" });
  });

  it('rate_limit_burst_setting_allows_spike: Tests burst setting allows request spike beyond per_second rate', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_burst_setting_allows_spike`;
    const response = await fetch(mockUrl, { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ status: "ok" });
  });

  it('rate_limit_exceeded_returns_429: Sends sequential requests until the configured limit is exceeded and validates the 429 response.', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_exceeded_returns_429`;
    const response = await fetch(mockUrl, { method: 'GET' });
    expect(response.status).toBe(429);
  });

  it('rate_limit_ip_based_tracking: Tests rate limiting is applied per IP address', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_ip_based_tracking`;
    const response = await fetch(mockUrl, { method: 'GET', headers: {
      "X-Forwarded-For": "192.168.1.100",
    } });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ status: "ok" });
  });

  it('rate_limit_per_second_setting_10_requests: Tests rate limit allows up to 10 requests per second', async () => {
    const mockUrl = `${process.env.MOCK_SERVER_URL}/fixtures/rate_limit_per_second_setting_10_requests`;
    const response = await fetch(mockUrl, { method: 'GET' });
    expect(response.status).toBe(200);
    const data = await response.json();
    expect(data).toEqual({ status: "ok" });
  });
});
