// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bacc7a45002bfd0392d1112dd9b076bb2219a2a5dfe05b31716acf3a21f9d347
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: cors
package e2e_test

import (
	"encoding/json"
	"io"
	"net/http"
	"os"
	"reflect"
	"strings"
	"testing"
)

func Test_06CorsPreflightMethodNotAllowed(t *testing.T) {
	// CORS preflight request for non-allowed method should be rejected
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("OPTIONS", baseURL+"/fixtures/06_cors_preflight_method_not_allowed/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Access-Control-Request-Headers`, `Content-Type`)
	req.Header.Set(`Access-Control-Request-Method`, `DELETE`)
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 403 {
		t.Fatalf("status: got %d want 403", resp.StatusCode)
	}
}

func Test_07CorsPreflightHeaderNotAllowed(t *testing.T) {
	// CORS preflight request with non-allowed header should be rejected
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("OPTIONS", baseURL+"/fixtures/07_cors_preflight_header_not_allowed/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Access-Control-Request-Headers`, `X-Custom-Header`)
	req.Header.Set(`Access-Control-Request-Method`, `POST`)
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 403 {
		t.Fatalf("status: got %d want 403", resp.StatusCode)
	}
}

func Test_08CorsMaxAge(t *testing.T) {
	// CORS preflight response should include Access-Control-Max-Age
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("OPTIONS", baseURL+"/fixtures/08_cors_max_age/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Access-Control-Request-Headers`, `Content-Type`)
	req.Header.Set(`Access-Control-Request-Method`, `POST`)
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 204 {
		t.Fatalf("status: got %d want 204", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Headers`), `Content-Type`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Headers`, resp.Header.Get(`Access-Control-Allow-Headers`), `Content-Type`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Methods`), `POST`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Methods`, resp.Header.Get(`Access-Control-Allow-Methods`), `POST`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Origin`, resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Max-Age`), `3600`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Max-Age`, resp.Header.Get(`Access-Control-Max-Age`), `3600`)
	}
}

func Test_09CorsExposeHeaders(t *testing.T) {
	// CORS response should include Access-Control-Expose-Headers for custom headers
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/09_cors_expose_headers/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Origin`, resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Expose-Headers`), `X-Total-Count, X-Request-Id`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Expose-Headers`, resp.Header.Get(`Access-Control-Expose-Headers`), `X-Total-Count, X-Request-Id`)
	}
	if !strings.Contains(resp.Header.Get(`X-Request-Id`), `abc123`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `X-Request-Id`, resp.Header.Get(`X-Request-Id`), `abc123`)
	}
	if !strings.Contains(resp.Header.Get(`X-Total-Count`), `42`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `X-Total-Count`, resp.Header.Get(`X-Total-Count`), `42`)
	}
}

func Test_10CorsOriginNull(t *testing.T) {
	// CORS request with 'null' origin should be handled according to policy
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/10_cors_origin_null/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Origin`, `null`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 403 {
		t.Fatalf("status: got %d want 403", resp.StatusCode)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"error":"Origin 'null' is not allowed"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_CorsAllowCredentialsFlag(t *testing.T) {
	// Tests CORS response with credentials flag when allowed
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/cors_allow_credentials_flag/api/secure", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Credentials`), `true`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Credentials`, resp.Header.Get(`Access-Control-Allow-Credentials`), `true`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Origin`, resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"message":"Success"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_CorsCustomAllowedHeadersXCustom(t *testing.T) {
	// Tests CORS allows custom X-Custom-Header in requests
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("OPTIONS", baseURL+"/fixtures/cors_custom_allowed_headers_x_custom/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Access-Control-Request-Headers`, `X-Custom-Header`)
	req.Header.Set(`Access-Control-Request-Method`, `POST`)
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 204 {
		t.Fatalf("status: got %d want 204", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Headers`), `X-Custom-Header`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Headers`, resp.Header.Get(`Access-Control-Allow-Headers`), `X-Custom-Header`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Methods`), `POST`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Methods`, resp.Header.Get(`Access-Control-Allow-Methods`), `POST`)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Origin`, resp.Header.Get(`Access-Control-Allow-Origin`), `https://example.com`)
	}
}

func Test_CorsRequestBlocked(t *testing.T) {
	// Tests CORS request from disallowed origin
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/cors_request_blocked/items/", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Origin`, `https://malicious-site.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 403 {
		t.Fatalf("status: got %d want 403", resp.StatusCode)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"detail":"CORS request from origin 'https://malicious-site.com' not allowed"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_CorsRestrictedMethodsPostGetOnly(t *testing.T) {
	// Tests CORS allows only specific HTTP methods (POST, GET)
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/cors_restricted_methods_post_get_only/api/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Origin`, `https://example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Methods`), `GET,POST`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Methods`, resp.Header.Get(`Access-Control-Allow-Methods`), `GET,POST`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"message":"Success"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}

func Test_CorsSafelistedHeadersWithoutPreflight(t *testing.T) {
	// Tests that safelisted headers (Content-Type: text/plain, Accept, Accept-Language) don't require preflight
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("POST", baseURL+"/fixtures/cors_safelisted_headers_without_preflight/api/form", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `application/json`)
	req.Header.Set(`Accept-Language`, `en-US`)
	req.Header.Set(`Content-Type`, `text/plain`)
	req.Header.Set(`Origin`, `https://app.example.com`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Access-Control-Allow-Origin`), `https://app.example.com`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Access-Control-Allow-Origin`, resp.Header.Get(`Access-Control-Allow-Origin`), `https://app.example.com`)
	}
	if !strings.Contains(resp.Header.Get(`Vary`), `Origin`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Vary`, resp.Header.Get(`Vary`), `Origin`)
	}
	var got any
	var want any
	if err := json.Unmarshal(bodyBytes, &got); err != nil {
		t.Fatalf("json unmarshal got: %v", err)
	}
	if err := json.Unmarshal([]byte(`{"message":"Success"}`), &want); err != nil {
		t.Fatalf("json unmarshal want: %v", err)
	}
	if !reflect.DeepEqual(got, want) {
		t.Fatalf("body mismatch: got %v want %v", got, want)
	}
}
