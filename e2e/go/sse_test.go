// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7f82b4aa5872fb93ca2202882dd9b4bb653b852da7a7dbf5ea7954b684d7ea89
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: sse
package e2e_test

import (
	"io"
	"net/http"
	"os"
	"strings"
	"testing"
)

func Test_SseEventRetry5000Milliseconds(t *testing.T) {
	// Tests SSE event with 5000ms retry interval
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_event_retry_5000_milliseconds/events/retry-5s", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseEventWithAllFields(t *testing.T) {
	// Tests SSE event with all optional fields (id, type, retry)
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_event_with_all_fields/events/complete", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseEventWithDataAndMetadata(t *testing.T) {
	// Tests SSE event with JSON data and metadata fields
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_event_with_data_and_metadata/events/data", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseEventWithIdField(t *testing.T) {
	// Tests SSE event includes unique ID field
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_event_with_id_field/events/with-id", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseEventWithRetryField(t *testing.T) {
	// Tests SSE event includes retry interval field
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_event_with_retry_field/events/with-retry", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseEventWithTypeField(t *testing.T) {
	// Tests SSE event includes event type field
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_event_with_type_field/events/typed", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseMultipleEventsStream(t *testing.T) {
	// Tests SSE stream with multiple consecutive events
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_multiple_events_stream/events/multi", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}

func Test_SseSimpleEventStream(t *testing.T) {
	// Tests Server-Sent Events with simple text events
	baseURL := os.Getenv("SUT_URL")
	if baseURL == "" {
		baseURL = "http://127.0.0.1:8012"
	}
	body := strings.NewReader("")
	req, err := http.NewRequest("GET", baseURL+"/fixtures/sse_simple_event_stream/events", body)
	if err != nil {
		t.Fatalf("new request failed: %v", err)
	}
	req.Header.Set(`Accept`, `text/event-stream`)
	noRedirectClient := &http.Client{
		CheckRedirect: func(req *http.Request, via []*http.Request) error {
			return http.ErrUseLastResponse
		},
	}
	resp, err := noRedirectClient.Do(req)
	if err != nil {
		t.Fatalf("request failed: %v", err)
	}
	defer func() { _ = resp.Body.Close() }()
	bodyBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		t.Fatalf("read body failed: %v", err)
	}
	_ = bodyBytes
	if resp.StatusCode != 200 {
		t.Fatalf("status: got %d want 200", resp.StatusCode)
	}
	if !strings.Contains(resp.Header.Get(`Cache-Control`), `no-cache`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Cache-Control`, resp.Header.Get(`Cache-Control`), `no-cache`)
	}
	if !strings.Contains(resp.Header.Get(`Content-Type`), `text/event-stream`) {
		t.Fatalf("header %s mismatch: got %q want to contain %q", `Content-Type`, resp.Header.Get(`Content-Type`), `text/event-stream`)
	}
}
