// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:75078ac8a5977c158fe86564b75ec811539d2030f9f05e45b6994e68f6d221dd
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests async factory that creates database pool asynchronously
pub fn async_factory_dependency_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/async_factory_dependency_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"max_size\":10,\"pool_status\":\"connected\"}")
}

// Tests that circular dependencies (A depends on B, B depends on A) are detected and rejected at registration time
pub fn circular_dependency_detection_error_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/circular_dependency_detection_error")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(500)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Circular dependency detected\",\"errors\":[{\"cycle\":[\"service_a\",\"service_b\",\"service_a\"],\"msg\":\"Circular dependency detected in dependency graph\",\"type\":\"circular_dependency\"}],\"status\":500,\"title\":\"Dependency Resolution Failed\",\"type\":\"https://spikard.dev/errors/dependency-error\"}")
}

// Tests accessing dependencies in lifecycle hooks (onRequest, preHandler) for auth, logging, etc.
pub fn dependency_injection_in_lifecycle_hooks_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/dependency_injection_in_lifecycle_hooks_success")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "authorization", "Bearer valid_token")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"authenticated\":true,\"logged\":true}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-auth-mode" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-log-level" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests factory dependency that creates instances on-demand
pub fn factory_dependency_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/factory_dependency_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"timestamp\":\"<<present>>\"}")
}

// Tests error when handler requires a dependency that was never registered
pub fn missing_dependency_error_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/missing_dependency_error")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(500)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Required dependency not found\",\"errors\":[{\"dependency_key\":\"non_existent_service\",\"msg\":\"Dependency 'non_existent_service' is not registered\",\"type\":\"missing_dependency\"}],\"status\":500,\"title\":\"Dependency Resolution Failed\",\"type\":\"https://spikard.dev/errors/dependency-error\"}")
}

// Tests mixing singleton dependencies (shared across requests) with per-request dependencies (cached within request)
pub fn mixed_singleton_and_per_request_caching_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/mixed_singleton_and_per_request_caching_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"app_name\":\"MyApp\",\"context_id\":\"<<uuid>>\",\"pool_id\":\"<<uuid>>\"}")
}

// Tests cleanup for multiple dependencies with generator pattern, ensuring all are cleaned up in reverse resolution order
pub fn multiple_dependencies_with_cleanup_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/multiple_dependencies_with_cleanup_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"session_active\":true}")
}

// Tests dependency resolution where auth depends on cache and db, which both depend on config
pub fn nested_dependencies_3_levels_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/nested_dependencies_3_levels_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"auth_enabled\":true,\"has_cache\":true,\"has_db\":true}")
}

// Tests Node.js/TypeScript-specific object destructuring pattern for dependency injection
pub fn node_js_object_destructuring_injection_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/node_js_object_destructuring_injection_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"db_name\":\"PostgreSQL\",\"log_level\":\"info\"}")
}

// Tests per-request caching where dependency is created once per request but shared by multiple usages within that request
pub fn per_request_dependency_caching_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/per_request_dependency_caching_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"first_id\":\"<<uuid>>\",\"second_id\":\"<<same_as:first_id>>\"}")
}

// Tests Python-specific parameter name-based dependency injection where dependencies are matched by parameter names
pub fn python_parameter_name_based_injection_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/python_parameter_name_based_injection_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"cache_status\":\"ready\",\"db_status\":\"connected\"}")
}

// Tests Python-specific type annotation-based dependency injection where dependencies are matched by type hints
pub fn python_type_annotation_based_injection_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/python_type_annotation_based_injection_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"cache_type\":\"Redis\",\"pool_type\":\"PostgreSQL\"}")
}

// Tests generator pattern cleanup where dependency resources are cleaned up after handler completes
pub fn resource_cleanup_after_request_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/resource_cleanup_after_request_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"session_id\":\"<<uuid>>\",\"status\":\"completed\"}")
}

// Tests route-level dependency override of app-level dependency for testing or special cases
pub fn route_level_dependency_override_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/route_level_dependency_override_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"mode\":\"test\",\"strict\":false}")
}

// Tests Ruby-specific keyword argument pattern for dependency injection
pub fn ruby_keyword_argument_injection_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/ruby_keyword_argument_injection_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"adapter\":\"postgresql\",\"user_id\":42}")
}

// Tests singleton dependency that is created once and shared across all requests
pub fn singleton_dependency_caching_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/singleton_dependency_caching_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"count\":1,\"counter_id\":\"<<uuid>>\"}")
}

// Tests error when handler expects a different type than what the dependency provides
pub fn type_mismatch_in_dependency_resolution_error_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/type_mismatch_in_dependency_resolution_error")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(500)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Dependency type mismatch\",\"errors\":[{\"actual_type\":\"string\",\"dependency_key\":\"config\",\"expected_type\":\"object\",\"msg\":\"Dependency 'config' type mismatch: expected object, got string\",\"type\":\"type_mismatch\"}],\"status\":500,\"title\":\"Dependency Resolution Failed\",\"type\":\"https://spikard.dev/errors/dependency-error\"}")
}

// Tests simple value injection (config, constants) into a handler
pub fn value_dependency_injection_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/value_dependency_injection_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"app_name\":\"SpikardApp\",\"max_connections\":100,\"version\":\"1.0.0\"}")
}
