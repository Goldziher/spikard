// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3378269a2b4615ee16a95b962fd27200b24afeb2bc1ca71059b03a8810e50020
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests response includes Cache-Control header
pub fn response_cache_control_header_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_cache_control_header")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"resource\":\"data\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "cache-control" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests response with Content-Type: application/json
pub fn response_content_type_application_json_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_content_type_application_json")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"key\":\"value\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests response with Content-Type: text/html
pub fn response_content_type_text_html_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_content_type_text_html")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests response includes Location header for redirects
pub fn response_location_header_redirect_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_location_header_redirect")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(301)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "location" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests response with multiple custom headers combined
pub fn response_multiple_headers_combined_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_multiple_headers_combined")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Complex response\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-custom-header" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-third-header" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-another-header" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests response sets cookie with Set-Cookie header
pub fn response_set_cookie_in_response_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_set_cookie_in_response")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"password\":\"pass\",\"username\":\"user\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Login successful\"}")
}

// Tests response with explicit 200 status code
pub fn response_status_code_200_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_status_code_200_success")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"status\":\"ok\"}")
}

// Tests response with 201 Created status code
pub fn response_status_code_201_created_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_status_code_201_created")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"name\":\"New Item\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(201)
  resp.body |> string.trim |> should.equal("{\"id\":\"1\",\"name\":\"New Item\"}")
}

// Tests response with 204 No Content status code
pub fn response_status_code_204_no_content_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_status_code_204_no_content")
  let req = request.set_method(req, http.Delete)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(204)
  resp.body |> string.trim |> should.equal("")
}

// Tests response includes custom headers
pub fn response_with_custom_headers_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_with_custom_headers")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":\"value\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-custom-header" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-total-count" })
    |> result.is_ok()
    |> should.be_true()
}
