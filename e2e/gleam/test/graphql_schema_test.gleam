// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b6b5564766d0725e249213f6b78d434acc7b5698b41b7a3bf557ed255d62dfa5
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests GraphQL authentication error when auth fails
pub fn graphql_authentication_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_authentication_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(401)
  resp.body |> string.trim |> should.equal("{\"error\":\"<<present>>\"}")
}

// Tests GraphQL authorization error when access denied
pub fn graphql_authorization_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_authorization_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_header(req, "Authorization", "Bearer invalid")
  let req = request.set_body(req, "{\"query\":\"{ admin { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(403)
  resp.body |> string.trim |> should.equal("{\"error\":\"<<present>>\"}")
}

// Tests GraphQL query complexity validation rejects overly complex queries
pub fn graphql_complexity_limit_exceeded_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_complexity_limit_exceeded")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { id name email posts { id title comments { id text } } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"Query complexity limit exceeded\"}]}")
}

// Tests GraphQL route configured at custom endpoint
pub fn graphql_custom_endpoint_configuration_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_custom_endpoint_configuration")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"user\":{\"id\":\"1\"}}}")
}

// Tests GraphQL query depth validation rejects deeply nested queries
pub fn graphql_depth_limit_exceeded_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_depth_limit_exceeded")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { posts { comments { author { posts { title } } } } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"Query depth limit exceeded\"}]}")
}

// Tests GraphQL error response format with correct structure
pub fn graphql_error_response_format_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_error_response_format")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ invalidField }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"extensions\":{\"code\":\"GRAPHQL_VALIDATION_FAILED\"},\"message\":\"Cannot query field 'invalidField' on type 'Query'\"}]}")
}

// Tests GraphQL execution error response
pub fn graphql_execution_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_execution_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ invalidField }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"<<present>>\"}]}")
}

// Tests GraphQL schema with queries, mutations, and subscriptions
pub fn graphql_full_schema_with_subscriptions_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_full_schema_with_subscriptions")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ __typename }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"__typename\":\"Query\"}}")
}

// Tests GraphQL introspection returns error when disabled
pub fn graphql_introspection_disabled_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_introspection_disabled")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ __schema { types { name } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"GraphQL introspection is disabled\"}]}")
}

// Tests GraphQL introspection is available
pub fn graphql_introspection_enabled_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_introspection_enabled")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ __schema { types { name } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"__schema\":{\"types\":null}}}")
}

// Tests GraphQL invalid input error
pub fn graphql_invalid_input_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_invalid_input_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"mutation { createUser(age: -5) { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"<<present>>\"}]}")
}

// Tests GraphQL not found error for missing resource
pub fn graphql_not_found_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_not_found_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user(id: \\\"nonexistent\\\") { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"<<present>>\"}]}")
}

// Tests GraphQL schema with queries and mutations
pub fn graphql_query_and_mutation_schema_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_query_and_mutation_schema")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"mutation { createUser(name: \\\"New\\\") { id name } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"createUser\":{\"id\":\"2\",\"name\":\"New\"}}}")
}

// Tests GraphQL schema configured with queries only
pub fn graphql_query_only_schema_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_query_only_schema")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { id name } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"user\":{\"id\":\"1\",\"name\":\"Test User\"}}}")
}

// Tests GraphQL rate limit exceeded error
pub fn graphql_rate_limit_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_rate_limit_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":\"<<present>>\"}")
}

// Tests GraphQL schema build error
pub fn graphql_schema_build_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_schema_build_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ test }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(500)
  resp.body |> string.trim |> should.equal("{\"error\":\"<<present>>\"}")
}

// Tests GraphQL serialization error for invalid response
pub fn graphql_serialization_error_variant_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_serialization_error_variant")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user { id } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":\"<<present>>\"}")
}
