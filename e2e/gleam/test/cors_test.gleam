// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:32dac6afe6ab3212d0d7c32a8b18b311a276718b217000c1c905ad1b0bc523cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// CORS preflight request for non-allowed method should be rejected
pub fn cors_preflight_method_not_allowed_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/06_cors_preflight_method_not_allowed")
  let req = request.set_method(req, http.Options)
  let req = request.set_header(req, "Access-Control-Request-Method", "DELETE")
  let req = request.set_header(req, "Access-Control-Request-Headers", "Content-Type")
  let req = request.set_header(req, "Origin", "https://example.com")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(403)
}

// CORS preflight request with non-allowed header should be rejected
pub fn cors_preflight_header_not_allowed_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/07_cors_preflight_header_not_allowed")
  let req = request.set_method(req, http.Options)
  let req = request.set_header(req, "Origin", "https://example.com")
  let req = request.set_header(req, "Access-Control-Request-Method", "POST")
  let req = request.set_header(req, "Access-Control-Request-Headers", "X-Custom-Header")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(403)
}

// CORS preflight response should include Access-Control-Max-Age
pub fn cors_max_age_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/08_cors_max_age")
  let req = request.set_method(req, http.Options)
  let req = request.set_header(req, "Origin", "https://example.com")
  let req = request.set_header(req, "Access-Control-Request-Method", "POST")
  let req = request.set_header(req, "Access-Control-Request-Headers", "Content-Type")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(204)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-headers" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-max-age" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-methods" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-origin" })
    |> result.is_ok()
    |> should.be_true()
}

// CORS response should include Access-Control-Expose-Headers for custom headers
pub fn cors_expose_headers_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/09_cors_expose_headers")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Origin", "https://example.com")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-origin" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-request-id" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-expose-headers" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-total-count" })
    |> result.is_ok()
    |> should.be_true()
}

// CORS request with 'null' origin should be handled according to policy
pub fn cors_origin_null_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/10_cors_origin_null")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Origin", "null")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(403)
  resp.body |> string.trim |> should.equal("{\"error\":\"Origin 'null' is not allowed\"}")
}

// Tests CORS response with credentials flag when allowed
pub fn cors_allow_credentials_flag_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cors_allow_credentials_flag")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Origin", "https://example.com")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Success\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-origin" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-credentials" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests CORS allows custom X-Custom-Header in requests
pub fn cors_custom_allowed_headers_x_custom_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cors_custom_allowed_headers_x_custom")
  let req = request.set_method(req, http.Options)
  let req = request.set_header(req, "Origin", "https://example.com")
  let req = request.set_header(req, "Access-Control-Request-Headers", "X-Custom-Header")
  let req = request.set_header(req, "Access-Control-Request-Method", "POST")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(204)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-headers" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-methods" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-origin" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests CORS request from disallowed origin
pub fn cors_request_blocked_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cors_request_blocked")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Origin", "https://malicious-site.com")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(403)
  resp.body |> string.trim |> should.equal("{\"detail\":\"CORS request from origin 'https://malicious-site.com' not allowed\"}")
}

// Tests CORS allows only specific HTTP methods (POST, GET)
pub fn cors_restricted_methods_post_get_only_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cors_restricted_methods_post_get_only")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Origin", "https://example.com")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Success\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-methods" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests that safelisted headers (Content-Type: text/plain, Accept, Accept-Language) don't require preflight
pub fn cors_safelisted_headers_without_preflight_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cors_safelisted_headers_without_preflight")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "Accept-Language", "en-US")
  let req = request.set_header(req, "Origin", "https://app.example.com")
  let req = request.set_header(req, "Content-Type", "text/plain")
  let req = request.set_header(req, "Accept", "application/json")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Success\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "access-control-allow-origin" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "vary" })
    |> result.is_ok()
    |> should.be_true()
}
