// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:205a80732a33955c5ad166bdccf81adecc949a099e9271efcfdf3dd31829f08b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// JSON with UTF-16 charset should be rejected (UTF-8 only)
pub fn json_with_charset_utf16_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/13_json_with_charset_utf16")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json; charset=utf-16")
  let req = request.set_body(req, "{\"value\":\"test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(415)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Unsupported charset 'utf-16' for JSON. Only UTF-8 is supported.\",\"status\":415,\"title\":\"Unsupported Charset\",\"type\":\"https://spikard.dev/errors/unsupported-charset\"}")
}

// Content-Type header should be case-insensitive
pub fn content_type_case_insensitive_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/14_content_type_case_insensitive")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "APPLICATION/JSON")
  let req = request.set_body(req, "{\"name\":\"test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(201)
  resp.body |> string.trim |> should.equal("{\"name\":\"test\"}")
}

// Multipart content-type without boundary parameter should fail
pub fn multipart_boundary_required_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/15_multipart_boundary_required")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(400)
  resp.body |> string.trim |> should.equal("{\"error\":\"multipart/form-data requires 'boundary' parameter\"}")
}

// text/plain content-type should be rejected when JSON is expected
pub fn text_plain_not_accepted_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/16_text_plain_not_accepted")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "text/plain")
  let req = request.set_body(req, "\"{\\\"data\\\": \\\"value\\\"}\"")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(415)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Unsupported media type\",\"status\":415,\"title\":\"Unsupported Media Type\",\"type\":\"https://spikard.dev/errors/unsupported-media-type\"}")
}

// Vendor-specific JSON content-type should be accepted
pub fn vendor_json_accepted_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/17_vendor_json_accepted")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/vnd.api+json")
  let req = request.set_body(req, "{\"data\":\"value\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(201)
  resp.body |> string.trim |> should.equal("{\"data\":\"value\"}")
}

// Content-Type with multiple parameters should be parsed correctly
pub fn content_type_with_multiple_params_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/18_content_type_with_multiple_params")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json; charset=utf-8; boundary=something")
  let req = request.set_body(req, "{\"value\":\"test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(201)
  resp.body |> string.trim |> should.equal("{\"value\":\"test\"}")
}

// Missing Content-Type header should default to JSON when body is present
pub fn missing_content_type_default_json_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/19_missing_content_type_default_json")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"name\":\"test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(201)
  resp.body |> string.trim |> should.equal("{\"name\":\"test\"}")
}

// Content-Length header mismatch with actual body size should fail
pub fn content_length_mismatch_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/20_content_length_mismatch")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"value\":\"short\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(400)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Content-Length header does not match actual body size\",\"status\":400,\"title\":\"Content-Length header mismatch\",\"type\":\"https://spikard.dev/errors/content-length-mismatch\"}")
}

// Tests rejection of unsupported content type
pub fn unsupported_media_type_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/415_unsupported_media_type")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/xml")
  let req = request.set_body(req, "\"<?xml version=\\\"1.0\\\"?><item><name>Item</name></item>\"")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(415)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Unsupported media type\",\"status\":415,\"title\":\"Unsupported Media Type\",\"type\":\"https://spikard.dev/errors/unsupported-media-type\"}")
}

// Tests content negotiation based on Accept header
pub fn content_negotiation_accept_header_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/content_negotiation_accept_header")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "application/json")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"name\":\"Item\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}
