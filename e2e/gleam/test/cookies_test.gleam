// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d0b957c8096e66a3664960f2910650fc4c7533d618304d4c59f8e3d14d7c5c2d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Cookie with SameSite=Strict attribute should be validated
pub fn cookie_samesite_strict_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/24_cookie_samesite_strict")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "session_id=abc123xyz789")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
}

// Cookie with SameSite=Lax attribute should be validated
pub fn cookie_samesite_lax_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/25_cookie_samesite_lax")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "tracking=track123")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
}

// Cookie with Secure flag should be validated for HTTPS
pub fn cookie_secure_flag_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/26_cookie_secure_flag")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "auth_token=secure_token_xyz")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
}

// Cookie with HttpOnly flag should prevent JavaScript access
pub fn cookie_httponly_flag_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/27_cookie_httponly_flag")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "session=session_abc123")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
}

// Tests APIKeyCookie authentication returns 403 when cookie missing
pub fn apikey_cookie_authentication_missing_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/apikey_cookie_authentication_missing")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"1 validation error in request\",\"errors\":[{\"input\":null,\"loc\":[\"cookie\",\"key\"],\"msg\":\"Field required\",\"type\":\"missing\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests APIKeyCookie authentication with valid cookie
pub fn apikey_cookie_authentication_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/apikey_cookie_authentication_success")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "key=secret")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"username\":\"secret\"}")
}

// Tests cookie with regex pattern validation failure
pub fn cookie_regex_pattern_validation_fail_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cookie_regex_pattern_validation_fail")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "tracking_id=invalid-format")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"1 validation error in request\",\"errors\":[{\"ctx\":{\"pattern\":\"^[A-Z0-9]{8}$\"},\"input\":\"invalid-format\",\"loc\":[\"cookie\",\"tracking_id\"],\"msg\":\"String should match pattern '^[A-Z0-9]{8}$'\",\"type\":\"string_pattern_mismatch\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests cookie with regex pattern validation success
pub fn cookie_regex_pattern_validation_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cookie_regex_pattern_validation_success")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "tracking_id=ABC12345")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"tracking_id\":\"ABC12345\"}")
}

// Tests cookie validation with max_length constraint failure
pub fn cookie_validation_max_length_constraint_fail_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cookie_validation_max_length_constraint_fail")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "session_id=this_cookie_value_is_way_too_long")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"1 validation error in request\",\"errors\":[{\"ctx\":{\"max_length\":20},\"input\":\"this_cookie_value_is_way_too_long\",\"loc\":[\"cookie\",\"session_id\"],\"msg\":\"String should have at most 20 characters\",\"type\":\"string_too_long\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests cookie validation with min_length constraint at boundary
pub fn cookie_validation_min_length_constraint_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cookie_validation_min_length_constraint_success")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "token=abc")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"token\":\"abc\"}")
}

// Tests cookie parameter with min_length constraint returns 422 when too short
pub fn cookie_validation_min_length_failure_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/cookie_validation_min_length_failure")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "tracking_id=ab")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"1 validation error in request\",\"errors\":[{\"input\":\"\",\"loc\":[\"cookie\",\"tracking_id\"],\"msg\":\"String should have at least 3 characters\",\"type\":\"string_too_short\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests multiple cookie parameters in a single request
pub fn multiple_cookies_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/multiple_cookies_success")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "session_id=session123; fatebook_tracker=tracker456; googall_tracker=ga789")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"fatebook_tracker\":\"tracker456\",\"googall_tracker\":\"ga789\",\"session_id\":\"session123\"}")
}

// Tests optional APIKeyCookie (auto_error=False) returns None without 403
pub fn optional_apikey_cookie_missing_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/optional_apikey_cookie_missing")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"msg\":\"Create an account first\"}")
}

// Tests optional cookie parameter returns None when not provided
pub fn optional_cookie_parameter_missing_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/optional_cookie_parameter_missing")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"ads_id\":null}")
}

// Tests optional cookie parameter with value provided
pub fn optional_cookie_parameter_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/optional_cookie_parameter_success")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "ads_id=abc123")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"ads_id\":\"abc123\"}")
}

// Tests validation error when required cookie is missing
pub fn required_cookie_missing_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/required_cookie_missing")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "cookie", "fatebook_tracker=tracker456")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"1 validation error in request\",\"errors\":[{\"input\":\"\",\"loc\":[\"cookie\",\"session_id\"],\"msg\":\"Field required\",\"type\":\"missing\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests setting cookie with specific domain
pub fn response_cookie_with_domain_attribute_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_cookie_with_domain_attribute")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"value\":\"domain_test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Cookie set with domain\"}")
}

// Tests setting cookie with specific path
pub fn response_cookie_with_path_attribute_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_cookie_with_path_attribute")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"value\":\"path_test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Cookie set with path\"}")
}

// Tests setting cookie with SameSite lax attribute
pub fn response_cookie_with_samesite_lax_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_cookie_with_samesite_lax")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"value\":\"lax_cookie\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Cookie set with SameSite=Lax\"}")
}

// Tests setting cookie with SameSite none (requires Secure)
pub fn response_cookie_with_samesite_none_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_cookie_with_samesite_none")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"value\":\"none_cookie\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Cookie set with SameSite=None\"}")
}

// Tests setting cookie with SameSite strict attribute
pub fn response_cookie_with_samesite_strict_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_cookie_with_samesite_strict")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"value\":\"strict_cookie\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Cookie set with SameSite=Strict\"}")
}

// Tests deleting a cookie by setting max_age to 0
pub fn response_delete_cookie_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_delete_cookie")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "cookie", "session=old_session_123")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Cookie deleted\"}")
}

// Tests setting multiple cookies in single response
pub fn response_multiple_cookies_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_multiple_cookies")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"session\":\"session123\",\"user\":\"john\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Multiple cookies set\"}")
}

// Tests setting session cookie without max_age (expires with browser)
pub fn response_session_cookie_no_max_age_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_session_cookie_no_max_age")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_body(req, "{\"value\":\"session_abc123\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Session cookie set\"}")
}

// Tests setting a cookie in the response
pub fn response_set_cookie_basic_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/response_set_cookie_basic")
  let req = request.set_method(req, http.Post)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Come to the dark side, we have cookies\"}")
}
