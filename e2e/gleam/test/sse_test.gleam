// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f339426c09e51c99bf38f9cb8f498c24ab030e8a3fb232a152f6fc669514f5cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests SSE event with 5000ms retry interval
pub fn sse_event_retry_5000_milliseconds_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_event_retry_5000_milliseconds")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests SSE event with all optional fields (id, type, retry)
pub fn sse_event_with_all_fields_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_event_with_all_fields")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests SSE event with JSON data and metadata fields
pub fn sse_event_with_data_and_metadata_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_event_with_data_and_metadata")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests SSE event includes unique ID field
pub fn sse_event_with_id_field_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_event_with_id_field")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests SSE event includes retry interval field
pub fn sse_event_with_retry_field_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_event_with_retry_field")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests SSE event includes event type field
pub fn sse_event_with_type_field_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_event_with_type_field")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests SSE stream with multiple consecutive events
pub fn sse_multiple_events_stream_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_multiple_events_stream")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests Server-Sent Events with simple text events
pub fn sse_simple_event_stream_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/sse_simple_event_stream")
  let req = request.set_method(req, http.Get)
  let req = request.set_header(req, "Accept", "text/event-stream")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "cache-control" })
    |> result.is_ok()
    |> should.be_true()
}
