// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4ab63a5d5a8c46d7c8128d26f23ade67c2e5d3ef990ff9ec4d3f7313735180b7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests JSON-RPC batch request with multiple calls
pub fn jsonrpc_batch_request_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_batch_request")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "[{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"add\",\"params\":[1,2]},{\"id\":2,\"jsonrpc\":\"2.0\",\"method\":\"subtract\",\"params\":[5,3]}]")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("[{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":3},{\"id\":2,\"jsonrpc\":\"2.0\",\"result\":2}]")
}

// Tests JSON-RPC call with positional array parameters
pub fn jsonrpc_call_with_positional_params_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_call_with_positional_params")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":2,\"jsonrpc\":\"2.0\",\"method\":\"multiply\",\"params\":[4,7]}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":2,\"jsonrpc\":\"2.0\",\"result\":28}")
}

// Tests JSON-RPC batch requests disabled returns error
pub fn jsonrpc_config_batch_disabled_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_config_batch_disabled")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "[{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"add\",\"params\":[1,2]},{\"id\":2,\"jsonrpc\":\"2.0\",\"method\":\"subtract\",\"params\":[5,3]}]")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(400)
  resp.body |> string.trim |> should.equal("{\"error\":{\"code\":-32600,\"message\":\"Batch requests not enabled\"},\"jsonrpc\":\"2.0\"}")
}

// Tests JSON-RPC server at custom endpoint path
pub fn jsonrpc_config_custom_endpoint_path_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_config_custom_endpoint_path")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"test\",\"params\":{}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":\"ok\"}")
}

// Tests JSON-RPC batch request respects maximum batch size
pub fn jsonrpc_config_max_batch_size_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_config_max_batch_size")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "[{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"method1\"},{\"id\":2,\"jsonrpc\":\"2.0\",\"method\":\"method2\"},{\"id\":3,\"jsonrpc\":\"2.0\",\"method\":\"method3\"}]")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("[{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":\"result1\"},{\"id\":2,\"jsonrpc\":\"2.0\",\"result\":\"result2\"},{\"id\":3,\"jsonrpc\":\"2.0\",\"result\":\"result3\"}]")
}

// Tests JSON-RPC method marked as deprecated
pub fn jsonrpc_deprecated_method_flag_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_deprecated_method_flag")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"oldMethod\",\"params\":{}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":\"deprecated\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "deprecation" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests JSON-RPC error response with error object
pub fn jsonrpc_error_response_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_error_response")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":3,\"jsonrpc\":\"2.0\",\"method\":\"divide\",\"params\":{\"denominator\":0,\"numerator\":10}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"error\":{\"code\":-32600,\"data\":\"Division by zero\",\"message\":\"Invalid Request\"},\"id\":3,\"jsonrpc\":\"2.0\"}")
}

// Tests JSON-RPC error for invalid parameters
pub fn jsonrpc_invalid_params_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_invalid_params")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":5,\"jsonrpc\":\"2.0\",\"method\":\"subtract\",\"params\":{\"wrong_field\":42}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"error\":{\"code\":-32602,\"message\":\"Invalid params\"},\"id\":5,\"jsonrpc\":\"2.0\"}")
}

// Tests JSON-RPC error when method does not exist
pub fn jsonrpc_method_not_found_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_method_not_found")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":4,\"jsonrpc\":\"2.0\",\"method\":\"nonexistent_method\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"error\":{\"code\":-32601,\"message\":\"Method not found\"},\"id\":4,\"jsonrpc\":\"2.0\"}")
}

// Tests JSON-RPC validates parameter types and values
pub fn jsonrpc_method_parameter_validation_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_method_parameter_validation")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":6,\"jsonrpc\":\"2.0\",\"method\":\"update_user\",\"params\":{\"email\":\"john@example.com\",\"id\":123,\"name\":\"John Doe\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":6,\"jsonrpc\":\"2.0\",\"result\":{\"email\":\"john@example.com\",\"id\":123,\"name\":\"John Doe\",\"updated\":true}}")
}

// Tests JSON-RPC method params validated against schema
pub fn jsonrpc_method_with_params_schema_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_method_with_params_schema")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"createUser\",\"params\":{\"age\":30,\"name\":\"Alice\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":{\"age\":30,\"id\":1,\"name\":\"Alice\"}}")
}

// Tests JSON-RPC method result schema validation
pub fn jsonrpc_method_with_result_schema_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_method_with_result_schema")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"getUser\",\"params\":{\"id\":1}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":{\"email\":\"john@example.com\",\"id\":1,\"name\":\"John\"}}")
}

// Tests JSON-RPC notification (no response expected)
pub fn jsonrpc_notification_no_id_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_notification_no_id")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"jsonrpc\":\"2.0\",\"method\":\"notify_event\",\"params\":{\"event\":\"user_login\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(204)
}

// Tests JSON-RPC notification (no id) doesn't return response
pub fn jsonrpc_notification_no_response_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_notification_no_response")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"jsonrpc\":\"2.0\",\"method\":\"notify\",\"params\":{\"message\":\"hello\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(204)
  resp.body |> string.trim |> should.equal("")
}

// Tests single JSON-RPC 2.0 call with successful response
pub fn jsonrpc_single_call_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/jsonrpc_single_call_success")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"jsonrpc\":\"2.0\",\"method\":\"add\",\"params\":{\"a\":5,\"b\":3}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"jsonrpc\":\"2.0\",\"result\":8}")
}
