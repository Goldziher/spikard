// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:64903984d5c6e19999a05f782d045c75dec63eef87c8e1e571febabbf22fd87b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests that a field-level resolver error returns partial data alongside an errors array
pub fn graphql_field_level_error_partial_data_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_field_level_error_partial_data")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ user(id: \\\"missing-user\\\") { id name avatar } status }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"status\":\"ok\",\"user\":null},\"errors\":[{\"message\":\"User not found\",\"path\":[\"user\"]}]}")
}

// Tests that the __schema introspection query returns the schema type list
pub fn graphql_introspection_query_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_introspection_query")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ __schema { queryType { name } types { name kind } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"__schema\":{\"queryType\":{\"name\":\"Query\"},\"types\":[{\"kind\":\"OBJECT\",\"name\":\"Query\"},{\"kind\":\"SCALAR\",\"name\":\"String\"},{\"kind\":\"SCALAR\",\"name\":\"Boolean\"}]}}}")
}

// Tests that a query with invalid GraphQL syntax returns a syntax error in the errors array
pub fn graphql_invalid_syntax_rejected_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_invalid_syntax_rejected")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ hello { this is not valid syntax {{{\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"locations\":[{\"column\":10,\"line\":1}],\"message\":\"Syntax Error\"}]}")
}

// Tests executing a specific named operation from a document containing multiple operations
pub fn graphql_multi_operation_with_operation_name_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_multi_operation_with_operation_name")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"operationName\":\"Version\",\"query\":\"query Ping { ping } query Version { version }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"version\":\"1.0.0\"}}")
}

// Tests GraphQL mutation with an input type that returns the newly created object
pub fn graphql_mutation_create_object_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_mutation_create_object")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"mutation CreateUser($input: CreateUserInput!) { createUser(input: $input) { id name email } }\",\"variables\":{\"input\":{\"email\":\"carol@example.com\",\"name\":\"Carol\"}}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"createUser\":{\"email\":\"carol@example.com\",\"id\":\"new-user-id\",\"name\":\"Carol\"}}}")
}

// Tests that a query exceeding the configured complexity limit returns an error
pub fn graphql_ops_complexity_limit_exceeded_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_ops_complexity_limit_exceeded")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ users { id friends { id friends { id friends { id friends { id } } } } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"Query complexity exceeds maximum allowed complexity of 10\"}]}")
}

// Tests that a query exceeding the configured depth limit returns an error
pub fn graphql_ops_depth_limit_exceeded_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_ops_depth_limit_exceeded")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ a { b { c { d { e } } } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"Query depth exceeds maximum allowed depth of 3\"}]}")
}

// Tests GraphQL query selecting nested fields at depth 2-3
pub fn graphql_query_nested_fields_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_query_nested_fields")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ order(id: \\\"ORD-1\\\") { id customer { name address { city } } items { sku quantity } } }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"order\":{\"customer\":{\"address\":{\"city\":\"Springfield\"},\"name\":\"Bob\"},\"id\":\"ORD-1\",\"items\":[{\"quantity\":2,\"sku\":\"WIDGET-A\"}]}}}")
}

// Tests GraphQL query that uses variables passed in the request body
pub fn graphql_query_with_variables_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_query_with_variables")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"query GetUser($id: ID!) { user(id: $id) { id name } }\",\"variables\":{\"id\":\"user-42\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"user\":{\"id\":\"user-42\",\"name\":\"Alice\"}}}")
}

// Tests simple GraphQL query execution returning data from the root query type
pub fn graphql_simple_query_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_simple_query")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ hello }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"hello\":\"world\"}}")
}

// Tests that a subscription operation type is declared and the initial response shape matches
pub fn graphql_subscription_declaration_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_subscription_declaration")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"subscription OnMessageAdded($roomId: ID!) { messageAdded(roomId: $roomId) { id text authorId } }\",\"variables\":{\"roomId\":\"room-1\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":{\"messageAdded\":{\"authorId\":\"user-1\",\"id\":\"msg-1\",\"text\":\"Hello room\"}}}")
}

// Tests that querying a field that does not exist in the schema returns a validation error
pub fn graphql_undefined_field_error_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/graphql_undefined_field_error")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"query\":\"{ hello nonExistentField }\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"errors\":[{\"message\":\"Cannot query field \\\"nonExistentField\\\" on type \\\"Query\\\".\"}]}")
}
