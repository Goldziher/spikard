// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bc7dab94a2693824c1544552e6e7005e2b8c89dd7ee7b776105da36ee39584f1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests file upload read as byte array
pub fn upload_file_as_bytes_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_as_bytes")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"file\":{\"content\":\"binary data\",\"filename\":\"binary.bin\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"as_bytes\":true,\"filename\":\"binary.bin\",\"size\":11}")
}

// Tests basic file upload with single file
pub fn upload_file_basic_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_basic_success")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"file\":{\"content\":\"Hello, World!\",\"filename\":\"test.txt\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"filename\":\"test.txt\",\"size\":13}")
}

// Tests file upload uses provided content_type or defaults
pub fn upload_file_content_type_or_default_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_content_type_or_default")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"file\":{\"content\":\"Some content\",\"filename\":\"unknown.xyz\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"content_type\":\"application/octet-stream\",\"filename\":\"unknown.xyz\"}")
}

// Tests file upload read as string
pub fn upload_file_read_to_string_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_read_to_string")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"file\":{\"content\":\"Hello, World!\",\"filename\":\"text.txt\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"as_string\":true,\"content\":\"Hello, World!\",\"filename\":\"text.txt\"}")
}

// Tests file upload preserves custom Content-Type
pub fn upload_file_with_custom_content_type_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_with_custom_content_type")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"file\":{\"content\":\"{\\\"key\\\": \\\"value\\\"}\",\"content_type\":\"application/json\",\"filename\":\"data.json\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"content_type\":\"application/json\",\"filename\":\"data.json\",\"size\":16}")
}

// Tests file upload with additional form fields
pub fn upload_file_with_form_fields_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_with_form_fields")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"description\":\"Important document\",\"file\":{\"content\":\"PDF content\",\"filename\":\"document.pdf\"},\"tags\":[\"important\",\"archive\"]}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"description\":\"Important document\",\"filename\":\"document.pdf\",\"tags_count\":2}")
}

// Tests file upload with gzip content encoding
pub fn upload_file_with_gzip_content_encoding_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_file_with_gzip_content_encoding")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"file\":{\"content\":\"compressed data\",\"content_encoding\":\"gzip\",\"filename\":\"data.gz\"}}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"content_encoding\":\"gzip\",\"filename\":\"data.gz\"}")
}

// Tests uploading multiple files at once
pub fn upload_multiple_files_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/upload_multiple_files")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "multipart/form-data")
  let req = request.set_body(req, "{\"files\":[{\"content\":\"Content 1\",\"filename\":\"file1.txt\"},{\"content\":\"Content 2\",\"filename\":\"file2.txt\"}]}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"total_size\":18,\"uploaded_files\":2}")
}
