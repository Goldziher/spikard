// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cbf2ccbade9fc4628a7593cc59a5be3eefa31618b1b42c5baf52202cbd7aab56
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests ProblemDetails for 400 Bad Request status
pub fn problem_details_400_bad_request_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_400_bad_request")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "\"invalid json\"")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(400)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Invalid JSON in request body\",\"status\":400,\"title\":\"Bad Request\",\"type\":\"https://spikard.dev/errors/bad-request\"}")
}

// Tests ProblemDetails for 401 Unauthorized status
pub fn problem_details_401_unauthorized_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_401_unauthorized")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(401)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Authentication required\",\"status\":401,\"title\":\"Unauthorized\",\"type\":\"https://spikard.dev/errors/unauthorized\"}")
}

// Tests ProblemDetails for 403 Forbidden status
pub fn problem_details_403_forbidden_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_403_forbidden")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(403)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Insufficient permissions to access this resource\",\"status\":403,\"title\":\"Forbidden\",\"type\":\"https://spikard.dev/errors/forbidden\"}")
}

// Tests ProblemDetails for 500 Internal Server Error status
pub fn problem_details_500_internal_error_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_500_internal_error")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(500)
  resp.body |> string.trim |> should.equal("{\"detail\":\"An unexpected error occurred\",\"status\":500,\"title\":\"Internal Server Error\",\"type\":\"https://spikard.dev/errors/internal-error\"}")
}

// Tests RFC 9457 ProblemDetails response with all standard fields
pub fn problem_details_standard_all_fields_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_standard_all_fields")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(404)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Item with ID 999 was not found\",\"instance\":\"/items/999\",\"status\":404,\"title\":\"Resource Not Found\",\"type\":\"https://spikard.dev/errors/not-found\"}")
}

// Tests ProblemDetails includes custom extensions for additional context
pub fn problem_details_with_extensions_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_with_extensions")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"data\":\"invalid\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Validation failed\",\"extensions\":{\"error_code\":\"VALIDATION_001\",\"timestamp\":null,\"trace_id\":null},\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests ProblemDetails includes instance URI for context
pub fn problem_details_with_instance_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_with_instance")
  let req = request.set_method(req, http.Get)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(404)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Post 456 not found for user 123\",\"instance\":\"/users/123/posts/456\",\"status\":404,\"title\":\"Post Not Found\",\"type\":\"https://spikard.dev/errors/not-found\"}")
}

// Tests ProblemDetails includes type URI for error classification
pub fn problem_details_with_type_uri_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/problem_details_with_type_uri")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"value\":-5}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"Request validation failed\",\"status\":422,\"title\":\"Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}
