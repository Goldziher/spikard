// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b92a5e2c0c11892013f60896758795dce2dbad010652c3cdb9c537c6b2acf2d0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests gRPC call with request metadata headers
pub fn grpc_call_with_metadata_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_call_with_metadata")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "x-custom-metadata", "value123")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "authorization", "Bearer token123")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_body(req, "{\"id\":\"resource-1\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":\"result\",\"id\":\"resource-1\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-response-metadata" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC compression is enabled for responses
pub fn grpc_config_compression_enabled_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_config_compression_enabled")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "grpc-encoding", "gzip")
  let req = request.set_body(req, "{\"request\":\"get_data\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-encoding" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC keepalive ping configuration
pub fn grpc_config_keepalive_settings_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_config_keepalive_settings")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_body(req, "{\"task\":\"background_job\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC server respects maximum message size configuration
pub fn grpc_config_max_message_size_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_config_max_message_size")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_body(req, "{\"data\":\"large_payload_data\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"received_size\":19}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC request timeout configuration
pub fn grpc_config_request_timeout_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_config_request_timeout")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "grpc-timeout", "5000m")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_body(req, "{\"operation\":\"process\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC DEADLINE_EXCEEDED error when timeout occurs
pub fn grpc_deadline_exceeded_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_deadline_exceeded")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "grpc-timeout", "100m")
  let req = request.set_body(req, "{\"duration_ms\":60000}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"code\":\"DEADLINE_EXCEEDED\",\"message\":\"Operation exceeded deadline\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC INVALID_ARGUMENT error status response
pub fn grpc_error_invalid_argument_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_error_invalid_argument")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_body(req, "{\"value\":-100}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"code\":\"INVALID_ARGUMENT\",\"message\":\"Value must be non-negative\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC NOT_FOUND error status response
pub fn grpc_error_not_found_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_error_not_found")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_body(req, "{\"item_id\":\"nonexistent\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"code\":\"NOT_FOUND\",\"message\":\"Item not found\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC request includes method_name field
pub fn grpc_request_method_name_field_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_request_method_name_field")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_body(req, "{\"id\":\"resource-1\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"data\":\"result\",\"method\":\"GetData\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC request includes service_name field
pub fn grpc_request_service_name_field_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_request_service_name_field")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_body(req, "{\"name\":\"World\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Hello World\",\"service\":\"helloworld.Greeter\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC request includes custom metadata in trailers
pub fn grpc_request_with_custom_metadata_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_request_with_custom_metadata")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "x-user-id", "user-42")
  let req = request.set_header(req, "x-trace-id", "trace-abc123")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "x-request-id", "req-12345")
  let req = request.set_body(req, "{\"data\":{\"key\":\"value\"},\"request_id\":\"req-12345\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"processed\":true,\"request_id\":\"req-12345\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-response-trace-id" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests gRPC response includes metadata in trailers
pub fn grpc_response_with_metadata_trailers_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_response_with_metadata_trailers")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_body(req, "{\"request\":\"test\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"result\":\"success\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "x-metadata-key" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests unary gRPC call with successful response
pub fn grpc_unary_call_success_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/grpc_unary_call_success")
  let req = request.set_method(req, http.Post)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/grpc")
  let req = request.set_header(req, "te", "trailers")
  let req = request.set_body(req, "{\"name\":\"World\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"message\":\"Hello World\"}")
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "grpc-status" })
    |> result.is_ok()
    |> should.be_true()
}
