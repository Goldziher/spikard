// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:209ac4029a9edc567118e334353182235a04a2999cae47bf3a1aaa4e9c5acebb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import gleam/httpc
import gleam/http
import gleam/http/request
import gleam/list
import gleam/result
import gleam/string
import envoy

// Tests DELETE method to remove a resource
pub fn delete_remove_resource_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/delete_remove_resource")
  let req = request.set_method(req, http.Delete)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{}")
}

// Tests DELETE on non-existent resource returns 404
pub fn delete_resource_not_found_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/delete_resource_not_found")
  let req = request.set_method(req, http.Delete)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{}")
}

// Tests DELETE returning deleted resource data
pub fn delete_with_response_body_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/delete_with_response_body")
  let req = request.set_method(req, http.Delete)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"message\":\"Item deleted successfully\",\"name\":\"Deleted Item\"}")
}

// Tests HEAD method returns headers without response body
pub fn head_get_metadata_without_body_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/head_get_metadata_without_body")
  let req = request.set_method(req, http.Head)
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-length" })
    |> result.is_ok()
    |> should.be_true()
  resp.headers
    |> list.find(fn(h: #(String, String)) { h.0 == "content-type" })
    |> result.is_ok()
    |> should.be_true()
}

// Tests PATCH method for partial resource updates
pub fn patch_partial_update_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/patch_partial_update")
  let req = request.set_method(req, http.Patch)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"price\":79.99}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"in_stock\":true,\"name\":\"Existing Item\",\"price\":79.99}")
}

// Tests PATCH updating multiple fields at once
pub fn patch_update_multiple_fields_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/patch_update_multiple_fields")
  let req = request.set_method(req, http.Patch)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"in_stock\":false,\"name\":\"Updated Name\",\"price\":89.99}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"in_stock\":false,\"name\":\"Updated Name\",\"price\":89.99}")
}

// Tests PUT method for complete resource replacement
pub fn put_complete_resource_replacement_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/put_complete_resource_replacement")
  let req = request.set_method(req, http.Put)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"description\":\"Completely replaced\",\"id\":1,\"in_stock\":true,\"name\":\"Updated Item\",\"price\":99.99}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"description\":\"Completely replaced\",\"id\":1,\"in_stock\":true,\"name\":\"Updated Item\",\"price\":99.99}")
}

// Tests PUT creating new resource at specific URI
pub fn put_create_resource_if_doesn_t_exist_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/put_create_resource_if_doesn_t_exist")
  let req = request.set_method(req, http.Put)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":999,\"name\":\"New Item\",\"price\":49.99}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":999,\"name\":\"New Item\",\"price\":49.99}")
}

// Tests PUT idempotency - repeated calls produce same result
pub fn put_idempotent_operation_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/put_idempotent_operation")
  let req = request.set_method(req, http.Put)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"name\":\"Fixed Name\",\"price\":50.0}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(200)
  resp.body |> string.trim |> should.equal("{\"id\":1,\"name\":\"Fixed Name\",\"price\":50.0}")
}

// Tests PUT with missing required fields returns 422
pub fn put_missing_required_field_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/put_missing_required_field")
  let req = request.set_method(req, http.Put)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"name\":\"Item Name\"}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"1 validation error in request\",\"errors\":[{\"input\":{\"id\":1,\"name\":\"Item Name\"},\"loc\":[\"body\",\"price\"],\"msg\":\"Field required\",\"type\":\"missing\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}

// Tests PUT with invalid data returns 422
pub fn put_validation_error_test() {
  let base_url = case envoy.get("MOCK_SERVER_URL") {
    Ok(u) -> u
    Error(_) -> "http://localhost:8080"
  }
  let assert Ok(req) = request.to(base_url <> "/fixtures/put_validation_error")
  let req = request.set_method(req, http.Put)
  let req = request.set_header(req, "content-type", "application/json")
  let req = request.set_header(req, "Content-Type", "application/json")
  let req = request.set_body(req, "{\"id\":1,\"name\":\"X\",\"price\":-10}")
  let assert Ok(resp) = httpc.send(req)
  resp.status |> should.equal(422)
  resp.body |> string.trim |> should.equal("{\"detail\":\"2 validation errors in request\",\"errors\":[{\"ctx\":{\"min_length\":3},\"input\":\"X\",\"loc\":[\"body\",\"name\"],\"msg\":\"String should have at least 3 characters\",\"type\":\"string_too_short\"},{\"ctx\":{\"gt\":0},\"input\":-10,\"loc\":[\"body\",\"price\"],\"msg\":\"Input should be greater than 0\",\"type\":\"greater_than\"}],\"status\":422,\"title\":\"Request Validation Failed\",\"type\":\"https://spikard.dev/errors/validation-error\"}")
}
