{
  "http": {
    "expected_response": {
      "body": {
        "detail": "1 validation error in request",
        "errors": [
          {
            "ctx": { "pattern": "^[A-Z]{3}[0-9]{4}$" },
            "input": "ABC-123",
            "loc": ["body", "sku"],
            "msg": "String should match pattern '^[A-Z]{3}[0-9]{4}$'",
            "type": "string_pattern_mismatch"
          }
        ],
        "status": 422,
        "title": "Request Validation Failed",
        "type": "https://spikard.dev/errors/validation-error"
      },
      "headers": {},
      "status_code": 422
    },
    "handler": {
      "body_schema": {
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string" },
          "sku": { "pattern": "^[A-Z]{3}[0-9]{4}$", "type": "string" }
        },
        "required": ["name", "sku"],
        "type": "object"
      },
      "method": "POST",
      "route": "/items/validated"
    },
    "request": { "path": "/items/validated" }
  }
}
