// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f2d876ff7b9b0cfb15a124c1bff7c0469d8a5a4dc43b6330629747de2e009cf0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.spikard.e2e;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.io.IOException;
import java.io.InputStream;
import java.util.HashMap;
import java.util.Map;

/**
 * Helper class for loading fixture JSON files from classpath.
 *
 * Fixtures are stored as individual JSON files in src/test/resources/fixtures/
 * to avoid exceeding Java's 65KB string literal limit.
 */
public class FixtureLoader {
    private static final ObjectMapper MAPPER = new ObjectMapper();

    /**
     * Load a single fixture by ID from classpath resources.
     *
     * @param fixtureId the fixture identifier (e.g., "smoke_basic")
     * @return the parsed fixture as a JsonNode, or null if not found
     */
    public static JsonNode loadFixture(String fixtureId) {
        String resourcePath = "/fixtures/" + fixtureId + ".json";
        try (InputStream is = FixtureLoader.class.getResourceAsStream(resourcePath)) {
            if (is == null) {
                System.err.println("Fixture not found: " + fixtureId);
                return null;
            }
            return MAPPER.readTree(is);
        } catch (IOException e) {
            System.err.println("Failed to load fixture " + fixtureId + ": " + e.getMessage());
            e.printStackTrace();
            return null;
        }
    }

    /**
     * Load all fixtures from the classpath resources directory.
     *
     * @return a map of fixture IDs to parsed fixture JsonNodes
     */
    public static Map<String, JsonNode> loadAllFixtures() {
        Map<String, JsonNode> fixtures = new HashMap<>();
        // Note: Loading all fixtures requires iterating the classpath.
        // For typical e2e test suites, only the fixtures needed by the
        // specific test class should be loaded via loadFixture(id).
        return fixtures;
    }
}
