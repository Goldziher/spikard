// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b0da2ef06c3ae573b1a115ebd7ad10695a9ecd17be4f71601298e29a6aca9320
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: problem_details

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_problem_details_400_bad_request() {
    // Tests ProblemDetails for 400 Bad Request status
    let expected_body = r#"{"detail":"Invalid JSON in request body","status":400,"title":"Bad Request","type":"https://spikard.dev/errors/bad-request"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/parse"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(400u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/parse")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"data":"example"}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 400u16);
}

#[tokio::test]
async fn test_problem_details_401_unauthorized() {
    // Tests ProblemDetails for 401 Unauthorized status
    let expected_body = r#"{"detail":"Authentication required","status":401,"title":"Unauthorized","type":"https://spikard.dev/errors/unauthorized"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/protected"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(401u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/protected").await;
    assert_eq!(response.status_code().as_u16(), 401u16);
}

#[tokio::test]
async fn test_problem_details_403_forbidden() {
    // Tests ProblemDetails for 403 Forbidden status
    let expected_body = r#"{"detail":"Insufficient permissions to access this resource","status":403,"title":"Forbidden","type":"https://spikard.dev/errors/forbidden"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/admin/config"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(403u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/admin/config").await;
    assert_eq!(response.status_code().as_u16(), 403u16);
}

#[tokio::test]
async fn test_problem_details_500_internal_error() {
    // Tests ProblemDetails for 500 Internal Server Error status
    let expected_body = r#"{"detail":"An unexpected error occurred","status":500,"title":"Internal Server Error","type":"https://spikard.dev/errors/internal-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/error"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(500u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/error").await;
    assert_eq!(response.status_code().as_u16(), 500u16);
}

#[tokio::test]
async fn test_problem_details_standard_all_fields() {
    // Tests RFC 9457 ProblemDetails response with all standard fields
    let expected_body = r#"{"detail":"Item with ID 999 was not found","instance":"/items/999","status":404,"title":"Resource Not Found","type":"https://spikard.dev/errors/not-found"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(404u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/items/999").await;
    assert_eq!(response.status_code().as_u16(), 404u16);
}

#[tokio::test]
async fn test_problem_details_with_extensions() {
    // Tests ProblemDetails includes custom extensions for additional context
    let expected_body = r#"{"detail":"Validation failed","extensions":{"error_code":"VALIDATION_001","timestamp":null,"trace_id":null},"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/api/resource"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/api/resource")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"data":"invalid"}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_problem_details_with_instance() {
    // Tests ProblemDetails includes instance URI for context
    let expected_body = r#"{"detail":"Post 456 not found for user 123","instance":"/users/123/posts/456","status":404,"title":"Post Not Found","type":"https://spikard.dev/errors/not-found"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/users/{user_id}/posts/{post_id}"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(404u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/users/123/posts/456").await;
    assert_eq!(response.status_code().as_u16(), 404u16);
}

#[tokio::test]
async fn test_problem_details_with_type_uri() {
    // Tests ProblemDetails includes type URI for error classification
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"ctx":{"ge":1},"input":-5,"loc":["body","value"],"msg":"Input should be greater than or equal to 1","type":"greater_than_equal"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/validate"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/validate")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"value":-5}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}
