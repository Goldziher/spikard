// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b9e55555870df5736d303ec25a2ec00c41ddd0d81fb4b2b8cb1219cd936697cc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: graphql_schema

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_graphql_authentication_error_variant() {
    // Tests GraphQL authentication error when auth fails
    let expected_body = r#"{"error":"<<present>>"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(401u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 401u16);
}

#[tokio::test]
async fn test_graphql_authorization_error_variant() {
    // Tests GraphQL authorization error when access denied
    let expected_body = r#"{"error":"<<present>>"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(403u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Authorization"#, r#"Bearer invalid"#)
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ admin { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 403u16);
}

#[tokio::test]
async fn test_graphql_complexity_limit_exceeded() {
    // Tests GraphQL query complexity validation rejects overly complex queries
    let expected_body = r#"{"errors":[{"message":"Query complexity limit exceeded"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { id name email posts { id title comments { id text } } } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_custom_endpoint_configuration() {
    // Tests GraphQL route configured at custom endpoint
    let expected_body = r#"{"data":{"user":{"id":"1"}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/api/v2/query"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/api/v2/query")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_depth_limit_exceeded() {
    // Tests GraphQL query depth validation rejects deeply nested queries
    let expected_body = r#"{"errors":[{"message":"Query depth limit exceeded"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { posts { comments { author { posts { title } } } } } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_error_response_format() {
    // Tests GraphQL error response format with correct structure
    let expected_body = r#"{"errors":[{"extensions":{"code":"GRAPHQL_VALIDATION_FAILED"},"message":"Cannot query field 'invalidField' on type 'Query'"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ invalidField }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_execution_error_variant() {
    // Tests GraphQL execution error response
    let expected_body = r#"{"errors":[{"message":"<<present>>"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ invalidField }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_full_schema_with_subscriptions() {
    // Tests GraphQL schema with queries, mutations, and subscriptions
    let expected_body = r#"{"data":{"__typename":"Query"}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ __typename }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_introspection_disabled() {
    // Tests GraphQL introspection returns error when disabled
    let expected_body = r#"{"errors":[{"message":"GraphQL introspection is disabled"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ __schema { types { name } } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_introspection_enabled() {
    // Tests GraphQL introspection is available
    let expected_body = r#"{"data":{"__schema":{"types":null}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ __schema { types { name } } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_invalid_input_error_variant() {
    // Tests GraphQL invalid input error
    let expected_body = r#"{"errors":[{"message":"<<present>>"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"mutation { createUser(age: -5) { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_not_found_error_variant() {
    // Tests GraphQL not found error for missing resource
    let expected_body = r#"{"errors":[{"message":"<<present>>"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user(id: \"nonexistent\") { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_query_and_mutation_schema() {
    // Tests GraphQL schema with queries and mutations
    let expected_body = r#"{"data":{"createUser":{"id":"2","name":"New"}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"mutation { createUser(name: \"New\") { id name } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_query_only_schema() {
    // Tests GraphQL schema configured with queries only
    let expected_body = r#"{"data":{"user":{"id":"1","name":"Test User"}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { id name } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_rate_limit_error_variant() {
    // Tests GraphQL rate limit exceeded error
    let expected_body = r#"{"data":"<<present>>"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_graphql_schema_build_error_variant() {
    // Tests GraphQL schema build error
    let expected_body = r#"{"error":"<<present>>"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(500u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"query":"{ test }"}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 500u16);
}

#[tokio::test]
async fn test_graphql_serialization_error_variant() {
    // Tests GraphQL serialization error for invalid response
    let expected_body = r#"{"data":"<<present>>"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::post("/graphql"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .post("/graphql")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"query":"{ user { id } }"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}
