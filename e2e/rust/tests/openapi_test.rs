// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6379227f1f9f7fa5f8665035fe88b22acaaa0566b7220e375dc39ccc0f9ce6d0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: openapi

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_openapi_basic_spec_generation() {
    // Tests OpenAPI specification is generated from route definitions
    let expected_body = r#"{"info":{"title":"API"},"openapi":"3.0.0"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_contact_information() {
    // Tests OpenAPI spec includes contact information
    let expected_body =
        r#"{"info":{"contact":{"name":"API Support","url":"https://example.com/support"}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_disabled_returns_404() {
    // Tests OpenAPI returns 404 when explicitly disabled
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(404u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 404u16);
}

#[tokio::test]
async fn test_openapi_enabled_flag_true() {
    // Tests OpenAPI can be explicitly enabled
    let expected_body = r#"{"openapi":"3.0.0"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_json_endpoint_path() {
    // Tests OpenAPI JSON is served at configured endpoint
    let expected_body = r#"{"openapi":"3.0.0"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/api/openapi.json"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/api/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_license_information() {
    // Tests OpenAPI spec includes license information
    let expected_body = r#"{"info":{"license":{"name":"MIT"}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_redoc_path() {
    // Tests ReDoc UI is served at configured path
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/redoc"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/redoc").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_security_schemes_definition() {
    // Tests OpenAPI spec includes security schemes
    let expected_body =
        r#"{"components":{"securitySchemes":{"bearerAuth":{"scheme":"bearer","type":"http"}}}}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_servers_configuration() {
    // Tests OpenAPI spec includes configured servers
    let expected_body =
        r#"{"servers":[{"url":"https://api.example.com"},{"url":"https://staging-api.example.com"}]}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/openapi.json"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/openapi.json").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_openapi_swagger_ui_path() {
    // Tests Swagger UI is served at configured path
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::get("/docs"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/docs").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}
