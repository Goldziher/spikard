// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:69c578b3c710506deb6a72d644dd152e42530921e80acf4f34919856a8c53d73
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: sse

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[test]
fn test_sse_event_retry_5000_milliseconds() {
    common::runtime().block_on(async {
        // Tests SSE event with 5000ms retry interval
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(
            spikard::get("/events/retry-5s"),
            move |_ctx: spikard::RequestContext| {
                let body = expected_body.clone();
                async move {
                    Ok(axum::http::Response::builder()
                        .status(200u16)
                        .header("content-type", r#"text/event-stream"#)
                        .body(axum::body::Body::from(body))
                        .unwrap())
                }
            },
        )
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/retry-5s")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_event_with_all_fields() {
    common::runtime().block_on(async {
        // Tests SSE event with all optional fields (id, type, retry)
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(
            spikard::get("/events/complete"),
            move |_ctx: spikard::RequestContext| {
                let body = expected_body.clone();
                async move {
                    Ok(axum::http::Response::builder()
                        .status(200u16)
                        .header("content-type", r#"text/event-stream"#)
                        .body(axum::body::Body::from(body))
                        .unwrap())
                }
            },
        )
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/complete")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_event_with_data_and_metadata() {
    common::runtime().block_on(async {
        // Tests SSE event with JSON data and metadata fields
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(spikard::get("/events/data"), move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", r#"text/event-stream"#)
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        })
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/data")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_event_with_id_field() {
    common::runtime().block_on(async {
        // Tests SSE event includes unique ID field
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(spikard::get("/events/with-id"), move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", r#"text/event-stream"#)
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        })
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/with-id")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_event_with_retry_field() {
    common::runtime().block_on(async {
        // Tests SSE event includes retry interval field
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(
            spikard::get("/events/with-retry"),
            move |_ctx: spikard::RequestContext| {
                let body = expected_body.clone();
                async move {
                    Ok(axum::http::Response::builder()
                        .status(200u16)
                        .header("content-type", r#"text/event-stream"#)
                        .body(axum::body::Body::from(body))
                        .unwrap())
                }
            },
        )
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/with-retry")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_event_with_type_field() {
    common::runtime().block_on(async {
        // Tests SSE event includes event type field
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(spikard::get("/events/typed"), move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", r#"text/event-stream"#)
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        })
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/typed")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_multiple_events_stream() {
    common::runtime().block_on(async {
        // Tests SSE stream with multiple consecutive events
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(spikard::get("/events/multi"), move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", r#"text/event-stream"#)
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        })
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events/multi")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}

#[test]
fn test_sse_simple_event_stream() {
    common::runtime().block_on(async {
        // Tests Server-Sent Events with simple text events
        let expected_body = r#""#.to_string();
        let mut app = spikard::App::new();
        app.route(spikard::get("/events"), move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("cache-control", r#"no-cache"#)
                    .header("content-type", r#"text/event-stream"#)
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        })
        .unwrap();
        let router = app.into_router().unwrap();
        let server = axum_test::TestServer::new(router);
        let response = server
            .get("/events")
            .add_header(r#"Accept"#, r#"text/event-stream"#)
            .await;
        assert_eq!(response.status_code().as_u16(), 200u16);
        assert_eq!(
            response.headers().get("cache-control").and_then(|v| v.to_str().ok()),
            Some(r#"no-cache"#),
            "unexpected value for header 'cache-control'"
        );
        assert_eq!(
            response.headers().get("content-type").and_then(|v| v.to_str().ok()),
            Some(r#"text/event-stream"#),
            "unexpected value for header 'content-type'"
        );
    });
}
