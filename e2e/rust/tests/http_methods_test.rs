// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2d52f3f3ace4969b27d64f1c96278f5159bdcb297a4c8aea8492b6907ccc4327
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: http_methods

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_delete_remove_resource() {
    // Tests DELETE method to remove a resource
    let expected_body = r#"{}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::delete("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.delete("/items/1").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_delete_resource_not_found() {
    // Tests DELETE on non-existent resource returns 404
    let expected_body = r#"{}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::delete("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.delete("/items/999").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_delete_with_response_body() {
    // Tests DELETE returning deleted resource data
    let expected_body = r#"{"id":1,"message":"Item deleted successfully","name":"Deleted Item"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::delete("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.delete("/items/1").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_head_get_metadata_without_body() {
    // Tests HEAD method returns headers without response body
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::RouteBuilder::new(spikard::Method::Head, "/items/{id}"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.method(axum::http::Method::HEAD, "/items/1").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_patch_partial_update() {
    // Tests PATCH method for partial resource updates
    let expected_body = r#"{"id":1,"in_stock":true,"name":"Existing Item","price":79.99}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::patch("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .patch("/items/1")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(r#"{"price":79.99}"#.as_bytes()))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_patch_update_multiple_fields() {
    // Tests PATCH updating multiple fields at once
    let expected_body = r#"{"id":1,"in_stock":false,"name":"Updated Name","price":89.99}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::patch("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .patch("/items/1")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"in_stock":false,"name":"Updated Name","price":89.99}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_put_complete_resource_replacement() {
    // Tests PUT method for complete resource replacement
    let expected_body =
        r#"{"description":"Completely replaced","id":1,"in_stock":true,"name":"Updated Item","price":99.99}"#
            .to_string();
    let mut app = spikard::App::new();
    app.route(spikard::put("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .put("/items/1")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"description":"Completely replaced","id":1,"in_stock":true,"name":"Updated Item","price":99.99}"#
                .as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_put_create_resource_if_doesn_t_exist() {
    // Tests PUT creating new resource at specific URI
    let expected_body = r#"{"id":999,"name":"New Item","price":49.99}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::put("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .put("/items/999")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"id":999,"name":"New Item","price":49.99}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_put_idempotent_operation() {
    // Tests PUT idempotency - repeated calls produce same result
    let expected_body = r#"{"id":1,"name":"Fixed Name","price":50.0}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::put("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(200u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .put("/items/1")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"id":1,"name":"Fixed Name","price":50.0}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_put_missing_required_field() {
    // Tests PUT with missing required fields returns 422
    let expected_body = r#"{"detail":"1 validation error in request","errors":[{"input":{"id":1,"name":"Item Name"},"loc":["body","price"],"msg":"Field required","type":"missing"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::put("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .put("/items/1")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"id":1,"name":"Item Name"}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}

#[tokio::test]
async fn test_put_validation_error() {
    // Tests PUT with invalid data returns 422
    let expected_body = r#"{"detail":"2 validation errors in request","errors":[{"ctx":{"min_length":3},"input":"X","loc":["body","name"],"msg":"String should have at least 3 characters","type":"string_too_short"},{"ctx":{"gt":0},"input":-10,"loc":["body","price"],"msg":"Input should be greater than 0","type":"greater_than"}],"status":422,"title":"Request Validation Failed","type":"https://spikard.dev/errors/validation-error"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(spikard::put("/items/{id}"), move |_ctx: spikard::RequestContext| {
        let body = expected_body.clone();
        async move {
            Ok(axum::http::Response::builder()
                .status(422u16)
                .header("content-type", "application/json")
                .body(axum::body::Body::from(body))
                .unwrap())
        }
    })
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .put("/items/1")
        .add_header(r#"Content-Type"#, r#"application/json"#)
        .bytes(bytes::Bytes::copy_from_slice(
            r#"{"id":1,"name":"X","price":-10}"#.as_bytes(),
        ))
        .await;
    assert_eq!(response.status_code().as_u16(), 422u16);
}
