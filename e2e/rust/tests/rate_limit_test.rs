// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5cda43db8f64def53c461be782227b0c2751e8ca01fcd213209ceb6324872e09
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: rate_limit

mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_rate_limit_below_threshold_succeeds() {
    // Verifies that requests below the configured rate limit are served normally.
    let expected_body = r#"{"request":"under-limit","status":"ok"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/rate-limit/basic"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/rate-limit/basic").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_rate_limit_burst_setting_allows_spike() {
    // Tests burst setting allows request spike beyond per_second rate
    let expected_body = r#"{"status":"ok"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/rate-limit/burst"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/rate-limit/burst").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_rate_limit_exceeded_returns_429() {
    // Sends sequential requests until the configured limit is exceeded and validates the 429 response.
    let expected_body = r#""#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/rate-limit/exceeded"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(429u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/rate-limit/exceeded").await;
    assert_eq!(response.status_code().as_u16(), 429u16);
}

#[tokio::test]
async fn test_rate_limit_ip_based_tracking() {
    // Tests rate limiting is applied per IP address
    let expected_body = r#"{"status":"ok"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/rate-limit/ip-based"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server
        .get("/rate-limit/ip-based")
        .add_header(r#"X-Forwarded-For"#, r#"192.168.1.100"#)
        .await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}

#[tokio::test]
async fn test_rate_limit_per_second_setting_10_requests() {
    // Tests rate limit allows up to 10 requests per second
    let expected_body = r#"{"status":"ok"}"#.to_string();
    let mut app = spikard::App::new();
    app.route(
        spikard::get("/rate-limit/per-second"),
        move |_ctx: spikard::RequestContext| {
            let body = expected_body.clone();
            async move {
                Ok(axum::http::Response::builder()
                    .status(200u16)
                    .header("content-type", "application/json")
                    .body(axum::body::Body::from(body))
                    .unwrap())
            }
        },
    )
    .unwrap();
    let router = app.into_router().unwrap();
    let server = axum_test::TestServer::new(router);
    let response = server.get("/rate-limit/per-second").await;
    assert_eq!(response.status_code().as_u16(), 200u16);
}
