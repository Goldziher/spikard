{
	"name": "Missing request ID when required",
	"description": "Tests error handling when request ID is required but not provided by client",
	"category": "request_id",
	"source": {
		"framework": "spikard",
		"inspired_by": "tower-http request-id",
		"test_case": "Enforce required request ID from client"
	},
	"handler": {
		"route": "/api/require-id",
		"method": "POST",
		"middleware": {
			"request_id": {
				"enabled": true,
				"header_name": "X-Request-ID",
				"format": "uuid_v4",
				"validate_format": true,
				"propagate_to_response": true
			}
		},
		"response_type": "validation_error"
	},
	"request": {
		"method": "POST",
		"path": "/api/require-id",
		"body": {
			"data": "test"
		}
	},
	"expected_response": {
		"status_code": 400,
		"body": {
			"type": "about:blank",
			"title": "Bad Request",
			"status": 400,
			"detail": "Missing required header: X-Request-ID"
		}
	},
	"tags": ["request_id", "middleware", "validation", "required-header"],
	"notes": "Some APIs require clients to provide request IDs for audit/compliance purposes. Note: This behavior depends on configuration; auto-generation is more common."
}
