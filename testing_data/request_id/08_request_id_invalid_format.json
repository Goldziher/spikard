{
	"name": "Invalid request ID format handling",
	"description": "Tests handling of malformed client-provided request ID when validation is enabled",
	"category": "request_id",
	"source": {
		"framework": "spikard",
		"inspired_by": "tower-http request-id",
		"test_case": "Reject or sanitize invalid request ID formats"
	},
	"handler": {
		"route": "/api/validated",
		"method": "GET",
		"middleware": {
			"request_id": {
				"enabled": true,
				"header_name": "X-Request-ID",
				"format": "uuid_v4",
				"validate_format": true,
				"propagate_to_response": true
			}
		},
		"response_type": "validation_error"
	},
	"request": {
		"method": "GET",
		"path": "/api/validated",
		"headers": {
			"X-Request-ID": "not-a-valid-uuid!!!"
		}
	},
	"expected_response": {
		"status_code": 400,
		"body": {
			"type": "about:blank",
			"title": "Bad Request",
			"status": 400,
			"detail": "Invalid request ID format: expected UUID v4"
		}
	},
	"tags": ["request_id", "middleware", "validation", "error-handling"],
	"notes": "When validate_format is enabled, malformed request IDs should be rejected with 400 Bad Request"
}
