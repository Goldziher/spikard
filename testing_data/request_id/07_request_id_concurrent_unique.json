{
	"name": "Multiple concurrent requests with unique IDs",
	"description": "Tests that concurrent requests each receive unique auto-generated request IDs",
	"category": "request_id",
	"source": {
		"framework": "spikard",
		"inspired_by": "tower-http request-id",
		"test_case": "Ensure request ID uniqueness under concurrent load"
	},
	"handler": {
		"route": "/api/concurrent",
		"method": "GET",
		"middleware": {
			"request_id": {
				"enabled": true,
				"header_name": "X-Request-ID",
				"format": "uuid_v4",
				"propagate_to_response": true
			}
		},
		"response_type": "success"
	},
	"request": {
		"method": "GET",
		"path": "/api/concurrent"
	},
	"expected_response": {
		"status_code": 200,
		"body": {
			"message": "success"
		}
	},
	"concurrent_requests": 100,
	"assert_unique_ids": true,
	"tags": ["request_id", "middleware", "concurrency", "uniqueness"],
	"notes": "UUID v4 generation must be thread-safe and collision-resistant even under high concurrent load"
}
