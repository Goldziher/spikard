{
  "name": "Pattern validation - fail",
  "description": "Tests form field with regex pattern constraint failure",
  "source": {
    "framework": "fastapi",
    "test_file": "tests/test_tutorial/test_request_form_models/test_tutorial001.py",
    "test_function": "test_form_pattern_fail"
  },
  "request": {
    "method": "POST",
    "path": "/form/validated",
    "headers": {
      "Content-Type": "application/x-www-form-urlencoded"
    },
    "form_data": {
      "username": "john doe"
    }
  },
  "expected_response": {
    "status_code": 422,
    "body": {
      "detail": [
        {
          "type": "string_pattern_mismatch",
          "loc": ["body", "username"],
          "msg": "String should match pattern '^[a-z0-9_]+$'",
          "input": "john doe",
          "ctx": {
            "pattern": "^[a-z0-9_]+$"
          }
        }
      ]
    }
  },
  "notes": "username pattern requires lowercase alphanumeric + underscore only (no spaces)"
}
