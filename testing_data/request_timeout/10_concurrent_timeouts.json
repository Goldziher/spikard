{
	"name": "Concurrent requests timing out",
	"description": "Tests multiple concurrent requests all exceeding timeout",
	"category": "request_timeout",
	"source": {
		"framework": "spikard",
		"inspired_by": "tower-http TimeoutLayer",
		"test_case": "Multiple concurrent slow requests time out independently"
	},
	"handler": {
		"route": "/api/slow-concurrent",
		"method": "GET",
		"middleware": {
			"request_timeout": {
				"timeout_seconds": 2
			}
		},
		"simulated_delay_ms": 4000,
		"response_type": "immediate"
	},
	"request": {
		"method": "GET",
		"path": "/api/slow-concurrent"
	},
	"expected_response": {
		"status_code": 504,
		"body": {
			"type": "https://spikard.dev/errors/request-timeout",
			"title": "Gateway Timeout",
			"status": 504,
			"detail": "Request processing exceeded the configured timeout of 2 seconds"
		}
	},
	"concurrent_requests": 5,
	"tags": ["timeout", "middleware", "504", "concurrent", "edge-case"],
	"notes": "5 concurrent requests each taking 4s with 2s timeout - all should return 504 independently"
}
