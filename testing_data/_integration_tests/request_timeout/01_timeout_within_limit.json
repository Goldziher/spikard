{
	"name": "Request completes within timeout",
	"description": "Tests successful response when request completes well within configured timeout",
	"category": "request_timeout",
	"source": {
		"framework": "spikard",
		"inspired_by": "tower-http TimeoutLayer",
		"test_case": "Fast request completes successfully"
	},
	"handler": {
		"route": "/api/fast",
		"method": "GET",
		"middleware": {
			"request_timeout": {
				"timeout_seconds": 30
			}
		},
		"simulated_delay_ms": 1000,
		"response_type": "immediate"
	},
	"request": {
		"method": "GET",
		"path": "/api/fast"
	},
	"expected_response": {
		"status_code": 200,
		"body": {
			"message": "success",
			"delay_ms": 1000
		}
	},
	"tags": ["timeout", "middleware", "success", "200"],
	"notes": "Request completes in 1 second with 30 second timeout - should succeed"
}
