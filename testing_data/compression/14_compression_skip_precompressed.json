{
	"name": "Compression - skip pre-compressed content",
	"description": "Tests that middleware skips compression for already compressed content types",
	"category": "compression",
	"source": {
		"framework": "spikard",
		"inspired_by": "tower_http::compression::CompressionLayer",
		"test_case": "Skip compression for pre-compressed MIME types"
	},
	"handler": {
		"route": "/api/compressed-file",
		"method": "GET",
		"middleware": {
			"compression": {
				"enabled": true,
				"algorithms": ["gzip", "br"],
				"level": 6,
				"min_size_bytes": 0,
				"skip_content_types": ["application/gzip", "image/jpeg", "video/mp4"]
			}
		}
	},
	"request": {
		"method": "GET",
		"path": "/api/compressed-file",
		"headers": {
			"Accept-Encoding": "gzip, br"
		}
	},
	"expected_response": {
		"status_code": 200,
		"headers": {
			"Content-Type": "application/gzip"
		},
		"body": {
			"message": "Archive file",
			"format": "gzip"
		}
	},
	"tags": ["compression", "middleware", "skip", "pre-compressed", "mime-types", "tower-http"],
	"notes": "Re-compressing already compressed data (JPEG, PNG, GZIP, ZIP, video) wastes CPU and can increase size. Smart middleware recognizes these MIME types."
}
