# Skipped

Java binding does not expose dependency injection APIs at the language binding level. Dependency management is delegated to the Rust core via route-level configuration in the handler registry.
